# PROGRESS.md — دفترچه‌ی پیشرفت پروژه‌ی LifeFlow

> **این فایل مرجع اصلی پروژه است.**
> هر دستیار هوش مصنوعی (یا خود کاربر) که می‌خواهد روی این پروژه کار کند، باید:
> 1. اول این فایل را کامل بخواند تا بداند تا کجا کار پیش رفته و تصمیم‌های طراحی چه بوده.
> 2. کاری را که قرار است انجام دهد، از بخش «صف کارها» بردارد.
> 3. بعد از اتمام هر کار (نه در پایان جلسه، بلکه بلافاصله)، این فایل را به‌روزرسانی کند: کار را از «صف کارها» به «کارهای انجام‌شده» منتقل کند، توضیح دهد **چه چیزی و چگونه** پیاده‌سازی شده، و اگر تصمیم طراحی مهمی گرفته شده، در بخش «تصمیم‌های طراحی» ثبت کند.
> 4. بعد از هر مرحله‌ی کاری معنادار (نه هر خط کد)، به ریپازیتوری `git push` بزند. حداقل: قبل از احتمال تمام‌شدن اعتبار/لیمیت، حتماً پوش شده باشد.

مخزن: https://github.com/My3Fake/Planner-plugin

---

## ۱. وضعیت فعلی مخزن (قبل از شروع این دور کاری)

نسخه‌ی اولیه‌ای که کاربر آپلود کرد فقط شامل خروجی build شده بود (`main.js` حدود ۲۴۵۰۰ خط، `manifest.json`, `styles.css`) — **پوشه‌ی `src/` در zip وجود نداشت**.

از داخل خودِ `main.js` (که با esbuild بدون minify ساخته شده و کامنت‌های مسیر فایل مبدأ در آن باقی مانده بود)، فایل‌های سورس زیر با موفقیت بازسازی و در `src/` قرار گرفتند:

- `src/main.ts` — کلاس پلاگین (`LifeFlowPlugin`) و View اصلی Obsidian (`LifeFlowView`, `VIEW_TYPE = "lifeflow-view"`). یک ریبون‌آیکون و یک کامند (`open-lifeflow`) ثبت می‌کند و یک ریشه‌ی React داخل ItemView مونت می‌کند.
- `src/react-globals.ts` — React/ReactDOM را روی `window` می‌گذارد تا در باندل قابل دسترس باشند.
- `src/jalali.js` — توابع تقویم جلالی (تبدیل میلادی↔جلالی، گرید ماه و ...)، به شکل `window.Jalali`.
- `src/app.jsx` — **کل اپ در یک فایل تک‌جزئی حدود ۲۷۷۰ خط** (کامپوننت React اصلی، ماتریس آیزنهاور، تسک‌ها، پومودورو، برنامه‌ریزی روزانه و ...). این فایل تنها بازسازی از باندل است؛ ساختار ماژولار اصلی (اگر کاربر قبلاً src جدا داشته) در دسترس نبود.

⚠️ **نکته‌ی مهم:** چون سورس اصلی (قبل از باندل) در دسترس نبود، این بازسازی ممکن است دقیقاً با کدی که سازنده‌ی اصلی نوشته یکی نباشد (مثلاً کامنت‌ها یا فرمت جزئی فرق کند)، ولی از نظر منطق برنامه با `main.js` قبلی یکی است. از این به بعد باید **از روی `src/` کار کرد و rebuild کرد**, نه ویرایش مستقیم `main.js`.

فعلاً **بدون سیستم build** (بدون esbuild config، بدون package.json) پوش شده — چون فقط بازسازی سورس از باندل بود. اولین قدم فنی در صف کارها، برپاسازی build واقعی (esbuild + tsconfig + package.json مطابق پلاگین‌های استاندارد Obsidian) است تا بشود از `src/` واقعاً به `main.js` رسید و تست کرد.

---

## ۲. صف کارها (Backlog) — بر اساس خواسته‌های کاربر

### کار ۰ — زیرساخت توسعه (پیش‌نیاز بقیه‌ی کارها)
- [ ] ساخت `package.json`, `tsconfig.json`, `esbuild.config.mjs` استاندارد پلاگین‌های Obsidian (sample plugin template)
- [ ] تقسیم `app.jsx` تک‌فایلی به ماژول‌های منطقی (components/, features/, store/) تا نگهداری ممکن شود
- [ ] راه‌اندازی یک لایه‌ی state management ساده (چون بخش‌های بعدی به هم وابسته‌اند: تسک، برنامه‌ریزی، پومودورو، یادگیری، گزارش باید یک منبع داده‌ی مشترک داشته باشند)

### کار ۱ — بومی‌سازی ظاهر با فضای Obsidian ✅ (تکمیل شد — جلسه‌ی ۳)
- [x] حذف استایل‌های «شبیه نرم‌افزار مستقل» و استفاده از CSS variables استاندارد Obsidian (`--interactive-accent`, `--background-primary`, `--text-normal` و ...)
- [x] دکمه‌ها، اینپوت‌ها، مودال‌ها با کلاس‌های رایج Obsidian (`.mod-cta` برای دکمه‌های اصلی وصل شد؛ `Modal`/`Setting` رسمیِ Obsidian API هنوز استفاده نشده — این بخش به کار ۲ (تنظیمات) موکول شد چون آنجا `SettingTab` واقعی ساخته می‌شود)
- [x] هماهنگی با تم روشن/تاریک کاربر (نه رنگ‌های ثابت هاردکد شده)

### کار ۲ — گسترش تنظیمات (Settings)
- [x] یک `PluginSettingTab` رسمی Obsidian ساخته شد (`src/settings-tab.ts`) — بخش‌های: زبان، نمایش/عدم‌نمایش ماتریس آیزنهاور، روشن/خاموش هر تب اصلی (برنامه‌ریزی/تقویم/مطالعه/تناسب‌اندام/یادگیری/پومودورو/یادداشت‌ها)، ۴ تاگل اعلان، کلید API هوش مصنوعی
- [x] پیش‌فرض‌های پنجره‌ی ساخت تسک (ربع ماتریس/اولویت/زمان روز/مدت) — جلسه‌ی ۵
- [ ] تنظیمات ظاهری عمیق‌تر (فونت/تراکم/رنگ‌های دسته‌بندی) — هنوز اضافه نشده
- [ ] تنظیمات گزارش‌گیری — منتظر ساخته‌شدن خودِ کار ۴ (گزارش‌گیری) می‌ماند تا معلوم شود چه چیزی قابل‌تنظیم است
- [x] ذخیره‌ی تمام داده‌های پلاگین (نه فقط تنظیمات — تسک‌ها، پومودورو، بکاپ‌ها، پیکربندی AI و ...) در `data.json` استاندارد پلاگین (`loadData`/`saveData`) — با مهاجرت یک‌باره از API خصوصی قبلی
- [x] هر بخش از UI از همان لایه‌ی مشترک `storage` (که حالا پشت‌صحنه‌اش `data.json` است) می‌خواند/می‌نویسد — چون همه‌ی ۵ کلید ذخیره‌سازی (تسک‌ها، تنظیمات، AI، سینک، بکاپ‌ها) از قبل از یک شیء `storage` مشترک عبور می‌کردند، این تغییر با ویرایش یک نقطه (نه ده‌ها call site) انجام شد

### کار ۳ — بازطراحی بزرگ برنامه‌ریزی (شبیه Google Calendar / TaskNotes)
- [ ] نمای تقویمی با درگ‌ودراپ برای ساخت تسک/رویداد مستقیم روی جدول زمانی
- [ ] پنل کناری («سبد تسک‌ها») برای تسک‌های زمان‌بندی‌نشده که بشود آن‌ها را با درگ به تایم‌لاین کشید و برعکس
- [ ] قابلیت تغییر اندازه (resize) تسک روی تایم‌لاین برای تغییر مدت‌زمان
- [ ] تفکیک نوع آیتم: تسک / رویداد / روتین / یادگیری (روند‌دار) — هرکدام ظاهر و رفتار خودش
- [ ] ویوهای متعدد: روزانه، ۳روزه، هفتگی، ماهانه، نمای ساعتی، نمای فهرستی (آجندا)

### کار ۴ — گزارش‌گیری پیشرفته
- [ ] گزارش‌های تجمیعی (روزانه/هفتگی/ماهانه) از تسک‌ها، پومودورو، یادگیری
- [ ] نمودارها (تکمیل تسک به تفکیک ربع آیزنهاور، زمان صرف‌شده به تفکیک نوع فعالیت، روند یادگیری)
- [ ] خروجی/صدور گزارش (احتمالاً Markdown داخل ولت، مطابق روحیه‌ی Obsidian)

### کار ۵ — بازطراحی «یادگیری/تسک‌های روند‌دار»
بر اساس مثال کاربر (حفظ قرآن)، مدل داده‌ی پیشنهادی:
- **پروژه‌ی یادگیری** (مثلاً «حفظ قرآن») شامل چند **مسیر/زیرکار** (حفظ جدید، مرور دور، مرور نزدیک، تثبیت) — هرکدام:
  - واحد اندازه‌گیری خودش (صفحه، مورد، دقیقه و ...)
  - [x] بازه‌ی تکرار مخصوص به خودش (هرروز / روزهای خاص هفته / پیروی از روتین کلی موضوع) — جلسه‌ی ۶، `recurrenceOverride`
  - [x] سهمیه‌ی دوره‌ای (مثلاً روزی ۳ صفحه مرور دور) — جلسه‌ی ۶، `quotaPerPeriod`
  - [x] محدوده/بازه‌ی فعلی (مثلاً مرور دور: صفحه ۱ تا ۲۰) — جلسه‌ی ۶، `rangeLabel` (فعلاً یک برچسب توصیفی ساده، نه محاسبه‌ی خودکار بازه)
  - [x] **لاگ پیشرفت شبیه commit گیت**: هر ثبت = مقدار انجام‌شده + توضیح متنی + تاریخ — جلسه‌ی ۶، `task.progressLog`؛ این برای *همه‌ی* تسک‌های روند‌دار کار می‌کند، نه فقط یادگیری (چون `ProgressiveTaskBar` مشترک است)
- [x] در بخش تسک‌ها، این‌ها به‌عنوان تسک روند‌دار (progressive) نمایش داده می‌شوند: نه صفر/صد بلکه نوار پیشرفت که با هر لاگ جلو می‌رود — این از قبل (قبل از جلسه‌ی ۶) کار می‌کرد؛ آنچه جلسه‌ی ۶ اضافه کرد فقط لاگِ باتوضیح بود
- [ ] در پومودورو و برنامه‌ریزی: امکان انتخاب یک یا چند مسیر یادگیری برای یک بلوک زمانی و تعیین سهم هرکدام از آن بلوک — هنوز نه
- [ ] امکان تعریف «روتین یادگیری روزانه» در سطح کل موضوع (نه فقط هر مسیر) — تا حدی از قبل هست (`LearningRoutineEditor` سطح موضوع)؛ ترکیب آن با سهمیه‌های جدید سطح مسیر هنوز کامل تست/جلا داده نشده
- [ ] بخش جدید «گزارش روزانه» در برنامه‌ریزی با بدهی/بستانکاری روزانه — هنوز نه (بزرگ‌ترین قلم باقیمانده‌ی این کار)
- [ ] آمار تطابق برنامه با عملکرد واقعی (planned vs actual) — هنوز نه

### کار ۶ — پومودورو حرفه‌ای
- [ ] چرخه‌ی کار/استراحت کوتاه/استراحت بلند قابل تنظیم، شمارش خودکار دور
- [ ] اتصال پومودورو به تسک/مسیر یادگیری انتخابی (طبق کار ۵)
- [ ] لاگ تاریخچه‌ی جلسات پومودورو برای گزارش‌گیری (کار ۴)
- [ ] صدای پایان تایمر، حالت Do-Not-Disturb، نوار پیشرفت در ریبون/نوار وضعیت Obsidian

---

## ۳. کارهای انجام‌شده (این بخش بعد از هر جلسه به‌روزرسانی می‌شود)

### جلسه‌ی ۱ (۲۴ مرداد/آگوست ۲۰۲۶)
- بازسازی `src/` از روی `main.js` باندل‌شده (main.ts, react-globals.ts, jalali.js, app.jsx)
- ایجاد ریپازیتوری اولیه و پوش نسخه‌ی import‌شده
- ایجاد این فایل (`PROGRESS.md`) با نقشه‌راه کامل ۷گانه (۰ تا ۶)
- **هیچ‌کدام از تغییرات فیچرِ خواسته‌شده (کارهای ۰ تا ۶) هنوز پیاده‌سازی نشده‌اند** — این جلسه فقط شامل import و برنامه‌ریزی بود.

### جلسه‌ی ۲ (۲۵–۲۶ مرداد/آگوست ۲۰۲۶) — کار ۰ کامل شد + شروع کار ۱

**کار ۰ (زیرساخت) — تکمیل شد:**
- `package.json`, `tsconfig.json`, `esbuild.config.mjs` استاندارد پلاگین‌های Obsidian ساخته شد (الگوی sample plugin: esbuild + external `obsidian`/`electron`/CodeMirror، فرمت cjs، هدف es2020).
- `src/jalali.js` از فرمت CommonJS باندل‌شده (`__commonJS` wrapper) به یک ماژول ES تمیز با `export {...}` تبدیل شد.
- `src/react-globals.ts` حذف شد (دیگر لازم نیست چون React/ReactDOM حالا با `import` مستقیم در `app.jsx`/`main.ts` استفاده می‌شوند، نه از طریق `window`).
- `src/main.ts` کاملاً بازنویسی شد: یک `LifeFlowPlugin` استاندارد (extends `Plugin`) با `LifeFlowView` (extends `ItemView`) که React را با `ReactDOMClient.createRoot` داخل View مونت می‌کند؛ ریبون‌آیکون + کامند `open-lifeflow` ثبت می‌شود.
- `src/app.jsx`: بالای فایل به `import React, {...} from "react"` و `import * as Jalali from "./jalali.js"` تغییر کرد؛ در انتها دیگر خودش را روی `window` mount نمی‌کند بلکه `export default LifeFlowApp` می‌کند (مونت‌کردن حالا وظیفه‌ی `main.ts`/`LifeFlowView` است).
- `npm install` انجام شد و `npm run build` (یعنی `node esbuild.config.mjs production`) با موفقیت یک `main.js` معتبر (~328KB، minified) از روی `src/` تولید کرد. **این اولین بار است که پروژه یک build pipeline واقعی و کارکننده دارد** — قبلاً `main.js` فقط یک فایل دستی/بازسازی‌شده بود.
- ✅ نتیجه: از این پس هر تغییری باید در `src/` اعمال شود و با `npm run build` بازتولید شود؛ **هرگز مستقیم `main.js` را ویرایش نکن.**

**کار ۱ (بومی‌سازی ظاهر با فضای Obsidian) — شروع شد (تکمیل نشده):**

بررسی دقیق کد نشان داد که مشکل "شبیه نرم‌افزار مستقل بودن" دقیقاً از این موارد ناشی می‌شد:
1. یک بلوک `<style>` تعبیه‌شده حدود ۱۱۰ خطی داخل کامپوننت اصلی با یک تم «شیشه‌ی مایع کهکشانی» کاملاً مستقل (گرادیان‌ها، `backdrop-filter`، سایه‌های نئون بنفش/صورتی) به‌همراه یک سیستم روشن/تاریک خودش (`data-theme` روی ریشه‌ی اپ) که کاملاً مستقل از تم خود Obsidian کار می‌کرد.
2. دو کامپوننت پس‌زمینه‌ی تزئینی (`GalaxyBackground` با انیمیشن ستاره روی canvas، و `LightBackground`) که کل صفحه را با گرادیان/نبولا پر می‌کردند — چیزی که در یک پلاگین جاسازی‌شده در Obsidian اصلاً معنا ندارد.
3. حدود ۶۵۰+ کلاس Tailwind رنگی (`text-slate-*`, `text-white`, `bg-white/*`, `border-white/*`, `text-rose/fuchsia/cyan/pink-*` و ...) که رنگ‌های ثابت (نه متغیرهای CSS خود Obsidian) تولید می‌کردند، از یک فایل Tailwind کامپایل‌شده (۳۰KB) در `styles.css`.
4. حدود ۱۰ نقطه‌ی مشخص («chrome» — نوار کناری، دکمه‌ی افزودن تسک، FAB، سوییچرهای تب/ویو، پیکر تم تنظیمات، منطقه‌ی درگ‌ودراپ بکاپ، هایلایت «امروز» در تقویم) با `style={{...}}` این‌لاین و رنگ‌های هگز ثابت (`#C026D3`, `#DB2777`, `#EAB4F2`, `#94a3b8`) مستقل از تنظیمات تم React انجام می‌شد.
5. `min-h-screen` روی ریشه‌ی اپ (فرض می‌کرد کل viewport مال خودش است — که در یک ItemView داخل Obsidian غلط است).

اقدامات انجام‌شده:
- بلوک `<style>` تعبیه‌شده به‌طور کامل بازنویسی شد: فقط انیمیشن‌های حرکتی (بدون رنگ) نگه داشته شدند (ترنزیشن صفحه، پاپ‌شدن مودال، رشد نمودارها)؛ تمام گرادیان‌ها/بلور/نبولا/override های تم روشن حذف شدند.
- `GalaxyBackground` و `LightBackground` به‌طور کامل حذف شدند (و فراخوانی‌شان از JSX برداشته شد) — پس‌زمینه‌ی پلاگین حالا همان پس‌زمینه‌ی Obsidian است.
- `data-theme` از ریشه‌ی اپ حذف شد؛ `min-h-screen`/`overflow-hidden` هم حذف شدند تا اپ داخل پنجره‌ی خودش (نه کل viewport) درست سایز شود.
- کلاس‌های سراسری `glass-panel`/`glass-strong`/`glass-sheen` (که در دهها جا مثل `GlassCard` استفاده می‌شوند) در `styles.css` با متغیرهای CSS خود Obsidian (`--background-secondary`, `--background-modifier-border`) بازتعریف شدند — بدون نیاز به تغییر هر نقطه‌ی استفاده در `app.jsx`.
- یک لایه‌ی override در انتهای `styles.css` اضافه شد (اسکوپ‌شده با `.lifeflow-app-root`) که کلاس‌های رنگی پرتکرار Tailwind (`text-white`, `text-slate-1..6`, `bg-white*`, `border-white*`, `bg-black*`, `text/border/bg-fuchsia/cyan/pink-*` → `--text-accent`, `text/border/bg-rose/red-*` → رنگ خطا) را به متغیرهای Obsidian هدایت می‌کند — یعنی بدون دست‌زدن به صدها `className` در `app.jsx`.
- اینپوت‌ها، مودال (`ModalShell` → کلاس‌های جدید `lf-modal-backdrop`/`lf-modal-sheet`)، و چیپ‌ها (`Chip` → کلاس‌های `lf-chip`/`lf-chip-active`) به سیستم رنگ Obsidian وصل شدند.
- ۱۰ نقطه‌ی «chrome» (نوار کناری/فعال، دکمه‌ی افزودن تسک + شعله‌ی استریک، سوییچر ویوهای تسک، FAB، سوییچر تب پروژه‌های یادگیری، منطقه‌ی درگ فایل بکاپ، پیکر تم تنظیمات، هایلایت امروز در تقویم، دکمه‌ی لیست جدید یادداشت) به‌صورت مستقیم ویرایش شدند تا از `var(--interactive-accent)` / `var(--text-accent)` / `var(--text-muted)` / `var(--background-modifier-hover)` به‌جای هگزهای ثابت استفاده کنند.
- Build دوباره اجرا و تأیید شد (main.js معتبر، بدون خطای سینتکس).

**⚠️ کار ۱ هنوز کامل نیست — آنچه باقی مانده:**
- رنگ‌های داده‌محور/دسته‌بندی (نه chrome) عمداً دست‌نخورده باقی ماندند چون تغییرشان معنای بصری را از بین می‌برد: رنگ ۴گانه‌ی ماتریس آیزنهاور (`QUADRANTS`)، رنگ‌های ۴گانه‌ی `dayColor`/`dayGlow` (نمودار دایره‌ای روز)، رنگ نشان‌های گیمیفیکیشن (`BADGES`)، رنگ پیش‌فرض نمودارها (`SimpleBarChart`/`SimpleLineChart`)، و رنگ آیتم‌های جست‌وجوی سراسری. این‌ها بعداً می‌توانند از طریق تنظیمات (کار ۲) قابل‌تنظیم شوند، اما فعلاً به‌عنوان پالت رنگی خودِ پلاگین (مشابه بسیاری از پلاگین‌های حرفه‌ای Obsidian که پالت رنگی مخصوص خود را برای نمودار/دسته‌بندی دارند) نگه داشته شدند.
- برخی دکمه‌های ثانویه/آیکون‌های کوچک هنوز ممکن است رنگ‌های Tailwind خام داشته باشند که در بازبینی این جلسه دیده نشدند؛ یک بازبینی بصری کامل داخل خودِ Obsidian (باز کردن پلاگین و چک هر تب) هنوز انجام نشده — چون این محیط اجرا Obsidian واقعی ندارد. **در جلسه‌ی بعد، در اولین فرصت که پلاگین در Obsidian واقعی تست شد، لیستی از موارد بصری باقی‌مانده تهیه و اصلاح شود.**
- فونت (`Vazirmatn`) هنوز به‌صورت inline در ریشه‌ی اپ ست شده؛ بررسی نشده که آیا باید حذف شود تا از فونت خود Obsidian/تم پیروی کند یا به‌عنوان یک گزینه در تنظیمات (کار ۲) بماند.
- دکمه‌ها هنوز از کلاس‌های بومی Obsidian مثل `.mod-cta` استفاده نمی‌کنند؛ فعلاً فقط *رنگ*شان با متغیرهای Obsidian هماهنگ شده، ولی شکل/سایه/رفتار hover هنوز دستی (Tailwind-style) است، نه دقیقاً هم‌شکل با دکمه‌های استاندارد Obsidian.

### جلسه‌ی ۳ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — تکمیل کار ۱

این جلسه توسط یک دستیار هوش مصنوعی دیگر (Claude) ادامه داده شد. کار از همان‌جایی که جلسه‌ی ۲ متوقف شده بود (کار ۱، نیمه‌کاره) برداشته شد.

**موارد باقی‌مانده‌ی کار ۱ که در این جلسه بسته شدند:**

1. **فونت (`Vazirmatn`)** — به‌جای اینکه فقط `Vazirmatn` به‌صورت هاردکد ست شود، حالا اول فونت خودِ تم Obsidian (`var(--font-text)`) را امتحان می‌کند و اگر آن متغیر مقدار نداشت (یا فونت فارسی پشتیبانی نشد) به `Vazirmatn` و بعد `Tahoma` سقوط می‌کند: `fontFamily: "var(--font-text, 'Vazirmatn'), 'Vazirmatn', Tahoma, sans-serif"`. این‌طوری هم از تم پیروی می‌کند، هم رندر درست حروف فارسی تضمین می‌ماند.
2. **دکمه‌های اصلی (CTA) به `.mod-cta` وصل شدند** — دو دکمه‌ی اصلی «افزودن تسک» (نوار کناری دسکتاپ) و «لیست جدید» (صفحه‌ی یادداشت‌ها) حالا کلاس بومی `mod-cta` را دارند (به‌جای اینکه فقط با `style={{background: var(--interactive-accent)}}` دستی رنگ‌آمیزی شوند) — یعنی رنگ/هاور/فوکوس آن‌ها را از خودِ Obsidian می‌گیرند. FAB شناور (دکمه‌ی گرد پایین صفحه‌ی موبایل) عمداً دست‌نخورده ماند چون `.mod-cta` در Obsidian یک `border-radius` پیش‌فرض مستطیلی تحمیل می‌کند که ممکن است شکل دایره‌ای FAB را به‌هم بزند و امکان تست بصری واقعی نبود؛ رنگش (`var(--interactive-accent)`) از قبل درست بود، فقط کلاس بومی نگرفت.
   - چون قانون `[class*="text-white"] { color: var(--text-normal) !important; }` (از جلسه‌ی ۲) با رنگ متن `.mod-cta` (که باید `var(--text-on-accent)` باشد) تداخل می‌کرد، یک قانون محافظ اضافه شد: `button.mod-cta[class*="text-white"] { color: var(--text-on-accent) !important; }`.
3. **دو رنگ Tailwind که در جلسه‌ی ۲ جا مانده بودند و بومی نبودند، پوشش داده شدند:**
   - `text-emerald-*`/`text-green-*` (پیام موفقیت‌آمیز sync) → `var(--text-success, #7fbb6e)`
   - `border-t-violet-300` (رنگ ثابت لبه‌ی بالای اسپینر لودینگ خلاصه‌ی AI) → `var(--interactive-accent)`
4. **باگ واقعی پیدا و رفع شد: صفحه‌ی تایمر پومودورو در تم روشن Obsidian ناخوانا بود.** بررسی کامل‌تر این جلسه نشان داد SVG رینگ پومودورو (که در لیست ۱۰ نقطه‌ی «chrome» جلسه‌ی ۲ نبود) رنگ‌های ثابت داشت: حلقه‌ی پس‌زمینه `rgba(255,255,255,.08)` و متن تایمر/برچسب «دور» با `fill:"#fff"` / `fill:"#94a3b8"`. این‌ها روی پس‌زمینه‌ی تیره‌ی قبلی خوب دیده می‌شدند ولی حالا که کارت‌ها به `--background-secondary` وصل‌اند (که در تم روشن می‌تواند خودش روشن باشد)، متن سفید و حلقه‌ی نیمه‌شفاف‌سفید عملاً محو می‌شدند. اصلاح شد به `var(--background-modifier-border)` / `var(--text-normal)` / `var(--text-muted)`. رنگ خودِ مسیر پیشرفت (`modeColor`: بنفش/فیروزه‌ای/صورتی بر اساس حالت کار/استراحت) دست‌نخورده ماند چون جزو پالت داده‌محورِ برندِ پلاگین است (مثل رنگ‌های ماتریس آیزنهاور)، نه chrome.
5. Build دوباره اجرا و تأیید شد (`npm install` چون `node_modules/` در ریپازیتوری نبود، سپس `npm run build`؛ `node --check main.js` بدون خطا).

**نتیجه:** کار ۱ از نظر تمام مواردی که بدون دسترسی به یک ولت Obsidian واقعی قابل انجام/تأیید بودند تکمیل شد. تنها موردی که همچنان باز است، بازبینی بصری واقعی داخل Obsidian است (سؤال باز شماره‌ی ۶ در بخش ۵ همچنان به قوت خودش باقی است).

**کار ۲ (گسترش تنظیمات) — شروع شد (تکمیل نشده):**

اولین و مهم‌ترین زیرساخت کار ۲ («ذخیره‌ی تنظیمات در `data.json` استاندارد پلاگین») پیاده‌سازی شد — و چون تمام داده‌های اپ (نه فقط تنظیمات) از یک شیء واحد `storage` در `app.jsx` عبور می‌کردند، این تغییر کل پلاگین را به‌روز کرد، نه فقط تنظیمات:

- بررسی کد نشان داد ذخیره‌سازی فعلی از یک API **خصوصی و مستندنشده‌ی** Obsidian استفاده می‌کرد: `app.loadLocalStorage(key)` / `app.saveLocalStorage(key, value)`. این API داده را در `localStorage` مرورگر و **مخصوص هر ولت** ذخیره می‌کند — یعنی با Obsidian Sync یا سینک فایل معمولی (مثل Syncthing/Git) بین دستگاه‌ها منتقل نمی‌شود، و چون خصوصی است ممکن است در نسخه‌های بعدی Obsidian بدون اطلاع تغییر کند یا حذف شود. این دقیقاً همان چیزی است که کار ۲ گفته «باید به `data.json` استاندارد منتقل شود».
- در `src/main.ts`: کلاس `LifeFlowPlugin` حالا یک کش درون‌حافظه‌ای (`this.data`) دارد که در `onload()` با `await this.loadData()` پر می‌شود؛ دو متد `getDataValue(key)` / `setDataValue(key, value)` رابط سنکرون (هم‌شکل با `localStorage.getItem/setItem` قبلی) به این کش می‌دهند، و هر نوشتن با ۴۰۰ میلی‌ثانیه debounce از طریق `this.saveData(this.data)` روی دیسک (در `data.json` داخل پوشه‌ی پلاگین) نوشته می‌شود — تا تایپ سریع باعث نوشتن مکرر روی دیسک نشود. در `onunload()` هم اگر نوشتنِ در صف مانده باشد، فوری flush می‌شود.
- **مهاجرت یک‌باره:** چون ممکن است کاربر از قبل با همان API خصوصی داده وارد کرده باشد، `migrateLegacyLocalStorageIfNeeded()` یک‌بار (با یک پرچم داخل خودِ `data.json`) تک‌تک کلیدهای شناخته‌شده (`lifeflow_data_v1`, `lifeflow_settings_v1`, `lifeflow_ai_v1`, `lifeflow_sync_v1`, `lifeflow_backups_v1`) را از `app.loadLocalStorage` می‌خواند و — فقط اگر در `data.json` جدید هنوز چیزی برای آن کلید نبود — کپی می‌کند، بعد پرچم مهاجرت را ست می‌کند. اگر API خصوصی دیگر وجود نداشته باشد (تغییر کرده/حذف شده)، `try/catch` آن را می‌گیرد و پلاگین با یک `data.json` خالی شروع به کار می‌کند — یعنی کرش نمی‌کند، فقط دیتای قدیمی منتقل نمی‌شود.
- کلیدهای زمان‌محور نوتیفیکیشن (`lifeflow_notif_*`، فقط برای جلوگیری از نوتیف تکراری در همان روز) عمداً مهاجرت داده نشدند — دیتای مصرفی/بی‌اهمیت‌اند.
- در `src/app.jsx`: خودِ شیء `storage` فقط به‌جای خواندن/نوشتن روی `window.__lifeflowObsidianApp` (شیء App خصوصی)، حالا از `window.__lifeflowPlugin` (خودِ نمونه‌ی پلاگین، با متدهای بالا) استفاده می‌کند؛ **هیچ‌کدام از ۱۶ نقطه‌ی فراخوانی `storage.get/set` در بقیه‌ی فایل تغییر نکردند** — چون از قبل درست لایه‌بندی شده بود.
- `npx tsc --noEmit` اجرا شد تا مطمئن شویم تغییرات TypeScript خطا ندارند (سه خطای موجود مربوط به تعریف‌های نوع خودِ پکیج `obsidian` در `node_modules` هستند، نه کد ما — پیش از این تغییرات هم بودند). سپس `npm run build` + `node --check main.js` تأیید شدند.

**⚠️ کار ۲ هنوز کامل نیست — آنچه باقی مانده:**
- خودِ `SettingsModal` (مودال React داخل اپ) هنوز تنها راه تغییر تنظیمات است؛ یک `PluginSettingTab` رسمی Obsidian (که در نوار تنظیمات خودِ Obsidian، نه داخل View پلاگین، ظاهر می‌شود) هنوز ساخته نشده.
- بخش‌بندی کامل خواسته‌شده در کار ۲ (روشن/خاموش کردن هر بخش از اپ، پیش‌فرض‌های پنجره‌ی ساخت تسک، نمایش/عدم‌نمایش ماتریس آیزنهاور، تنظیمات گزارش‌گیری) هنوز اضافه نشده — `SettingsModal` فعلی فقط تم/زبان/نوتیفیکیشن‌ها/کلید AI را پوشش می‌دهد.

**قدم بعدی پیشنهادی:** ادامه‌ی کار ۲ — طراحی و ساخت خودِ `PluginSettingTab` و بخش‌های باقی‌مانده‌ی تنظیمات.

### جلسه‌ی ۴ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — ساخت `PluginSettingTab` رسمی + شروع نمایش/عدم‌نمایش بخش‌ها

⚠️ **این جلسه با یک هشدار کاربر شروع شد:** «حتماً پروژه را بخوان، ممکن است تغییر کرده باشد». بررسی نشان داد ریپازیتوری واقعاً از جلسه‌ی قبلیِ *این* نشست جلوتر رفته بود (یک دستیار دیگر جلسه‌ی ۳ را در یک sandbox جدا انجام و پوش کرده بود، در حالی که sandbox این نشست هنوز روی نسخه‌ی قدیمی‌تر (بعد از جلسه‌ی ۲) کار می‌کرد و چند ویرایش محلی/کامیت‌نشده‌ی منسوخ داشت). این ویرایش‌های محلی منسوخ (بازنویسی دوباره‌ی بلوک `<style>` که در جلسه‌ی ۳ قبلاً درست شده بود) **دور ریخته شدند** (`git checkout -- . && git reset --hard origin/main`) تا کار جلسه‌ی ۳ خراب/دوباره‌کاری نشود. **درس مهم برای جلسات بعدی: همیشه اول `git fetch && git log origin/main` را چک کن، حتی اگر فکر می‌کنی می‌دانی وضعیت مخزن چیست — کار ممکن است در یک sandbox دیگر ادامه پیدا کرده باشد.**

پس از همگام‌سازی، کار از «قدم بعدی پیشنهادی» بالا (ساخت `PluginSettingTab`) ادامه یافت:

1. **`src/settings-tab.ts` (فایل جدید):** یک `PluginSettingTab` رسمی Obsidian که در تنظیمات خودِ Obsidian (نه داخل View پلاگین) زیر نام «زندگی‌آرام» ظاهر می‌شود. این تب مستقیماً همان دو کلید `data.json` را می‌خواند/می‌نویسد که `app.jsx` از قبل استفاده می‌کرد (`lifeflow_settings_v1` و `lifeflow_ai_v1`) — از طریق `plugin.getDataValue`/`setDataValue` که در جلسه‌ی ۳ اضافه شده بودند — یعنی این تب و مودال تنظیمات داخل خودِ اپ (`SettingsModal` در `app.jsx`) دقیقاً روی یک منبع داده کار می‌کنند، نه دو نسخه‌ی جدا.
   - بخش‌ها: زبان (dropdown ۴ زبانه)، نمایش/عدم‌نمایش ماتریس آیزنهاور (تاگل)، روشن/خاموش هر یک از ۷ تب اصلی (برنامه‌ریزی/تقویم/مطالعه/تناسب‌اندام/یادگیری/پومودورو/یادداشت‌ها — داشبورد و تسک‌ها همیشه اجباری‌اند)، ۴ تاگل اعلان (همان‌هایی که در `SettingsModal` بودند)، کلید API هوش مصنوعی (فیلد پسورد).
   - توضیح صریح اضافه شد که تم روشن/تاریک دیگر یک گزینه‌ی جداگانه نیست — از تنظیمات ظاهری خودِ Obsidian پیروی می‌کند (طبق تصمیم کار ۱).
2. **همگام‌سازی زنده‌ی دو سطح تنظیمات:** چون `PluginSettingTab` (کد TypeScript خارج از درخت React) و `SettingsModal` (React، ممکن است هم‌زمان در یک View باز باشد) با هم state سنکرون React ندارند، هر نوشتنِ `LifeFlowSettingTab` یک `CustomEvent("lifeflow-settings-changed")` روی `window` پخش می‌کند؛ در `app.jsx` (کامپوننت اصلی `LifeFlowApp`) یک `useEffect` تازه این event را می‌شنود و بلافاصله `setSettings(loadSettings())` را صدا می‌زند — یعنی اگر کاربر همزمان پلاگین باز و تب تنظیمات Obsidian را هم باز داشته باشد، تغییر فوری در هر دو دیده می‌شود، بدون نیاز به بستن/باز کردن دوباره‌ی View.
3. **مدل داده‌ی جدید `settings.features` در `app.jsx`:** یک ثابت `DEFAULT_FEATURES = { showMatrix: true, tabs: { planning, calendar, study, fitness, learning, pomodoro, notes: همه true } }` و تابع کمکی `mergeFeatures()` اضافه شدند؛ `loadSettings()` حالا همیشه یک `features` کامل (merge‌شده با پیش‌فرض‌ها، حتی اگر داده‌ی قدیمی‌تر آن را نداشته باشد) برمی‌گرداند — یعنی کاربرانی که از قبل تنظیمات ذخیره‌شده دارند با آپدیت این نسخه کرش نمی‌کنند.
4. **واقعاً وصل کردن این تنظیمات به UI (نه فقط ذخیره‌شدن بی‌اثر):**
   - `NAV` (هم نوار کناری دسکتاپ، هم نوار پایین موبایل) حالا با `NAV.filter(isNavVisible)` قبل از map رندر می‌شود؛ `isNavVisible` یک تب را همیشه نشان می‌دهد اگر `dashboard`/`tasks` باشد، وگرنه بر اساس `settings.features.tabs[id]`.
   - دکمه‌ی «ماتریس» در سوییچر ویوهای تب تسک‌ها (لیست/ماتریس/کانبان/زمان‌بندی) حالا با `.filter(([id]) => id !== "matrix" || settings.features?.showMatrix !== false)` قبل از map فیلتر می‌شود.
5. **یک باگ واقعیِ جداگانه (نه از تنظیمات) هم در همین بازبینی پیدا و رفع شد:** نوار کناری دسکتاپ کلاس `h-screen` داشت (فرض می‌کرد کل ارتفاع viewport مال خودش است) — دقیقاً همان نوع مشکلی که در جلسه‌ی ۲ روی ریشه‌ی کل اپ (`min-h-screen`) رفع شده بود ولی روی خودِ نوار کناری جا مانده بود. به `h-full` تغییر کرد.
6. `npx tsc --noEmit` (فقط همان ۳ خطای قبلی/بی‌ربط به کد ما در تایپ‌های خودِ پکیج `obsidian`، هیچ خطای جدیدی از `settings-tab.ts` یا تغییرات `main.ts`/`app.jsx` نبود)، سپس `npm run build` + `node --check main.js` تأیید شدند.

**⚠️ کار ۲ هنوز کامل نیست — آنچه باقی مانده:**
- پیش‌فرض‌های پنجره‌ی ساخت تسک (اولویت/ربع پیش‌فرض و ...) هنوز در تنظیمات نیستند.
- تنظیمات ظاهری عمیق‌تر (فونت انتخابی، تراکم/فشردگی چیدمان، رنگ‌های دسته‌بندی/ماتریس قابل‌تنظیم — سؤال باز شماره‌ی ۵ در بخش ۵) هنوز اضافه نشده‌اند.
- تست بصری واقعی `PluginSettingTab` در یک Obsidian واقعی هنوز انجام نشده (همچنان سؤال باز شماره‌ی ۶).
- گزینه‌ی «فیلتر کردن تب فعال اگر همان تب مخفی شد» یک حالت حاشیه‌ای دارد: اگر کاربر همان لحظه که در تب «X» است آن را از تنظیمات مخفی کند، محتوای آن تب تا رفتن به تب دیگر همچنان نمایش داده می‌شود (چون `tab` state جدا از فیلتر نوار ناوبری است). اولویت پایین، ولی برای جلسه‌ی بعد یادداشت شد.

---

### جلسه‌ی ۵ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — پیش‌فرض‌های پنجره‌ی «تسک جدید»

طبق دستور کاربر («ادامه بده، ولی در هر پیام فقط یک کامیت کن»)، این جلسه دقیقاً یک آیتم از باقیمانده‌های کار ۲ را بست:

- `app.jsx`: `DEFAULT_TASK_DEFAULTS = { quad: "q2", priority: 2, daypart: "morning", duration: 45 }` اضافه شد و `loadSettings()` حالا یک `settings.taskDefaults` کامل (merge‌شده، سازگار با داده‌ی قدیمی) برمی‌گرداند.
- `AddTaskModal` یک prop جدید `taskDefaults` می‌گیرد و مقادیر اولیه‌ی `quad`/`priority`/`daypart`/`duration` را (فقط در حالت «تسک جدید»، نه ویرایش) از آن می‌خواند به‌جای مقادیر ثابت قبلی (`"q2"`, `2`, `"morning"`, `45`). تنها نقطه‌ی فراخوانی `AddTaskModal` در `app.jsx` حالا `taskDefaults: settings.taskDefaults` را پاس می‌دهد.
- `settings-tab.ts`: بخش جدید «پیش‌فرض‌های پنجره‌ی تسک جدید» با ۴ کنترل (dropdown ربع ماتریس، dropdown اولویت، dropdown زمان روز، عدد مدت به دقیقه) اضافه شد؛ لیبل‌های ربع/اولویت/زمان‌روز چون این فایل جدا از درخت React است، عیناً از مقادیر `QUADRANTS`/`PRIORITIES`/`DAYPARTS` در `app.jsx` کپی شدند (چهار مورد کوچک و باثبات، ریسک واگرایی کم).
- `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، `npm run build`، `node --check main.js` تأیید شدند.

**نتیجه:** آیتم «پیش‌فرض‌های پنجره‌ی ساخت تسک» از صف کار ۲ بسته شد. باقیِ کار ۲ (تنظیمات ظاهری عمیق‌تر، تنظیمات گزارش‌گیری) هنوز باز است.

---

## ۴. تصمیم‌های طراحی

- چون هیچ سیستم build ای همراه فایل‌ها نبود، تصمیم گرفته شد که در گام بعد (کار ۰) یک اسکلت استاندارد esbuild ساخته شود تا از این پس روی `src/` کار شود، نه `main.js` مستقیم.
- ترتیب اجرا پیشنهادی: کار۰ (زیرساخت) → کار۱ (ظاهر بومی) → کار۲ (تنظیمات) → کار۵ (مدل داده‌ی یادگیری، چون کار۳ و کار۶ به آن وابسته‌اند) → کار۳ (برنامه‌ریزی) → کار۶ (پومودورو) → کار۴ (گزارش‌گیری، چون به داده‌ی همه‌ی بخش‌های قبل نیاز دارد). این ترتیب پیشنهادی است و می‌تواند تغییر کند.
- **استراتژی کار ۱ (مهم برای ادامه‌ی کار):** به‌جای بازنویسی خط‌به‌خط ۶۵۰+ کلاس Tailwind در `app.jsx` (که ریسک شکستن چیزی را در یک فایل تک‌جزئی ۲۶۰۰+ خطی بالا می‌برد)، رویکرد انتخاب‌شده **override کردن در سطح CSS** بود: کلاس‌های Tailwind دقیقاً با همان نام در `styles.css` با سلکتور `.lifeflow-app-root [class*="..."]` و `!important` بازتعریف شدند تا به متغیرهای Obsidian اشاره کنند، بدون دست‌زدن به `className` ها در `app.jsx`. فقط رنگ‌های **این‌لاین** (`style={{color: "#hex"}}`) که با CSS قابل override نیستند، مستقیماً در `app.jsx` ویرایش شدند — و فقط در نقاطی که رنگ واقعاً «chrome» بود (نه داده/دسته‌بندی). این الگو را برای ادامه‌ی پاک‌سازی رنگ‌ها هم باید ادامه داد.
- رنگ‌های دسته‌بندی/داده (ماتریس آیزنهاور، نمودار روز، نشان‌ها، نمودارها) عمداً حذف/جایگزین نشدند — این تصمیم می‌تواند در کار ۲ (تنظیمات) بازبینی شود، مثلاً با اضافه‌کردن گزینه‌ی «پیروی از رنگ accent خود Obsidian برای همه‌چیز» در تنظیمات ظاهری.
- **قاعده‌ی کلی برای تشخیص «chrome» در برابر «داده/چارت» (جلسه‌ی ۳):** اگر رنگ ثابت روی چیزی است که کاربر برایش معنای برندی/دسته‌بندی قائل است (رنگ ربع ماتریس آیزنهاور، رنگ خط نمودار، رنگ نشان) → نگه داشته می‌شود. اگر رنگ ثابت فقط برای *خوانا بودن متن/خط روی پس‌زمینه‌ی خودِ پلاگین* است (مثل متن تایمر، حلقه‌ی پس‌زمینه‌ی تایمر، برچسب‌ها) → باید به `var(--text-normal)`/`var(--text-muted)`/`var(--background-modifier-border)` وصل شود، چون این‌ها اگر ثابت بمانند در تم روشن ممکن است کاملاً ناخوانا شوند (نمونه‌ی واقعی: صفحه‌ی تایمر پومودورو که در جلسه‌ی ۲ از قلم افتاده بود).
- برای دکمه‌های اصلی (CTA) که هم می‌خواهیم رنگ/هاور بومی `.mod-cta` را داشته باشند و هم کلاس عمومی `text-white` (که در جلسه‌ی ۲ به `var(--text-normal)` مپ شده) را حفظ کنیم، یک قانون محافظ با اولویت بالاتر اضافه شد (`button.mod-cta[class*="text-white"]`) تا رنگ متن روی دکمه‌ی accent‌رنگ همیشه `var(--text-on-accent)` بماند، نه رنگ متن عادی. این الگو را برای هر دکمه‌ی `.mod-cta` جدید در آینده هم باید رعایت کرد (یا کلاس `text-white` را از آن دکمه‌ها حذف کرد).
- روی FAB شناور (دکمه‌ی گرد افزودن در نمای موبایل) عمداً `.mod-cta` گذاشته نشد چون شکل دایره‌ای‌اش با `border-radius` پیش‌فرض این کلاس در تم‌های Obsidian ممکن است تداخل کند و امکان تست بصری واقعی نبود؛ اگر در جلسه‌ی بعد دسترسی به Obsidian واقعی فراهم شد، این مورد را می‌شود امتحان و تنظیم کرد.
- **الگوی «کش سنکرون + نوشتن async» برای `data.json` (جلسه‌ی ۳):** `Plugin.loadData()`/`saveData()` رسمی Obsidian هر دو `Promise` برمی‌گردانند، ولی تمام کد React فعلی (`useState(() => loadSavedData())` و مشابه‌هایش) انتظار یک `storage.get/set` **سنکرون** دارد — دقیقاً مثل قرارداد قدیمی `localStorage`. به‌جای بازنویسی این بخش‌ها به الگوی async (که ریسک بازنویسی گسترده در فایل تک‌جزئی ۲۶۰۰+ خطی `app.jsx` را داشت)، تصمیم گرفته شد پلاگین در `onload()` یک‌بار کل `data.json` را با `await` بخواند و در یک متغیر عضو (`this.data`) کش کند؛ از آن به بعد `getDataValue`/`setDataValue` سنکرون کار می‌کنند (خواندن/نوشتن از/به همان کش)، و فقط نوشتن واقعی روی دیسک (`this.saveData(this.data)`) با ۴۰۰ میلی‌ثانیه debounce به‌صورت async در پس‌زمینه انجام می‌شود. این یعنی حداکثر یک بار (بین باز شدن ولت تا باز شدن اولین View پلاگین) دیتا لود می‌شود، و از آن به بعد رفتار برای React دقیقاً مثل `localStorage` قبلی است — بدون نیاز به `useEffect`/لودینگ اسپینر اضافه در جاهایی که قبلاً نبود.
- این تصمیم یعنی اگر Obsidian یا سیستم‌عامل به‌طور غیرمنتظره کرش کند *در همان ۴۰۰ میلی‌ثانیه‌ی بین آخرین تغییر و نوشتن روی دیسک*، آخرین تغییر ممکن است ذخیره نشود (نه بدتر از قبل، چون `localStorage`/API خصوصی قبلی هم تضمین فوری نداشت). `onunload()` این پنجره را با flush فوری در زمان غیرفعال‌شدن پلاگین کوچک‌تر می‌کند.
- **دو منبع تنظیمات، یک منبع داده (جلسه‌ی ۴):** به‌جای اینکه `PluginSettingTab` جدید یک کپی جدا از تنظیمات داشته باشد (که می‌توانست با `SettingsModal` داخل اپ ناهماهنگ شود)، هر دو مستقیماً همان کلیدهای `data.json` (`lifeflow_settings_v1`/`lifeflow_ai_v1`) را می‌خوانند/می‌نویسند. هماهنگی زنده‌ی بین این دو رابط کاربری (وقتی هر دو هم‌زمان باز باشند) با یک `CustomEvent` سبک روی `window` انجام می‌شود، نه با یک state manager سراسری — چون فقط یک جهت ارتباط (تنظیمات رسمی → View باز) لازم بود، نه ارتباط دوطرفه‌ی پیچیده.
- **همیشه اول وضعیت remote را چک کن (درس جلسه‌ی ۴):** وقتی کار روی یک پروژه در چند نشست/sandbox جدا ادامه پیدا می‌کند، حافظه‌ی محلی هر نشست ممکن است قدیمی باشد. قانون: قبل از هر ویرایش، `git fetch origin && git log --oneline origin/main -10` را چک کن و اگر HEAD محلی عقب‌تر بود، `git reset --hard origin/main` بزن (بعد از مطمئن‌شدن که چیز کامیت‌نشده‌ی مهمی از دست نمی‌رود) — نه اینکه فرض کنی حافظه‌ی خودت کامل است.

---

### جلسه‌ی ۶ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — شروع کار ۵: سهمیه/بازه/تکرار مخصوص هر مسیر یادگیری + لاگ پیشرفت با توضیح

طبق دستور کاربر («ادامه بده، ولی هر پیام فقط یک کامیت و بعد متوقف شو»)، بعد از بستن آیتم‌های کوچک‌تر کار ۲، این جلسه اولین قدم واقعی کار ۵ را برداشت — دقیقاً همان چیزی که کاربر در مثال «حفظ قرآن» به‌عنوان مهم‌ترین کمبود اشاره کرده بود: مسیرهای یادگیری (subsections) قبلاً همه از یک روتین/سهمیه‌ی مشترک در سطح کل موضوع استفاده می‌کردند و امکان «ثبت با توضیح مثل کامیت گیت» اصلاً وجود نداشت.

**قبل از تغییر، مدل قدیمی بررسی شد** (`LearningHub`, `SubsectionCard`, `addTaskProgress`): هر subsection فقط `{id, title, unit, target, linkedTaskId}` داشت؛ `linkedTaskId` به یک تسک «روند‌دار» (`progressType: "progressive"`) در سیستم عمومی تسک‌ها وصل می‌شد که `recurrence` را از *کل موضوع* به ارث می‌برد (`topic.recurrence`) — یعنی «مرور دور» و «مرور نزدیک» مجبور بودند دقیقاً همان روزهای هفته/بازه را داشته باشند، که دقیقاً برخلاف خواسته‌ی کاربر بود. ثبت پیشرفت هم فقط یک عدد بود (`addTaskProgress(id, amount)`), بدون تاریخچه یا توضیح.

**تغییرات:**
1. **`addTaskProgress` سراسری (در `app.jsx`، مؤثر روی *همه‌ی* تسک‌های روند‌دار، نه فقط یادگیری):** امضا به `(id, amount, note)` تغییر کرد؛ هر فراخوانی یک ورودی `{id, date, amount, note}` به آرایه‌ی جدید `task.progressLog` اضافه می‌کند (جدیدترین اول)، علاوه‌بر افزایش معمول `progressCurrent`. تسک‌های قدیمی که `progressLog` ندارند مشکلی ندارند (`t2.progressLog || []` قبل از spread).
2. **`ProgressiveTaskBar` (کامپوننت مشترک بین تسک‌های عمومی و مسیرهای یادگیری):** یک ورودی متن اختیاری «توضیح» کنار ورودی عدد اضافه شد؛ زیر نوار پیشرفت، یک `ProgressLogList` جدید تاریخچه‌ی ثبت‌ها را به‌شکل شبیه لاگ گیت نشان می‌دهد (`+مقدار تاریخ — توضیح`)، با نمایش ۲ مورد آخر و دکمه‌ی «N ثبت قدیمی‌تر…» برای باز کردن بقیه.
3. **مدل داده‌ی subsection گسترش یافت** (`{id, title, unit, target, quotaPerPeriod, rangeLabel, recurrenceOverride, linkedTaskId}`):
   - `quotaPerPeriod`: سهمیه‌ی هر دوره (مثلاً «۳ صفحه در روز») — فعلاً فقط نمایشی/اطلاعاتی است (زیرنویس کارت)؛ هنوز به یادآوری خودکار یا محاسبه‌ی خودکار کسری/مازاد وصل نیست (آن بخش در «گزارش روزانه»ی هنوز‌نساخته می‌آید).
   - `rangeLabel`: یک برچسب متنی آزاد برای بازه (مثلاً «صفحه ۱ تا ۲۰») — فقط توصیفی، نه یک بازه‌ی عددی محاسبه‌شده.
   - `recurrenceOverride`: اگر `null` باشد، از روتین کلی موضوع (`topic.recurrence`) پیروی می‌کند (رفتار قبلی)؛ اگر مقدار داشته باشد (`{recurrence: "daily"|"weekly", recurrenceWeekdays?}`) روی تسک لینک‌شده‌ی *همان* subsection override می‌شود، بدون اینکه روی بقیه‌ی subsection‌های دیگر آن موضوع اثر بگذارد — این دقیقاً رفع محدودیتی است که کاربر توصیف کرده بود («مرور دور» هرروز، «مرور نزدیک» فقط روزهای زوج، مثلاً).
4. **`SubsectionCard` بازنویسی شد:** فرم ویرایش حالا واحد/هدف‌کل/سهمیه‌ی دوره‌ای/بازه‌ی توصیفی/دوره‌ی تکرار (با گزینه‌ی «طبق روتین کلی موضوع» یا «هرروز» یا «روزهای خاص» + چیپ‌های روزهای هفته) را دارد؛ در حالت غیرویرایش یک زیرنویس خلاصه (سهمیه · تکرار · بازه) نمایش داده می‌شود. **نکته‌ی فنی:** نسخه‌ی اول این کامپوننت را به‌شکل یک عبارت تودرتوی خیلی طولانی (`React.createElement` تودرتو در یک خط) نوشتم که یک پرانتز کم داشت و build را می‌شکست (`esbuild` خطای «Expected ")" but found ":"» داد). به‌جای پیدا کردن دستیِ پرانتز گمشده در آن عبارت غول‌آسا، کل کامپوننت با متغیرهای میانی (`header`, `subtitle`, `editForm`, `body`) بازنویسی شد که هم خطا رفع شد و هم خواناتر است. **درس برای جلسات بعد: برای JSX پیچیده/تودرتو با چند شرط، از متغیرهای میانی استفاده کن، نه یک عبارت یک‌خطی غول‌آسا — ریسک اشتباه پرانتز در فایل‌های تولیدشده توسط ابزار (نه دست‌نویس با فرمت‌بندی خودکار) بالاست.**
5. `updateSubsection` طوری تغییر کرد که وقتی `recurrenceOverride` پچ می‌شود، `recurrence`/`recurrenceWeekdays` تسک لینک‌شده را هم هماهنگ به‌روزرسانی کند (یا override را اعمال کند، یا در صورت `null` به روتین کلی موضوع برگردد).
6. `AddTaskModal` هنگام ساخت تسک تازه `progressLog: []` می‌گذارد؛ در حالت ویرایش، `progressLog` قبلی تسک را دست‌نخورده نگه می‌دارد.
7. `npm run build` + `node --check main.js` بعد از رفع خطای پرانتز تأیید شدند.

**⚠️ کار ۵ هنوز خیلی مانده — مهم‌ترین قلم‌های باقی:**
- بخش «گزارش روزانه» در برنامه‌ریزی (کاربر در پایان روز می‌گوید چقدر از هرکدام انجام داد → خودکار در `progressLog` مسیر مربوطه ثبت شود، و کسری/مازاد نسبت به `quotaPerPeriod` به‌عنوان بدهی/بستانکاری به فردا منتقل شود) — این بزرگ‌ترین و از‌نظر کاربر مهم‌ترین قلم باقیمانده است.
- اتصال انتخاب مسیر یادگیری در پومودورو/برنامه‌ریزی (تعیین اینکه «در این یک ساعت، انقدر از مرور دور، انقدر از مرور نزدیک» انجام شود).
- آمار «طبق‌برنامه در برابر واقعی» (planned vs actual) که به گزارش روزانه بستگی دارد.
- `quotaPerPeriod` هنوز فقط نمایشی است؛ منطق واقعی «امروز چقدر عقب/جلو هستم نسبت به سهمیه» ساخته نشده.
- تست بصری واقعی در Obsidian (مثل بقیه‌ی موارد قبلی) هنوز انجام نشده.

---

## ۵. سؤالات باز برای کاربر

1. آیا نسخه‌ی سورس اصلی (قبل از باندل، با ساختار پوشه‌ای دلخواه) جایی موجود است؟ اگر بله بهتر است همان جایگزین بازسازی فعلی شود تا چیزی گم نشود.
2. برای «گزارش روزانه» در کار ۵: وقتی کاربر می‌گوید «معادل یک روز و نیم قرآن حفظ کردم»، آیا این مقدار باید به‌صورت خودکار بین مسیرهای مختلف (حفظ جدید/مرور دور/مرور نزدیک) تقسیم شود یا کاربر خودش برای هر مسیر جدا وارد می‌کند؟
3. آیا تقویم جلالی باید در همه‌جای برنامه‌ریزی (نمای ماهانه و غیره) پیش‌فرض باشد؟ (در `jalali.js` موجود است، پس زیرساختش هست)
4. برای درگ‌ودراپ در تایم‌لاین، آیا کتابخانه‌ی خاصی مدنظر است یا پیاده‌سازی دستی با React کافی است؟ (پیش‌فرض: پیاده‌سازی دستی، چون افزودن کتابخانه‌ی سنگین در پلاگین Obsidian معمول نیست)
5. درباره‌ی کار ۱: الان رنگ‌های دسته‌بندی/دادهٔ پلاگین (بنفش/صورتی/فیروزه‌ای در ماتریس آیزنهاور، نمودار روز، نشان‌ها) دست‌نخورده ماندند و فقط رنگ‌های «chrome» (نوار کناری، دکمه‌ها، حالت انتخاب‌شده) به رنگ accent خود Obsidian وصل شدند. آیا این ترکیب مطلوب است، یا ترجیح می‌دهید کل پلاگین کاملاً تک‌رنگ و منطبق بر تم Obsidian باشد (حتی رنگ‌های دسته‌بندی)؟
6. تست بصری واقعی این تغییرات هنوز در یک ولت Obsidian واقعی انجام نشده (این محیط Obsidian ندارد). لطفاً پلاگین را در Obsidian باز کنید و اگر جایی هنوز شبیه اپ جداگانه به نظر می‌رسد (رنگ/فونت/دکمه‌ای که جا مانده)، بگویید تا در جلسه‌ی بعد دقیق اصلاح شود.
