# PROGRESS.md — دفترچه‌ی پیشرفت پروژه‌ی LifeFlow

> **این فایل مرجع اصلی پروژه است.**
> **⚠️ به‌روزرسانی ۷ شهریور ۱۴۰۵:** یک مشخصات جامع و بزرگ از کاربر دریافت شد و پروژه بین ۵ لِین بازتقسیم شد. **قبل از هر کاری، بخش ۰.۶ (نقشه‌ی راه/تقسیم‌کار/شاخه‌ها/وابستگی‌ها) و بخش ۶ (متن کامل مشخصات) را بخوان** — این دو بخش از این‌جا به بعد مرجع اصلیِ «چه کسی چه کاری می‌کند»اند و مدلِ کاری را از «کامیت مستقیم روی main» به «شاخه‌ی طولانی‌مدت + اجازه‌ی صریح کاربر قبل از merge» تغییر داده‌اند.
> هر دستیار هوش مصنوعی (یا خود کاربر) که می‌خواهد روی این پروژه کار کند، باید:
> 1. **اول بخش «۰. سیستم کار موازیِ لِین‌بندی‌شده» را بخواند** و لِین خودش را پیدا کند — این پروژه هم‌زمان توسط چند دستیار هوش مصنوعی مختلف توسعه داده می‌شود؛ کار بدون دانستن این تقسیم‌بندی تقریباً تضمین‌شده منجر به برخورد/دوباره‌کاری می‌شود.
> 2. بعد کل این فایل را کامل بخواند تا بداند تا کجا کار پیش رفته و تصمیم‌های طراحی چه بوده.
> 3. کاری را که قرار است انجام دهد، از **صف کارهای لِین خودش** (بخش ۰) یا در نبود کار مشخص، از بخش «صف کارها»ی عمومی (بخش ۲) بردارد — به‌شرطی که در قلمروی همان لِین باشد.
> 4. بعد از اتمام هر کار (نه در پایان جلسه، بلکه بلافاصله)، این فایل را به‌روزرسانی کند: کار را از صف به «لاگ لِین» خودش (بخش ۴) منتقل کند، توضیح دهد **چه چیزی و چگونه** پیاده‌سازی شده، و اگر تصمیم طراحی مهمی گرفته شده، ثبت کند.
> 5. بعد از هر مرحله‌ی کاری معنادار (نه هر خط کد)، به ریپازیتوری `git push` بزند. حداقل: قبل از احتمال تمام‌شدن اعتبار/لیمیت، حتماً پوش شده باشد. **قبل از push همیشه `git fetch` بزند و کامیت‌های تازه‌ی طرف مقابل را با `git show --stat` بررسی کند** — روش دقیق برخورد با تعارض در بخش ۰.۴ توضیح داده شده.

مخزن: https://github.com/My3Fake/Planner-plugin

---

## ۰. سیستم کار موازیِ لِین‌بندی‌شده (Lane System)

### ۰.۱ چرا این سیستم لازم شد

از جلسه‌ی ۱۴ به بعد، این پروژه به‌طور هم‌زمان توسط **چند نشستِ مستقلِ هوش مصنوعی** (که از حال هم خبر ندارند و فقط از طریق گیت با هم «حرف می‌زنند») توسعه داده شده. این باعث چند نوع برخورد واقعی و مکرر شده:
- دو نشست دقیقاً یک قابلیت را با نام/معماری متفاوت پیاده کرده‌اند (مثلاً «تنظیم‌پذیرکردن پوشه‌ی گزارش‌ها» دوبار، با فیلدهای `reports.folder` در برابر `reports.folderName`).
- دو نشست هم‌زمان دقیقاً همان کشفِ مستندات را کرده‌اند و هر دو یک ویژگی مشابه («نمای آجندا») ساخته‌اند.
- تعارض‌های مکرر در `PROGRESS.md` (چون همه به انتهای همان بخش لاگ اضافه می‌کنند) و در `main.js` (چون یک فایل تولیدی/باندل‌شده است، نه دستی).

**اصل راه‌حل:** به‌جای این‌که همه از یک صف مشترک کار بردارند (که تضمین می‌کند گاهی دو نفر همزمان یک آیتم را بردارند)، کل کار به **۵ ناحیه‌ی مجزا (لِین)** افراز شده، بر اساس تب‌های اصلی برنامه (که در کد هم تا حد زیادی از هم جدا هستند). هر لِین یک قلمروِ ثابت از فایل‌ها/کامپوننت‌ها دارد و یک صف کارِ مخصوصِ خودش. **این افراز فضایی/بر-اساس-فایل است، نه نوبتی** — قرار است هر ۵ لِین **هم‌زمان و مستقل** کار کنند، نه به‌نوبت.

### ۰.۲ جدول لِین‌ها

| # | نام لِین | تب(های) NAV | مالک فعلی | صف کار (وقتی دستور تازه‌ای نیامده، به همین ترتیب جلو برو) |
|---|----------|--------------|-----------|----------------------------------------------------------|
| ۱ | **تسک‌ها** | `tasks` (لیست/ماتریس/کانبان/زمان‌بندی) | نشستی که «پنجره‌ی تسک» را می‌سازد | ۱) کار ۳: **تفکیک نوع آیتم** (تسک/رویداد/روتین/یادگیری) — فیلد تازه‌ی `itemType` روی مدل تسک + UI انتخاب در `AddTaskModal` (لِین ۳ بعداً از این فیلد برای رنگ/آیکون در تقویم استفاده می‌کند؛ در لاگ صریح اعلام کن که این فیلد ساخته شد تا لِین ۳ خبردار شود) ۲) بهبود مداوم `AddTaskModal`/`EisenhowerBoard`/`KanbanBoard`/`TimelineView` (تست، پاک‌سازی باگ، UX) |
| ۲ | **یادگیری** | `learning` | نشستی که روی «یادگیری» کار می‌کند | ۱) ~~کار ۵: امکان تخصیص یک جلسه‌ی پومودورو به چند مسیر یادگیری همزمان~~ — انجام شد (لاگ ۴.۲، جلسه‌ی ۲؛ سمت UI/مدل‌داده هر دو در همین لِین ساخته شد، چون فقط از `tasks` موجود فیلتر می‌کرد و نیازی به هماهنگی عمیق با لِین ۴ نبود) ۲) تکمیل تست/جلای ترکیب «روتین سطح موضوع» با «سهمیه‌ی سطح مسیر» ۳) هر بهبود دیگر در قلمروی `LearningHub` |
| ۳ | **برنامه‌ریزی و تقویم** | `planning` + `calendar` | نشستی که روی «برنامه‌ریزی»/تقویم کار می‌کند | ۱) کار ۳: نمای «۳روزه» در تقویم ۲) وقتی لِین ۱ فیلد `itemType` را ساخت، از آن برای تفکیک ظاهری در `DayPlannerView`/`WeekHourlyView`/`AgendaView` استفاده کن ۳) بازه‌ی دلخواه برای گزارش‌های Markdown (به‌جای بازه‌های ثابت ۷/۳۰ روزه‌ی فعلی) |
| ۴ | **پومودورو / تناسب‌اندام / مطالعه** | `pomodoro` + `fitness` + `study` | **این نشست (Claude)، از این جلسه به بعد** | ۱) ~~کار ۶: نوار پیشرفت پومودورو در نوار وضعیت Obsidian~~ — انجام شد (لاگ ۴.۴، جلسه‌ی ۱)؛ باقی‌مانده‌ی اختیاری: اجرای واقعیِ منطق پایانِ جلسه (ثبت/چایم/دور بعد) در سطح پلاگین، نه فقط نمایش، برای رفع کامل محدودیت مستندشده ۲) سمت پومودوروی «تخصیص یک جلسه به چند مسیر یادگیری» (هماهنگ با لِین ۲) ۳) *(پیشنهادی، نه خواسته‌ی صریح کاربر)* تناسب‌اندام/مطالعه فعلاً خیلی ساده‌اند نسبت به بقیه‌ی اپ — امکاناتی هم‌تراز با یادگیری (استریک، یادآوری) می‌تواند ارزش اضافه کند |
| ۵ | **داشبورد / یادداشت‌ها / زیرساخت و تنظیمات** | `dashboard` + `notes` + تنظیمات + زیرساخت | **نشست پنجم، از این جلسه به بعد** | ۱) کار ۲: آخرین آیتم باز — **رنگ‌های دسته‌بندی قابل‌تنظیم** (`settings-tab.ts` + `SettingsModal`، همان الگوی فونت/تراکم/پوشه‌ی گزارش که قبلاً اضافه شدند) ۲) کار ۰: تقسیم تدریجی `app.jsx` به ماژول‌های کوچک — **⚠️ فقط با احتیاط شدید و در قدم‌های خیلی کوچک** (مثلاً اول فقط ثابت‌ها/هلسپرهای تاریخ/کامپوننت‌های چارت پایه را به فایل‌های جدا استخراج کن)، چون ۴ لِین دیگر هم‌زمان `app.jsx` را ویرایش می‌کنند — رفکتور بزرگ یک‌جا تضمین می‌کند با همه تعارض بخوری؛ اگر واقعاً نیاز به یک تغییر بزرگ شد، پروتکل شاخه در ۰.۳ را ببین ۳) *(پیشنهادی)* داشبورد/یادداشت‌ها تا الان دست‌نخورده مانده‌اند — بهبودهای کوچک (جستجو در یادداشت‌ها، سفارشی‌سازی داشبورد) |

توابع/کامپوننت‌های **مشترک بین همه‌ی لِین‌ها** (در `app.jsx`) — با احتیاط لمس کن و در لاگ صریح بگو چرا: `isTaskDueOn`, `todayKey`/`dateKeyOf`/`lastNDays`, `QUADRANTS`/`DAYPARTS`/ثابت‌های مشابه, `GlassCard`/`Ic`/`Chip`/`ToggleSwitch`/بقیه‌ی UI primitives، `TaskRow`, `SimpleBarChart`/`SimpleLineChart`, `LifeFlowApp` (پوسته‌ی اصلی — هر prop تازه‌ای که بین کامپوننت‌ها پاس می‌دهی احتمالاً اینجا هم دست می‌خورد), `loadSettings`/`saveSettings`. فایل‌های `src/main.ts` و `src/settings-tab.ts` عملاً قلمروِ لِین ۵ هستند (چون تنظیمات/زیرساخت‌اند) ولی هر لِینی که یک تنظیمِ تازه لازم دارد ممکن است مجبور شود آن‌ها را هم لمس کند — طبیعی است، فقط در لاگ بنویس.

### ۰.۳ قانون شاخه (Branch)

پیش‌فرض: **همه مستقیم روی `main` کامیت می‌کنند** (همان روندی که تا الان جواب داده). شاخه‌ی جدا فقط برای یک حالت: یک تغییرِ بزرگ و پرخطر که قرار است چند فایل/تابعِ مشترک را هم‌زمان جابه‌جا کند (مثل تقسیم‌کردن `app.jsx` به ماژول در لِین ۵). در آن حالت:
1. قبل از شروع، در بخش ۴ (لاگ لِین خودت) یک هشدار بگذار: «از فلان تاریخ تا اطلاع ثانوی روی شاخه‌ی `X` کار می‌کنم چون Y؛ لطفاً لِین‌های دیگر تغییرات خودشان را طبق روال عادی روی `main` ادامه دهند.»
2. شاخه را از `main` بزن (`git checkout -b refactor/...`)، کارت را آنجا کامل و تست کن.
3. وقتی تمام شد، **قبل از merge به `main`**، دوباره `git fetch` بزن و ببین `main` در این‌فاصله چقدر جلو رفته؛ اگر خیلی جلو رفته، شاخه‌ات را با `git rebase main` به‌روز کن (نه برعکس).
4. **⚠️ قانون تازه (از ۷ شهریور ۱۴۰۵ به دستور مستقیم کاربر): قبل از merge نهاییِ هر شاخه به `main`، حتماً باید از خودِ کاربر (نه فقط لِین‌های دیگر) اجازه بگیری.** شاخه را کامل و تست‌شده push کن، بعد در چت به کاربر بگو چه چیزی آماده‌ی merge است و منتظر تأیید صریح او بمان — merge خودکار/بدون‌پرسیدن دیگر مجاز نیست، حتی اگر `main` جلو نرفته باشد و merge فنی بدون تعارض ممکن باشد.
5. merge نهایی (بعد از اجازه) باید تمیز باشد (ترجیحاً fast-forward یا یک merge commit ساده)؛ اگر تعارض زیاد بود، به‌جای زور زدن برای merge یک‌جا، رفکتور را به تکه‌های کوچک‌تر بشکن.

### ۰.۴ پروتکل برخورد با تعارض (push/pull) — خلاصه‌ی تجربه‌شده در جلسات ۱۹/۲۰

هر بار `git push` رد شد چون ریموت جلوتر رفته:
1. **هرگز کورکورانه merge/rebase نکن.** اول `git fetch origin` و `git log --oneline origin/main -N` بزن، بعد برای هر کامیتِ تازه‌ی طرف مقابل `git show --stat <hash>` را ببین.
2. **اگر کاری که طرف مقابل کرده دقیقاً همان چیزی است که تو هم تازه ساختی (تکرار کامل)** → کامیت خودت را کامل کنار بگذار: `git reset --hard origin/main`. تلاش برای merge دو پیاده‌سازیِ متفاوت از یک قابلیت (نام فیلد متفاوت، معماری متفاوت) معمولاً باگ‌های ظریف‌تر از دوباره‌کاری تولید می‌کند.
3. **اگر کاری که طرف مقابل کرده واقعاً مستقل از کار توست** (فایل‌ها/توابع متفاوت — دقیقاً همین برای همین سیستمِ لِین‌بندی طراحی شده تا حالت غالب همین باشد) → `git pull --rebase origin main`. اگر `src/app.jsx` بدون تعارض auto-merge شد، یعنی قلمروها واقعاً از هم جدا بوده‌اند (نشانه‌ی خوب). `main.js` را **هرگز دستی merge نکن** — بعد از حل تعارض در `src/`، همیشه با `npm run build` از نو بسازش. تعارض در `PROGRESS.md` تقریباً همیشه بی‌خطر است (هر دو طرف انتهای فایل را اضافه کرده‌اند)؛ هر دو بخش را نگه‌دار، فقط اگر لازم شد شماره‌ی جلسه/سربرگ را طوری تنظیم کن که با کامیت‌های واقعاً زودتر merge‌شده هم‌خوان بماند.
4. بعد از هر رفع‌تعارض، حتماً `npm run build` + `node --check main.js` + `npx tsc --noEmit` + بررسی کاراکتر خراب انکودینگ را از نو اجرا کن، حتی اگر مطمئنی چیزی نشکسته.

### ۰.۵ ساختار تازه‌ی لاگ (از این جلسه به بعد)

لاگ چندسال‌ه‌ی «کارهای انجام‌شده» (بخش ۳، جلسات ۱ تا ۲۷) دست‌نخورده و به‌ترتیب تاریخی می‌ماند — آن مال قبل از این سیستم است. **از این به بعد، هر لِین لاگِ جلساتِ خودش را در زیربخشِ مخصوصِ خودش در بخش ۴ (پایین همین فایل) می‌نویسد**، با شماره‌گذاریِ محلیِ همان لِین (نه یک شماره‌ی سراسریِ مشترک بین لِین‌ها — چون شماره‌ی سراسری خودش منبع تعارض است). این کار باعث می‌شود وقتی دو لِین هم‌زمان به فایل اضافه می‌کنند، تغییرات‌شان در دو بخش متفاوت از فایل باشد و احتمال auto-merge بدون تعارض بیشتر شود.

### ۰.۶ نقشه‌ی راه بزرگ (۷ شهریور ۱۴۰۵) — مدیریت پروژه، تقسیم کار ۵لِینی، شاخه‌ها و وابستگی‌ها

**⚠️ این زیربخش از این‌جا به بعد منبع اصلی «چه کسی چه کاری می‌کند» است.** در تاریخ بالا، کاربر یک مشخصات جامع و بسیار بزرگ (۱۴۲ قابلیت/بند، در دو اولویت) ارائه داد و مستقیماً خواست که به‌عنوان مدیر پروژه کار را بین ۵ عامل تقسیم کنم. متن کامل و بدون‌کم‌وکاستِ همه‌ی ۱۴۲ بند در **بخش ۶ (پایین همین فایل)** آمده — این زیربخش فقط نقشه‌ی «کی، کجا، به چه ترتیبی» است؛ برای خودِ محتوای هر بند حتماً بخش ۶ را بخوان.

#### تغییر مهم در مدل کاری: شاخه‌های طولانی (نه کامیت مستقیم روی `main`)

تا امروز قانون پیش‌فرض (بخش ۰.۳) «مستقیم روی `main` کامیت کن، شاخه فقط برای تغییرات پرخطر» بود. **از این نقشه‌ی راه به بعد، این قانون برای کار روی مشخصات جامع (بخش ۶) برعکس شده**: به‌دستور مستقیم کاربر، هر لِین باید یک شاخه‌ی طولانی‌مدت مخصوص خودش بسازد و **عمیق و پیوسته** روی آن کار کند (خیلی push بزند، ولی به `main` merge نکند) تا وقتی یک تکه‌ی معنادار و باکیفیت از کار تمام شود. پیش از merge:
1. لِین باید کار خودش را از نظر کیفیت بازبینی کند (دقیقاً همان استانداردهای همیشگی: `npm run build` + `node --check main.js` + `npx tsc --noEmit` + بررسی نبود کاراکتر خراب انکودینگ + تست منطق پیچیده با اسکریپت Node مستقل جدا از باندل، هرجا ممکن است).
2. طبق قانون تازه‌ی بخش ۰.۳.۴ (که همچنان پابرجاست)، **قبل از merge نهایی به `main`، حتماً باید از خودِ کاربر اجازه گرفت** — شاخه را کامل push کن، در چت بگو چه چیزی آماده‌ی merge است، منتظر تأیید صریح بمان.
3. `PROGRESS.md` اما استثنا است: چون بقیه‌ی لِین‌ها و کاربر باید وضعیت کار را **زنده** ببینند، هر لِین باید لاگِ زیربخش خودش (بخش ۴) را به‌طور مرتب (نه فقط در پایان) مستقیماً روی `main` هم به‌روز نگه دارد — حتی وقتی کدِ واقعی هنوز روی شاخه است. یعنی: کد روی شاخه، ولی خلاصه‌ی «تا کجا پیش رفتم» روی `main` (یک کامیت کوچک و مکرر فقط روی `PROGRESS.md`، که طبق ۰.۴ تقریباً همیشه بی‌خطر است).
4. اگر یک لِین از قبل روی یک شاخه‌ی مرتبط کار می‌کرده (مثلاً `lane4/pomodoro-multitrack`, `fix/calendar-interaction-bugs`, `refactor/modal-audit-pass2` که همین الان در ریپازیتوری موجودند) و آن شاخه با آیتم‌های این نقشه‌ی راه هم‌پوشانی دارد، **همان شاخه را ادامه بده**، نیازی به شاخه‌ی تازه نیست — فقط در لاگ خودت بنویس که همان شاخه را برای این کار هم استفاده می‌کنی.

#### جدول تخصیص لِین ↔ شاخه ↔ بخش‌های مشخصات

| لِین | تب‌های قبلی | شاخه‌ی پیشنهادی | مسئولِ کدام بندهای بخش ۶ (اولویت ۱) | بار کاری تقریبی |
|---|---|---|---|---|
| **۱ — تسک‌ها** | `tasks` | `lane1/custom-task-types` | بند ۸ (مدل‌های سفارشی تسک — بزرگ) + آیکون تسک (بخشی از بند ۸) + ادامه‌ی «تفکیک نوع آیتم» قدیمی (بخش ۲، کار ۳) که دقیقاً زیرمجموعه‌ی همین بند است | بزرگ |
| **۲ — یادگیری** | `learning` | `lane2/learning-system-v2` | بندهای ۲۴ تا ۳۰ (تخصیص چندمسیره‌ی پومودورو، ساختار چندلایه، هدف زیربخش، جزئی‌سازی اختیاری، همگام‌سازی فعالیت‌ها، همگام‌سازی افزونه‌های دیگر (فقط طراحی)، نمایش اختصاصی یادگیری) | متوسط–بزرگ |
| **۳ — برنامه‌ریزی و تقویم** | `planning` + `calendar` | ادامه‌ی کار فعلی (یا `lane3/calendar-planning-v2` اگر شاخه‌ی تازه لازم شد) | بندهای ۹ تا ۲۳ (ادغام تقویم/برنامه‌ریزی، برنامه‌ی مستقل روزانه، بخش‌های صبح/ظهر/عصر/شب قابل‌تنظیم، Drag&Drop حرفه‌ای، فهرست تسک کنار برنامه‌ریزی، انتقال دوطرفه، رفع باگ resize، ساخت تسک از فضای خالی، پنل کناری پویا، نماهای تقویم، View سفارشی، نمایش فشرده/متنوع، نمایش حرفه‌ای اطلاعات، رنگ‌بندی گسترده، Zoom) — **بیشترین حجم؛ بخش زیادی (مراحل ۱ تا ۴، طبق لاگ ۴.۳) از قبل انجام شده** | بزرگ‌ترین |
| **۴ — پومودورو/تناسب‌اندام/مطالعه** | `pomodoro` + `fitness` + `study` | ادامه‌ی `lane4/pomodoro-multitrack` (اگر مناسب است) یا `lane4/pomodoro-and-reporting-v2` | بندهای ۳۱ تا ۴۶ (گزارش‌گیری کامل، گزارش خودکار/دستی، گزارش پیشرفت تسک رونددار، گزارش پومودورو، پومودورو به‌عنوان سیستم کامل، اجرای پس‌زمینه، برنامه‌ی چندمرحله‌ای، ذخیره‌ی الگو، اختصاص دقیق، ثبت نتیجه/تمرکز، صدا/آلارم، نوار وضعیت، هماهنگی نوار، پومودورو-در-گزارش، نام فارسی پومودورو) — **⚠️ نکته‌ی هماهنگی:** فایل نمایشِ گزارش (`DailyReportView`) از نظر تاریخی داخل کدِ تبِ «برنامه‌ریزی» است (قلمروِ لِین ۳)؛ وقتی این بندها را پیش می‌بری و لازم شد آن کامپوننت را لمس کنی، در لاگ صریح بگو تا لِین ۳ غافلگیر نشود (دقیقاً طبق روال قبلیِ خودِ این پروژه، مثل جلسه‌ی ۲۸) | بزرگ |
| **۵ — داشبورد/یادداشت‌ها/زیرساخت/تنظیمات** | `dashboard` + `notes` + تنظیمات + زیرساخت | `lane5/foundation-ui-and-routine` | **فاز اول (فوریت بالا):** بندهای ۱ تا ۷ (فارسی‌سازی کامل متن — اعداد فارسی از قبل تا حد زیادی توسط ابتکار ۴.۷ پوشش داده شده، فقط باقیمانده را چک کن؛ بازطراحی/نظم کلی UI؛ رفع باگ عمومی؛ پنجره‌ها **از‌جمله باگِ فوری «خروج از پنجره کل صفحه را می‌شکند»**؛ جایگذاری دکمه‌ها؛ شخصی‌سازی گسترده به‌عنوان یک اصل کلی؛ داشبورد+میانبر). **فاز دوم (بعد از فاز اول):** بندهای ۴۷ تا ۵۰ (روتین، قرآن — فقط طراحی/ثبت اولیه، همگام‌سازی کلی بین سیستم‌ها — عمداً دیرتر چون به پایدار شدن مدل‌داده‌ی بقیه‌ی لِین‌ها وابسته است، اصل «افزودن بدون شلوغ‌کردن UI») | بزرگ (دو فاز) |

**اولویت ۲ (بندهای ۵۱ تا ۱۴۲):** فعلاً کارِ فعالِ هیچ لِینی نیست — طبق خواسته‌ی صریح کاربر («مرحله‌ی هشتم») این‌ها بعد از تکمیل اولویت ۱ بررسی می‌شوند. تنها استثنا: **لِین ۵ می‌تواند در زمان‌های خالی/کم‌کار، این فهرست را مرور کند و در بخش ۶.۲ علامت بزند کدام‌ها به‌نظرش واقعاً ارزش افزودن دارند** (بدون این‌که آن‌ها را واقعاً پیاده کند) — یک triage سبک، نه اجرا.

#### وابستگی‌ها (چه چیزی باید منتظر چه چیزی بماند)

- **بدون وابستگی، همین الان شروع کن (۵ لِین، هم‌زمان):** همه‌ی ۵ لِین می‌توانند فوراً و مستقل روی موج اولِ کارِ خودشان شروع کنند — این نقشه‌ی راه دقیقاً برای همین طراحی شده. هیچ لِینی منتظر لِین دیگر **نمی‌ماند** تا شروع کند.
- **وابستگی نرم ۱ — بند ۸ (مدل تسک سفارشی، لِین ۱) → لِین ۳/۴:** لِین ۳ (نمایش نوع تسک در تقویم) و لِین ۴ (اختصاص پومودورو به نوع تسک سفارشی) در نهایت می‌خواهند به مدل تسک سفارشی لِین ۱ وصل شوند. **لِین ۳/۴ نباید صبر کنند** — روی کارهای مستقل خودشان (که حجم زیادی هم دارند) ادامه بدهند؛ فقط وقتی لِین ۱ اعلام کرد نسخه‌ی اول مدل «پایدار» است (در لاگ ۴.۱)، لِین ۳/۴ برای اتصال عمیق هماهنگ کنند.
- **وابستگی نرم ۲ — بند ۲۴ (تخصیص چندمسیره‌ی پومودورو به یادگیری):** مشترک بین لِین ۲ و ۴ (دقیقاً مثل قبل از این نقشه‌ی راه هم در جدول ۰.۲ علامت خورده بود). سمت مدل‌داده/یادگیری با لِین ۲، سمت UI انتخاب چندتایی در `PomodoroTimerView` با لِین ۴. هرکدام زودتر به یک نقطه‌ی معقول رسید، در لاگ اعلام کند تا دیگری هماهنگ کند؛ هیچ‌کدام منتظر دیگری برای *شروع* کارِ مستقل خودش نمی‌ماند.
- **وابستگی نرم ۳ — گزارش‌گیری (بندهای ۳۱–۳۴، لِین ۴) ↔ مدل‌های داده‌ی لِین ۱/۲/۳:** گزارش‌های عمیق (مثلاً «مقایسه‌ی برنامه و واقعیت» که به مدل تسک سفارشی و سهمیه‌ی یادگیری وابسته‌اند) باید صبر کنند تا آن مدل‌ها حداقل نسخه‌ی اول‌شان پایدار شود. بخش‌های مستقل گزارش (ساختار کلی، گزارش دستی، خروجی Markdown که از قبل تا حدی وجود دارد) را لِین ۴ همین الان جلو ببرد.
- **وابستگی سخت — همگام‌سازی کلی (بند ۴۹، لِین ۵، فاز دوم):** این بند ذاتاً به تثبیت مدل‌داده‌ی همه‌ی لِین‌های دیگر وابسته است؛ لِین ۵ باید این بند را **آخرین چیز** در فاز دوم خودش بگذارد، بعد از این‌که لِین‌های ۱ تا ۴ حداقل یک دور از کار اولویت ۱ خودشان را تمام کرده‌اند. تا آن موقع، لِین ۵ می‌تواند طراحی/یادداشت‌برداری کند ولی کدنویسیِ واقعیِ این بند را نگه دارد.
- **وابستگی سخت — اولویت ۲ (بندهای ۵۱–۱۴۲):** طبق بالا، منتظر تکمیل اولویت ۱ می‌ماند (مرحله‌ی هشتم کاربر).

#### وضعیت (این جدول را هر لِین با هر جلسه‌ی معنادار به‌روز کند — همان ستون آخر)

| لِین | شاخه‌ی فعال | فاز/بند فعلی | وضعیت |
|---|---|---|---|
| ۱ — تسک‌ها | `feature/task-item-type` *(شاخه‌ی از‌قبل‌موجود ادامه داده شد — دقیقاً همان فیلد `itemType` که این بند گسترشش می‌دهد)* | بند ۸ | 🟡 در حال انجام |
| ۲ — یادگیری | `lane2/learning-system-v2` | بندهای ۲۴–۳۰ | 🟡 در حال شروع |
| ۳ — برنامه‌ریزی/تقویم | `fix/calendar-interaction-bugs` (بند ۱۲/۱۵) + `lane3/custom-range-report` | بندهای ۹–۲۳ | 🟡 در حال شروع — این نشست خودتخصیصی گرفت (سابقه‌ی طولانیِ کار روی همین لِین در همین مکالمه، طبق لاگ ۴.۳)؛ شروع با بند ۹ (یکپارچه‌سازیِ تقویم+برنامه‌ریزی) |
| ۴ — پومودورو/تناسب‌اندام/مطالعه | `lane4/pomodoro-and-reporting-v2` | بندهای ۴۰–۴۱ (ثبت نتیجه/تمرکز جلسه) | 🟡 در حال کار |
| ۵ — داشبورد/زیرساخت/تنظیمات | *(هنوز مشخص نشده)* | بندهای ۱–۷ (فاز اول) | 🔲 شروع‌نشده |

#### پروتکل خودتخصیصیِ لِین (برای این‌که «ادامه بده» به‌تنهایی کافی باشد)

اگر یک نشست/چتِ تازه این فایل را می‌خواند و از قبل نمی‌داند کدام لِین است (یعنی کاربر صراحتاً نگفته «تو لِین X هستی» و در همین مکالمه هم سابقه‌ای از کار روی یک لِین مشخص نیست)، این مراحل را دقیقاً به همین ترتیب انجام بده — نیازی به پرسیدن از کاربر نیست:

1. جدول بالا را بخوان. اولین لِینی که هم ستون «شاخه‌ی فعال»‌اش «هنوز مشخص نشده» است و هم ستون «وضعیت»‌اش «شروع‌نشده» است را به ترتیب عددی (۱ سپس ۲ سپس ۳ ...) انتخاب کن.
2. بلافاصله (قبل از هر کارِ دیگری) همان ردیف را در همین جدول به‌روزرسانی کن: ستون «شاخه‌ی فعال» را با نام شاخه‌ی پیشنهادیِ همان لِین (از جدولِ تخصیصِ لِین بالای همین بخش، ستون «شاخه‌ی پیشنهادی») پر کن، و «وضعیت» را به «در حال شروع» تغییر بده.
3. طبق پروتکلِ همیشگیِ بخشِ ۰.۴، `git fetch` بزن و مطمئن شو کسی هم‌زمان جلوتر نرفته، بعد این تغییرِ کوچک را کامیت و فوراً push کن (این کامیت فقط همین جدول است، نه کدِ واقعی — سریع و کم‌ریسک).
4. اگر push رد شد (یعنی هم‌زمان یک نشستِ دیگر هم دقیقاً همین لِین را claim کرده): `git pull`، ببین کدام لِین توسط او claim شده، برو سراغِ اولین لِینِ بعدیِ هنوز-claim‌نشده در جدول، و از مرحله‌ی ۲ برای آن لِین دوباره تلاش کن.
5. بعد از claim موفق، شاخه‌ی خودت را بساز (طبق نام همان ستون) و طبق بخشِ ۰.۶ (تخصیصِ بندها) و بخشِ ۶ (خودِ مشخصات) کارت را شروع کن. در اولین لاگی که در زیربخشِ خودت (بخش ۴) می‌نویسی، صراحتاً بگو «این لِین را خودتخصیصی گرفتم» تا کاربر بداند این تصمیم خودکار بوده، نه دستوریِ او.

اگر همه‌ی ۵ لِین در حالِ‌کار یا تمام‌شده باشند (هیچ‌کدام «شروع‌نشده» نمانده)، به کاربر بگو همه‌ی لِین‌ها claim شده‌اند و بپرس آیا می‌خواهد کمکِ اضافه به یکی از لِین‌های موجود بدهی یا کارِ دیگری (مثلاً یکی از موارد بخشِ ۶.۲) انجام دهی.



---

## ۱. وضعیت فعلی مخزن (قبل از شروع این دور کاری)

نسخه‌ی اولیه‌ای که کاربر آپلود کرد فقط شامل خروجی build شده بود (`main.js` حدود ۲۴۵۰۰ خط، `manifest.json`, `styles.css`) — **پوشه‌ی `src/` در zip وجود نداشت**.

از داخل خودِ `main.js` (که با esbuild بدون minify ساخته شده و کامنت‌های مسیر فایل مبدأ در آن باقی مانده بود)، فایل‌های سورس زیر با موفقیت بازسازی و در `src/` قرار گرفتند:

- `src/main.ts` — کلاس پلاگین (`LifeFlowPlugin`) و View اصلی Obsidian (`LifeFlowView`, `VIEW_TYPE = "lifeflow-view"`). یک ریبون‌آیکون و یک کامند (`open-lifeflow`) ثبت می‌کند و یک ریشه‌ی React داخل ItemView مونت می‌کند.
- `src/react-globals.ts` — React/ReactDOM را روی `window` می‌گذارد تا در باندل قابل دسترس باشند.
- `src/jalali.js` — توابع تقویم جلالی (تبدیل میلادی↔جلالی، گرید ماه و ...)، به شکل `window.Jalali`.
- `src/app.jsx` — **کل اپ در یک فایل تک‌جزئی حدود ۲۷۷۰ خط** (کامپوننت React اصلی، ماتریس آیزنهاور، تسک‌ها، پومودورو، برنامه‌ریزی روزانه و ...). این فایل تنها بازسازی از باندل است؛ ساختار ماژولار اصلی (اگر کاربر قبلاً src جدا داشته) در دسترس نبود.

⚠️ **نکته‌ی مهم:** چون سورس اصلی (قبل از باندل) در دسترس نبود، این بازسازی ممکن است دقیقاً با کدی که سازنده‌ی اصلی نوشته یکی نباشد (مثلاً کامنت‌ها یا فرمت جزئی فرق کند)، ولی از نظر منطق برنامه با `main.js` قبلی یکی است. از این به بعد باید **از روی `src/` کار کرد و rebuild کرد**, نه ویرایش مستقیم `main.js`.

فعلاً **بدون سیستم build** (بدون esbuild config، بدون package.json) پوش شده — چون فقط بازسازی سورس از باندل بود. اولین قدم فنی در صف کارها، برپاسازی build واقعی (esbuild + tsconfig + package.json مطابق پلاگین‌های استاندارد Obsidian) است تا بشود از `src/` واقعاً به `main.js` رسید و تست کرد.

---

## ۲. صف کارها (Backlog) — بر اساس خواسته‌های کاربر

### کار ۰ — زیرساخت توسعه (پیش‌نیاز بقیه‌ی کارها)
- [x] ساخت `package.json`, `tsconfig.json`, `esbuild.config.mjs` استاندارد پلاگین‌های Obsidian (sample plugin template) — تکمیل شده از جلسه‌ی ۲؛ این چک‌باکس تا جلسه‌ی ۱۴ به‌اشتباه تیک‌نخورده مانده بود (تصحیح مستندات، نه کد)
- [ ] تقسیم `app.jsx` تک‌فایلی به ماژول‌های منطقی (components/, features/, store/) تا نگهداری ممکن شود
- [ ] راه‌اندازی یک لایه‌ی state management ساده (چون بخش‌های بعدی به هم وابسته‌اند: تسک، برنامه‌ریزی، پومودورو، یادگیری، گزارش باید یک منبع داده‌ی مشترک داشته باشند)

### کار ۱ — بومی‌سازی ظاهر با فضای Obsidian ✅ (تکمیل شد — جلسه‌ی ۳؛ بازطراحی مودال‌ها در جلسه‌ی ۲۲)
- [x] حذف استایل‌های «شبیه نرم‌افزار مستقل» و استفاده از CSS variables استاندارد Obsidian (`--interactive-accent`, `--background-primary`, `--text-normal` و ...)
- [x] دکمه‌ها، اینپوت‌ها، مودال‌ها با کلاس‌های رایج Obsidian (`.mod-cta` برای دکمه‌های اصلی وصل شد؛ `Modal`/`Setting` رسمیِ Obsidian API هنوز استفاده نشده — این بخش به کار ۲ (تنظیمات) موکول شد چون آنجا `SettingTab` واقعی ساخته می‌شود)
- [x] هماهنگی با تم روشن/تاریک کاربر (نه رنگ‌های ثابت هاردکد شده)
- [x] **جلسه‌ی ۲۲ — بازطراحی بصری همه‌ی پنجره‌های «افزودن» (تسک/کتاب/ویدیو/پادکست/تمرین/موضوع یادگیری/لیست):** طبق درخواست صریح کاربر («متن به‌هم‌ریخته و دکمه‌های بدجا»). جزئیات کامل در یادداشت جلسه‌ی ۲۲.

### کار ۲ — گسترش تنظیمات (Settings)
- [x] یک `PluginSettingTab` رسمی Obsidian ساخته شد (`src/settings-tab.ts`) — بخش‌های: زبان، نمایش/عدم‌نمایش ماتریس آیزنهاور، روشن/خاموش هر تب اصلی (برنامه‌ریزی/تقویم/مطالعه/تناسب‌اندام/یادگیری/پومودورو/یادداشت‌ها)، ۴ تاگل اعلان، کلید API هوش مصنوعی
- [x] پیش‌فرض‌های پنجره‌ی ساخت تسک (ربع ماتریس/اولویت/زمان روز/مدت) — جلسه‌ی ۵
- [x] تنظیمات ظاهری عمیق‌تر (فونت/تراکم/رنگ‌های دسته‌بندی) — «فونت» (جلسه‌ی ۱۵)، «تراکم» (جلسه‌ی ۱۷)، و «رنگ‌های دسته‌بندی» (شاخه‌ی `feature/category-colors-settings`، merge شد) — **کار ۲ به‌طور کامل تمام شد.**
- [x] تنظیمات گزارش‌گیری — جلسه‌ی ۱۴: نام پوشه‌ی خروجی گزارش‌ها (روزانه/هفتگی/ماهانه) قابل‌تنظیم شد
- [x] ذخیره‌ی تمام داده‌های پلاگین (نه فقط تنظیمات — تسک‌ها، پومودورو، بکاپ‌ها، پیکربندی AI و ...) در `data.json` استاندارد پلاگین (`loadData`/`saveData`) — با مهاجرت یک‌باره از API خصوصی قبلی
- [x] هر بخش از UI از همان لایه‌ی مشترک `storage` (که حالا پشت‌صحنه‌اش `data.json` است) می‌خواند/می‌نویسد — چون همه‌ی ۵ کلید ذخیره‌سازی (تسک‌ها، تنظیمات، AI، سینک، بکاپ‌ها) از قبل از یک شیء `storage` مشترک عبور می‌کردند، این تغییر با ویرایش یک نقطه (نه ده‌ها call site) انجام شد

### کار ۳ — بازطراحی بزرگ برنامه‌ریزی (شبیه Google Calendar / TaskNotes)
**🔎 کشف مهم جلسه‌ی ۲۰:** تا این جلسه، چک‌لیست این کار همه‌جا «انجام‌نشده» علامت خورده بود (چون هیچ جلسه‌ای مستقیم روی آن کار نکرده بود)، ولی بررسی دقیق‌تر کد نشان داد نمای «روز» (`DayPlannerView`) از قبل (از همان نسخه‌ی اولیه‌ی اپ، قبل از جلسه‌ی ۱) به‌طور شگفت‌انگیزی کامل بود: درگ‌ودراپ برای زمان‌بندی، تغییرِ‌اندازه با کشیدن (resize)، و یک نوار «سبد تسک‌های زمان‌بندی‌نشده» بالای جدول ساعتی — همه از قبل کار می‌کردند. چک‌لیست زیر با این واقعیت اصلاح شد؛ **این تصحیح مستندات است، نه کد جدید** (به‌جز آیتم آخر که در همین جلسه‌ی ۲۰ واقعاً اضافه شد).
- [x] نمای تقویمی با درگ‌ودراپ برای زمان‌بندی *تسک‌های موجود* روی جدول — از قبل بوده (`DayPlannerView`: `dropAt`/`startMove`). **به‌روزرسانی جلسه‌ی ۲۱:** کلیک روی یک بازه‌ی *خالی* از جدول ساعتی حالا مستقیماً پنجره‌ی «تسک جدید» را با همان زمان از‌پیش‌پرشده باز می‌کند — یعنی این محدودیت هم عملاً بسته شد (نه دقیقاً با «کشیدن»/drag، بلکه با کلیک؛ توضیح در یادداشت جلسه‌ی ۲۱).
- [x] پنل «سبد تسک‌های زمان‌بندی‌نشده» — از قبل بوده (`DayPlannerView`، نوار چیپ‌های قابل‌درگ بالای جدول ساعتی)؛ ⚠️ طراحی کاربر «کنار» تایم‌لاین را توصیف کرده بود (شبیه ستون کناری)، پیاده‌سازی فعلی «بالای» تایم‌لاین است (یک نوار افقی) — تفاوت چیدمان، نه قابلیت؛ اگر چیدمان کناری مهم است باید در یک جلسه‌ی جدا بازطراحی شود.
- [x] تغییر اندازه (resize) با کشیدن برای تغییر مدت‌زمان — از قبل بوده (`DayPlannerView`: `startResize`)
- [ ] تفکیک نوع آیتم: تسک / رویداد / روتین / یادگیری — هنوز نه؛ فعلاً همه‌چیز یک «تسک» با `progressType`/`recurrence` است، هیچ فیلد `itemType` جدایی وجود ندارد (رویداد و روتین به‌شکل تسک‌های عادی/تکرارشونده نمایش داده می‌شوند، نه یک نوع بصری/رفتاری جدا)
- [x] ویوهای متعدد — روزانه (ساعتی، تفصیلی)، هفتگی (نمای مینی‌کارت، نه ساعتی)، ماهانه، سالانه از قبل بودند؛ **نمای فهرستی/آجندا در جلسه‌ی ۲۰ اضافه شد**. **نمای هفتگیِ ساعتی در جلسه‌ی ۲۷ اضافه شد.** **تصحیح مستندات:** این چک‌لیست تا همین‌جا اشتباهاً «۳روزه هنوز نیست» نوشته بود؛ لِین ۳ در «مرحله‌ی ۳» (بخش ۴.۳) نمای ۳روزه را کامل ساخته (سوییچر view، `WeekHourlyView` با `dayCount` عمومی، ناوبری ۳روزه‌ای) — همه‌ی ویوهای این آیتم تکمیل شده‌اند.

**نتیجه‌گیری کار ۳ (تصحیح‌شده):** با کارِ لِین ۳ (مراحل ۱ تا ۴: خط «اکنون»، درگ‌ودراپ/تغییرِاندازه در نمای هفتگی، نمای ۳روزه، نمایش تسک‌های بی‌زمان)، تنها «تفکیک نوع آیتم (تسک/رویداد/روتین)» و (کم‌اهمیت‌تر) «بازآرایی سبد تسک‌های زمان‌بندی‌نشده به ستون کناری» از قلم‌های اصلی این کار باقی مانده‌اند؛ چند یافته‌ی ظاهریِ کوچک (کلاس‌های `cursor-grab`/`cursor-ns-resize`/`select-none` کامپایل‌نشده در `styles.css`، فشردگیِ سوییچرِ ۶دکمه‌ای روی صفحه‌ی خیلی باریک) برای «مرحله‌ی ۶: بازبینی ظاهری» ثبت شده‌اند.

### کار ۴ — گزارش‌گیری پیشرفته
- [x] گزارش‌های تجمیعی — روزانه (جلسه‌ی ۱۰)، هفتگی (جلسه‌ی ۱۱)، و ماهانه (جلسه‌ی ۱۳، بازه‌ی غلتان ۳۰ روزه) — هر سه پیاده شدند
- [x] نمودارها — جلسه‌ی ۱۲: تکمیل تسک به تفکیک ربع آیزنهاور، زمان تمرکز پومودورو به تفکیک ربع آیزنهاور، روند یادگیری (هر سه برای بازه‌ی ۷ روز اخیر)
- [x] خروجی/صدور گزارش (Markdown داخل ولت) — جلسه‌ی ۱۰ («امروز»)، جلسه‌ی ۱۱ («۷ روز اخیر»)، جلسه‌ی ۱۳ («۳۰ روز اخیر»)

**✅ کار ۴ از نظر چک‌لیست اصلی این فایل کامل شد** (سه‌گانه‌ی روزانه/هفتگی/ماهانه + نمودارها + خروجی Markdown). بهبودهای آتی ممکن (نه بخشی از چک‌لیست فعلی): بازه‌ی دلخواه به‌جای بازه‌های ثابت، انتخاب تقویم شمسی برای بازه‌ها، نمودار جدا به‌ازای هر مسیر یادگیری.

### کار ۵ — بازطراحی «یادگیری/تسک‌های روند‌دار»
بر اساس مثال کاربر (حفظ قرآن)، مدل داده‌ی پیشنهادی:
- **پروژه‌ی یادگیری** (مثلاً «حفظ قرآن») شامل چند **مسیر/زیرکار** (حفظ جدید، مرور دور، مرور نزدیک، تثبیت) — هرکدام:
  - واحد اندازه‌گیری خودش (صفحه، مورد، دقیقه و ...)
  - [x] بازه‌ی تکرار مخصوص به خودش (هرروز / روزهای خاص هفته / پیروی از روتین کلی موضوع) — جلسه‌ی ۶، `recurrenceOverride`
  - [x] سهمیه‌ی دوره‌ای (مثلاً روزی ۳ صفحه مرور دور) — جلسه‌ی ۶، `quotaPerPeriod`
  - [x] محدوده/بازه‌ی فعلی (مثلاً مرور دور: صفحه ۱ تا ۲۰) — جلسه‌ی ۶، `rangeLabel` (فعلاً یک برچسب توصیفی ساده، نه محاسبه‌ی خودکار بازه)
  - [x] **لاگ پیشرفت شبیه commit گیت**: هر ثبت = مقدار انجام‌شده + توضیح متنی + تاریخ — جلسه‌ی ۶، `task.progressLog`؛ این برای *همه‌ی* تسک‌های روند‌دار کار می‌کند، نه فقط یادگیری (چون `ProgressiveTaskBar` مشترک است)
- [x] در بخش تسک‌ها، این‌ها به‌عنوان تسک روند‌دار (progressive) نمایش داده می‌شوند: نه صفر/صد بلکه نوار پیشرفت که با هر لاگ جلو می‌رود — این از قبل (قبل از جلسه‌ی ۶) کار می‌کرد؛ آنچه جلسه‌ی ۶ اضافه کرد فقط لاگِ باتوضیح بود
- [ ] در پومودورو و برنامه‌ریزی: امکان انتخاب یک یا چند مسیر یادگیری برای یک بلوک زمانی و تعیین سهم هرکدام از آن بلوک — هنوز نه
- [ ] امکان تعریف «روتین یادگیری روزانه» در سطح کل موضوع (نه فقط هر مسیر) — تا حدی از قبل هست (`LearningRoutineEditor` سطح موضوع)؛ ترکیب آن با سهمیه‌های جدید سطح مسیر هنوز کامل تست/جلا داده نشده
- [x] بخش جدید «گزارش روزانه» در برنامه‌ریزی با بدهی/بستانکاری روزانه — جلسه‌ی ۷ (نسخه‌ی اول، جزئیات محدودیت‌ها را در یادداشت جلسه‌ی ۷ ببینید)
- [x] آمار تطابق برنامه با عملکرد واقعی (planned vs actual) — جلسه‌ی ۱۶: بخش «تطابق برنامه با عملکرد واقعی» در گزارش روزانه (۷ روز اخیر، تعداد تسک و دقیقه‌ی پومودورو)
- [x] مدیریت مسیر: توقف موقت (pause)، آرشیو (archive)، یادداشت آزاد، کپی/کلون، مرتب‌سازی — جلسه‌ی ۲۵
- [x] streak، نمودار حرارتی فعالیت، جشن تکمیل هدف («افزایش هدف» برای مسیرهای مکرر)، خروجی Markdown یک موضوع — جلسه‌ی ۲۶

### کار ۶ — پومودورو حرفه‌ای
- [x] چرخه‌ی کار/استراحت کوتاه/استراحت بلند قابل تنظیم، شمارش خودکار دور — از قبل کار می‌کرد (`cyclesUntilLong`)، فقط در این چک‌لیست علامت نخورده بود؛ در جلسه‌ی ۸ تأیید و تیک خورد
- [x] اتصال پومودورو به تسک/مسیر یادگیری انتخابی (طبق کار ۵) — از قبل کار می‌کرد (دراپ‌داون انتخاب تسک شامل همه‌ی تسک‌های روند‌دار/مسیرهای یادگیری می‌شود، چون هر مسیر خودش یک تسک است)؛ آنچه هنوز نیست تقسیم *یک* جلسه بین *چند* مسیر همزمان است (در یادداشت‌های کار ۵ هم ثبت شده)
- [x] لاگ تاریخچه‌ی جلسات پومودورو برای گزارش‌گیری (کار ۴) — از قبل کار می‌کرد (`PomodoroReportView` + `pomodoroStatsFor`)
- [x] صدای پایان تایمر — جلسه‌ی ۸، `playPomodoroChime` (Web Audio API، بدون فایل صوتی خارجی، دو نُت متفاوت برای پایان «کار» در برابر «استراحت»)؛ با تاگل «صدای پایان تایمر» در تنظیمات پومودورو قابل خاموش‌کردن است (فیلد `settings.sound` که در داده از قبل وجود داشت ولی به هیچ UI/منطقی وصل نبود)
- [x] شروع خودکار جلسه‌ی بعدی — جلسه‌ی ۸، تاگل «شروع خودکار دور بعد» (فیلد `settings.autoStartNext` که مشابه `sound` از قبل در داده بود ولی بی‌اثر بود)
- [x] حالت Do-Not-Disturb — جلسه‌ی ۹ (سرکوب موقت یادآوری تسک/مهلت یادگیری/خلاصه‌ی صبحگاهی حین یک دور «کار» فعال پومودورو)
- [x] نوار پیشرفت در ریبون/نوار وضعیت Obsidian — انجام شد در سیستم لِین‌بندی، لِین ۴، جلسه‌ی ۱ (بخش ۴.۴). **راه‌حل نهایی، بدون بالابردن کامل state تایمر:** یک عکس‌لحظه‌ای مشتق‌پذیر بر اساس زمان سپری‌شده (`pomodoro.activeTimer`) که `PomodoroTimerView` فقط در نقاط گذار می‌نویسد؛ `main.ts` مستقیماً از `data.json` می‌خواند و «الان چند ثانیه مانده» را با ریاضیِ زمان سپری‌شده حساب می‌کند — کار می‌کند حتی وقتی تب پومودورو بسته است. محدودیت باقی‌مانده (مستند در ۴.۴): اگر تایمر درست وقتی تب بسته است تمام شود، رخدادهای پایانِ جلسه (ثبت/چایم/دور بعد) تا بازکردن دوباره‌ی تب اجرا نمی‌شوند.

### کار ۷ — سازگاری کامل با اندروید ⚠️ **در حال انجام — دستور مستقیم کاربر، اولویت اول (جلسه‌ی ۲۸)**

`manifest.json` از قبل `isDesktopOnly: false` دارد و `src/main.ts` از قبل کاملاً روی API‌های vault-adapter استاندارد Obsidian است (بدون هیچ `require('fs')`/Node/Electron-only API) — یعنی زیرساخت پلاگین از قبل موبایل-سازگار بود. مشکل واقعی در تعامل‌های **لمسی** داخل `app.jsx` بود، نه در زیرساخت. این کار **چندجلسه‌ای** است؛ چک‌لیست زیر با هر جلسه به‌روزرسانی می‌شود.

**یافته‌های جلسه‌ی ۲۸ (بازبینی کامل کد برای الگوهای ناسازگار با لمس):**
- [x] **مهم‌ترین یافته:** سه تعامل «درگ» در `DayPlannerView` (نمای روزانه‌ی ساعتی برنامه‌ریزی) — (۱) کشیدن یک تسک برنامه‌ریزی‌نشده از «سبد» روی جدول ساعتی برای زمان‌بندی آن، (۲) جابه‌جایی یک تسک زمان‌بندی‌شده با کشیدن، (۳) تغییر مدت با کشیدن لبه‌ی پایین — همه‌شان فقط با `onMouseDown`+`window.mousemove/mouseup` (برای ۲ و ۳) یا HTML5 Drag-and-Drop خام (`draggable`/`onDragStart`/`onDragOver`/`onDrop`, برای ۱) پیاده شده بودند. **هیچ‌کدام این‌ها روی اندروید کار نمی‌کنند** — نه HTML5 DnD (که هیچ مرورگر موبایلی پشتیبانی معناداری از آن ندارد) و نه رویدادهای `mouse*` (که از لمس اصلاً fire نمی‌شوند). رفع شد: هر سه با **Pointer Events** (`onPointerDown` + `window.addEventListener("pointermove"/"pointerup"/"pointercancel")`) بازنویسی شدند، که موس/لمس/قلم را یکسان پوشش می‌دهند. برای تعامل شماره‌ی ۱ (که هدف رهاسازی‌اش قبلاً از طریق native drop-target مشخص می‌شد)، یک تابع `minutesFromClientY` نوشته شد که با گرفتن `getBoundingClientRect()` جدول (از طریق `gridRef`)، معکوسِ دقیق تابعِ از‌قبل‌موجود `topFor` است — یعنی موقعیت لحظه‌ای انگشت/موس را مستقیماً به دقیقه تبدیل می‌کند، بدون نیاز به drop-targetهای HTML5. یک آستانه‌ی حرکت (۸ پیکسل، با `Math.hypot`) هم اضافه شد تا یک **تپ ساده** روی تسکِ داخل سبد (بدون حرکت واقعی) به‌جای زمان‌بندی‌شدنِ اشتباه در ساعت ۶ صبح (چون مختصات تپ روی خودِ سبد است، نه روی جدول)، به همان مسیر `onClick` قبلی (باز شدن پنجره‌ی ویرایش) بیفتد — این خیلی مهم بود چون بدون این آستانه، هر تپ ساده روی تسک برنامه‌ریزی‌نشده بی‌صدا آن را در ۶ صبح زمان‌بندی می‌کرد.
- [x] **باگ دوم:** دکمه‌ی حذفِ آیتم چک‌لیست در تب «یادداشت‌ها» (`opacity-0 group-hover:opacity-100`) کاملاً نامرئی بود مگر با هاور — یعنی روی اندروید (بدون حالت هاور واقعی) این دکمه هیچ‌وقت دیده/لمس نمی‌شد؛ عملاً حذف آیتم از چک‌لیست روی موبایل ممکن نبود. به `opacity-60 hover:opacity-100` تغییر کرد (همان الگویی که یک نمونه‌ی مشابه دیگر، خط ۹۲۲، از قبل درست پیاده کرده بود) — همیشه قابل مشاهده/لمس، فقط روی هاور کمی روشن‌تر می‌شود.
- [x] بازبینی شد و **مشکلی نداشتند** (نمونه‌های خوبِ از قبل موجود که به آن‌ها دست نخورد): `KanbanBoard` (جابه‌جایی وضعیت با دکمه، نه درگ)، `TimelineView` (زمان‌بندی با `<input type="time">` بومی، نه درگ)، منطقه‌ی «آپلود فایل بکاپ» (هم `onDrop` هم `onClick` برای باز کردن File Picker بومی — روی لمس از طریق کلیک/تپ کار می‌کند)، `WeekHourlyView` (طبق کامیت لِین ۳، از ابتدا «فقط کلیک، بدون درگ» ساخته شده). هیچ `onContextMenu` (راست‌کلیک بدون معادل لمسی) در کل فایل پیدا نشد.
- [ ] **بازبینی بصری واقعی روی یک دستگاه/شبیه‌ساز اندروید واقعی هنوز انجام نشده** (این محیط توسعه دسترسی به آن را ندارد) — رفع‌های بالا بر اساس دانش استاندارد Pointer Events API و بررسی دقیق کد انجام شدند و با اسکریپت‌های Node مستقل روی ریاضیاتشان تست شدند، ولی تست لمسی واقعی (آیا واقعا حس خوبی دارد، آیا جایی تأخیر/لرزش دارد) نیاز به یک Obsidian موبایل واقعی دارد.
- [ ] **اندازه‌ی نقاط لمسی (touch target size)** — بررسی دقیق‌تر در جلسه‌ی بعدی این ابتکار (بعد از merge با ۱۲ کامیت لِین‌های دیگر) انجام شد؛ این‌ها **فقط یافته‌اند، هنوز عمداً تغییر داده نشده‌اند** چون در قلمروی چند لِین مختلف پخش‌اند و بدون تست بصری واقعی، بزرگ‌کردن حدسی‌شان ریسک به‌هم‌ریختن چیدمان‌های فشرده (flex/gap) را دارد:
- [x] **بیشترین اولویت — رفع شد (جلسه‌ی سوم این ابتکار):** دکمه‌ی تیک زدن آیتم چک‌لیست در تب «یادداشت‌ها» (`w-4 h-4`) و دکمه‌ی مشابه در `GroupCard` (چک‌کردن تسک‌های یک گروه در بخش‌های زمان روز، `DaypartSection` — **قلمروی لِین ۳**، طبق جدول بخش ۰.۲، مثل `DayPlannerView`). هر دو با یک روش امن رفع شدند: به‌جای بزرگ‌کردن اندازه‌ی بصری (که می‌توانست چیدمان `flex gap-2` را به‌هم بریزد)، هرکدام در یک دکمه‌ی بیرونی با کلاس تازه‌ی `lifeflow-tap-pad` پیچیده شدند و چک‌باکس بصری اصلی (همان اندازه‌ی قبلی، ۱۶px) به یک `<span>` غیرفعال داخل آن منتقل شد. **یافته‌ی فنی مهم حین این کار:** `styles.css` یک باندل Tailwind از‌پیش‌تولیدشده و ثابت است (هیچ مرحله‌ی build/PostCSS برای Tailwind وجود ندارد — `esbuild.config.mjs` فقط `src/main.ts` را باندل می‌کند)، پس کلاس Tailwind تازه‌ای که قبلاً هیچ‌جای کد استفاده نشده (مثل `-m-1.5` که برای این تکنیک لازم بود) هیچ اثر واقعی نداشت (چک شد با `grep`/Python روی خودِ `styles.css` — نبود). به‌جایش یک کلاس CSS دستیِ ساده (`padding: 6px; margin: -6px;`) مستقیم به `styles.css` اضافه شد که مستقل از Tailwind کار می‌کند. **این یک تله‌ی عمومی برای هر لِینی است که بخواهد یک کلاس Tailwind «جدید» (که قبلاً جای دیگری استفاده نشده) به `app.jsx` اضافه کند — قبل از اعتماد به چنین کلاسی، باید در `styles.css` هم وجودش تأیید شود.**
- [ ] بقیه‌ی موارد اولویت متوسط عمداً دست‌نخورده ماندند — در قلمروی چند لِین دیگر پخش‌اند و ۲۴px آن‌قدرها کوچک نیست که بدون تست بصری واقعی توجیه‌کننده‌ی ریسک دست‌زدن به کد چند لِین دیگر در یک جلسه باشد:
  - **اولویت متوسط:** چند دکمه‌ی `w-6 h-6` (۲۴×۲۴px) پرکاربرد: دکمه‌های جابه‌جایی/حذف در `KanbanBoard`، دکمه‌ی حذف تمرین در تب ورزش، و دکمه‌های `+`/`−` استپر سهمیه‌ی یادگیری (که در همین اواخر توسط یک لِین دیگر اضافه شد، ۲ نسخه در کد). ۲۴px کمتر از حداقل توصیه‌شده‌ی معمول (۴۴px اپل / ۴۸dp گوگل) است ولی یک قرارداد رایج وب هم هست؛ قضاوت قطعی نیاز به دیدن چیدمان واقعی دارد.
  - **قبلاً در جلسه‌ی ۲۸ رفع شد (برای یادآوری، نه یک یافته‌ی تازه):** دستگیره‌ی تغییر-اندازه در `DayPlannerView` تنها ۱۰px (`h-2.5`) بلندی داشت؛ منطقِ درگ خودش با Pointer Events رفع شد، ولی خودِ اندازه‌ی بصری این دستگیره تغییر نکرد (بزرگ‌کردنش ریسک تداخل با ژست «جابه‌جایی» روی تسک‌های خیلی کوتاه دارد — نیاز به یک تصمیم طراحی جداگانه، نه یک تغییر ساده).
  - **توصیه برای جلسه‌ی بعد:** به‌جای بزرگ‌کردن کورکورانه‌ی اندازه‌ی بصری (که چیدمان‌های فشرده را به‌هم می‌ریزد)، یک راه امن‌تر بررسی شود: بزرگ‌کردن فقط ناحیه‌ی لمسیِ نامرئی (مثلاً با `padding`/`::before` گسترده‌تر از ظاهر بصری) بدون تغییر اندازه‌ی دیداری — ولی این هم نیاز به تست بصری واقعی برای اطمینان از عدم همپوشانی با دکمه‌های مجاور دارد.
- [x] **بازبینی پس از merge با ۱۲ کامیت لِین‌های دیگر (جلسه‌ی بعدی این ابتکار):** بعد از `git pull` یک کار بزرگ از لِین ۳ (`WeekHourlyView`: افزودن move/resize با درگ) و بازسازی کامل معماری مودال توسط لِین ۱ (روی `Obsidian` `Modal` بومی) وارد شد. هر دو بررسی شدند: لِین ۳ **خودش** از الگوی Pointer Events همین کار ۷ (حتی با کامنت صریح در کد که به آن اشاره می‌کند) استفاده کرده و `pointercancel`/`touchAction:"none"` را هم درست مدیریت کرده — نیازی به رفع نبود. بازسازی مودال لِین ۱ روی `Modal` بومی Obsidian هم ذاتاً یک بهبود برای موبایل است (نه ریگرسیون)، چون آن کلاس از قبل توسط خودِ Obsidian برای هر دو پلتفرم تست شده. یک `grep` سراسری تازه (`onMouseDown`, `draggable`, `onDragStart/Over/Drop`, `onContextMenu`, `opacity-0`+hover, `mousemove`/`mouseup`/`onMouseUp`/`onMouseMove`) روی کل فایل تکرار شد — **هیچ نمونه‌ی تازه‌ای پیدا نشد** (تنها `onDragOver` باقیمانده همان ناحیه‌ی آپلود بکاپ است که از قبل fallback کلیکی دارد). ویژگی progressive-disclosure پنجره‌ی تسک (جلسه‌ی ۱۶) هم سالم ماند؛ حتی توسط یک لِین دیگر با شرط‌های تازه (`prefillTime`, `defaults.advancedOpenByDefault`) گسترش داده شده. `npm run build` + `node --check main.js` + `npx tsc --noEmit` بعد از merge دوباره تأیید شدند (همان ۳ خطای قدیمی/بی‌ربط).
- [ ] **⚠️ نکته‌ی مهم برای جلسات بعد (به‌خصوص کار ۲/تراکم):** حالت «فشرده» (compact density، از جلسه‌ی ۱۷) فاصله‌گذاری را عمداً کم می‌کند — این می‌تواند اندازه‌ی مؤثر نقاط لمسی را روی موبایل حتی کوچک‌تر کند. شاید لازم باشد در آینده حالت فشرده روی صفحه‌های باریک/لمسی به‌طور خودکار غیرفعال شود یا هشدار داده شود؛ فعلاً دست‌نخورده ماند چون نیاز به تصمیم طراحی جداگانه دارد.
- [x] **بررسی شد و مشکلی نیست:** اسکرول افقی در `WeekHourlyView` (نمای هفتگی، `overflow-x-auto` با `minWidth: 40 + dayCount*85`) عمدی است، نه باگ — دقیقاً همان الگویی که اپ تقویم گوگل هم روی موبایل استفاده می‌کند، و اسکرول افقی با انگشت روی اندروید کاملاً طبیعی/بومی کار می‌کند (برخلاف درگ‌های موس-فقطی که در بالا رفع شدند). مهم‌تر، از قبل نمای «۳روزه» (`dayCount: 3`) هم وجود دارد که عرض حداقلش (۴۰+۳×۸۵=۲۹۵px) بدون هیچ اسکرولی در عرض یک گوشی معمولی جا می‌شود — یعنی کاربری که اسکرول افقی را دوست ندارد، از قبل یک جایگزین دارد. نیازی به تغییر نیست.
- [ ] **وضعیت صادقانه‌ی فعلی این ابتکار:** موارد قطعی و قابل‌تشخیص با بررسی کد (بدون نیاز به دستگاه واقعی) همه رفع/بررسی شدند: درگ‌های موس-فقط/HTML5-DnD، دکمه‌ی نامرئی-تا-هاور، دو مورد اولویت‌دار اندازه‌ی لمسی، و اسکرول افقی هفتگی. آنچه باقی مانده یا (الف) نیاز به یک **دستگاه/شبیه‌ساز اندروید واقعی** دارد که این محیط توسعه به آن دسترسی ندارد (تست حسیِ واقعیِ لمس، بقیه‌ی دکمه‌های ۲۴px)، یا (ب) پخش‌شده در قلمروی چند لِین دیگر و توجیه‌کننده‌ی ریسک دست‌زدن هم‌زمان به کد چند لِین بدون تست بصری نیست. جلسه‌ی بعدی این ابتکار باید یا منتظر دسترسی به دستگاه واقعی/تصمیم کاربر بماند، یا به سراغ یکی از کارهای دیگر صف (کار ۲/۳/۵/۶) برود.

**⚠️ نکته‌ی هماهنگی لِین (مهم):** `DayPlannerView` قلمروی **لِین ۳ (برنامه‌ریزی و تقویم)** است طبق جدول بخش ۰.۲، نه یک لِین مشخص برای «سازگاری اندروید» (که در تقسیم‌بندی ۵گانه‌ی اصلی وجود ندارد، چون قبل از این جلسه به‌عنوان یک اولویت جداگانه مطرح نشده بود). این جلسه طبق **دستور مستقیم کاربر** («اولین کار تو این است که پلاگین را کاملاً با اندروید سازگار کنی») و به‌عنوان یک ابتکار متقاطع (cross-lane) وارد این تابع شد. اگر لِین ۳ هم‌زمان روی `DayPlannerView` کار می‌کند، لطفاً قبل از push طبق پروتکل بخش ۰.۴ عمل شود (`git fetch` + `git show --stat` برای کامیت‌های تازه). لاگ کامل این جلسه در بخش ۴.۶ (زیربخش تازه‌ی «ابتکار متقاطع سازگاری اندروید») است.

---

## ۳. کارهای انجام‌شده (این بخش بعد از هر جلسه به‌روزرسانی می‌شود)

### جلسه‌ی ۱ (۲۴ مرداد/آگوست ۲۰۲۶)
- بازسازی `src/` از روی `main.js` باندل‌شده (main.ts, react-globals.ts, jalali.js, app.jsx)
- ایجاد ریپازیتوری اولیه و پوش نسخه‌ی import‌شده
- ایجاد این فایل (`PROGRESS.md`) با نقشه‌راه کامل ۷گانه (۰ تا ۶)
- **هیچ‌کدام از تغییرات فیچرِ خواسته‌شده (کارهای ۰ تا ۶) هنوز پیاده‌سازی نشده‌اند** — این جلسه فقط شامل import و برنامه‌ریزی بود.

### جلسه‌ی ۲ (۲۵–۲۶ مرداد/آگوست ۲۰۲۶) — کار ۰ کامل شد + شروع کار ۱

**کار ۰ (زیرساخت) — تکمیل شد:**
- `package.json`, `tsconfig.json`, `esbuild.config.mjs` استاندارد پلاگین‌های Obsidian ساخته شد (الگوی sample plugin: esbuild + external `obsidian`/`electron`/CodeMirror، فرمت cjs، هدف es2020).
- `src/jalali.js` از فرمت CommonJS باندل‌شده (`__commonJS` wrapper) به یک ماژول ES تمیز با `export {...}` تبدیل شد.
- `src/react-globals.ts` حذف شد (دیگر لازم نیست چون React/ReactDOM حالا با `import` مستقیم در `app.jsx`/`main.ts` استفاده می‌شوند، نه از طریق `window`).
- `src/main.ts` کاملاً بازنویسی شد: یک `LifeFlowPlugin` استاندارد (extends `Plugin`) با `LifeFlowView` (extends `ItemView`) که React را با `ReactDOMClient.createRoot` داخل View مونت می‌کند؛ ریبون‌آیکون + کامند `open-lifeflow` ثبت می‌شود.
- `src/app.jsx`: بالای فایل به `import React, {...} from "react"` و `import * as Jalali from "./jalali.js"` تغییر کرد؛ در انتها دیگر خودش را روی `window` mount نمی‌کند بلکه `export default LifeFlowApp` می‌کند (مونت‌کردن حالا وظیفه‌ی `main.ts`/`LifeFlowView` است).
- `npm install` انجام شد و `npm run build` (یعنی `node esbuild.config.mjs production`) با موفقیت یک `main.js` معتبر (~328KB، minified) از روی `src/` تولید کرد. **این اولین بار است که پروژه یک build pipeline واقعی و کارکننده دارد** — قبلاً `main.js` فقط یک فایل دستی/بازسازی‌شده بود.
- ✅ نتیجه: از این پس هر تغییری باید در `src/` اعمال شود و با `npm run build` بازتولید شود؛ **هرگز مستقیم `main.js` را ویرایش نکن.**

**کار ۱ (بومی‌سازی ظاهر با فضای Obsidian) — شروع شد (تکمیل نشده):**

بررسی دقیق کد نشان داد که مشکل "شبیه نرم‌افزار مستقل بودن" دقیقاً از این موارد ناشی می‌شد:
1. یک بلوک `<style>` تعبیه‌شده حدود ۱۱۰ خطی داخل کامپوننت اصلی با یک تم «شیشه‌ی مایع کهکشانی» کاملاً مستقل (گرادیان‌ها، `backdrop-filter`، سایه‌های نئون بنفش/صورتی) به‌همراه یک سیستم روشن/تاریک خودش (`data-theme` روی ریشه‌ی اپ) که کاملاً مستقل از تم خود Obsidian کار می‌کرد.
2. دو کامپوننت پس‌زمینه‌ی تزئینی (`GalaxyBackground` با انیمیشن ستاره روی canvas، و `LightBackground`) که کل صفحه را با گرادیان/نبولا پر می‌کردند — چیزی که در یک پلاگین جاسازی‌شده در Obsidian اصلاً معنا ندارد.
3. حدود ۶۵۰+ کلاس Tailwind رنگی (`text-slate-*`, `text-white`, `bg-white/*`, `border-white/*`, `text-rose/fuchsia/cyan/pink-*` و ...) که رنگ‌های ثابت (نه متغیرهای CSS خود Obsidian) تولید می‌کردند، از یک فایل Tailwind کامپایل‌شده (۳۰KB) در `styles.css`.
4. حدود ۱۰ نقطه‌ی مشخص («chrome» — نوار کناری، دکمه‌ی افزودن تسک، FAB، سوییچرهای تب/ویو، پیکر تم تنظیمات، منطقه‌ی درگ‌ودراپ بکاپ، هایلایت «امروز» در تقویم) با `style={{...}}` این‌لاین و رنگ‌های هگز ثابت (`#C026D3`, `#DB2777`, `#EAB4F2`, `#94a3b8`) مستقل از تنظیمات تم React انجام می‌شد.
5. `min-h-screen` روی ریشه‌ی اپ (فرض می‌کرد کل viewport مال خودش است — که در یک ItemView داخل Obsidian غلط است).

اقدامات انجام‌شده:
- بلوک `<style>` تعبیه‌شده به‌طور کامل بازنویسی شد: فقط انیمیشن‌های حرکتی (بدون رنگ) نگه داشته شدند (ترنزیشن صفحه، پاپ‌شدن مودال، رشد نمودارها)؛ تمام گرادیان‌ها/بلور/نبولا/override های تم روشن حذف شدند.
- `GalaxyBackground` و `LightBackground` به‌طور کامل حذف شدند (و فراخوانی‌شان از JSX برداشته شد) — پس‌زمینه‌ی پلاگین حالا همان پس‌زمینه‌ی Obsidian است.
- `data-theme` از ریشه‌ی اپ حذف شد؛ `min-h-screen`/`overflow-hidden` هم حذف شدند تا اپ داخل پنجره‌ی خودش (نه کل viewport) درست سایز شود.
- کلاس‌های سراسری `glass-panel`/`glass-strong`/`glass-sheen` (که در دهها جا مثل `GlassCard` استفاده می‌شوند) در `styles.css` با متغیرهای CSS خود Obsidian (`--background-secondary`, `--background-modifier-border`) بازتعریف شدند — بدون نیاز به تغییر هر نقطه‌ی استفاده در `app.jsx`.
- یک لایه‌ی override در انتهای `styles.css` اضافه شد (اسکوپ‌شده با `.lifeflow-app-root`) که کلاس‌های رنگی پرتکرار Tailwind (`text-white`, `text-slate-1..6`, `bg-white*`, `border-white*`, `bg-black*`, `text/border/bg-fuchsia/cyan/pink-*` → `--text-accent`, `text/border/bg-rose/red-*` → رنگ خطا) را به متغیرهای Obsidian هدایت می‌کند — یعنی بدون دست‌زدن به صدها `className` در `app.jsx`.
- اینپوت‌ها، مودال (`ModalShell` → کلاس‌های جدید `lf-modal-backdrop`/`lf-modal-sheet`)، و چیپ‌ها (`Chip` → کلاس‌های `lf-chip`/`lf-chip-active`) به سیستم رنگ Obsidian وصل شدند.
- ۱۰ نقطه‌ی «chrome» (نوار کناری/فعال، دکمه‌ی افزودن تسک + شعله‌ی استریک، سوییچر ویوهای تسک، FAB، سوییچر تب پروژه‌های یادگیری، منطقه‌ی درگ فایل بکاپ، پیکر تم تنظیمات، هایلایت امروز در تقویم، دکمه‌ی لیست جدید یادداشت) به‌صورت مستقیم ویرایش شدند تا از `var(--interactive-accent)` / `var(--text-accent)` / `var(--text-muted)` / `var(--background-modifier-hover)` به‌جای هگزهای ثابت استفاده کنند.
- Build دوباره اجرا و تأیید شد (main.js معتبر، بدون خطای سینتکس).

**⚠️ کار ۱ هنوز کامل نیست — آنچه باقی مانده:**
- رنگ‌های داده‌محور/دسته‌بندی (نه chrome) عمداً دست‌نخورده باقی ماندند چون تغییرشان معنای بصری را از بین می‌برد: رنگ ۴گانه‌ی ماتریس آیزنهاور (`QUADRANTS`)، رنگ‌های ۴گانه‌ی `dayColor`/`dayGlow` (نمودار دایره‌ای روز)، رنگ نشان‌های گیمیفیکیشن (`BADGES`)، رنگ پیش‌فرض نمودارها (`SimpleBarChart`/`SimpleLineChart`)، و رنگ آیتم‌های جست‌وجوی سراسری. این‌ها بعداً می‌توانند از طریق تنظیمات (کار ۲) قابل‌تنظیم شوند، اما فعلاً به‌عنوان پالت رنگی خودِ پلاگین (مشابه بسیاری از پلاگین‌های حرفه‌ای Obsidian که پالت رنگی مخصوص خود را برای نمودار/دسته‌بندی دارند) نگه داشته شدند.
- برخی دکمه‌های ثانویه/آیکون‌های کوچک هنوز ممکن است رنگ‌های Tailwind خام داشته باشند که در بازبینی این جلسه دیده نشدند؛ یک بازبینی بصری کامل داخل خودِ Obsidian (باز کردن پلاگین و چک هر تب) هنوز انجام نشده — چون این محیط اجرا Obsidian واقعی ندارد. **در جلسه‌ی بعد، در اولین فرصت که پلاگین در Obsidian واقعی تست شد، لیستی از موارد بصری باقی‌مانده تهیه و اصلاح شود.**
- فونت (`Vazirmatn`) هنوز به‌صورت inline در ریشه‌ی اپ ست شده؛ بررسی نشده که آیا باید حذف شود تا از فونت خود Obsidian/تم پیروی کند یا به‌عنوان یک گزینه در تنظیمات (کار ۲) بماند.
- دکمه‌ها هنوز از کلاس‌های بومی Obsidian مثل `.mod-cta` استفاده نمی‌کنند؛ فعلاً فقط *رنگ*شان با متغیرهای Obsidian هماهنگ شده، ولی شکل/سایه/رفتار hover هنوز دستی (Tailwind-style) است، نه دقیقاً هم‌شکل با دکمه‌های استاندارد Obsidian.

### جلسه‌ی ۳ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — تکمیل کار ۱

این جلسه توسط یک دستیار هوش مصنوعی دیگر (Claude) ادامه داده شد. کار از همان‌جایی که جلسه‌ی ۲ متوقف شده بود (کار ۱، نیمه‌کاره) برداشته شد.

**موارد باقی‌مانده‌ی کار ۱ که در این جلسه بسته شدند:**

1. **فونت (`Vazirmatn`)** — به‌جای اینکه فقط `Vazirmatn` به‌صورت هاردکد ست شود، حالا اول فونت خودِ تم Obsidian (`var(--font-text)`) را امتحان می‌کند و اگر آن متغیر مقدار نداشت (یا فونت فارسی پشتیبانی نشد) به `Vazirmatn` و بعد `Tahoma` سقوط می‌کند: `fontFamily: "var(--font-text, 'Vazirmatn'), 'Vazirmatn', Tahoma, sans-serif"`. این‌طوری هم از تم پیروی می‌کند، هم رندر درست حروف فارسی تضمین می‌ماند.
2. **دکمه‌های اصلی (CTA) به `.mod-cta` وصل شدند** — دو دکمه‌ی اصلی «افزودن تسک» (نوار کناری دسکتاپ) و «لیست جدید» (صفحه‌ی یادداشت‌ها) حالا کلاس بومی `mod-cta` را دارند (به‌جای اینکه فقط با `style={{background: var(--interactive-accent)}}` دستی رنگ‌آمیزی شوند) — یعنی رنگ/هاور/فوکوس آن‌ها را از خودِ Obsidian می‌گیرند. FAB شناور (دکمه‌ی گرد پایین صفحه‌ی موبایل) عمداً دست‌نخورده ماند چون `.mod-cta` در Obsidian یک `border-radius` پیش‌فرض مستطیلی تحمیل می‌کند که ممکن است شکل دایره‌ای FAB را به‌هم بزند و امکان تست بصری واقعی نبود؛ رنگش (`var(--interactive-accent)`) از قبل درست بود، فقط کلاس بومی نگرفت.
   - چون قانون `[class*="text-white"] { color: var(--text-normal) !important; }` (از جلسه‌ی ۲) با رنگ متن `.mod-cta` (که باید `var(--text-on-accent)` باشد) تداخل می‌کرد، یک قانون محافظ اضافه شد: `button.mod-cta[class*="text-white"] { color: var(--text-on-accent) !important; }`.
3. **دو رنگ Tailwind که در جلسه‌ی ۲ جا مانده بودند و بومی نبودند، پوشش داده شدند:**
   - `text-emerald-*`/`text-green-*` (پیام موفقیت‌آمیز sync) → `var(--text-success, #7fbb6e)`
   - `border-t-violet-300` (رنگ ثابت لبه‌ی بالای اسپینر لودینگ خلاصه‌ی AI) → `var(--interactive-accent)`
4. **باگ واقعی پیدا و رفع شد: صفحه‌ی تایمر پومودورو در تم روشن Obsidian ناخوانا بود.** بررسی کامل‌تر این جلسه نشان داد SVG رینگ پومودورو (که در لیست ۱۰ نقطه‌ی «chrome» جلسه‌ی ۲ نبود) رنگ‌های ثابت داشت: حلقه‌ی پس‌زمینه `rgba(255,255,255,.08)` و متن تایمر/برچسب «دور» با `fill:"#fff"` / `fill:"#94a3b8"`. این‌ها روی پس‌زمینه‌ی تیره‌ی قبلی خوب دیده می‌شدند ولی حالا که کارت‌ها به `--background-secondary` وصل‌اند (که در تم روشن می‌تواند خودش روشن باشد)، متن سفید و حلقه‌ی نیمه‌شفاف‌سفید عملاً محو می‌شدند. اصلاح شد به `var(--background-modifier-border)` / `var(--text-normal)` / `var(--text-muted)`. رنگ خودِ مسیر پیشرفت (`modeColor`: بنفش/فیروزه‌ای/صورتی بر اساس حالت کار/استراحت) دست‌نخورده ماند چون جزو پالت داده‌محورِ برندِ پلاگین است (مثل رنگ‌های ماتریس آیزنهاور)، نه chrome.
5. Build دوباره اجرا و تأیید شد (`npm install` چون `node_modules/` در ریپازیتوری نبود، سپس `npm run build`؛ `node --check main.js` بدون خطا).

**نتیجه:** کار ۱ از نظر تمام مواردی که بدون دسترسی به یک ولت Obsidian واقعی قابل انجام/تأیید بودند تکمیل شد. تنها موردی که همچنان باز است، بازبینی بصری واقعی داخل Obsidian است (سؤال باز شماره‌ی ۶ در بخش ۵ همچنان به قوت خودش باقی است).

**کار ۲ (گسترش تنظیمات) — شروع شد (تکمیل نشده):**

اولین و مهم‌ترین زیرساخت کار ۲ («ذخیره‌ی تنظیمات در `data.json` استاندارد پلاگین») پیاده‌سازی شد — و چون تمام داده‌های اپ (نه فقط تنظیمات) از یک شیء واحد `storage` در `app.jsx` عبور می‌کردند، این تغییر کل پلاگین را به‌روز کرد، نه فقط تنظیمات:

- بررسی کد نشان داد ذخیره‌سازی فعلی از یک API **خصوصی و مستندنشده‌ی** Obsidian استفاده می‌کرد: `app.loadLocalStorage(key)` / `app.saveLocalStorage(key, value)`. این API داده را در `localStorage` مرورگر و **مخصوص هر ولت** ذخیره می‌کند — یعنی با Obsidian Sync یا سینک فایل معمولی (مثل Syncthing/Git) بین دستگاه‌ها منتقل نمی‌شود، و چون خصوصی است ممکن است در نسخه‌های بعدی Obsidian بدون اطلاع تغییر کند یا حذف شود. این دقیقاً همان چیزی است که کار ۲ گفته «باید به `data.json` استاندارد منتقل شود».
- در `src/main.ts`: کلاس `LifeFlowPlugin` حالا یک کش درون‌حافظه‌ای (`this.data`) دارد که در `onload()` با `await this.loadData()` پر می‌شود؛ دو متد `getDataValue(key)` / `setDataValue(key, value)` رابط سنکرون (هم‌شکل با `localStorage.getItem/setItem` قبلی) به این کش می‌دهند، و هر نوشتن با ۴۰۰ میلی‌ثانیه debounce از طریق `this.saveData(this.data)` روی دیسک (در `data.json` داخل پوشه‌ی پلاگین) نوشته می‌شود — تا تایپ سریع باعث نوشتن مکرر روی دیسک نشود. در `onunload()` هم اگر نوشتنِ در صف مانده باشد، فوری flush می‌شود.
- **مهاجرت یک‌باره:** چون ممکن است کاربر از قبل با همان API خصوصی داده وارد کرده باشد، `migrateLegacyLocalStorageIfNeeded()` یک‌بار (با یک پرچم داخل خودِ `data.json`) تک‌تک کلیدهای شناخته‌شده (`lifeflow_data_v1`, `lifeflow_settings_v1`, `lifeflow_ai_v1`, `lifeflow_sync_v1`, `lifeflow_backups_v1`) را از `app.loadLocalStorage` می‌خواند و — فقط اگر در `data.json` جدید هنوز چیزی برای آن کلید نبود — کپی می‌کند، بعد پرچم مهاجرت را ست می‌کند. اگر API خصوصی دیگر وجود نداشته باشد (تغییر کرده/حذف شده)، `try/catch` آن را می‌گیرد و پلاگین با یک `data.json` خالی شروع به کار می‌کند — یعنی کرش نمی‌کند، فقط دیتای قدیمی منتقل نمی‌شود.
- کلیدهای زمان‌محور نوتیفیکیشن (`lifeflow_notif_*`، فقط برای جلوگیری از نوتیف تکراری در همان روز) عمداً مهاجرت داده نشدند — دیتای مصرفی/بی‌اهمیت‌اند.
- در `src/app.jsx`: خودِ شیء `storage` فقط به‌جای خواندن/نوشتن روی `window.__lifeflowObsidianApp` (شیء App خصوصی)، حالا از `window.__lifeflowPlugin` (خودِ نمونه‌ی پلاگین، با متدهای بالا) استفاده می‌کند؛ **هیچ‌کدام از ۱۶ نقطه‌ی فراخوانی `storage.get/set` در بقیه‌ی فایل تغییر نکردند** — چون از قبل درست لایه‌بندی شده بود.
- `npx tsc --noEmit` اجرا شد تا مطمئن شویم تغییرات TypeScript خطا ندارند (سه خطای موجود مربوط به تعریف‌های نوع خودِ پکیج `obsidian` در `node_modules` هستند، نه کد ما — پیش از این تغییرات هم بودند). سپس `npm run build` + `node --check main.js` تأیید شدند.

**⚠️ کار ۲ هنوز کامل نیست — آنچه باقی مانده:**
- خودِ `SettingsModal` (مودال React داخل اپ) هنوز تنها راه تغییر تنظیمات است؛ یک `PluginSettingTab` رسمی Obsidian (که در نوار تنظیمات خودِ Obsidian، نه داخل View پلاگین، ظاهر می‌شود) هنوز ساخته نشده.
- بخش‌بندی کامل خواسته‌شده در کار ۲ (روشن/خاموش کردن هر بخش از اپ، پیش‌فرض‌های پنجره‌ی ساخت تسک، نمایش/عدم‌نمایش ماتریس آیزنهاور، تنظیمات گزارش‌گیری) هنوز اضافه نشده — `SettingsModal` فعلی فقط تم/زبان/نوتیفیکیشن‌ها/کلید AI را پوشش می‌دهد.

**قدم بعدی پیشنهادی:** ادامه‌ی کار ۲ — طراحی و ساخت خودِ `PluginSettingTab` و بخش‌های باقی‌مانده‌ی تنظیمات.

### جلسه‌ی ۴ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — ساخت `PluginSettingTab` رسمی + شروع نمایش/عدم‌نمایش بخش‌ها

⚠️ **این جلسه با یک هشدار کاربر شروع شد:** «حتماً پروژه را بخوان، ممکن است تغییر کرده باشد». بررسی نشان داد ریپازیتوری واقعاً از جلسه‌ی قبلیِ *این* نشست جلوتر رفته بود (یک دستیار دیگر جلسه‌ی ۳ را در یک sandbox جدا انجام و پوش کرده بود، در حالی که sandbox این نشست هنوز روی نسخه‌ی قدیمی‌تر (بعد از جلسه‌ی ۲) کار می‌کرد و چند ویرایش محلی/کامیت‌نشده‌ی منسوخ داشت). این ویرایش‌های محلی منسوخ (بازنویسی دوباره‌ی بلوک `<style>` که در جلسه‌ی ۳ قبلاً درست شده بود) **دور ریخته شدند** (`git checkout -- . && git reset --hard origin/main`) تا کار جلسه‌ی ۳ خراب/دوباره‌کاری نشود. **درس مهم برای جلسات بعدی: همیشه اول `git fetch && git log origin/main` را چک کن، حتی اگر فکر می‌کنی می‌دانی وضعیت مخزن چیست — کار ممکن است در یک sandbox دیگر ادامه پیدا کرده باشد.**

پس از همگام‌سازی، کار از «قدم بعدی پیشنهادی» بالا (ساخت `PluginSettingTab`) ادامه یافت:

1. **`src/settings-tab.ts` (فایل جدید):** یک `PluginSettingTab` رسمی Obsidian که در تنظیمات خودِ Obsidian (نه داخل View پلاگین) زیر نام «زندگی‌آرام» ظاهر می‌شود. این تب مستقیماً همان دو کلید `data.json` را می‌خواند/می‌نویسد که `app.jsx` از قبل استفاده می‌کرد (`lifeflow_settings_v1` و `lifeflow_ai_v1`) — از طریق `plugin.getDataValue`/`setDataValue` که در جلسه‌ی ۳ اضافه شده بودند — یعنی این تب و مودال تنظیمات داخل خودِ اپ (`SettingsModal` در `app.jsx`) دقیقاً روی یک منبع داده کار می‌کنند، نه دو نسخه‌ی جدا.
   - بخش‌ها: زبان (dropdown ۴ زبانه)، نمایش/عدم‌نمایش ماتریس آیزنهاور (تاگل)، روشن/خاموش هر یک از ۷ تب اصلی (برنامه‌ریزی/تقویم/مطالعه/تناسب‌اندام/یادگیری/پومودورو/یادداشت‌ها — داشبورد و تسک‌ها همیشه اجباری‌اند)، ۴ تاگل اعلان (همان‌هایی که در `SettingsModal` بودند)، کلید API هوش مصنوعی (فیلد پسورد).
   - توضیح صریح اضافه شد که تم روشن/تاریک دیگر یک گزینه‌ی جداگانه نیست — از تنظیمات ظاهری خودِ Obsidian پیروی می‌کند (طبق تصمیم کار ۱).
2. **همگام‌سازی زنده‌ی دو سطح تنظیمات:** چون `PluginSettingTab` (کد TypeScript خارج از درخت React) و `SettingsModal` (React، ممکن است هم‌زمان در یک View باز باشد) با هم state سنکرون React ندارند، هر نوشتنِ `LifeFlowSettingTab` یک `CustomEvent("lifeflow-settings-changed")` روی `window` پخش می‌کند؛ در `app.jsx` (کامپوننت اصلی `LifeFlowApp`) یک `useEffect` تازه این event را می‌شنود و بلافاصله `setSettings(loadSettings())` را صدا می‌زند — یعنی اگر کاربر همزمان پلاگین باز و تب تنظیمات Obsidian را هم باز داشته باشد، تغییر فوری در هر دو دیده می‌شود، بدون نیاز به بستن/باز کردن دوباره‌ی View.
3. **مدل داده‌ی جدید `settings.features` در `app.jsx`:** یک ثابت `DEFAULT_FEATURES = { showMatrix: true, tabs: { planning, calendar, study, fitness, learning, pomodoro, notes: همه true } }` و تابع کمکی `mergeFeatures()` اضافه شدند؛ `loadSettings()` حالا همیشه یک `features` کامل (merge‌شده با پیش‌فرض‌ها، حتی اگر داده‌ی قدیمی‌تر آن را نداشته باشد) برمی‌گرداند — یعنی کاربرانی که از قبل تنظیمات ذخیره‌شده دارند با آپدیت این نسخه کرش نمی‌کنند.
4. **واقعاً وصل کردن این تنظیمات به UI (نه فقط ذخیره‌شدن بی‌اثر):**
   - `NAV` (هم نوار کناری دسکتاپ، هم نوار پایین موبایل) حالا با `NAV.filter(isNavVisible)` قبل از map رندر می‌شود؛ `isNavVisible` یک تب را همیشه نشان می‌دهد اگر `dashboard`/`tasks` باشد، وگرنه بر اساس `settings.features.tabs[id]`.
   - دکمه‌ی «ماتریس» در سوییچر ویوهای تب تسک‌ها (لیست/ماتریس/کانبان/زمان‌بندی) حالا با `.filter(([id]) => id !== "matrix" || settings.features?.showMatrix !== false)` قبل از map فیلتر می‌شود.
5. **یک باگ واقعیِ جداگانه (نه از تنظیمات) هم در همین بازبینی پیدا و رفع شد:** نوار کناری دسکتاپ کلاس `h-screen` داشت (فرض می‌کرد کل ارتفاع viewport مال خودش است) — دقیقاً همان نوع مشکلی که در جلسه‌ی ۲ روی ریشه‌ی کل اپ (`min-h-screen`) رفع شده بود ولی روی خودِ نوار کناری جا مانده بود. به `h-full` تغییر کرد.
6. `npx tsc --noEmit` (فقط همان ۳ خطای قبلی/بی‌ربط به کد ما در تایپ‌های خودِ پکیج `obsidian`، هیچ خطای جدیدی از `settings-tab.ts` یا تغییرات `main.ts`/`app.jsx` نبود)، سپس `npm run build` + `node --check main.js` تأیید شدند.

**⚠️ کار ۲ هنوز کامل نیست — آنچه باقی مانده:**
- پیش‌فرض‌های پنجره‌ی ساخت تسک (اولویت/ربع پیش‌فرض و ...) هنوز در تنظیمات نیستند.
- تنظیمات ظاهری عمیق‌تر (فونت انتخابی، تراکم/فشردگی چیدمان، رنگ‌های دسته‌بندی/ماتریس قابل‌تنظیم — سؤال باز شماره‌ی ۵ در بخش ۵) هنوز اضافه نشده‌اند.
- تست بصری واقعی `PluginSettingTab` در یک Obsidian واقعی هنوز انجام نشده (همچنان سؤال باز شماره‌ی ۶).
- گزینه‌ی «فیلتر کردن تب فعال اگر همان تب مخفی شد» یک حالت حاشیه‌ای دارد: اگر کاربر همان لحظه که در تب «X» است آن را از تنظیمات مخفی کند، محتوای آن تب تا رفتن به تب دیگر همچنان نمایش داده می‌شود (چون `tab` state جدا از فیلتر نوار ناوبری است). اولویت پایین، ولی برای جلسه‌ی بعد یادداشت شد.

---

### جلسه‌ی ۵ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — پیش‌فرض‌های پنجره‌ی «تسک جدید»

طبق دستور کاربر («ادامه بده، ولی در هر پیام فقط یک کامیت کن»)، این جلسه دقیقاً یک آیتم از باقیمانده‌های کار ۲ را بست:

- `app.jsx`: `DEFAULT_TASK_DEFAULTS = { quad: "q2", priority: 2, daypart: "morning", duration: 45 }` اضافه شد و `loadSettings()` حالا یک `settings.taskDefaults` کامل (merge‌شده، سازگار با داده‌ی قدیمی) برمی‌گرداند.
- `AddTaskModal` یک prop جدید `taskDefaults` می‌گیرد و مقادیر اولیه‌ی `quad`/`priority`/`daypart`/`duration` را (فقط در حالت «تسک جدید»، نه ویرایش) از آن می‌خواند به‌جای مقادیر ثابت قبلی (`"q2"`, `2`, `"morning"`, `45`). تنها نقطه‌ی فراخوانی `AddTaskModal` در `app.jsx` حالا `taskDefaults: settings.taskDefaults` را پاس می‌دهد.
- `settings-tab.ts`: بخش جدید «پیش‌فرض‌های پنجره‌ی تسک جدید» با ۴ کنترل (dropdown ربع ماتریس، dropdown اولویت، dropdown زمان روز، عدد مدت به دقیقه) اضافه شد؛ لیبل‌های ربع/اولویت/زمان‌روز چون این فایل جدا از درخت React است، عیناً از مقادیر `QUADRANTS`/`PRIORITIES`/`DAYPARTS` در `app.jsx` کپی شدند (چهار مورد کوچک و باثبات، ریسک واگرایی کم).
- `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، `npm run build`، `node --check main.js` تأیید شدند.

**نتیجه:** آیتم «پیش‌فرض‌های پنجره‌ی ساخت تسک» از صف کار ۲ بسته شد. باقیِ کار ۲ (تنظیمات ظاهری عمیق‌تر، تنظیمات گزارش‌گیری) هنوز باز است.

---

## تصمیم‌های طراحی (بخش تاریخی، جلسات ۱ تا ۵ — قبل از سیستم لِین‌بندی؛ عمداً بدون شماره تا با «بخش ۴. لاگ لِین‌ها» که بعداً و مستقل اضافه شد تداخل نکند)

- چون هیچ سیستم build ای همراه فایل‌ها نبود، تصمیم گرفته شد که در گام بعد (کار ۰) یک اسکلت استاندارد esbuild ساخته شود تا از این پس روی `src/` کار شود، نه `main.js` مستقیم.
- ترتیب اجرا پیشنهادی: کار۰ (زیرساخت) → کار۱ (ظاهر بومی) → کار۲ (تنظیمات) → کار۵ (مدل داده‌ی یادگیری، چون کار۳ و کار۶ به آن وابسته‌اند) → کار۳ (برنامه‌ریزی) → کار۶ (پومودورو) → کار۴ (گزارش‌گیری، چون به داده‌ی همه‌ی بخش‌های قبل نیاز دارد). این ترتیب پیشنهادی است و می‌تواند تغییر کند.
- **استراتژی کار ۱ (مهم برای ادامه‌ی کار):** به‌جای بازنویسی خط‌به‌خط ۶۵۰+ کلاس Tailwind در `app.jsx` (که ریسک شکستن چیزی را در یک فایل تک‌جزئی ۲۶۰۰+ خطی بالا می‌برد)، رویکرد انتخاب‌شده **override کردن در سطح CSS** بود: کلاس‌های Tailwind دقیقاً با همان نام در `styles.css` با سلکتور `.lifeflow-app-root [class*="..."]` و `!important` بازتعریف شدند تا به متغیرهای Obsidian اشاره کنند، بدون دست‌زدن به `className` ها در `app.jsx`. فقط رنگ‌های **این‌لاین** (`style={{color: "#hex"}}`) که با CSS قابل override نیستند، مستقیماً در `app.jsx` ویرایش شدند — و فقط در نقاطی که رنگ واقعاً «chrome» بود (نه داده/دسته‌بندی). این الگو را برای ادامه‌ی پاک‌سازی رنگ‌ها هم باید ادامه داد.
- رنگ‌های دسته‌بندی/داده (ماتریس آیزنهاور، نمودار روز، نشان‌ها، نمودارها) عمداً حذف/جایگزین نشدند — این تصمیم می‌تواند در کار ۲ (تنظیمات) بازبینی شود، مثلاً با اضافه‌کردن گزینه‌ی «پیروی از رنگ accent خود Obsidian برای همه‌چیز» در تنظیمات ظاهری.
- **قاعده‌ی کلی برای تشخیص «chrome» در برابر «داده/چارت» (جلسه‌ی ۳):** اگر رنگ ثابت روی چیزی است که کاربر برایش معنای برندی/دسته‌بندی قائل است (رنگ ربع ماتریس آیزنهاور، رنگ خط نمودار، رنگ نشان) → نگه داشته می‌شود. اگر رنگ ثابت فقط برای *خوانا بودن متن/خط روی پس‌زمینه‌ی خودِ پلاگین* است (مثل متن تایمر، حلقه‌ی پس‌زمینه‌ی تایمر، برچسب‌ها) → باید به `var(--text-normal)`/`var(--text-muted)`/`var(--background-modifier-border)` وصل شود، چون این‌ها اگر ثابت بمانند در تم روشن ممکن است کاملاً ناخوانا شوند (نمونه‌ی واقعی: صفحه‌ی تایمر پومودورو که در جلسه‌ی ۲ از قلم افتاده بود).
- برای دکمه‌های اصلی (CTA) که هم می‌خواهیم رنگ/هاور بومی `.mod-cta` را داشته باشند و هم کلاس عمومی `text-white` (که در جلسه‌ی ۲ به `var(--text-normal)` مپ شده) را حفظ کنیم، یک قانون محافظ با اولویت بالاتر اضافه شد (`button.mod-cta[class*="text-white"]`) تا رنگ متن روی دکمه‌ی accent‌رنگ همیشه `var(--text-on-accent)` بماند، نه رنگ متن عادی. این الگو را برای هر دکمه‌ی `.mod-cta` جدید در آینده هم باید رعایت کرد (یا کلاس `text-white` را از آن دکمه‌ها حذف کرد).
- روی FAB شناور (دکمه‌ی گرد افزودن در نمای موبایل) عمداً `.mod-cta` گذاشته نشد چون شکل دایره‌ای‌اش با `border-radius` پیش‌فرض این کلاس در تم‌های Obsidian ممکن است تداخل کند و امکان تست بصری واقعی نبود؛ اگر در جلسه‌ی بعد دسترسی به Obsidian واقعی فراهم شد، این مورد را می‌شود امتحان و تنظیم کرد.
- **الگوی «کش سنکرون + نوشتن async» برای `data.json` (جلسه‌ی ۳):** `Plugin.loadData()`/`saveData()` رسمی Obsidian هر دو `Promise` برمی‌گردانند، ولی تمام کد React فعلی (`useState(() => loadSavedData())` و مشابه‌هایش) انتظار یک `storage.get/set` **سنکرون** دارد — دقیقاً مثل قرارداد قدیمی `localStorage`. به‌جای بازنویسی این بخش‌ها به الگوی async (که ریسک بازنویسی گسترده در فایل تک‌جزئی ۲۶۰۰+ خطی `app.jsx` را داشت)، تصمیم گرفته شد پلاگین در `onload()` یک‌بار کل `data.json` را با `await` بخواند و در یک متغیر عضو (`this.data`) کش کند؛ از آن به بعد `getDataValue`/`setDataValue` سنکرون کار می‌کنند (خواندن/نوشتن از/به همان کش)، و فقط نوشتن واقعی روی دیسک (`this.saveData(this.data)`) با ۴۰۰ میلی‌ثانیه debounce به‌صورت async در پس‌زمینه انجام می‌شود. این یعنی حداکثر یک بار (بین باز شدن ولت تا باز شدن اولین View پلاگین) دیتا لود می‌شود، و از آن به بعد رفتار برای React دقیقاً مثل `localStorage` قبلی است — بدون نیاز به `useEffect`/لودینگ اسپینر اضافه در جاهایی که قبلاً نبود.
- این تصمیم یعنی اگر Obsidian یا سیستم‌عامل به‌طور غیرمنتظره کرش کند *در همان ۴۰۰ میلی‌ثانیه‌ی بین آخرین تغییر و نوشتن روی دیسک*، آخرین تغییر ممکن است ذخیره نشود (نه بدتر از قبل، چون `localStorage`/API خصوصی قبلی هم تضمین فوری نداشت). `onunload()` این پنجره را با flush فوری در زمان غیرفعال‌شدن پلاگین کوچک‌تر می‌کند.
- **دو منبع تنظیمات، یک منبع داده (جلسه‌ی ۴):** به‌جای اینکه `PluginSettingTab` جدید یک کپی جدا از تنظیمات داشته باشد (که می‌توانست با `SettingsModal` داخل اپ ناهماهنگ شود)، هر دو مستقیماً همان کلیدهای `data.json` (`lifeflow_settings_v1`/`lifeflow_ai_v1`) را می‌خوانند/می‌نویسند. هماهنگی زنده‌ی بین این دو رابط کاربری (وقتی هر دو هم‌زمان باز باشند) با یک `CustomEvent` سبک روی `window` انجام می‌شود، نه با یک state manager سراسری — چون فقط یک جهت ارتباط (تنظیمات رسمی → View باز) لازم بود، نه ارتباط دوطرفه‌ی پیچیده.
- **همیشه اول وضعیت remote را چک کن (درس جلسه‌ی ۴):** وقتی کار روی یک پروژه در چند نشست/sandbox جدا ادامه پیدا می‌کند، حافظه‌ی محلی هر نشست ممکن است قدیمی باشد. قانون: قبل از هر ویرایش، `git fetch origin && git log --oneline origin/main -10` را چک کن و اگر HEAD محلی عقب‌تر بود، `git reset --hard origin/main` بزن (بعد از مطمئن‌شدن که چیز کامیت‌نشده‌ی مهمی از دست نمی‌رود) — نه اینکه فرض کنی حافظه‌ی خودت کامل است.

---

### جلسه‌ی ۶ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — شروع کار ۵: سهمیه/بازه/تکرار مخصوص هر مسیر یادگیری + لاگ پیشرفت با توضیح

طبق دستور کاربر («ادامه بده، ولی هر پیام فقط یک کامیت و بعد متوقف شو»)، بعد از بستن آیتم‌های کوچک‌تر کار ۲، این جلسه اولین قدم واقعی کار ۵ را برداشت — دقیقاً همان چیزی که کاربر در مثال «حفظ قرآن» به‌عنوان مهم‌ترین کمبود اشاره کرده بود: مسیرهای یادگیری (subsections) قبلاً همه از یک روتین/سهمیه‌ی مشترک در سطح کل موضوع استفاده می‌کردند و امکان «ثبت با توضیح مثل کامیت گیت» اصلاً وجود نداشت.

**قبل از تغییر، مدل قدیمی بررسی شد** (`LearningHub`, `SubsectionCard`, `addTaskProgress`): هر subsection فقط `{id, title, unit, target, linkedTaskId}` داشت؛ `linkedTaskId` به یک تسک «روند‌دار» (`progressType: "progressive"`) در سیستم عمومی تسک‌ها وصل می‌شد که `recurrence` را از *کل موضوع* به ارث می‌برد (`topic.recurrence`) — یعنی «مرور دور» و «مرور نزدیک» مجبور بودند دقیقاً همان روزهای هفته/بازه را داشته باشند، که دقیقاً برخلاف خواسته‌ی کاربر بود. ثبت پیشرفت هم فقط یک عدد بود (`addTaskProgress(id, amount)`), بدون تاریخچه یا توضیح.

**تغییرات:**
1. **`addTaskProgress` سراسری (در `app.jsx`، مؤثر روی *همه‌ی* تسک‌های روند‌دار، نه فقط یادگیری):** امضا به `(id, amount, note)` تغییر کرد؛ هر فراخوانی یک ورودی `{id, date, amount, note}` به آرایه‌ی جدید `task.progressLog` اضافه می‌کند (جدیدترین اول)، علاوه‌بر افزایش معمول `progressCurrent`. تسک‌های قدیمی که `progressLog` ندارند مشکلی ندارند (`t2.progressLog || []` قبل از spread).
2. **`ProgressiveTaskBar` (کامپوننت مشترک بین تسک‌های عمومی و مسیرهای یادگیری):** یک ورودی متن اختیاری «توضیح» کنار ورودی عدد اضافه شد؛ زیر نوار پیشرفت، یک `ProgressLogList` جدید تاریخچه‌ی ثبت‌ها را به‌شکل شبیه لاگ گیت نشان می‌دهد (`+مقدار تاریخ — توضیح`)، با نمایش ۲ مورد آخر و دکمه‌ی «N ثبت قدیمی‌تر…» برای باز کردن بقیه.
3. **مدل داده‌ی subsection گسترش یافت** (`{id, title, unit, target, quotaPerPeriod, rangeLabel, recurrenceOverride, linkedTaskId}`):
   - `quotaPerPeriod`: سهمیه‌ی هر دوره (مثلاً «۳ صفحه در روز») — فعلاً فقط نمایشی/اطلاعاتی است (زیرنویس کارت)؛ هنوز به یادآوری خودکار یا محاسبه‌ی خودکار کسری/مازاد وصل نیست (آن بخش در «گزارش روزانه»ی هنوز‌نساخته می‌آید).
   - `rangeLabel`: یک برچسب متنی آزاد برای بازه (مثلاً «صفحه ۱ تا ۲۰») — فقط توصیفی، نه یک بازه‌ی عددی محاسبه‌شده.
   - `recurrenceOverride`: اگر `null` باشد، از روتین کلی موضوع (`topic.recurrence`) پیروی می‌کند (رفتار قبلی)؛ اگر مقدار داشته باشد (`{recurrence: "daily"|"weekly", recurrenceWeekdays?}`) روی تسک لینک‌شده‌ی *همان* subsection override می‌شود، بدون اینکه روی بقیه‌ی subsection‌های دیگر آن موضوع اثر بگذارد — این دقیقاً رفع محدودیتی است که کاربر توصیف کرده بود («مرور دور» هرروز، «مرور نزدیک» فقط روزهای زوج، مثلاً).
4. **`SubsectionCard` بازنویسی شد:** فرم ویرایش حالا واحد/هدف‌کل/سهمیه‌ی دوره‌ای/بازه‌ی توصیفی/دوره‌ی تکرار (با گزینه‌ی «طبق روتین کلی موضوع» یا «هرروز» یا «روزهای خاص» + چیپ‌های روزهای هفته) را دارد؛ در حالت غیرویرایش یک زیرنویس خلاصه (سهمیه · تکرار · بازه) نمایش داده می‌شود. **نکته‌ی فنی:** نسخه‌ی اول این کامپوننت را به‌شکل یک عبارت تودرتوی خیلی طولانی (`React.createElement` تودرتو در یک خط) نوشتم که یک پرانتز کم داشت و build را می‌شکست (`esbuild` خطای «Expected ")" but found ":"» داد). به‌جای پیدا کردن دستیِ پرانتز گمشده در آن عبارت غول‌آسا، کل کامپوننت با متغیرهای میانی (`header`, `subtitle`, `editForm`, `body`) بازنویسی شد که هم خطا رفع شد و هم خواناتر است. **درس برای جلسات بعد: برای JSX پیچیده/تودرتو با چند شرط، از متغیرهای میانی استفاده کن، نه یک عبارت یک‌خطی غول‌آسا — ریسک اشتباه پرانتز در فایل‌های تولیدشده توسط ابزار (نه دست‌نویس با فرمت‌بندی خودکار) بالاست.**
5. `updateSubsection` طوری تغییر کرد که وقتی `recurrenceOverride` پچ می‌شود، `recurrence`/`recurrenceWeekdays` تسک لینک‌شده را هم هماهنگ به‌روزرسانی کند (یا override را اعمال کند، یا در صورت `null` به روتین کلی موضوع برگردد).
6. `AddTaskModal` هنگام ساخت تسک تازه `progressLog: []` می‌گذارد؛ در حالت ویرایش، `progressLog` قبلی تسک را دست‌نخورده نگه می‌دارد.
7. `npm run build` + `node --check main.js` بعد از رفع خطای پرانتز تأیید شدند.

**⚠️ کار ۵ هنوز خیلی مانده — مهم‌ترین قلم‌های باقی (به‌روزرسانی جلسه‌ی ۷ در پرانتز):**
- ~~بخش «گزارش روزانه» در برنامه‌ریزی...~~ نسخه‌ی اول در جلسه‌ی ۷ ساخته شد؛ محدودیت‌هایش را در یادداشت جلسه‌ی ۷ ببینید.
- اتصال انتخاب مسیر یادگیری در پومودورو (تعیین اینکه «در این یک ساعت، انقدر از مرور دور، انقدر از مرور نزدیک» انجام شود) — هنوز نه. (پومودورو از قبل امکان انتخاب *یک* تسک/مسیر در هر جلسه را داشت و در پایان جلسه مقدار پیشرفت را می‌پرسید؛ آنچه هنوز نیست تقسیم *یک* بلوک زمانی بین *چند* مسیر است.)
- آمار «طبق‌برنامه در برابر واقعی» (planned vs actual) — هنوز نه؛ حالا که `computeTrackBalance` وجود دارد (جلسه‌ی ۷) پایه‌اش آماده‌تر است.
- ~~`quotaPerPeriod` هنوز فقط نمایشی است...~~ در جلسه‌ی ۷ ساخته شد.
- تست بصری واقعی در Obsidian (مثل بقیه‌ی موارد قبلی) هنوز انجام نشده.

---

### جلسه‌ی ۷ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — بخش «گزارش روزانه» در برنامه‌ریزی + محاسبه‌ی بدهی/بستانکار

طبق دستور کاربر («ادامه بده، یک کامیت در هر پیام»)، مهم‌ترین قلم باقیمانده‌ی کار ۵ که در جلسه‌ی ۶ مشخص شده بود ساخته شد: بخش «گزارش روزانه» دقیقاً در همان‌جایی که کاربر خواسته بود — تب «برنامه‌ریزی» — نه در تب یادگیری.

**تغییرات:**
1. **`createdDate` روی subsection:** هنگام ساخت یک مسیر یادگیری تازه، `createdDate: todayKey()` هم ذخیره می‌شود — نقطه‌ی شروع محاسبه‌ی «چند روز باید تا الان انجام می‌شد». مسیرهای قدیمی‌تر (از قبل از این جلسه) این فیلد را ندارند؛ در `computeTrackBalance` با `subsection.createdDate || todayKey()` این حالت پوشش داده شد (یعنی برای داده‌ی قدیمی، محاسبه‌ی بدهی از *امروز* شروع می‌شود، نه از یک تاریخ نامعلوم در گذشته — تصمیم عمدی برای جلوگیری از نمایش یک بدهی بزرگ و گمراه‌کننده برای مسیرهایی که این جلسه هنوز نمی‌داند واقعاً از چه زمانی شروع شده‌اند).
2. **`isTrackDueOn(subsection, topic, date)`:** آیا یک مسیر در یک تاریخ مشخص «باید» انجام شود — بر اساس `recurrenceOverride` مسیر (اگر باشد) یا `recurrence`/`recurrenceWeekdays` کل موضوع (اگر نباشد). برای `recurrence` غیر از `daily`/`weekly` (یعنی `monthly`/`yearly`) فعلاً ساده‌سازی شد و «هرروز» فرض می‌شود — محاسبه‌ی دقیق سهمیه‌ی روزانه از یک هدف ماهانه/سالانه در این جلسه پیاده نشد (اولویت پایین‌تر بود).
3. **`computeTrackBalance(subsection, topic, task)`:** از `subsection.createdDate` تا امروز روز‌به‌روز جلو می‌رود (با یک سقف ۳۶۶۰ تکرار برای جلوگیری از حلقه‌ی بی‌نهایت در صورت داده‌ی خراب)، برای هر روزی که طبق `isTrackDueOn` مسیر باید انجام می‌شد `quotaPerPeriod` را به `dueTotal` اضافه می‌کند، و آن را با مجموع *خام* `task.progressLog` (نه `progressCurrent` که برای اهداف کلی سقف‌دار است) مقایسه می‌کند. خروجی: `{dueTotal, doneTotal, balance}` — `balance` مثبت یعنی عقب (بدهی)، منفی یا صفر یعنی جلو/مطابق (بستانکار).
4. **`DailyReportView` (کامپوننت جدید):** همه‌ی مسیرهای یادگیری *با `quotaPerPeriod` تنظیم‌شده* را از همه‌ی پروژه‌ها جمع می‌کند و برای هرکدام یک ردیف نشان می‌دهد: عنوان (پروژه — مسیر)، سهمیه‌ی امروز، وضعیت بدهی/بستانکار (رنگی)، مقدار ثبت‌شده‌ی امروز (اگر بود)، و یک فرم کوچک (مقدار پیش‌فرض = سهمیه + توضیح اختیاری) که با ثبت، مستقیماً همان `onAddProgress` سراسری (همان مسیر جلسه‌ی ۶: مقدار+توضیح+تاریخ در `progressLog`) را صدا می‌زند — یعنی گزارش روزانه هیچ مسیر داده‌ی جدا و موازی نساخت، فقط یک **ورودی سریع‌تر و متمرکز** برای همان مکانیزم لاگ‌گیری قبلی است. اگر هیچ مسیری سهمیه نداشته باشد، یک پیام راهنما نشان می‌دهد که کاربر را به تب یادگیری ارجاع می‌دهد.
5. **`PlanningHub` یک زیرتب سوم گرفت:** «گزارش روزانه» کنار «برنامه روزانه» و «اهداف». برای این کار `PlanningHub` سه prop تازه گرفت (`projects`, `tasks`, `onAddProgress`) که از نقطه‌ی فراخوانی سطح بالا (`LifeFlowApp`) پاس داده می‌شوند — این پراپ‌ها قبلاً به `PlanningHub` نمی‌رسیدند.
6. **چیزی که *عمداً* ساخته نشد:** «انتقال خودکار کسری به فردا» به‌شکل یک عدد ذخیره‌شده‌ی جداگانه (مثل یک فیلد `carryOverDebt`) ساخته نشد؛ چون `computeTrackBalance` هر بار از نو از روی `progressLog` واقعی محاسبه می‌شود، کسری/مازاد *خودش‌به‌خود* هر روز که دوباره باز شود دیده می‌شود — نیازی به یک مرحله‌ی جداگانه‌ی «انتقال» در پایان روز نیست. این ساده‌تر و کمتر مستعد باگ (مثلاً دوبار انتقال شدن یک کسری) است، ولی یعنی گزارش «امروز چقدر عقبیم» هزینه‌ی محاسباتی‌اش با طول عمر مسیر رشد می‌کند (حلقه‌ی روزانه) — برای مسیرهای چندساله ممکن است لازم شود cache شود؛ فعلاً با سقف نگهبان ۳۶۶۰ روز (~۱۰ سال) کنترل شده.
7. `npm run build` + `node --check main.js` تأیید شدند؛ فایل هم از نظر انکودینگ UTF-8 چک شد (بعد از یک اشتباه انکودینگ در جلسه‌ی ۶ که در همان جلسه رفع شد، این بار به‌عنوان یک قدم استاندارد قبل از کامیت اضافه شد).

**⚠️ محدودیت‌های شناخته‌شده‌ی این نسخه‌ی اول گزارش روزانه:**
- فقط مسیرهایی با `quotaPerPeriod` تنظیم‌شده در گزارش روزانه ظاهر می‌شوند؛ اگر کاربر برای یک مسیر سهمیه تنظیم نکرده باشد، در این لیست دیده نمی‌شود (باید برود در تب یادگیری، ویرایش مسیر، سهمیه تنظیم کند).
- محاسبه‌ی «باید انجام می‌شد» برای `monthly`/`yearly` ساده‌سازی شده (هرروز فرض می‌شود)، دقیق نیست.
- تقسیم یک مقدار واحد بین چند مسیر (سؤال باز شماره‌ی ۲ در بخش «سؤالات باز») هنوز پاسخ داده نشده — فعلاً هر مسیر فرم ثبت جدای خودش را دارد، نه یک فرم واحد «امروز جمعاً X واحد کار کردم، بین مسیرها تقسیم کن».
- تست بصری واقعی در Obsidian هنوز انجام نشده.

---

### جلسه‌ی ۸ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — دو ویژگی کوچک و مستقل پومودورو حرفه‌ای

طبق دستور صریح کاربر («ادامه بده، مطمئن شو به لیمیت نمی‌خوری وسطش»)، این جلسه عمداً به‌جای یک قلم بزرگ، **دو تغییر کوچک، مستقل، و کم‌ریسک** از کار ۶ انتخاب شد که هرکدام به‌تنهایی build را تأیید کردند، تا احتمال گیرکردن وسط یک تغییر بزرگ (مثل خطای پرانتز جلسه‌ی ۶) پایین بیاید:

1. **صدای پایان تایمر (`playPomodoroChime`):** یک تابع کوچک و کاملاً مستقل با Web Audio API (`AudioContext` + دو `OscillatorNode`) که یک صدای زنگ کوتاه دو-نتی پخش می‌کند — بدون نیاز به هیچ فایل صوتی خارجی (که باید در باندل پلاگین جا می‌گرفت). نُت‌ها برای پایان «کار» (۸۸۰/۱۱۰۹ هرتز) با پایان «استراحت» (۶۵۹/۹۸۸ هرتز) فرق دارند تا کاربر بدون نگاه‌کردن به صفحه بفهمد کدام نوع جلسه تمام شده. در `finishSession` گنجانده شد، مستقل از مجوز Notification مرورگر (که فقط popup سیستمی را کنترل می‌کند، نه صدا).
2. **کشف مهم:** فیلدهای `autoStartNext` و `sound` از قبل در `DEFAULT_POMODORO.settings` تعریف شده بودند ولی **در هیچ‌جای دیگر کد استفاده نمی‌شدند** (نه در منطق، نه در UI) — احتمالاً از طراحی اولیه‌ی اپ (قبل از بازسازی این پروژه) باقی مانده بودند. این جلسه هر دو را واقعاً وصل کرد:
   - `sound`: پخش صدا (بند ۱) را کنترل می‌کند؛ اگر `false` باشد صدا پخش نمی‌شود (مستقل از `notifSettings.pomodoroEnd` که فقط popup سیستمی را کنترل می‌کند).
   - `autoStartNext`: اگر `true` باشد، بلافاصله بعد از پایان یک جلسه (کار یا استراحت)، جلسه‌ی بعدی به‌صورت خودکار شروع می‌شود (به‌جای اینکه کاربر مجبور باشد دستی دکمه‌ی شروع را بزند) — دقیقاً رفتار پومودوروهای حرفه‌ای.
   - دو تاگل جدید (`Chip`) در کارت تنظیمات پومودورو اضافه شدند: «شروع خودکار دور بعد» و «صدای پایان تایمر».
3. **بازبینی چک‌لیست کار ۶:** در همین جلسه معلوم شد که سه آیتم از چهار آیتم اصلی کار ۶ (چرخه‌ی قابل‌تنظیم، اتصال به تسک/مسیر یادگیری، لاگ جلسات برای گزارش‌گیری) در واقع **از قبل کار می‌کردند** ولی در چک‌لیست تیک نخورده بودند — این یک نکته‌ی مستندسازی بود، نه کد جدید؛ چک‌لیست اصلاح شد.
4. **یک محدودیت معماری مهم کشف و مستند شد (ولی عمداً در این جلسه دست نخورد):** تایمر پومودورو کاملاً local state داخل `PomodoroTimerView` است؛ با رفتن به تب دیگر، کامپوننت unmount و تایمر واقعاً متوقف می‌شود، نه فقط از دید کاربر پنهان. این یعنی هرگونه «نوار وضعیت/ریبون Obsidian که همیشه زمان باقی‌مانده را نشان دهد» (از خواسته‌های اصلی کار ۶) قبل از هر چیز نیاز به بالابردن state تایمر به سطح بالاتر (`LifeFlowApp` یا حتی خودِ پلاگین در `main.ts`) دارد — یک تغییر معماری با ریسک بالاتر که عمداً به یک جلسه‌ی جداگانه موکول شد.
5. `npm run build`، `node --check main.js`، `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، و بررسی عدم‌وجود کاراکتر خراب انکودینگ (`\xef\xbf\xbd`) — همه بعد از *هر دو* تغییر (نه فقط در پایان) اجرا شدند تا اگر مشکلی پیش می‌آمد، زود متوجه می‌شدیم کدام تغییر مقصر است.

---

### جلسه‌ی ۹ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — حالت Do-Not-Disturb برای پومودورو

⚠️ **این جلسه هم با هشدار صریح کاربر شروع شد** («ادامه بده، ولی اول ریپازیتوری را چک کن که هماهنگ باشی»). طبق درسِ ثبت‌شده در تصمیم‌های طراحی جلسه‌ی ۴، قبل از هر ویرایشی `git fetch` + `git log origin/main` اجرا شد — و مشخص شد که یک نشست دیگر همین بین، ۵ کامیت جلوتر رفته (جلسات ۴ تا ۸ این فایل، شامل تکمیل کامل کار ۲ با `settings-tab.ts`، بخش زیادی از کار ۵، و بخشی از کار ۶). یک سری تغییرات محلی/کامیت‌نشده از این نشست (که دقیقاً همان کار Task 2 SettingTab را با نام‌گذاری/رویداد متفاوت دوباره می‌ساخت) با `git stash` کنار گذاشته و بعد از `git pull --ff-only` **کاملاً دور ریخته شدند** (`git stash drop`) چون نسخه‌ی نشست دیگر کامل‌تر و جلوتر بود — دوباره‌کاری/تداخل نداشتیم چون قبل از نوشتن چیزی commit نشده بود.

پس از همگام‌سازی، آخرین آیتم باز کار ۶ («حالت Do-Not-Disturb») که در جلسه‌ی ۸ ثبت شده بود انتخاب شد — چون کوچک، مستقل، و کم‌ریسک بود (طبق همان استراتژی جلسه‌ی ۸: یک آیتم کوچک، نه یک بازنویسی بزرگ).

**تصمیم طراحی برای معنای DND در این پلاگین:** «عدم‌مزاحمت» به این معنا تعریف شد که وقتی یک دور **کاریِ** پومودورو در حال اجراست، سه نوع اعلانِ دیگر (یادآوری تسک، نزدیک‌شدن مهلت یادگیری، خلاصه‌ی صبحگاهی) موقتاً سرکوب شوند — نه خودِ اعلان «پایان پومودورو» (که دقیقاً لحظه‌ی تمام‌شدن تمرکز است، سرکوب آن بی‌معنا بود).

**چالش فنی و راه‌حل کم‌ریسک:** طبق کشف مهم جلسه‌ی ۸ (مستند در بخش «کار ۶» بالا)، `running`/`mode`/`secondsLeft` تایمر پومودورو کاملاً **local state داخل `PomodoroTimerView`** هستند، در حالی که سه افکت نوتیفیکیشنی که باید سرکوب شوند در سطح بالاتر (`LifeFlowApp`) قرار دارند. به‌جای «بالابردن کامل state تایمر» (تغییر معماری بزرگ‌تر که در جلسه‌ی ۸ عمداً برای یک نشست جدا کنار گذاشته شده بود)، یک **پل کوچک و هدفمند** ساخته شد: `PomodoroTimerView` یک prop تازه `onFocusChange(isFocusing: boolean)` می‌گیرد و با یک `useEffect` روی `[running, mode]`، فقط این یک مقدار بولی را به بالا گزارش می‌دهد (`running && mode === "work"`). یک `useEffect` جدا هم موقع unmount شدن کامپوننت (مثلاً وقتی کاربر از تب پومودورو خارج می‌شود) این مقدار را `false` می‌کند — چون طبق همان محدودیت شناخته‌شده، خروج از تب یعنی تایمر واقعاً متوقف شده، پس تمرکز هم واقعاً تمام شده.

**تغییرات:**
1. `app.jsx`: `DEFAULT_NOTIFICATIONS` یک فیلد پنجم گرفت: `dndDuringFocus: true` (پیش‌فرض روشن).
2. `LifeFlowApp`: یک `const [focusMode, setFocusMode] = useState(false)` تازه اضافه شد؛ به `PomodoroHub` به‌شکل `onFocusChange: setFocusMode` پاس داده می‌شود (و `PomodoroHub` آن را به `PomodoroTimerView` فوروارد می‌کند).
3. هر سه افکت نوتیفیکیشن (یادآوری تسک، مهلت یادگیری، خلاصه‌ی صبحگاهی) یک خط گارد یکسان گرفتند: `if (focusMode && settings.notifications?.dndDuringFocus !== false) return;` و `focusMode` به آرایه‌ی وابستگی‌شان اضافه شد. اعلان «پایان پومودورو» (داخل خودِ `PomodoroTimerView`) دست‌نخورده ماند.
4. `SettingsModal` (React، داخل اپ) و `settings-tab.ts` (تب رسمی Obsidian) هر دو یک تاگل پنجم «عدم‌مزاحمت حین جلسه‌ی کاری پومودورو» گرفتند — چون هر دو از قبل یک آرایه‌ی `notifItems`/`notifRows` را map می‌کنند، افزودن یک آیتم تازه به هر آرایه کافی بود، نیازی به ساختار UI جدید نبود.
5. `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، `npm run build`، `node --check main.js`، و بررسی نبودن کاراکتر خراب انکودینگ (طبق عادت جلسه‌ی ۷ به بعد) همگی تأیید شدند.

**⚠️ محدودیت‌های شناخته‌شده:**
- چون `onFocusChange` فقط هنگام mount بودن `PomodoroTimerView` فعال است، اگر کاربر از تب پومودورو خارج شود، DND متوقف می‌شود — حتی اگر (فرضاً در آینده) تایمر به‌شکلی مستقل از mount ادامه پیدا کند. این با محدودیت بزرگ‌تر و از‌قبل‌مستندِ «نوار پیشرفت در ریبون/status bar» (هنوز باز، آخرین آیتم کار ۶) گره خورده — وقتی آن تغییر معماری (بالابردن کامل state تایمر) انجام شود، این پل موقت هم باید بازبینی شود.
- تست بصری واقعی در Obsidian هنوز انجام نشده.

**نتیجه:** با این جلسه، تمام چهار آیتم اصلی چک‌لیست کار ۶ که در روزمرد کاربر خواسته شده بود تیک خوردند؛ تنها «نوار پیشرفت در ریبون/status bar» باقی مانده که به تغییر معماری بزرگ‌تری (بالابردن state تایمر) نیاز دارد.

---

### جلسه‌ی ۱۰ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — اولین قدم کار ۴: خروجی گزارش روزانه به Markdown

طبق دستور کاربر («ادامه بده، مطمئن شو به لیمیت نمی‌خوری»)، ابتدا `git fetch` چک شد (چیز جدیدی از نشست‌های دیگر نبود، هماهنگ بودیم)، سپس یک آیتم کوچک و مستقل جدید انتخاب شد: اولین قدم کار ۴ (گزارش‌گیری)، که تا این جلسه ۰٪ پیش رفته بود — **خروجی/صدور گزارش Markdown داخل ولت**، دقیقاً همان‌طور که در صف کارها پیشنهاد شده بود («مطابق روحیه‌ی Obsidian»).

**چرا این آیتم (نه بقیه‌ی موارد باز کار ۴/۵):** «گزارش‌های تجمیعی هفتگی/ماهانه» و «نمودارها» (بقیه‌ی کار ۴) و «آمار planned vs actual» (کار ۵) همگی نیاز به طراحی مدل داده‌ی تازه‌ای دارند (مثلاً یک لاگ تکمیل تاریخچه‌دار برای تسک‌های معمولی، که فعلاً فقط برای تسک‌های روند‌دار/یادگیری با `progressLog` وجود دارد) — یعنی ریسک/دامنه‌ی بزرگ‌تری داشتند. خروجی Markdown برای «امروز» اما کاملاً روی داده‌ی همین حالا موجود (تسک‌ها، `pomodoro.sessions`، `progressLog` مسیرها) سوار می‌شد، بدون نیاز به مدل داده‌ی تازه — یک آیتم کوچک، مستقل، و کم‌ریسک، دقیقاً طبق همان استراتژی که در جلسه‌های ۸ و ۹ جواب داده بود.

**تغییرات:**
1. **`main.ts`: متد تازه‌ی `exportDailyReport(filename, content)`** روی `LifeFlowPlugin` — یک پوشه‌ی `LifeFlow Reports/` در ریشه‌ی ولت می‌سازد (اگر نبود)، و فایل را می‌نویسد (اگر همان اسم از قبل بود overwrite می‌کند، وگرنه `vault.create` می‌زند). از همان بریج سراسری `window.__lifeflowPlugin` (که در جلسه‌ی ۳ برای `getDataValue`/`setDataValue` ساخته شده بود) در دسترس React قرار گرفت — نیازی به بریج تازه نبود.
2. **`app.jsx`: تابع تازه‌ی `buildDailyMarkdownReport({ tasks, projects, pomodoro })`** یک رشته‌ی Markdown می‌سازد شامل سه بخش: «تسک‌ها» (چک‌لیست انجام‌شده/نشده‌ی امروز، بر اساس همان `isTaskDueOn` که در بقیه‌ی اپ برای «امروز» استفاده می‌شود)، «پومودورو» (تعداد دورهای کاریِ کامل‌شده‌ی امروز + مجموع دقیقه‌ی تمرکز، از `pomodoro.sessions`)، و «یادگیری» (برای هر مسیر یادگیری که یا امروز پیشرفتی رویش ثبت شده یا سهمیه دارد: مقدار امروز + سهمیه + بدهی/بستانکار، با استفاده‌ی مستقیم از `computeTrackBalance` که در جلسه‌ی ۷ ساخته شده بود — بدون منطق تکراری).
3. **دکمه‌ی «خروجی گزارش امروز»** به بالای تب «گزارش روزانه» (همان `DailyReportView` در Planning، جلسه‌ی ۷) اضافه شد — چه لیست مسیرها خالی باشد چه پر، دکمه همیشه بالا نمایش داده می‌شود (کاربر شاید مسیر یادگیری نداشته باشد ولی هنوز بخواهد گزارش تسک/پومودورو بگیرد). پیام موفقیت (مسیر فایل ذخیره‌شده) یا خطا زیر دکمه با همان الگوی رنگی موجود در بقیه‌ی اپ (`var(--text-success)` / `text-rose-400`) نمایش داده می‌شود.
4. `PlanningHub` و فراخوانی آن در `LifeFlowApp` یک prop تازه (`pomodoro`) گرفتند تا به `DailyReportView` برسد — قبلاً این مسیر پاس داده نمی‌شد.
5. **یک باگ ظریفِ منطقه‌زمانی در همین کد تازه پیدا و همان‌جا رفع شد:** اولین نسخه‌ی فیلتر «جلسات پومودوروی امروز» از `session.startedAt.slice(0, 10)` استفاده می‌کرد — که چون `startedAt` با `toISOString()` (UTC) ساخته می‌شود، نزدیک نیمه‌شب می‌توانست یک روز عقب‌تر/جلوتر از `todayKey()` (که بر مبنای ساعت **محلی** است) حساب شود. با تابع از‌قبل‌موجود `dateKeyOf(new Date(session.startedAt))` (که همان قرارداد محلی `todayKey()` را دارد) جایگزین شد.
6. `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، `npm run build`، `node --check main.js`، و بررسی نبود کاراکتر خراب انکودینگ تأیید شدند.

**⚠️ محدودیت‌های شناخته‌شده:**
- فقط گزارش «امروز» — نسخه‌ی هفتگی/ماهانه (چک‌لیست اصلی کار ۴) هنوز نیست؛ وقتی ساخته شود، منطقی است `buildDailyMarkdownReport` را به یک تابع عمومی‌تر با بازه‌ی دلخواه تعمیم دهیم به‌جای کپی کد.
- اگر کاربر دو بار در یک روز خروجی بگیرد، فایل قبلی *overwrite* می‌شود (تصمیم عمدی، نه یک لاگ تجمیعی/append — چون گزارش هر بار از نو از روی وضعیت لحظه‌ای تسک‌ها ساخته می‌شود، نه یک رویداد گذشته).
- تست بصری واقعی در Obsidian هنوز انجام نشده (از جمله اینکه آیا پوشه‌ی «LifeFlow Reports» درست ساخته و فایل درست باز می‌شود).

---

### جلسه‌ی ۱۱ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — قدم دوم کار ۴: گزارش هفتگی به Markdown

طبق دستور کاربر (ادامه‌ی روند «ادامه بده، مطمئن شو به لیمیت نمی‌خوری»)، ابتدا طبق قاعده‌ی ثابت‌شده‌ی جلسه‌ی ۴، `git fetch origin && git log --oneline origin/main -15` اجرا شد و تأیید شد HEAD محلی و ریموت دقیقاً یکی هستند (آخرین کامیت: جلسه‌ی ۱۰) — نیازی به `pull`/`stash` نبود. سپس `npm install` + `npm run build` + `node --check main.js` + `npx tsc --noEmit` به‌عنوان خط‌مبنا قبل از هر تغییری اجرا شدند تا مطمئن شویم پروژه از قبل سالم است (همان ۳ خطای قدیمی/بی‌ربط `obsidian.d.ts`، بدون خطای دیگر).

آیتم انتخاب‌شده: دومین قلم کار ۴ («گزارش‌های تجمیعی هفتگی») — دقیقاً همان ادامه‌ای که در پایان جلسه‌ی ۱۰ به‌عنوان قدم بعدی یادداشت شده بود.

**تصمیم طراحی (چرا یک تابع جدا، نه تعمیمِ `buildDailyMarkdownReport`):** در پایان جلسه‌ی ۱۰ پیشنهاد شده بود `buildDailyMarkdownReport` را به یک تابع عمومی‌تر با بازه‌ی دلخواه تعمیم دهیم. در عمل بررسی شد که ساختار گزارش هفتگی از نظر معنایی با روزانه فرق دارد — نه فقط طول بازه: در گزارش روزانه لیست تسک‌های *انجام‌نشده‌ی امروز* هم نشان داده می‌شود (چون هنوز فرصت هست)، ولی در گزارش هفتگی نمایش «انجام‌نشده‌ها»ی هر هفت روز معنایی ندارد (خیلی از آن‌ها روزهای گذشته‌اند)؛ و برای بخش یادگیری، روزانه به‌طور طبیعی از `computeTrackBalance` (بدهی/بستانکار *تجمعی از روز شروع مسیر*) استفاده می‌کند، ولی این عدد برای «هفته» بی‌ربط است — گزارش هفتگی باید فقط سهمیه/انجام‌شده‌ی *همین هفته* را نشان دهد (سهمیه‌ی هفته = تعداد روزهای «باید»ی هفته × `quotaPerPeriod`، در مقابل مجموع `progressLog` همان بازه). به‌همین‌دلیل به‌جای تحمیل یک پارامتر بازه به تابع موجود (که یا باید دو رفتار متفاوت را با شرط‌های پراکنده در یک تابع می‌گنجاند، یا ریسک شکستن گزارش روزانه‌ی کارکننده را داشت)، یک تابع کاملاً مستقل `buildWeeklyMarkdownReport({ tasks, projects, pomodoro })` نوشته شد که منطق مشابه را با هلسپرهای مشترک (`isTaskDueOn`, `isTrackDueOn`, `dateKeyOf`, `todayKey`) دوباره پیاده می‌کند اما با قواعد جمع‌بندی مخصوص به خودش. `buildDailyMarkdownReport` عمداً دست‌نخورده ماند — دقیقاً همان استراتژی کم‌ریسکِ «یک قلم کوچک و مستقل» که در جلسات ۸ و ۹ جواب داده بود.

**تغییرات:**
1. **`app.jsx`: تابع تازه‌ی `buildWeeklyMarkdownReport({ tasks, projects, pomodoro })`** — بازه‌ی ۷ روز اخیر (شامل امروز) را می‌سازد و سه بخش تولید می‌کند: «تسک‌ها» (مجموع انجام‌شده از مجموع برنامه‌ریزی‌شده در کل هفته، به‌همراه فهرست تسک‌های انجام‌شده با تاریخ هرکدام)، «پومودورو» (تعداد دورهای کاریِ کامل‌شده و مجموع دقیقه‌ی تمرکز در کل بازه، با فیلتر `dateKeyOf` که همان قرارداد منطقه‌زمانی محلی جلسه‌ی ۱۰ را رعایت می‌کند)، و «یادگیری» (برای هر مسیر با `quotaPerPeriod`: مجموع `progressLog` در بازه در برابر سهمیه‌ی هفته = تعداد روزهای «باید» طبق `isTrackDueOn` × سهمیه‌ی هر روز).
2. **`DailyReportView`:** یک دکمه‌ی دوم «خروجی گزارش هفتگی (۷ روز اخیر)» زیر دکمه‌ی گزارش روزانه‌ی موجود اضافه شد، با state پیام موفقیت/خطای مستقل از دکمه‌ی روزانه (`weeklyMsg`/`weeklyErr`) تا کلیک روی یکی، پیام دیگری را پاک نکند. هر دو دکمه از همان متد عمومی `plugin.exportDailyReport(filename, content)` (ساخته‌شده در جلسه‌ی ۱۰) استفاده می‌کنند — فقط نام فایل فرق دارد (`weekly-${todayKey()}.md` در برابر `${todayKey()}.md`)؛ چون آن متد از قبل کاملاً عمومی بود (فقط اسم فایل + محتوا می‌گیرد)، نیازی به تغییر در `main.ts` نبود.
3. `npm run build` + `node --check main.js` + `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط) بعد از تغییر تأیید شدند؛ بررسی نبود کاراکتر خراب انکودینگ (`\xef\xbf\xbd`) هم روی `main.js` و `src/app.jsx` انجام شد (چیزی پیدا نشد). علاوه‌بر این، منطق تولید بازه‌ی ۷روزه با یک اسکریپت Node مستقل (خارج از باندل) روی یک تاریخ ثابت تست شد تا از صحت مرزهای بازه (شامل بودن امروز، ۶ روز قبل) مطمئن شویم.

**⚠️ محدودیت‌های شناخته‌شده:**
- نسخه‌ی ماهانه (سومین بخش چک‌لیست کار ۴) هنوز ساخته نشده؛ اگر تعداد این «توابع گزارش مستقل» بیشتر شود (مثلاً با اضافه‌شدن ماهانه)، ارزش تعمیم به یک تابع پارامتری واحد بیشتر می‌شود — این تصمیم برای جلسه‌ای که واقعاً گزارش ماهانه ساخته می‌شود، بازبینی شود.
- فهرست «تسک‌های انجام‌شده» در گزارش هفتگی، تسک‌های تکرارشونده (مثل یک تسک روزانه که هر روز انجام شده) را به‌شکل یک ردیف جدا برای هر روز نشان می‌دهد (نه یک ردیف با شمارنده) — برای اکثر موارد خواناتر است، ولی برای تسک‌های خیلی پرتکرار ممکن است لیست طولانی شود.
- تست بصری واقعی در Obsidian (مثل بقیه‌ی موارد قبلی) هنوز انجام نشده، از جمله اینکه آیا دو دکمه‌ی زیر هم در فضای موبایل/دسکتاپ خوب جا می‌شوند.

---

### جلسه‌ی ۱۲ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — سومین قلم کار ۴: سه نمودار گزارش‌گیری

طبق دستور کاربر («ادامه بده»، توکن گیت‌هاب هم توسط کاربر تأیید شد که محدود به همین ریپازیتوری است و نگرانی امنیتی ندارد)، طبق قاعده‌ی ثابت‌شده ابتدا `git fetch origin && git log --oneline origin/main -5` چک شد — کاملاً هماهنگ با HEAD محلی (آخرین کامیت: جلسه‌ی ۱۱). آخرین آیتم باز چک‌لیست کار ۴ («نمودارها») انتخاب شد.

**چرا این آیتم کم‌ریسک بود:** بررسی کد نشان داد پروژه از قبل دو کامپوننت چارت عمومی و قابل‌استفاده مجدد دارد (`SimpleBarChart`, `SimpleLineChart` — همان‌هایی که در `PomodoroReportView`/`StudyProgress`/`WeeklyOverviewChart` استفاده می‌شوند)؛ کاری که این جلسه لازم بود فقط **تجمیع داده** بود، نه ساخت زیرساخت چارت تازه — دقیقاً همان الگوی کم‌ریسکی که در جلسات قبل جواب داده.

**تغییرات:**
1. **`app.jsx`: کامپوننت تازه‌ی `ReportChartsSection({ tasks, projects, pomodoro })`** سه چارت برای بازه‌ی ۷ روز اخیر (با همان helper موجود `lastNDays(7)`) می‌سازد:
   - **تکمیل تسک به تفکیک ربع آیزنهاور:** تعداد تسک‌های `status === "done"` که `completedDate` آن‌ها در ۷ روز اخیر است، بر اساس فیلد `quad` تسک گروه‌بندی و در یک `SimpleBarChart` نشان داده می‌شود.
   - **زمان تمرکز پومودورو به تفکیک ربع آیزنهاور:** برای هر جلسه‌ی کاریِ کامل‌شده‌ی پومودورو در ۷ روز اخیر، از طریق `taskId` به تسک لینک‌شده و ربع آن می‌رسیم و `durationMin` را جمع می‌زنیم؛ نتیجه در یک `SimpleBarChart` دوم (رنگ متفاوت از چارت اول برای تمایز بصری) نشان داده می‌شود.
   - **روند یادگیری:** مجموع `progressLog.amount` همه‌ی مسیرهای یادگیری در هر روز از ۷ روز اخیر، در یک `SimpleLineChart` نشان داده می‌شود؛ اگر هیچ ثبتی در این بازه نباشد، به‌جای چارت خالی یک پیام راهنما نشان داده می‌شود.
   - چون هر ۴ ربع آیزنهاور در `SimpleBarChart` با یک رنگ ثابت (نه رنگ اختصاصی هر ربع) رسم می‌شوند (برای پرهیز از تغییردادن کامپوننت مشترک `SimpleBarChart` که در جاهای دیگر هم استفاده می‌شود)، زیر هر دو چارت ربع یک خط راهنمای متنی («ربع ۱: فوری و مهم — ربع ۲: ...») اضافه شد تا محورهای «ربع ۱..۴» خوانا بمانند.
   - **نکته‌ی طراحی درباره‌ی نمودار روند یادگیری:** چون واحد اندازه‌گیری بین مسیرهای مختلف یادگیری می‌تواند فرق کند (صفحه، مورد، دقیقه و ...)، جمع‌کردن مقادیرشان در یک عدد واحد از نظر واحد دقیق نیست؛ این محدودیت آگاهانه پذیرفته و زیر چارت با یک توضیح کوچک اعلام شد («فقط یک روند کلی است، نه مقدار دقیق قابل مقایسه») — ساخت یک چارت جدا به‌ازای هر مسیر برای این جلسه به‌عنوان یک بهبود آینده (نه یک باگ) در نظر گرفته شد، چون در این جلسه فقط یک روند کلی خواسته شده بود، نه تفکیک به مسیر.
   - محاسبه‌ی روز تقویمیِ `completedAt` جلسات پومودورو عمداً با `dateKeyOf(new Date(s.completedAt))` انجام شد (نه `.slice(0,10)` که در تابع قدیمی‌تر `pomodoroStatsFor` استفاده می‌شود) — طبق همان درسِ رفع باگ منطقه‌زمانی در جلسه‌ی ۱۰ (چون `completedAt` با `toISOString()`/UTC ساخته می‌شود، `slice(0,10)` نزدیک نیمه‌شب می‌تواند یک روز اشتباه بدهد). تابع قدیمی‌تر `pomodoroStatsFor` عمداً دست‌نخورده ماند (خارج از دامنه‌ی این جلسه، ریسک بی‌جهت تغییر کد کارکننده).
2. **`PlanningHub`:** یک زیرتب چهارم «نمودارها» به `SubTabs` اضافه شد (کنار «برنامه روزانه»/«اهداف»/«گزارش روزانه»)؛ رندر شرطی جدید `sub === "charts"` کامپوننت تازه را با `tasks`/`projects`/`pomodoro` که از قبل به `PlanningHub` می‌رسیدند (بدون نیاز به prop تازه) صدا می‌زند.
3. منطق تجمیع (فیلتر بازه‌ی ۷روزه برای تکمیل تسک و جلسات پومودورو) با یک اسکریپت Node مستقل خارج از باندل، روی داده‌ی فرضی تست شد تا مطمئن شویم مرز ۷ روز درست اعمال می‌شود و نوع «break» در محاسبه‌ی دقیقه‌ی پومودورو حساب نمی‌شود.
4. `npm run build` + `node --check main.js` + `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط) تأیید شدند؛ بررسی نبود کاراکتر خراب انکودینگ روی `main.js`/`src/app.jsx` هم انجام شد (چیزی پیدا نشد).

**⚠️ محدودیت‌های شناخته‌شده:**
- چارت روند یادگیری واحدهای مختلف را جمع می‌زند (محدودیت آگاهانه، توضیح داده‌شده بالا).
- بازه‌ی هر سه چارت ثابت روی «۷ روز اخیر» است؛ انتخاب بازه‌ی دلخواه (مثل ۳۰ روز) هنوز اضافه نشده.
- تست بصری واقعی در Obsidian (مثل بقیه‌ی موارد قبلی) هنوز انجام نشده، از جمله خوانایی خط راهنمای ربع‌ها در صفحه‌ی کوچک موبایل.

**نتیجه:** با این جلسه، هر سه‌ی زیرمجموعه‌های چک‌لیست «نمودارها»ی کار ۴ ساخته شدند. تنها آیتم باز کار ۴ اکنون «گزارش تجمیعی ماهانه» است.

---

### جلسه‌ی ۱۳ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — چهارمین و آخرین قلم کار ۴: گزارش ماهانه

طبق دستور کاربر («ادامه بده»)، طبق قاعده‌ی ثابت‌شده ابتدا `git fetch origin && git log --oneline origin/main -5` چک شد — هماهنگ با HEAD محلی (آخرین کامیت: جلسه‌ی ۱۲). آخرین آیتم باز کار ۴ («گزارش تجمیعی ماهانه») انتخاب شد تا این کار به‌طور کامل بسته شود.

**تصمیم طراحی (بازه‌ی «ماهانه» = ۳۰ روز غلتان، نه ماه تقویمی):** چک‌لیست فقط گفته بود «ماهانه» بدون مشخص‌کردن اینکه منظور ماه تقویمی (شمسی یا میلادی) است یا یک بازه‌ی ثابت. برای هماهنگی با الگوی از‌قبل‌جاافتاده‌ی گزارش هفتگی (که «۷ روز اخیر»، نه «هفته‌ی تقویمی از شنبه» است) و برای پرهیز از پیچیدگی تبدیل تقویم شمسی/میلادی در این جلسه، «ماهانه» هم به‌همان‌شکل **بازه‌ی غلتان ۳۰ روز اخیر (شامل امروز)** تعریف شد. اگر کاربر بازه‌ی تقویمی واقعی (مثل «مرداد ماه») را ترجیح دهد، این یک تغییر جدا و کوچک برای جلسه‌ی بعد است.

**تفاوت طراحی نسبت به گزارش هفتگی (چرا کپی مستقیم کافی نبود):** برای گزارش هفتگی، فهرست تک‌تک تسک‌های انجام‌شده با تاریخ (`- [x] تاریخ — عنوان`) منطقی بود چون حداکثر ۷ خط بود. برای ۳۰ روز، همان الگو برای یک تسک تکرارشونده‌ی روزانه می‌توانست تا ۳۰ خط تکراری تولید کند — عملاً غیرقابل‌خواندن. به‌جایش، `buildMonthlyMarkdownReport` تسک‌های انجام‌شده را **بر اساس عنوان جمع‌بندی** می‌کند (`{عنوان: تعداد دفعات انجام در این ۳۰ روز}`، مرتب‌شده نزولی بر اساس تعداد) — یعنی به‌جای «۳۰ خط»، خروجی چیزی مثل «نماز صبح: ۲۸ بار» است. این دقیقاً همان نوع تصمیمی است که در جلسه‌ی ۱۱ برای گزارش هفتگی گرفته نشد (چون طول لیست آنجا مسئله نبود)؛ به همین دلیل هم `buildMonthlyMarkdownReport` یک تابع کاملاً مستقل نوشته شد، نه کپی گزارش هفتگی با پارامتر طول بازه — دقیقاً به همان دلیلی که جلسه‌ی ۱۱ برای مستقل نگه‌داشتن از روزانه ثبت کرده بود: رفتارهای معنایی متفاوت، نه فقط طول بازه‌ی متفاوت.

**تغییرات:**
1. **`app.jsx`: تابع تازه‌ی `buildMonthlyMarkdownReport({ tasks, projects, pomodoro })`** — بازه‌ی ۳۰ روز اخیر را می‌سازد: «تسک‌ها» (مجموع انجام‌شده از مجموع برنامه‌ریزی‌شده در کل بازه + فهرست عنوان‌های انجام‌شده به‌همراه تعداد دفعات، نه تک‌تک تاریخ‌ها)، «پومودورو» (تعداد دورهای کاریِ کامل‌شده و مجموع دقیقه‌ی تمرکز در کل بازه)، «یادگیری» (مجموع `progressLog` هر مسیر در بازه در برابر سهمیه‌ی ۳۰روزه = تعداد روزهای «باید» × سهمیه‌ی هر روز؛ همان الگوی `computeTrackBalance`/گزارش هفتگی ولی برای بازه‌ی بلندتر).
2. **`DailyReportView`:** دکمه‌ی سوم «خروجی گزارش ماهانه (۳۰ روز اخیر)» زیر دو دکمه‌ی قبلی اضافه شد، با state پیام موفقیت/خطای کاملاً مستقل (`monthlyMsg`/`monthlyErr`) — همان الگوی دکمه‌ی هفتگی، تکرار شد نه بازنویسی. نام فایل خروجی: `monthly-${todayKey()}.md`؛ از همان متد عمومی `plugin.exportDailyReport` (بدون نیاز به تغییر `main.ts`) استفاده شد.
3. `npm run build` + `node --check main.js` + `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط) تأیید شدند؛ بررسی نبود کاراکتر خراب انکودینگ روی `main.js`/`src/app.jsx` هم انجام شد (چیزی پیدا نشد).

**⚠️ محدودیت‌های شناخته‌شده:**
- «ماهانه» یک بازه‌ی غلتان ۳۰روزه است، نه ماه تقویمی واقعی (شمسی یا میلادی) — تصمیم عمدی، توضیح بالا.
- فهرست تسک‌های انجام‌شده فقط شمارنده نشان می‌دهد، نه تاریخ دقیق هر بار (برخلاف گزارش هفتگی) — یک تصمیم آگاهانه برای خوانایی، نه یک محدودیت فنی؛ اگر لازم شد جزئیات کامل هم قابل‌اضافه‌شدن است.
- تست بصری واقعی سه دکمه‌ی روی هم (روزانه/هفتگی/ماهانه) در فضای موبایل Obsidian هنوز انجام نشده.

**نتیجه‌ی کلی کار ۴:** با این جلسه، تمام موارد چک‌لیست اصلی کار ۴ (گزارش‌های روزانه/هفتگی/ماهانه، نمودارهای سه‌گانه، خروجی Markdown) تکمیل شدند. کار ۴ به‌طور کامل از صف کارهای باز خارج شد.

---

### جلسه‌ی ۱۴ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — آخرین آیتم باز کار ۲: تنظیمات گزارش‌گیری

طبق دستور کاربر، طبق قاعده‌ی ثابت‌شده ابتدا `git fetch origin && git log --oneline origin/main -15` چک شد — هماهنگ با HEAD محلی (آخرین کامیت: جلسه‌ی ۱۳). سپس `npm install` + `npm run build` + `node --check main.js` + `npx tsc --noEmit` به‌عنوان خط‌مبنا اجرا شدند (همان ۳ خطای قدیمی/بی‌ربط `obsidian.d.ts`، بدون خطای دیگر).

**چرا این آیتم:** تنها آیتم باز کار ۲ («تنظیمات گزارش‌گیری») توضیح داده بود که منتظر ساخته‌شدن کار ۴ می‌ماند تا معلوم شود چه چیزی قابل‌تنظیم است. حالا که کار ۴ (جلسه‌ی ۱۰ تا ۱۳) کامل شده، بررسی کد نشان داد تنها نقطه‌ی پیکربندی‌پذیرِ منطقی در مسیر گزارش‌گیریِ فعلی، **نام پوشه‌ای است که گزارش‌های Markdown (روزانه/هفتگی/ماهانه) در آن ذخیره می‌شوند** (فعلاً هاردکد به `"LifeFlow Reports"` در `main.ts`) — این یک آیتم کوچک، مستقل و کم‌ریسک بود، دقیقاً همان الگویی که در جلسات قبل جواب داده.

**تصمیم طراحی (این تنظیم فقط در `PluginSettingTab`، نه در `SettingsModal` داخل اپ):** بررسی `SettingsModal` (کامپوننت React تنظیمات داخل خودِ اپ) نشان داد این کامپوننت هیچ‌وقت برای تنظیمات جدیدتر (`features`/تب‌ها در جلسه‌ی ۴، `taskDefaults` در جلسه‌ی ۵) به‌روزرسانی نشده بود — آن‌ها فقط در `settings-tab.ts` (تب رسمی تنظیمات خودِ Obsidian) اضافه شدند، چون هر دو UI از یک منبع `data.json` می‌خوانند/می‌نویسند (تصمیم جلسه‌ی ۴) و نیازی به تکرار در هر دو سطح نیست. این جلسه هم از همان الگو پیروی کرد: تنظیم تازه فقط به `settings-tab.ts` اضافه شد.

**تغییرات:**
1. **`src/settings-tab.ts`:** فیلد `reports: { folderName: string }` به اینترفیس `LifeFlowSettings` و `DEFAULT_SETTINGS` (پیش‌فرض: `"LifeFlow Reports"`، همان مقدار قبلی هاردکد) اضافه شد؛ `readSettings()` این فیلد را هم مثل بقیه merge می‌کند (سازگار با `data.json` قدیمی‌تر که این کلید را ندارد). یک بخش تازه‌ی «گزارش‌گیری» با یک `Setting` از نوع متن (نام پوشه) اضافه شد؛ در `onChange`، مقدار trim می‌شود و اگر خالی باشد به‌جای ذخیره‌ی رشته‌ی خالی، همان پیش‌فرض ذخیره می‌شود (دقیقاً همان الگوی fallback که «مدت پیش‌فرض» در جلسه‌ی ۵ برای عدد استفاده کرده بود).
2. **`src/main.ts`:** به‌جای رشته‌ی هاردکد `"LifeFlow Reports"` در `exportDailyReport`، متد خصوصی تازه‌ی `getReportsFolderName()` اضافه شد که مستقیماً کلید `lifeflow_settings_v1` را از همان کش داخلی (`this.data`، از طریق `getDataValue` موجود) می‌خواند، `JSON.parse` می‌کند، و `reports.folderName` را برمی‌گرداند — با `try/catch` و بررسی نوع، اگر تنظیمات نبود/خراب بود/فیلد خالی بود، به پیش‌فرض قدیمی برمی‌گردد (هیچ‌وقت export را نمی‌شکند). ثابت‌های کوچک `SETTINGS_KEY`/`DEFAULT_REPORTS_FOLDER` عمداً در خودِ `main.ts` هم تعریف شدند (نه import از `settings-tab.ts`) — همان دلیل کم‌ریسکِ کپی‌کردن مقادیر کوچک و باثبات که `settings-tab.ts` برای `QUADRANT_OPTIONS`/`PRIORITY_OPTIONS`/`DAYPART_OPTIONS` (کپی از `app.jsx`) استفاده کرده بود. **نکته:** چون این خواندن هربار در لحظه‌ی export مستقیماً از `this.data` انجام می‌شود (نه یک state ذخیره‌شده در یک کامپوننت React)، نیازی به مکانیزم زنده‌سازی با `CustomEvent("lifeflow-settings-changed")` (که بین `settings-tab.ts` و `app.jsx` برای هماهنگی UI باز استفاده می‌شود) نبود — همیشه آخرین مقدار ذخیره‌شده را می‌خواند.
3. `src/app.jsx` **دست‌نخورده ماند** — منطق ساخت پوشه/فایل کاملاً در `main.ts` است؛ اپ React فقط `plugin.exportDailyReport(filename, content)` را صدا می‌زند (بدون آگاهی از نام پوشه)، پس نیازی به تغییر مدل تنظیمات در فایل تک‌جزئی ۳۲۰۰+ خطی نبود — کمترین ریسک ممکن برای این آیتم.
4. `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط، بدون خطای جدید)، `npm run build`، `node --check main.js` تأیید شدند؛ بررسی نبود کاراکتر خراب انکودینگ (`\xef\xbf\xbd`) روی `main.js`/`src/main.ts`/`src/settings-tab.ts` هم انجام شد (چیزی پیدا نشد؛ متن فارسی تازه هم مثل بقیه‌ی باندل به‌شکل escape شده‌ی esbuild در خروجی نهایی موجود بود — با `python3` تأیید شد).

**بازبینی جانبی مستندات (نه کد):** در همین جلسه یک ناهماهنگی قدیمی در چک‌لیست پیدا شد — آیتم اول کار ۰ («ساخت `package.json`/`tsconfig.json`/`esbuild.config.mjs`») واقعاً از جلسه‌ی ۲ تکمیل شده بود (طبق لاگ همان جلسه)، ولی چک‌باکسش هیچ‌وقت تیک نخورده بود. این‌جا تصحیح شد؛ کدی تغییر نکرد.

**⚠️ محدودیت‌های شناخته‌شده:**
- نام پوشه فقط یک رشته‌ی ساده است؛ اگر کاربر مقداری شامل `/` (زیرپوشه‌ی چندسطحی) وارد کند، به `vault.createFolder` عیناً پاس داده می‌شود — رفتار آن (ساخت تودرتو یا خطا) تست نشده، چون کاربرد پیش‌بینی‌شده یک نام پوشه‌ی تخت است.
- اگر کاربر بعد از چندین بار export نام پوشه را عوض کند، گزارش‌های قبلی در پوشه‌ی قدیمی می‌مانند (منتقل نمی‌شوند) — تصمیم عمدی، در توضیح تنظیم هم ذکر شده.
- تست بصری واقعی این تنظیم در یک Obsidian واقعی هنوز انجام نشده (مثل بقیه‌ی موارد قبلی، سؤال باز شماره‌ی ۶ در بخش ۵ همچنان به قوت خودش باقی است).

**نتیجه:** آخرین آیتم باز کار ۲ («تنظیمات گزارش‌گیری») بسته شد. کار ۲ به‌طور کامل فقط با یک آیتم باز مانده («تنظیمات ظاهری عمیق‌تر» — فونت/تراکم/رنگ‌های دسته‌بندی) که در جلسه‌ی بعد قابل انتخاب است.

---

### جلسه‌ی ۱۵ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — بخش «فونت» از تنظیمات ظاهری عمیق‌تر

طبق دستور کاربر («ادامه بده»)، طبق قاعده‌ی ثابت‌شده ابتدا `git fetch origin && git log --oneline origin/main -5` چک شد — هماهنگ با HEAD محلی (آخرین کامیت: جلسه‌ی ۱۴). سپس `npm run build` + `node --check main.js` + `npx tsc --noEmit` به‌عنوان خط‌مبنا اجرا شدند (همان ۳ خطای قدیمی/بی‌ربط، بدون خطای دیگر).

**چرا این آیتم:** تنها آیتم باز کار ۲ («تنظیمات ظاهری عمیق‌تر: فونت/تراکم/رنگ‌های دسته‌بندی») سه زیرموضوع جداگانه را در یک خط جمع کرده بود. طبق همان استراتژی «یک قلم کوچک و مستقل در هر جلسه»، فقط زیربخش **فونت** انتخاب شد — چون بررسی کد نشان داد ریشه‌ی اپ (`lifeflow-app-root`) از قبل دقیقاً یک نقطه‌ی `style={{fontFamily: ...}}` هاردکد داشت (نتیجه‌ی کار ۱)، یعنی قلاب‌شدن یک تنظیم به آن یک تغییر نقطه‌ای و کم‌ریسک بود. «تراکم» (که به تغییر فاصله‌گذاری در ده‌ها جای فایل تک‌جزئی ۳۲۰۰+ خطی نیاز دارد) و «رنگ‌های دسته‌بندی» (که به یک تصمیم طراحی جدا درباره‌ی این‌که کدام رنگ‌ها اصلاً باید قابل‌تنظیم باشند نیاز دارد — نگاه کنید به یادداشت جلسه‌ی ۳ درباره‌ی رنگ‌های «chrome» در برابر «داده/دسته‌بندی») عمداً برای جلسات بعد باقی گذاشته شدند.

**تغییرات:**
1. **`app.jsx`:** ثابت `DEFAULT_APPEARANCE = { fontFamily: "default" }` و تابع `resolveFontFamily(id)` اضافه شدند (دقیقاً بعد از `DEFAULT_FEATURES`/`mergeFeatures`، هم‌جوار بقیه‌ی ثابت‌های تنظیمات). چهار گزینه: `"default"` (همان رشته‌ی قبلاً هاردکدشده‌ی `var(--font-text, 'Vazirmatn'), 'Vazirmatn', Tahoma, sans-serif` — بدون تغییر بصری برای کاربران فعلی)، `"obsidian"` (`inherit`، دقیقاً فونت تم)، `"vazirmatn"` (همیشه Vazirmatn، بدون فالبک به فونت تم)، `"system"` (فونت سیستم‌عامل). مقدار ناشناخته/نامشخص هم به `"default"` برمی‌گردد (رفتار قبلی حفظ می‌شود). `loadSettings()` حالا `appearance` را هم مثل `features`/`taskDefaults` merge می‌کند (سازگار با `data.json` قدیمی‌تر).
2. **ریشه‌ی `LifeFlowApp`:** خط `style: { fontFamily: "var(--font-text, ...)" }` با `style: { fontFamily: resolveFontFamily(settings.appearance?.fontFamily) }` جایگزین شد — تنها نقطه‌ی تغییر در کل فایل تک‌جزئی، دقیقاً همان‌جایی که فونت از قبل تعیین می‌شد.
3. **`settings-tab.ts`:** فیلد `appearance: { fontFamily: string }` به اینترفیس/`DEFAULT_SETTINGS`/منطق merge اضافه شد (همان الگوی `reports`/`taskDefaults`). ثابت `FONT_OPTIONS` (برچسب‌های فارسی، کپی‌شده از همان چهار id بالا — همان دلیل کم‌ریسکِ کپی‌کردن مقادیر کوچک که برای `QUADRANT_OPTIONS` و غیره استفاده شده) و یک `Setting` از نوع dropdown با عنوان «فونت متن پلاگین» زیر بخش «ظاهر و زبان» (کنار زبان رابط کاربری) اضافه شد.
4. یک اسکریپت Node مستقل خارج از باندل روی خودِ منطق `resolveFontFamily` (کپی دستی همان چهار حالت + یک id نامعتبر) اجرا شد تا مطمئن شویم مقدار `undefined`/`"default"`/id نامعتبر همگی دقیقاً همان رشته‌ی قدیمی را برمی‌گردانند (یعنی کاربرانی که هنوز این تنظیم را باز نکرده‌اند هیچ تغییر بصری نمی‌بینند) و سه حالت دیگر مقدار درست خودشان را می‌دهند.
5. `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط، بدون خطای جدید)، `npm run build`، `node --check main.js` تأیید شدند؛ بررسی نبود کاراکتر خراب انکودینگ روی `main.js`/`src/app.jsx`/`src/settings-tab.ts` هم انجام شد (چیزی پیدا نشد).

**⚠️ محدودیت‌های شناخته‌شده:**
- گزینه‌ی «فونت سیستم‌عامل» یک فهرست فونت وب رایج (`-apple-system, BlinkMacSystemFont, "Segoe UI", Tahoma, sans-serif`) است، نه واقعاً «فونت پیش‌فرض سیستم‌عامل کاربر» به‌شکل پویا — محدودیت شناخته‌شده‌ی وب/CSS، نه باگ.
- این تنظیم فقط `font-family` را عوض می‌کند؛ اندازه/ضخامت فونت (که بخشی از «تراکم» محسوب می‌شود) دست‌نخورده ماند.
- تست بصری واقعی گزینه‌های مختلف فونت در یک Obsidian واقعی هنوز انجام نشده (سؤال باز شماره‌ی ۶ در بخش ۵ همچنان به قوت خودش باقی است).

**نتیجه:** از سه زیرموضوع «تنظیمات ظاهری عمیق‌تر»، فقط «فونت» بسته شد. «تراکم» و «رنگ‌های دسته‌بندی» در صف کارها باز مانده‌اند (چک‌لیست بالای کار ۲ به‌روزرسانی شد).

---

### جلسه‌ی ۱۶ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — بهبود پنجره‌ی «تسک جدید/ویرایش تسک»

طبق دستور مستقیم کاربر («اول پنجره‌ای که هنگام ساخت تسک و ... باز می‌شود را بهبود بده»)، این جلسه به‌جای ادامه‌ی صف کارها، یک درخواست مشخص از خودِ کاربر را انجام داد. طبق قاعده‌ی ثابت‌شده ابتدا `git fetch origin` چک شد — هماهنگ با HEAD محلی (آخرین کامیت: جلسه‌ی ۱۵). سپس `npm run build` + `node --check main.js` + `npx tsc --noEmit` به‌عنوان خط‌مبنا اجرا شدند (همان ۳ خطای قدیمی/بی‌ربط، بدون خطای دیگر).

**مشکل:** `AddTaskModal` (کامپوننت پنجره‌ی «تسک جدید»/«ویرایش تسک») همیشه همه‌ی فیلدها را همزمان نشان می‌داد: عنوان، توضیح، ربع آیزنهاور، اولویت، زمان روز، زمان‌بندی دقیق (ساعت+مدت)، نوع تسک (ساده/روند‌دار) با فیلدهای شرطی، تکرار با فیلدهای شرطی (هفتگی/ماهانه/سالانه)، یادآوری، برچسب، و زیرتسک‌ها — یک فرم بلند با ده‌ها ردیف، حتی برای ساده‌ترین «افزودن سریع یک تسک». این محتمل‌ترین منظور از «بهبود» بود: کاهش طول/شلوغی پنجره برای حالت رایج، بدون حذف هیچ قابلیتی.

**راه‌حل (progressive disclosure):** فیلدهای اصلی/پرکاربرد (عنوان، توضیح، ربع، اولویت، زمان روز) همیشه نمایان ماندند. بقیه (زمان‌بندی دقیق، نوع تسک/روند‌دار، تکرار، یادآوری، برچسب، زیرتسک‌ها) پشت یک دکمه‌ی تاشو با برچسب «گزینه‌های بیشتر — زمان‌بندی، تکرار، یادآوری، برچسب، زیرتسک» و یک شورون (استفاده از آیکون `chevron-right` موجود با `rotate(90deg)`/`rotate(-90deg)` به‌جای اضافه‌کردن آیکون تازه به `ICON_PATHS` — کم‌ریسک‌تر، چون به جدول آیکون‌های مشترک دست‌نخورد) پنهان شدند.

**جزئیات مهم UX (برای این‌که چیزی گم نشود):** اگر پنجره در **حالت ویرایش** یک تسک باز شود که از قبل هرکدام از این فیلدهای «بیشتر» را پر کرده (ساعت مشخص، یادآوری روشن، تکرار غیر از «هیچ»، برچسب، زیرتسک، یا نوع «روند‌دار»)، بخش «گزینه‌های بیشتر» **از قبل باز** است — کاربر هیچ‌وقت با ویرایش یک تسک، داده‌ی موجودش را «گم‌شده» نمی‌بیند. این با یک `hasAdvancedData` محاسبه‌شده از `initialTask` در بدو mount کنترل می‌شود.

**تغییرات (`app.jsx`، فقط داخل `AddTaskModal`؛ هیچ فایل دیگری لمس نشد):**
1. `hasAdvancedData` (فقط در حالت ویرایش، بر اساس `time`/`reminder`/`recurrence!=="none"`/`tag`/`subtasks.length`/`progressType==="progressive"` تسک موجود) و `const [showMore, setShowMore] = useState(hasAdvancedData)` اضافه شدند.
2. بعد از چیپ‌های «زمان روز»، یک دکمه‌ی تاشوی تمام‌عرض اضافه شد که `showMore` را toggle می‌کند.
3. بلوک «زمان‌بندی دقیق» تا «زیرتسک‌ها» (که قبلاً مستقیم زیر هم رندر می‌شدند) داخل `showMore && React.createElement(React.Fragment, null, ...)` پیچیده شدند — بدون تغییر در ترتیب/محتوای خودِ آن فیلدها یا در منطق `submit()`.
4. یک باگ کوچک هم در همین بازبینی پیدا و اصلاح شد: فیلد «زیرتسک‌ها» برخلاف همه‌ی فیلدهای دیگر فرم، هیچ لیبل `<p>` بالای خودش نداشت (فقط placeholder). لیبل «زیرتسک‌ها (اختیاری)» اضافه شد و بخش «(اختیاری)» تکراری از placeholder حذف شد (چون حالا در لیبل آمده).
5. هیچ تغییری در `submit()`، مدل داده‌ی تسک، یا رفتار پیش‌فرض مقادیر (`taskDefaults` از جلسه‌ی ۵) داده نشد — این یک تغییر خالص UI/نمایش است.

**تأیید:**
- `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، `npm run build`، `node --check main.js` تأیید شدند.
- نبود کاراکتر خراب انکودینگ (`\xef\xbf\xbd`) روی `main.js`/`src/app.jsx` چک شد (چیزی پیدا نشد).
- برخلاف جلسه‌ی ۱۵ (که برای متن فارسی تازه در `app.jsx` از فرمت escape‌شده‌ی `\uXXXX` پیروی کرده بود)، این‌جا دو رشته‌ی فارسی تازه (برچسب دکمه‌ی تاشو، لیبل زیرتسک‌ها) به‌صورت UTF-8 خام نوشته شدند؛ با `python3` تأیید شد که esbuild در زمان build خودش این‌ها را به همان فرمت `\uXXXX` (با حروف بزرگ) escape کرده و داخل `main.js` نهایی موجودند — یعنی نوشتن UTF-8 خام در سورس مشکلی ندارد، چون فرمت خروجی نهایی را خودِ esbuild یکدست می‌کند، نه رسم‌الخط سورس.

**⚠️ محدودیت‌های شناخته‌شده:**
- تست بصری واقعی (باز/بسته‌شدن روان دکمه‌ی تاشو، جای درست شورون در RTL) هنوز در یک Obsidian واقعی انجام نشده (سؤال باز شماره‌ی ۶ در پایین همچنان به قوت خودش باقی است).
- «گزینه‌های بیشتر» فقط شامل فیلدهای اختیاری/کم‌کاربردتر است؛ اگر در آینده معلوم شود کاربران غالباً از یکی از این فیلدها (مثلاً یادآوری) در همان نگاه اول استفاده می‌کنند، می‌شود آن را به بخش همیشه‌نمایان منتقل کرد.
- این جلسه بخشی از صف کارهای رسمی (بخش ۲) را جلو نبرد؛ یک درخواست مستقیم و جداگانه از کاربر بود. صف کارها (تراکم/رنگ‌های دسته‌بندی از کار ۲، و کار ۵/۳/۶) دست‌نخورده باقی است.

**نتیجه:** پنجره‌ی «تسک جدید» برای حالت رایج (افزودن سریع) حدوداً نصف طول قبلی را دارد؛ همه‌ی قابلیت‌های قبلی بدون تغییر پشت «گزینه‌های بیشتر» در دسترس‌اند و در حالت ویرایش خودکار باز می‌شوند اگر قبلاً استفاده شده باشند.

---

### جلسه‌ی ۱۷ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — بخش «تراکم» از تنظیمات ظاهری عمیق‌تر

طبق دستور کاربر («ادامه بده»)، برگشتیم به صف کارها. طبق قاعده‌ی ثابت‌شده ابتدا `git fetch origin` چک شد — هماهنگ با HEAD محلی (آخرین کامیت: جلسه‌ی ۱۶). سپس `npm run build` + `node --check main.js` + `npx tsc --noEmit` به‌عنوان خط‌مبنا اجرا شدند (همان ۳ خطای قدیمی/بی‌ربط، بدون خطای دیگر).

**چرا این آیتم:** از سه زیرموضوع باقیمانده‌ی «تنظیمات ظاهری عمیق‌تر»، در جلسه‌ی ۱۵ «فونت» بسته شده بود و «تراکم» عمداً به‌خاطر بزرگ‌تر بودن دامنه‌اش عقب افتاده بود. طبق همان یادداشت جلسه‌ی ۱۵ («تراکم به تغییر فاصله‌گذاری در ده‌ها جای فایل تک‌جزئی نیاز دارد»)، این‌بار به‌جای دست‌زدن مستقیم به `className`‌های `app.jsx`، از همان استراتژی CSS-override کار ۱ (نگاه کنید به یادداشت جلسه‌ی ۳ در بخش تصمیم‌های طراحی) استفاده شد — یعنی همان‌قدر ریسک‌پذیر و کم‌ریسک ماند، فقط دامنه‌اش بزرگ‌تر بود.

**تحلیل مقدماتی:** با `grep` روی `app.jsx`، مقادیر واقعاً استفاده‌شده‌ی کلاس‌های فاصله‌گذاری Tailwind (`p-*`, `px-*`, `py-*`, `gap-*`, `mb-*`, `mt-*`) استخراج شدند (نه هر مقدار نظری ممکن Tailwind، فقط آن‌هایی که واقعاً در فایل به کار رفته‌اند).

**طراحی نگاشت «فشرده»:** یک قاعده‌ی یکدست تعریف شد: هر مقدار spacing یک «پله» در مقیاس رسمی Tailwind (`0.5→1→1.5→2→2.5→3→3.5→4→5→6→8→10`) پایین می‌آید (مثلاً `p-4` به‌اندازه‌ی `p-3` می‌شود، `gap-2` به‌اندازه‌ی `gap-1.5`). مقدار `0.5` (کوچک‌ترین) بدون تغییر ماند تا هیچ عنصری کاملاً بدون padding نشود. این نگاشت با یک اسکریپت Python مکانیکی (نه دستی) روی ۴۶ مقدار واقعی استفاده‌شده تولید شد تا خطای تایپی حذف شود؛ مقادیری که مقصدشان با مبدأ یکسان بود (یعنی `0.5→0.5`) حذف شدند (چون تغییری در ظاهر ایجاد نمی‌کنند) و ۴۷ قاعده‌ی نهایی باقی ماند.

**تغییرات:**
1. **`styles.css`:** ۴۷ قاعده‌ی override اضافه شد، همه زیر سلکتور `.lifeflow-app-root.lifeflow-density-compact [class~="..."]` با `!important`. **نکته‌ی فنی مهم:** از `[class~="..."]` (تطبیق دقیق یک توکن کامل کلاس) استفاده شد، نه `[class*="..."]` (تطبیق زیررشته‌ای) که کار ۱ برای رنگ‌ها استفاده کرده بود — چون مقادیر spacing مثل `"p-1"` زیررشته‌ی تحت‌اللفظی `"p-1.5"` هم هستند و `*=` هر دو را (اشتباهاً) با هم کوچک می‌کرد؛ `~=` این مشکل را ندارد چون فقط یک توکن کامل و جدا با فاصله را تطبیق می‌دهد.
2. **`app.jsx`:** `DEFAULT_APPEARANCE` یک فیلد `density: "comfortable"` گرفت (چون `loadSettings()` از قبل کل شیء `appearance` را merge می‌کند، نیازی به تغییر دیگری در آن تابع نبود — فقط افزودن کلید کافی بود). کلاس ریشه‌ی `LifeFlowApp` حالا شرطی است: اگر `settings.appearance?.density === "compact"` باشد، `" lifeflow-density-compact"` به کلاس‌های قبلی اضافه می‌شود؛ در غیر این صورت (شامل `undefined`/`"comfortable"`/هر مقدار نامعتبر دیگر) هیچ تغییری در ظاهر رخ نمی‌دهد — کاربران فعلی که این تنظیم را باز نکرده‌اند هیچ تفاوتی نمی‌بینند.
3. **`settings-tab.ts`:** فیلد `appearance.density` به اینترفیس/`DEFAULT_SETTINGS` اضافه شد (منطق merge از قبل کل `appearance` را spread می‌کند، پس دست‌نخورده ماند). ثابت `DENSITY_OPTIONS` («راحت (پیش‌فرض)» / «فشرده (فاصله‌گذاری کمتر بین عناصر)») و یک `Setting` از نوع dropdown درست بعد از dropdown فونت اضافه شد.
4. با `node -e` رفتار تابع ساخت‌کلاس (نسخه‌ی مستقل خارج از باندل) روی چهار حالت (`undefined`, `"comfortable"`, `"compact"`, مقدار نامعتبر) تست شد — فقط `"compact"` کلاس تراکم را اضافه می‌کند، بقیه بدون تغییر می‌مانند.
5. چون `styles.css` توسط esbuild باندل نمی‌شود (طبق `esbuild.config.mjs`، فقط `src/main.ts` باندل می‌شود؛ `styles.css` مستقیماً توسط خودِ Obsidian از ریشه‌ی پلاگین خوانده می‌شود)، تغییرات آن فوراً «نهایی»‌اند و نیاز به `npm run build` ندارند — فقط تعادل آکولادها (`{`/`}`) با یک اسکریپت Python چک شد (۴۴۲ در برابر ۴۴۲، متعادل).
6. `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، `npm run build`، `node --check main.js` برای تغییرات `app.jsx`/`settings-tab.ts` تأیید شدند.

**⚠️ محدودیت‌های شناخته‌شده:**
- کلاس‌های `space-y-*`/`space-x-*` (که به‌جای مقدار ثابت CSS، از سلکتور فرزند `> :not([hidden]) ~ :not([hidden])` با `calc()` و `--tw-space-y-reverse` استفاده می‌کنند) در این حالت فشرده **پوشش داده نشدند** — override کردن آن‌ها به یک سلکتور جداگانه و پیچیده‌تر نیاز دارد؛ عمداً برای جلسه‌ای دیگر گذاشته شد تا این جلسه با عجله انجام نشود.
- فقط پرتکرارترین مقادیر واقعاً استفاده‌شده در `app.jsx` پوشش داده شدند (نه هر مقدار نظری Tailwind)؛ اگر در آینده کلاس فاصله‌گذاری تازه‌ای (مثلاً `p-7` یا `gap-5`) به فایل اضافه شود، در حالت فشرده مثل حالت راحت رندر می‌شود (بدون خطا، فقط بدون فشرده‌سازی).
- اندازه‌ی فونت/دکمه‌ها دست‌نخورده ماند؛ فقط فاصله‌گذاری.
- تست بصری واقعی این حالت (چقدر واقعاً «فشرده‌تر» به‌نظر می‌رسد، آیا جایی تنگ/شکسته می‌شود) هنوز در یک Obsidian واقعی انجام نشده (سؤال باز شماره‌ی ۶ در پایین همچنان به قوت خودش باقی است).

**نتیجه:** از سه زیرموضوع «تنظیمات ظاهری عمیق‌تر»، حالا «فونت» و «تراکم» هر دو بسته شدند. فقط «رنگ‌های دسته‌بندی» باز مانده — که طبق یادداشت جلسه‌ی ۳ یک تصمیم طراحی جداگانه می‌طلبد (چون این رنگ‌ها عمداً از پاک‌سازی‌های کار ۱ کنار گذاشته شده بودند).

---

### جلسه‌ی ۱۸ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — دیباگ: منطق «تکرار هفتگی» تسک‌ها و مسیرهای یادگیری

طبق دستور مستقیم کاربر («کمی دیباگ کن»)، این جلسه به‌جای افزودن ویژگی تازه، دنبال باگ واقعی در کد موجود گشت. چون این محیط به Obsidian واقعی دسترسی ندارد (طبق سؤال باز شماره‌ی ۶)، تمرکز روی **تست منطق خالص** (توابع مستقل از React/Obsidian) و **بازبینی دقیق کد** بود، نه تست بصری. طبق قاعده‌ی ثابت‌شده ابتدا `git fetch origin` چک شد — هماهنگ بود. سپس `npm run build` + `node --check main.js` + `npx tsc --noEmit` به‌عنوان خط‌مبنا تأیید شدند.

**۱. تست جامع `jalali.js` (بدون باگ، صرفاً اعتبارسنجی):** چون این فایل یک ماژول مستقل و بدون وابستگی به React/Obsidian است، مستقیماً با یک اسکریپت Node مجزا (خارج از پروژه) وارد و تست شد: ۲۳۰ تاریخ (۱۹۹۰ تا ۲۰۳۵، شامل نقاط اطراف نوروز) برای round-trip گرگوری↔جلالی، طول ماه اسفند (۲۹ در برابر ۳۰) برای ۵۰ سال، تشخیص سال کبیسه در برابر یک فهرست شناخته‌شده، کلمپ‌شدن روز هنگام `jalaliAddMonths` بین ماه‌های ۳۰/۳۱ روزه و عبور از مرز سال، و صحت `jalaliMonthGrid` (شامل بودن اولین/آخرین روز ماه، مضرب ۷ بودن طول گرید). همه پاس شدند — این فایل قابل‌اعتماد است.

**۲. باگ واقعی پیدا/رفع‌شده: انتخاب «هفتگی» بدون انتخاب هیچ روزی، مسیر یادگیری را بی‌صدا «هیچ‌وقت due» می‌کرد.** با بررسی `isTrackDueOn` (استفاده‌شده برای due-بودن روزانه‌ی زیربخش‌های یادگیری، صف کار ۵) پیدا شد که وقتی `recurrenceWeekdays` خالی باشد، `[].includes(...)` همیشه `false` برمی‌گرداند — یعنی هیچ‌وقت due نیست. سپس دو محل UI که این حالت را واقعاً **قابل تولید** می‌کردند پیدا شدند:
   - `LearningRoutineEditor` (روتین کل موضوع): کلیک روی چیپ «هفتگی» فقط `topic.recurrence` را عوض می‌کرد؛ `topic.recurrenceWeekdays` دست‌نخورده می‌ماند (که از ساخت موضوع `[]` است) تا کاربر خودش حداقل یک روز را جداگانه کلیک کند. بین این دو کلیک، موضوع «هفتگی بدون هیچ روزی» بود.
   - `SubsectionCard` (override تکرار یک زیربخش خاص): همان الگو — `cadenceWeekdays` با `[]` شروع می‌شود و دکمه‌ی «ذخیره» هیچ گاردی نداشت که جلوی ذخیره‌ی «هفتگی + صفر روز» را بگیرد.

   این دقیقاً همان الگوی امنی نبود که `AddTaskModal` از قبل برای تسک‌های معمولی داشت: آن‌جا `weekdays` با روز جاری هفته مقداردهی اولیه می‌شود و `toggleWeekday` اجازه نمی‌دهد آخرین روز انتخاب‌شده هم حذف شود (`p.length > 1 ? ... : p`) — یعنی یک تسک هفتگی هیچ‌وقت نمی‌تواند با صفر روز ذخیره شود.

**رفع (هر دو محل، همان الگوی امنِ `AddTaskModal`):**
   - `LearningRoutineEditor`: کلیک روی هر چیپ تکرار حالا این‌طور است: اگر مقصد `"weekly"` باشد و `topic.recurrenceWeekdays` از قبل خالی باشد، به‌طور خودکار روز جاری هفته (`new Date().getDay()`) انتخاب می‌شود؛ در غیر این صورت (روزهای قبلی موجود، یا تغییر به تکرار دیگر) دست‌نخورده می‌ماند.
   - `SubsectionCard.saveEdit`: اگر `cadenceMode === "weekly"` و `cadenceWeekdays` خالی باشد، به‌جای ذخیره‌ی آرایه‌ی خالی، روز جاری هفته ذخیره می‌شود.

**۳. هم‌راستاسازی/مستندسازی یک ناسازگاری منطقی مرتبط در `isTaskDueOn` (بدون تغییر رفتار قابل‌مشاهده):** همان الگوی فال‌بک در تابع due-بودن **تسک‌های معمولی** (`isTaskDueOn`، نه مسیر یادگیری) بررسی شد و مشخص شد این تابع دقیقاً **رفتار مخالف** `isTrackDueOn` دارد: قبلاً وقتی `recurrenceWeekdays` خالی/نامعتبر بود، کد به `[dateObj.getDay()]` فال‌بک می‌کرد و سپس `.includes(dateObj.getDay())` چک می‌شد — یعنی مقدار را با خودش مقایسه می‌کرد، که مستقل از تاریخ ورودی همیشه `true` است (یک توتولوژی، نه یک شرط واقعی). چون `AddTaskModal` هیچ‌وقت اجازه‌ی صفر-روز نمی‌دهد، این مسیر در عمل هیچ‌وقت از داده‌ی معتبر UI فعال نمی‌شود — ولی اگر کسی در آینده این کد را می‌خواند یا بازنویسی می‌کرد، منطق گمراه‌کننده بود. جایگزین شد با یک `if (!days || !days.length) return true;` صریح — **دقیقاً همان نتیجه**، ولی حالا با دلیل درست، نه یک تصادف. یک کامنت هم به `isTrackDueOn` اضافه شد که صریحاً بگوید رفتار خالی‌بودن آن (`false`) عمداً برعکس `isTaskDueOn` (`true`) است، تا در آینده کسی این دو را با هم اشتباه نگیرد.

**تأیید:**
   - یک اسکریپت Node مستقل (بازپیاده‌سازی دقیق منطق رفع‌شده، ۴ بخش: شاخه‌ی هفتگیِ `isTaskDueOn`، شاخه‌ی هفتگیِ `isTrackDueOn`، منطق onClick چیپ `LearningRoutineEditor`، منطق `saveEdit` در `SubsectionCard`) روی ۱۴ حالت اجرا شد — همه پاس. از‌جمله: خالی/نامشخص‌بودن روزها در حالت تسک همیشه `true`، در حالت مسیر یادگیری همیشه `false`؛ کلیک روی «هفتگی» با روزهای از قبل انتخاب‌شده آن‌ها را پاک نمی‌کند؛ ذخیره‌ی override با صفر روز به روز جاری فال‌بک می‌کند نه آرایه‌ی خالی.
   - `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، `npm run build`، `node --check main.js` تأیید شدند؛ نبود کاراکتر خراب انکودینگ روی `main.js`/`src/app.jsx` هم چک شد.
   - فقط `app.jsx` (و `main.js` تولیدشده) تغییر کرد — هیچ فایل دیگری لمس نشد.

**⚠️ محدودیت‌های شناخته‌شده:**
- این رفع باگ فقط جلوی **ایجاد** حالت خراب را از این پس می‌گیرد؛ اگر یک `data.json` واقعی از قبل با `recurrence: "weekly", recurrenceWeekdays: []` روی یک موضوع/زیربخش وجود داشته باشد (مثلاً یک ولت واقعی که قبل از این رفع باگ این حالت را تجربه کرده)، آن رکورد همچنان تا وقتی کاربر دوباره ویرایشش نکند «هیچ‌وقت due» باقی می‌ماند — یک migration خودکار برای این حالت نوشته نشد (چون هیچ ولت واقعی/داده‌ی واقعی در این پروژه هنوز وجود ندارد که نیاز به migration داشته باشد؛ اگر در آینده معلوم شد داده‌ی واقعی درگیر این باگ بوده، باید جداگانه رسیدگی شود).
- این جلسه صرفاً «کمی دیباگ» بود، نه بازبینی کامل کل فایل ۳۳۰۰+ خطی؛ بخش‌های دیگر (پومودورو، بکاپ/ریستور، AI summary) هنوز به همین دقت بازبینی نشده‌اند.
- تست بصری واقعی این رفع (آیا چیپ‌های روزهای هفته بلافاصله بعد از کلیک «هفتگی» درست highlight می‌شوند) هنوز در یک Obsidian واقعی انجام نشده.

**نتیجه:** یک باگ واقعی و قابل‌تکرار (نه صرفاً فرضی) در منطق تکرار هفتگیِ مسیرهای یادگیری پیدا و رفع شد، به‌همراه رفع یک ناسازگاری/توتولوژی مرتبط در منطق تکرار هفتگیِ تسک‌های معمولی که رفتارش را حفظ کرد ولی خوانا/امن‌تر کرد. `jalali.js` هم به‌طور کامل با ۲۳۰+ حالت تست و بدون باگ تأیید شد.

---

### جلسه‌ی ۱۹ (۵ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶) — ⚠️ برخورد هم‌زمان با نشست(های) دیگر + آیتم «planned vs actual» از کار ۵

**برخورد هم‌زمان (مهم برای نشست‌های بعدی):** این جلسه با یک کامیت محلیِ آماده برای پوش شروع شد (تنظیم‌پذیرکردن پوشه‌ی گزارش‌ها، دقیقاً مثل چیزی که قبلاً به نام «جلسه‌ی ۱۴» ثبت شده بود). موقع `git push`، مشخص شد یک **نشست موازیِ دیگر** دقیقاً همان قابلیت را با نام فیلد متفاوت (`reports.folderName` به‌جای `reports.folder`) و معماری متفاوت (منطق کاملاً داخل `main.ts`، بدون دست‌زدن به `app.jsx`) پیاده و پوش کرده بود؛ علاوه‌بر آن کامیت‌های دیگری هم برای «فونت پلاگین» اضافه شده بود. به‌جای تلاش برای merge دو پیاده‌سازیِ متفاوت از یک قابلیتِ یکسان، تصمیم گرفته شد کامیت محلیِ تکراری به‌طور کامل **کنار گذاشته شود** (`git reset --hard origin/main`) و نسخه‌ی نشستِ دیگر پذیرفته شود.

سپس بعد از انتخاب و پیاده‌سازی آیتم «planned vs actual» (شرح کامل در پایین) و موقع `git push` دوم، معلوم شد **باز هم** سه کامیت تازه‌ی دیگر از (احتمالاً) نشست‌های موازیِ دیگر روی ریموت اضافه شده بودند (بهبود پنجره‌ی تسک جدید، تنظیم «تراکم»، و رفع یک باگ تکرار هفتگی — که همگی بالای همین بخش با شماره‌ی «جلسه‌ی ۱۶» تا «۱۸» ثبت شده‌اند). این‌بار، برخلاف بار اول، بررسی `git show --stat` روی هر سه کامیت نشان داد **هیچ‌کدام با کاری که در این جلسه انجام شده هم‌پوشانی ندارند** (نه در فایل‌های تغییریافته و نه در بخش‌هایی از `app.jsx` که لمس شده بودند) — پس به‌جای کنار گذاشتن دوباره، این‌بار از `git pull --rebase origin main` استفاده شد. `src/app.jsx` بدون هیچ تعارضی auto-merge شد (تأییدی بر نبود هم‌پوشانی)؛ فقط `PROGRESS.md` (چون هر دو طرف به همین بخش لاگ جلسات اضافه می‌کنند) و `main.js` (چون یک فایل باندل‌شده‌ی تولیدی است، نه دستی) تعارض داشتند — `main.js` به‌جای merge دستی، از نو با `npm run build` از روی `src/` بازتولید شد؛ `PROGRESS.md` هم دستی merge شد (این جلسه، که قبلاً به‌اشتباه با شماره‌ی «جلسه‌ی ۱۶» ثبت شده بود، به «جلسه‌ی ۱۹» تغییر نام یافت تا با کامیت‌های واقعاً زودتر merge‌شده‌ی نشست دیگر همخوانی داشته باشد).

**درسی که از این دو برخورد گرفته شد:** وقتی `git push` رد می‌شود چون ریموت جلوتر رفته، قبل از هر تصمیمی، کامیت(های) طرف مقابل را با `git show --stat <hash>` (و در صورت شک، `git show <hash> -- <files>`) بررسی کن. **دو حالت متفاوت، دو راه‌حل متفاوت دارند:**
- اگر همان قابلیتی که تازه ساختی از قبل توسط طرف دیگر پیاده و merge شده (تکرار کامل) → کنار گذاشتن نسخه‌ی خودت با `git reset --hard origin/main` امن‌تر از merge دستی دو پیاده‌سازی متفاوت از یک چیز است (باگ‌های ظریفِ دو فیلد هم‌معنا با نام متفاوت در `data.json` سخت‌تر از انجام دوباره‌ی کار کشف می‌شوند).
- اگر کاری که طرف دیگر کرده واقعاً **مستقل** از کار توست (فایل‌ها/بخش‌های متفاوت) → `git pull --rebase` امن است؛ `app.jsx` معمولاً بدون تعارض auto-merge می‌شود اگر بخش‌های متفاوتی از فایل تک‌جزئی را لمس کرده باشید. `main.js` را هرگز دستی merge نکن — همیشه بعد از حل تعارض سورس، با `npm run build` از نو بسازش. `PROGRESS.md` تقریباً همیشه در بخش لاگ جلسات (که هر دو طرف انتهای آن را ویرایش می‌کنند) تعارض می‌خورد؛ این را با نگه‌داشتن هر دو جلسه (نه انتخاب یکی) و **شماره‌گذاری مجدد جلسه‌ی خودت بعد از آخرین جلسه‌ی طرف مقابل** حل کن، نه با جایگزین‌کردن.

**آیتم انتخاب‌شده برای این جلسه:** برای کاهش ریسک برخورد دوباره، عمداً از کار ۲ (که نشست دیگر رویش فعال بود) دوری شد و به‌جایش آخرین آیتم باز کار ۵ («آمار تطابق برنامه با عملکرد واقعی») انتخاب شد.

**درسی که از این برخورد گرفته شد (برای ثبت در بخش «تصمیم‌های طراحی»):** وقتی `git push` رد می‌شود چون ریموت جلوتر رفته، قبل از هر `pull`/`merge`، **حتماً** کامیت(های) طرف مقابل را با `git show --stat <hash>` و `git show <hash> -- <files>` بررسی کن. اگر همان قابلیتی که تازه ساختی از قبل توسط طرف دیگر پیاده و merge شده، امن‌ترین کار معمولاً **کنار گذاشتن نسخه‌ی خودت** با `git reset --hard origin/main` است، نه تلاش برای merge دستیِ دو پیاده‌سازی متفاوت از یک چیز — چون این کار معمولاً باگ‌های ظریف (دو فیلد هم‌معنا با نام متفاوت در `data.json`، دو بخش UI تکراری) تولید می‌کند که کشف‌شان سخت‌تر از انجام دوباره‌ی کار است.

**آیتم انتخاب‌شده برای این جلسه:** برای کاهش ریسک برخورد دوباره، عمداً از کار ۲ (که نشست دیگر رویش فعال است) دوری شد و به‌جایش آخرین آیتم باز کار ۵ («آمار تطابق برنامه با عملکرد واقعی») انتخاب شد.

**طراحی:** بررسی کد نشان داد بخشی از این مفهوم از قبل به‌شکل دیگری پوشش داده شده بود (`computeTrackBalance` برای سهمیه/پیشرفت هر مسیر یادگیری، و شمارنده‌ی «X از Y تسک» در گزارش‌های روزانه/هفتگی/ماهانه). چیزی که واقعاً خالی بود، مقایسه‌ی **زمانیِ** برنامه‌ریزی‌شده در برابر واقعی بود: هر تسک یک `duration` (دقیقه‌ی برنامه‌ریزی‌شده) دارد که هیچ‌جا با زمان واقعیِ صرف‌شده (از جلسات پومودوروی مرتبط) مقایسه نشده بود. کامپوننت تازه‌ی `PlannedVsActualStats({ tasks, pomodoro })` این را برای ۷ روز اخیر می‌سازد: برای هر روز، `plannedMinutes` (مجموع `duration` تسک‌های آن روز طبق `isTaskDueOn`) در برابر `actualMinutes` (مجموع `durationMin` جلسات کاریِ کامل‌شده‌ی پومودورو در همان روز تقویمی، محاسبه‌شده با `dateKeyOf` — نه `.slice(0,10)`، طبق همان درسِ منطقه‌زمانی جلسه‌ی ۱۰/۱۲)، به‌همراه شمارنده‌ی تسک (`doneCount`/`dueCount`). یک خط خلاصه (نسبت تسک انجام‌شده + نسبت زمانی به‌صورت درصد) بالای یک فهرست ۷سطری (هر سطر یک روز) نمایش داده می‌شود.

**چرا نمودار نه، فهرست متنی:** `SimpleBarChart`/`SimpleLineChart` (کامپوننت‌های چارت مشترک پروژه) فقط از یک سری داده پشتیبانی می‌کنند؛ برای نمایش هم‌زمان «برنامه‌ریزی‌شده» و «واقعی» در یک نمودار به یک نمودار میله‌ای گروهی (چندسری) نیاز بود که یا باید کامپوننت مشترک را تغییر می‌داد (ریسک روی جاهای دیگری که از آن استفاده می‌کنند) یا دو نمودار جدا کنار هم می‌گذاشت (که برای مقایسه‌ی روزبه‌روز کمتر خوانا از یک جدول ساده است). به‌همین‌دلیل یک فهرست متنیِ فشرده انتخاب شد — بدون هیچ تغییری در کامپوننت‌های چارت مشترک.

**محل قرارگیری:** بالای بخش «گزارش روزانه» (`DailyReportView`)، بلافاصله بعد از دکمه‌های خروجی Markdown و قبل از ردیف‌های هر مسیر یادگیری — در **هر دو** حالت بازگشتیِ این کامپوننت (چه مسیر یادگیریِ سهمیه‌داری وجود داشته باشد چه نه)، چون این آمار به تسک‌ها/پومودورو مربوط است، نه فقط یادگیری.

**تأیید:** منطق تجمیع با یک اسکریپت Node مستقل خارج از باندل روی داده‌ی فرضی تست شد (شامل ۱ تسک انجام‌شده با یک جلسه‌ی پومودوروی ۲۵دقیقه‌ای مرتبط، و بررسی این‌که نوع «break» در محاسبه حساب نمی‌شود). `npm run build` + `node --check main.js` + `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط) تأیید شدند؛ بررسی نبود کاراکتر خراب انکودینگ روی `main.js`/`src/app.jsx` انجام شد (چیزی پیدا نشد).

**⚠️ محدودیت‌های شناخته‌شده:**
- اگر یک تسک به هیچ جلسه‌ی پومودورویی مرتبط نباشد (کاربر تسک را بدون تایمر انجام داده)، آن زمان در «واقعی» دیده نمی‌شود — این آمار فقط زمان *ردیابی‌شده با پومودورو* را می‌سنجد، نه زمان واقعیِ صرف‌شده به‌هرشکل.
- تسک‌های بدون `duration` (نظری، چون همه‌ی مسیرهای ساخت تسک این فیلد را پر می‌کنند) با `duration || 0` بی‌اثر در نظر گرفته می‌شوند، نه حذف — اگر چنین تسکی وجود داشته باشد، در شمارنده‌ی تسک حساب می‌شود ولی در دقیقه‌ی برنامه‌ریزی‌شده صفر اضافه می‌کند.
- بازه‌ی ثابت روی «۷ روز اخیر» است (مثل بقیه‌ی چارت‌های موجود)؛ بازه‌ی دلخواه اضافه نشده.
- تست بصری واقعی در Obsidian هنوز انجام نشده.

**نتیجه:** آخرین آیتم باز کار ۵ («planned vs actual») بسته شد. کار ۵ اکنون فقط یک آیتم باز دارد: «امکان انتخاب یک یا چند مسیر یادگیری برای یک بلوک زمانی در پومودورو» (که یک تغییر معماریِ بزرگ‌تر است، عمداً برای جلسه‌ای جدا نگه داشته شده).
### جلسه‌ی ۲۰ (۵ شهریور ۱۴۰۵ / ۲۷ آگوست ۲۰۲۶) — کشف وضعیت واقعی کار ۳ + نمای «فهرست/آجندا»

⚠️ طبق دستور صریح کاربر («ادامه بده. مطمئن شو که کاری که می‌کنی به لیمیت نمی‌خوری وسطش»)، این جلسه هم مثل جلسه‌ی ۸ عمداً یک قلم کوچک و کم‌ریسک انتخاب کرد، نه یک بازنویسی بزرگ.

طبق قاعده‌ی ثابت‌شده، اول `git fetch origin && git log --oneline origin/main` چک شد — مشخص شد HEAD محلی خیلی عقب مانده (نشست‌های دیگری جلسات ۱۰ تا ۱۵ را انجام داده بودند: تکمیل کامل کار ۴، آخرین آیتم کار ۲، و بخشی از تنظیمات ظاهری). با `git reset --hard origin/main` هماهنگ شد، و کل `PROGRESS.md` (۴۵۳ خط) از اول تا آخر خوانده شد تا مطمئن شویم دوباره‌کاری نمی‌شود.

**چرا کار ۳ انتخاب شد:** بعد از خواندن کامل فایل، تنها کاری که هنوز **هیچ جلسه‌ای** روی آن کار نکرده بود کار ۳ (بازطراحی برنامه‌ریزی) بود — همه‌ی کارهای دیگر (۰، ۱، ۲، ۴، ۵، ۶) یا کامل بودند یا حداقل چند جلسه پیشرفت داشتند.

**کشف مهم قبل از نوشتن هر کدی:** بررسی خودِ `CalendarViews`/`DayPlannerView` (نه فقط خواندن چک‌لیست) نشان داد نمای «روز» از قبل (احتمالاً از نسخه‌ی اولیه‌ی اپ، قبل از جلسه‌ی ۱ این پروژه) درگ‌ودراپ، تغییرِ‌اندازه با کشیدن، و یک نوار سبد تسک‌های زمان‌بندی‌نشده داشت — سه مورد از پنج آیتم چک‌لیست کار ۳ که همه‌جا «انجام‌نشده» علامت خورده بودند در واقع از قبل کار می‌کردند. این دقیقاً همان الگوی «مستندات با کد هم‌خوانی ندارد» بود که در جلسه‌ی ۸ (برای کار ۶) و جلسه‌ی ۱۴ (برای کار ۰) هم دیده شده بود. چک‌لیست کار ۳ در بخش ۲ این فایل با جزئیات دقیق اصلاح شد (کدام قسمت از قبل بوده، کدام قسمت واقعاً هنوز نیست).

**تغییر کد واقعی این جلسه (کوچک و مستقل، طبق دستور کاربر):** نمای پنجم «فهرست/آجندا» (یکی از انواع ویو‌ی صریحاً خواسته‌شده‌ی کاربر: «نمای تسکی رویدادی») اضافه شد:
1. **`AgendaView` (کامپوننت جدید):** ۱۴ روز آینده را روز‌به‌روز فهرست می‌کند (فقط روزهایی که حداقل یک تسکِ *انجام‌نشده* در آن‌ها موعد دارد، با `isTaskDueOn` که از قبل برای همین منظور در چند جای دیگر پروژه استفاده می‌شد)، هر روز یک سرتیتر (برچسب «امروز» یا تاریخ جلالی) و زیرش همان `TaskRow` کاملاً آماده و تست‌شده (همان کامپوننتی که در تب «تسک‌ها» استفاده می‌شود — چک‌باکس تیک‌زدن، نوار پیشرفتِ تسک‌های روند‌دار با فرم لاگ+توضیح، برچسب تکرار/اولویت، دکمه‌ی زمان‌بندی، ویرایش، حذف — همه‌ی این‌ها رایگان و بدون نوشتن کد تعاملی تازه به‌دست آمد چون کامپوننت مشترک است). این پایین‌ترین ریسک ممکن بود چون هیچ منطق درگ‌ودراپ/مختصات جدیدی لازم نداشت — فقط فیلتر+گروه‌بندی+رندر.
2. **`CalendarHeader`:** گزینه‌ی «فهرست» به سوییچر ویو (کنار روز/هفته/ماه/سال) اضافه شد؛ یک شاخه‌ی تازه برای عنوان («۱۴ روز آینده») اضافه شد؛ چون نمای فهرست بر پایه‌ی `cursor` حرکت نمی‌کند (همیشه از «امروز واقعی» شروع می‌شود)، دکمه‌های قبلی/بعدی/امروز در این حالت مخفی می‌شوند (`navButtons` حالا `null` وقتی `view === "agenda"`).
3. **`CalendarViews`:** شاخه‌ی `view === "agenda"` اضافه شد؛ یک prop تازه `onAddProgress` هم گرفت (قبلاً نداشت — چون `TaskRow` برای تسک‌های روند‌دار به آن نیاز دارد) و به نقطه‌ی فراخوانی سطح بالا در `LifeFlowApp` هم پاس داده شد.
4. **یک اشتباه کوچک در حین کار پیدا و فوراً رفع شد:** در اولین تلاش، هنگام جداکردن دکمه‌های ناوبری از `CalendarHeader` (برای مخفی‌کردن‌شان در حالت فهرست)، به‌اشتباه پوسته‌ی بیرونی از `GlassCard` به یک `"div"` ساده تغییر کرد (با یک تکه متن اضافه‌ی بی‌معنای `+ ""`) — قبل از build با یک `str_replace` دوم تصحیح شد. **درسِ کوچک: وقتی یک عبارت طولانی را برای تغییر جزئی دوباره می‌نویسی، خروجی را با نسخه‌ی قبلی مقایسه کن، نه فقط با چشم بخوان که "درست به‌نظر می‌رسد".**
5. `npm run build`، `node --check main.js`، `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، و بررسی نبود کاراکتر خراب انکودینگ — همگی تأیید شدند.

**⚠️ محدودیت‌های شناخته‌شده:**
- نمای فهرست فقط تسک‌های *انجام‌نشده* را نشان می‌دهد (تسک‌های انجام‌شده در گذشته/امروز دیده نمی‌شوند) — تصمیم عمدی برای یک «کارهای پیشِ‌رو» تمیز، نه یک تاریخچه.
- بازه‌ی ۱۴ روزه ثابت است (قابل‌تنظیم نیست).
- تست بصری واقعی در Obsidian هنوز انجام نشده.

**نتیجه:** یک آیتم کوچک از کار ۳ واقعاً اضافه شد (نمای فهرست)؛ مهم‌ترین دستاورد این جلسه در واقع **تصحیح مستندات** بود — کار ۳ خیلی جلوتر از چیزی بود که چک‌لیست نشان می‌داد. قلم‌های واقعاً باقیمانده در بالای چک‌لیست کار ۳ فهرست شدند (تفکیک نوع آیتم، نمای هفتگیِ ساعتی، ساخت تسک با کشیدن روی بازه‌ی خالی).

---

### جلسه‌ی ۲۱ (۵ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶) — کلیک روی بازه‌ی خالی جدول ساعتی برای ساخت تسک تازه

طبق دستور کاربر («ادامه بده... قبل از لیمیت کامیت کن»)، طبق قاعده‌ی ثابت‌شده اول `git fetch origin && git log --oneline origin/main -5` چک شد — هماهنگ با HEAD محلی. سپس کل `PROGRESS.md` (۶۰۳ خط) دوباره از اول خوانده شد تا از هماهنگی کامل با آخرین وضعیت (تا جلسه‌ی ۲۰) مطمئن شویم.

**چرا این آیتم:** طبق نتیجه‌گیری جلسه‌ی ۲۰، از قلم‌های واقعاً باقیمانده‌ی کار ۳، «ساخت تسک با کشیدن/کلیک روی بازه‌ی خالی» کوچک‌ترین و مستقل‌ترین بود (برخلاف «تفکیک نوع آیتم» که یک تغییر مدل داده در سرتاسر فایل می‌طلبد، یا «نمای هفتگیِ ساعتی» که یک کامپوننت تقریباً از‌صفر است) — دقیقاً همان استراتژیِ «یک قلم کوچک و کم‌ریسک» که در جلسات ۸، ۱۶/۲۰ و دیگران جواب داده بود.

**تصمیم طراحی (کلیک، نه کشیدن):** پیاده‌سازی کامل drag-to-create (کشیدن روی یک بازه برای تعیین محدوده‌ی شروع/پایان، با یک مستطیل پیش‌نمایش زنده) نیازمند ردیابی state جدید (`dragStart`/`dragEnd` پیکسلی)، تبدیل آن‌ها به دقیقه، و رندر یک overlay موقت بود — پیچیدگی و ریسک به‌مراتب بیشتر از ارزش افزوده‌اش نسبت به یک **کلیک ساده** بود که همان هدف اصلی کاربر («تسک تازه مستقیم روی جدول بسازم») را با کد بسیار کمتر و بدون state تازه‌ای به‌دست می‌آورد. این تصمیم عمدی، طبق همان الگوی «کوچک‌ترین قدم کافی» جلسات قبل بود.

**کشف مهم قبل از کدنویسی (که طراحی را ساده‌تر کرد):** بررسی `isTaskDueOn` نشان داد تسک‌ها اصلاً یک فیلد «تاریخ مشخص» ندارند — یک تسک با `recurrence: "none"` (پیش‌فرض تسک تازه) در **هر روزی** «due» محسوب می‌شود، نه فقط روزی که ساخته شده؛ زمان‌بندی فقط شامل `time` (ساعت روز) است، نه تاریخ. یعنی قابلیت drag/click-to-schedule *موجود* (`onSchedule`) هم از قبل همین رفتار را داشت (یک تسک زمان‌بندی‌شده در نمای «روز» X را هر روز دیگری هم در همان ساعت نشان می‌دهد). پس قابلیت تازه‌ی این جلسه (ساخت تسک با یک `time` از‌پیش‌پرشده، بدون تاریخ) دقیقاً با همین مدل داده‌ی موجود هم‌خوان است — نیازی به افزودن یک فیلد «تاریخ» تازه یا نگرانی درباره‌ی «این تسک فقط باید امروز باشد نه فردا» نبود.

**تغییرات (`app.jsx`؛ هیچ فایل دیگری لمس نشد):**
1. **`AddTaskModal`:** یک prop تازه‌ی `prefillTime` گرفت؛ در حالت «تسک تازه» (نه ویرایش)، مقدار اولیه‌ی state `time` از `prefillTime` (اگر بود) پر می‌شود. همچنین `showMore` (بخش تاشوی «گزینه‌های بیشتر» که در جلسه‌ی ۱۶ اضافه شده بود) حالا وقتی `prefillTime` ست شده هم به‌طور پیش‌فرض باز است — وگرنه کاربر زمان از‌پیش‌پرشده را که علت اصلی باز کردن این پنجره بوده، نمی‌دید (پشت یک دکمه‌ی تاشوی بسته پنهان می‌ماند).
2. **`LifeFlowApp`:** یک state تازه `prefillTime` + یک تابع کمکی `openAddAt(time)` (که `prefillTime` را ست و `showAdd` را باز می‌کند) اضافه شدند؛ `onClose` پنجره‌ی افزودن تسک حالا `prefillTime` را هم به `null` برمی‌گرداند (تا دفعه‌ی بعد که کاربر از دکمه‌ی معمولیِ «افزودن» استفاده می‌کند، زمان قبلی باقی نماند).
3. **`DayPlannerView`:** یک prop تازه‌ی `onCreateAt` گرفت؛ هر ردیف ۳۰دقیقه‌ای جدول ساعتی (که قبلاً فقط `onDrop` برای اسکجول‌کردن تسک‌های درگ‌شده داشت) حالا `onClick: () => createAt(mins)` هم دارد که (با همان کلمپِ ۰۶:۰۰–۲۳:۳۰ که `dropAt` هم استفاده می‌کند) `onCreateAt` را با زمان همان ردیف صدا می‌زند. چون بلوک‌های تسکِ زمان‌بندی‌شده عناصر خواهر (نه فرزند) همین ردیف‌هایند و بعد از آن‌ها در DOM رندر می‌شوند (پس در چیدمانِ `absolute` روی آن‌ها قرار می‌گیرند)، کلیک روی یک تسک موجود کماکان مستقیم به همان تسک می‌رسد، نه به کلیکِ ردیفِ زیرش — تأیید شد که تعارضی بین «ویرایش تسک موجود» و «ساخت تسک تازه» وجود ندارد.
4. متن راهنمای بالای جدول ساعتی («کل بلوک رو بکش برای جابه‌جایی؛ لبه‌ی پایینش رو بکش برای تغییر مدت») یک جمله‌ی کوتاه گرفت: «جای خالی بزن تا یک تسک تازه بسازی».
5. **`CalendarViews`:** یک prop تازه‌ی `onCreateAt` گرفت و مستقیم به `DayPlannerView` پاس داد؛ نقطه‌ی فراخوانی سطح‌بالا در `LifeFlowApp` هم `onCreateAt: openAddAt` را پاس می‌دهد.
6. `npm run build`، `node --check main.js`، `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، و بررسی نبود کاراکتر خراب انکودینگ — همگی تأیید شدند.

**⚠️ محدودیت‌های شناخته‌شده:**
- این قابلیت فقط برای نمای «روز» است؛ نمای «هفته»/«ماه»/«فهرست» راهی برای ساخت مستقیم تسک با کلیک روی یک بازه‌ی زمانی مشخص ندارند (طبیعی، چون آن ویوها اصلاً جدول ساعتی نیستند).
- تسک تازه‌ساخته فقط یک `time` می‌گیرد، نه تاریخ مشخص — طبق کشف بالا، این دقیقاً همان رفتاری‌ست که قابلیت زمان‌بندیِ درگ‌ودراپِ از‌قبل‌موجود هم دارد، پس ناسازگاری تازه‌ای ایجاد نکرد؛ ولی همچنان یعنی کاربر نمی‌تواند «فقط برای فردا» یک تسک زمان‌دار یک‌باره بسازد بدون اینکه هر روز دیگر هم در همان ساعت ظاهر شود، مگر با تنظیم `recurrence`.
- تست بصری واقعی (رفتار کلیک روی جدول در یک Obsidian واقعی) هنوز انجام نشده.

**نتیجه:** از قلم‌های باقیمانده‌ی کار ۳، «ساخت تسک با کشیدن روی بازه‌ی خالی» (با کلیک، نه کشیدن) بسته شد. باقی: تفکیک نوع آیتم (تسک/رویداد/روتین)، نمای هفتگیِ ساعتی، و (اختیاری، کم‌اهمیت‌تر) بازآرایی سبد تسک‌های زمان‌بندی‌نشده به ستون کناری.

---

### جلسه‌ی ۲۲ (۵ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶) — بازطراحی بصری همه‌ی پنجره‌های «افزودن»

⚠️ این جلسه، برخلاف روال معمولِ «یک قلم از صف کارها»، مستقیماً طبق درخواست صریح کاربر بود: «پنجره‌هایی که باز می‌شود هنگام ساخت تسک و ساخت کتاب و ... را بسیار بهبود بده. الان با متن به‌هم‌ریخته و دکمه‌های بدجا است» + «در تنظیمات هم بتوانم آن پنجره را تنظیم کنم». طبق دستور کاربر، ابتدا `git fetch origin` و `git log origin/main` چک شد — چیز جدیدی از نشست‌های دیگر نبود، کاملاً هماهنگ.

**بررسی و تشخیص علت اصلیِ «به‌هم‌ریختگی»:** همه‌ی پنجره‌های افزودن (`AddTaskModal`, `AddBookModal`, `AddVideoModal`, `AddPodcastModal`, `AddExerciseModal`, `NewLearningTopicModal`, `NewListModal`) از یک پوسته‌ی مشترک (`ModalShell`) استفاده می‌کنند، ولی هرکدام دکمه‌ی تأیید خودش را دستی و جدا می‌ساخت — با یک گرادیانت رنگیِ هاردکدشده که در هر فایل کمی فرق داشت (ترکیب‌های مختلف بنفش/صورتی/فیروزه‌ای، جهت گرادیانت گاهی برعکس). همین ناهماهنگیِ ریز-ولی-محسوس، به‌علاوه‌ی برچسب‌های فیلد که فقط متن خاکستریِ کوچک بدون هیچ جداکننده‌ی بصری بودند (بدون قاب/پس‌زمینه‌ی گروه‌بندی)، دقیقاً همان چیزی بود که کاربر «به‌هم‌ریخته» توصیفش کرد — مخصوصاً در `AddTaskModal` که وقتی «گزینه‌های بیشتر» باز می‌شد، ده‌ها فیلد پشت سر هم بدون هیچ مرز بصری در یک لیست یک‌دست می‌ریختند.

**⚠️ نکته‌ی فنی مهم که در همین جلسه کشف و رعایت شد:** `styles.css` این پروژه یک فایل **از‌پیش‌کامپایل‌شده‌ی استاتیک Tailwind** است (نه یک بیلد زنده‌ی Tailwind) — یعنی هر کلاس Tailwind که در JSX نوشته شود ولی از قبل در این فایل کامپایل نشده باشد، **هیچ اثری نخواهد داشت** (نه خطا می‌دهد، نه چیزی رندر می‌شود — سکوت کامل). یک اسکریپت کوچک پایتون (`/tmp/check_classes.py`) ساخته شد که هر کلاس مدنظر را قبل از استفاده در برابر `styles.css` واقعی چک می‌کند. با همین ابزار، تلاش اولیه برای استفاده از `mt-5`/`first:mt-0` (که در فایل کامپایل‌شده وجود نداشتند) به‌موقع تشخیص داده و به `style={{marginTop: 18}}` این‌لاین تغییر داده شد. **این نکته برای هر جلسه‌ی بعدی که قصد افزودن کلاس Tailwind تازه (نه فقط استفاده‌ی مجدد از کلاس‌های قبلاً دیده‌شده در کد) دارد، حیاتی است** — همیشه باید یا از کلاسی استفاده شود که جای دیگری از پروژه از قبل استفاده شده، یا از `style` این‌لاین.

**تغییرات:**
1. **`ModalShell` بازطراحی شد** تا به‌جای پذیرفتن یک دکمه‌ی دلخواه از طریق `footer`، دو prop تازه‌ی `submitLabel`/`submitDisabled` (و اختیاری `cancelLabel`) بگیرد و خودش یک فوتر دوبخشیِ یکپارچه بسازد: یک دکمه‌ی «انصراف» کم‌رنگ (حاشیه‌دار، بدون پس‌زمینه) + یک دکمه‌ی اصلیِ `.mod-cta` بومی Obsidian (به‌جای گرادیانت‌های قبلی). سازگاری با گذشته کامل حفظ شد: اگر جایی `footer` صریح پاس داده شود (مثلاً برای مودال‌هایی با نیاز خاص)، همان اولویت دارد.
2. **کامپوننت تازه‌ی `FieldLabel`** برای عنوان بخش‌ها با کمی وزن بصری بیشتر (بولد + کمی فاصله‌ی بالا) اضافه شد.
3. **`AddTaskModal`:** سه لیبل اصلی («ربع آیزنهاور»، «اولویت»، «زمان روز») به `FieldLabel` تبدیل شدند. مهم‌تر: کل بخش «گزینه‌های بیشتر» (زمان‌بندی دقیق، مدت، نوع تسک، تکرار، یادآوری، برچسب، زیرتسک‌ها) داخل **یک پنل واحد با حاشیه و پس‌زمینه‌ی `var(--background-secondary)`** جمع شد — به‌جای شناور بودن آزاد در همان اسکرول اصلی، حالا یک قاب مشخص دارد که آن را از فیلدهای اصلیِ بالا جدا می‌کند. عبارت انگلیسیِ نچسبِ «Time Blocking» هم از توضیح فیلد زمان حذف شد.
4. **همه‌ی ۶ مودال دیگر** (`AddBookModal`, `AddVideoModal`, `AddPodcastModal`, `AddExerciseModal`, `NewLearningTopicModal`, `NewListModal`) به همان `submitLabel`/`submitDisabled` تازه سوئیچ کردند — یعنی همه‌شان یک‌جا و مکانیکی هم‌شکل و بومی شدند.
5. **بخش تنظیمات‌پذیر بودن (خواسته‌ی دوم کاربر):** یک فیلد تازه به `taskDefaults` اضافه شد: `advancedOpenByDefault` (بولی، پیش‌فرض `false`). در `AddTaskModal`، بخش تاشوی «گزینه‌های بیشتر» حالا اگر این تنظیم روشن باشد، برای تسک‌های تازه (نه ویرایش) از ابتدا باز است. تاگل این تنظیم («گزینه‌های بیشتر به‌طور پیش‌فرض باز باشند») به `settings-tab.ts` (تب رسمی تنظیمات Obsidian، کنار بقیه‌ی پیش‌فرض‌های پنجره‌ی تسک جدید) اضافه شد — طبق همان قرارداد جلسه‌ی ۱۴ (فقط `settings-tab.ts` به‌روزرسانی می‌شود، نه `SettingsModal` داخل اپ، چون این دومی هنوز به `taskDefaults` وصل نشده).
6. `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، `npm run build`، `node --check main.js`، و بررسی نبود کاراکتر خراب انکودینگ همگی تأیید شدند؛ همچنین با `grep` تأیید شد که هیچ گرادیانت دستیِ قدیمی در فوتر هیچ مودالی باقی نمانده.

**⚠️ محدودیت‌های شناخته‌شده / آنچه عمداً به این جلسه محدود نشد:**
- ~~فیلد «زیرتسک‌ها»...~~ در جلسه‌ی ۲۳ به یک تگ‌اینپوت واقعی تبدیل شد (رجوع کنید).
- دو دکمه‌ی «ثبت» کوچک داخل فرم‌های این‌لاینِ ثبت پیشرفت (در `GoalsView` و کارت پیشرفت پومودورو) هنوز گرادیانت قدیمی دارند — این‌ها فرم‌های تعبیه‌شده‌ی داخل کارت‌اند، نه «پنجره‌ای که باز می‌شود» (مودال)، پس طبق متن دقیق خواسته‌ی کاربر («پنجره‌هایی که باز می‌شود») از این جلسه عمداً کنار گذاشته شدند؛ اگر کاربر این‌ها را هم می‌خواهد، باید در جلسه‌ی بعد جداگانه مشخص شود.
- `SettingsModal` (مودال داخل اپ، نه تب رسمی) برای تنظیم `advancedOpenByDefault` به‌روزرسانی نشد — طبق قرارداد از‌قبل‌مستندِ جلسه‌ی ۱۴.
- تست بصری واقعی در یک Obsidian واقعی هنوز انجام نشده (مخصوصاً مهم اینجا، چون این جلسه کاملاً درباره‌ی ظاهر بود).

**نتیجه:** خواسته‌ی صریح کاربر (پنجره‌های افزودن + قابل‌تنظیم‌بودنشان) با یک تغییر متمرکز در `ModalShell` (نه هفت‌بار تکرار جداگانه) پاسخ داده شد، به‌علاوه‌ی یک تنظیم تازه در تب رسمی Obsidian.

---

### جلسه‌ی ۲۳ (۵ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶) — زیرتسک‌ها از رشته‌ی کاما-جدا به تگ‌اینپوت واقعی + رفع یک باگ واقعی

طبق دستور کاربر («به بهبود این بخشی که کار کردی بپرداز... مهم است لیمیت نخوری»)، اول `git fetch`/`git log origin/main` چک شد (هماهنگ، چیز جدیدی نبود)، سپس دقیقاً همان محدودیتِ عمداً واگذاشته‌ی جلسه‌ی ۲۲ («فیلد زیرتسک‌ها هنوز یک ورودی متنیِ کاما-جداست») به‌عنوان کوچک‌ترین و مشخص‌ترین قدم بعدیِ همان بخش (مودال‌ها) انتخاب شد.

**یک باگ واقعیِ داده‌ای هم در همین بررسی پیدا شد، نه فقط یک ایراد ظاهری:** منطق قبلی، هر بار که فرم تسک ثبت می‌شد (حتی وقتی کاربر فقط عنوان یا اولویت را ویرایش می‌کرد، کاری به زیرتسک‌ها نداشت)، کل آرایه‌ی `subtasks` را از روی متنِ کاما-جدا **از نو می‌ساخت** — یعنی `id` تازه به هرکدام می‌داد و `done` را همیشه `false` می‌گذاشت. نتیجه: **ویرایش هر تسکی که زیرتسکِ تیک‌خورده داشت، همه‌ی تیک‌ها را پاک می‌کرد** و شناسه‌ی زیرتسک‌ها را هم عوض می‌کرد (که اگر جایی دیگر به همان id ارجاع داده شده بود، آن ارجاع را می‌شکست). این یک باگ واقعیِ از‌دست‌رفتنِ داده بود، نه فقط سلیقه‌ی ظاهری.

**تغییرات (`app.jsx`، فقط `AddTaskModal`):**
1. `subInput` (رشته) به یک state واقعیِ آرایه‌ای `subtasks` تبدیل شد که موقع ویرایش، دقیقاً همان `initialTask.subtasks` (با همان id ها و همان وضعیت `done`) را به‌عنوان مقدار اولیه می‌گیرد — نه یک رشته‌ی بازسازی‌شده از روی آن. `subInput` حالا فقط برای متنِ ورودیِ در حال تایپِ زیرتسکِ تازه استفاده می‌شود.
2. دو تابع کمکی: `addSubtask()` (عنوان تایپ‌شده را — اگر خالی نباشد — با `{id: uid(), title, done: false}` به آرایه اضافه می‌کند و ورودی را خالی می‌کند) و `removeSubtask(id)` (یک آیتم را با فیلتر بر اساس id حذف می‌کند).
3. رندر: زیرتسک‌های موجود حالا به‌شکل چیپ‌های گرد و قابل‌حذف (هرکدام با یک × کوچک) در یک ردیف `flex-wrap` نشان داده می‌شوند؛ زیرشان یک ورودی متنی + دکمه‌ی «+» است — زدن Enter داخل ورودی هم (با `preventDefault` روی کلید Enter تا کل فرم تسک زودهنگام ساب‌میت نشود) معادل کلیک روی «+» است.
4. در `submit()`، مقدار نهایی مستقیماً همان آرایه‌ی state (`subtasks`) پاس داده می‌شود — نه یک `split(",")` تازه.
5. `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، `npm run build`، `node --check main.js`، و بررسی نبود کاراکتر خراب انکودینگ تأیید شدند.

**⚠️ محدودیت شناخته‌شده:** تست بصری واقعی در یک Obsidian واقعی هنوز انجام نشده.

**نتیجه:** آخرین محدودیتِ عمداً واگذاشته‌ی جلسه‌ی ۲۲ بسته شد، و در همین مسیر یک باگ واقعیِ از‌دست‌رفتن پیشرفتِ زیرتسک‌ها (نه فقط ظاهر) کشف و رفع شد. به‌دلیل نگرانی صریح کاربر درباره‌ی نخوردن به لیمیت، این جلسه همین‌جا با یک تغییرِ کامل و تأیید‌شده بسته و push شد؛ بهبود تنظیمات/UI بیشتر به جلسه‌ی بعد موکول شد.

---

### جلسه‌ی ۲۴ (۵ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶) — سوییچ‌های بومی + تنظیم «گزینه‌های بیشتر» در مودال داخل اپ

طبق دستور کاربر («ادامه بده به همان بخش، بعد تنظیمات، بعد UI — لیمیت نخور»)، اول `git fetch`/`git log origin/main` چک شد (هماهنگ). سپس دنبال باقیمانده‌های همان بخش (مودال‌ها) گشتم و دو سوییچِ روشن/خاموشِ رنگِ ثابت پیدا شد که از قلمِ بازطراحیِ جلسه‌ی ۲۲ جا مانده بودند: سوییچ «یادآوری» در `AddTaskModal` و سوییچ‌های اعلان در `SettingsModal` — هر دو رنگ فعال‌بودن‌شان را با `#C026D3` هاردکد کرده بودند، دقیقاً همان الگویی که در جلسه‌ی ۱ برای دکمه‌ها رفع شده بود ولی برای این نوع کنترل (toggle) جا مانده بود.

**تغییرات (`app.jsx`):**
1. کامپوننت مشترک تازه‌ی `ToggleSwitch({on, onClick, disabled})` ساخته شد — رنگ حالت روشن حالا `var(--interactive-accent)` است (نه `#C026D3` هاردکد)، حالت خاموش `var(--background-modifier-border)`.
2. سوییچ «یادآوری» در `AddTaskModal` و هر ۵ سوییچ اعلان در `SettingsModal` به این کامپوننت مشترک سوئیچ کردند.
3. **بهبود تنظیمات:** یک بخش کوچک تازه («پنجره‌ی تسک جدید») به `SettingsModal` (مودال داخل اپ) اضافه شد با یک `ToggleSwitch` برای همان `taskDefaults.advancedOpenByDefault` که در جلسه‌ی ۲۲ فقط به تب رسمی Obsidian اضافه شده بود — حالا کاربرهایی که هیچ‌وقت تب تنظیمات رسمی Obsidian را باز نمی‌کنند هم می‌توانند این تنظیم را از همان مودال آشنای داخل اپ عوض کنند.
4. `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، `npm run build`، `node --check main.js`، بررسی نبود کاراکتر خراب انکودینگ، و تأیید با `grep` که دیگر هیچ سوییچی رنگ `#C026D3` هاردکد ندارد — همگی تأیید شدند.

**⚠️ محدودیت شناخته‌شده:** تست بصری واقعی هنوز انجام نشده. به‌دلیل تأکید صریح کاربر بر نخوردن به لیمیت، جلسه همین‌جا (بعد از یک تغییرِ کامل و تأییدشده که هم مودال و هم تنظیمات را پوشش داد) بسته و push شد؛ بهبود گسترده‌تر UI به جلسه‌ی بعد موکول شد.

---

### جلسه‌ی ۲۵ (۶ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶) — مدیریت مسیر یادگیری: توقف موقت، آرشیو، یادداشت، کپی، مرتب‌سازی

طبق دستور کاربر («قسمت یادگیری را بهبود بده و قابلیت‌های متنوع اضافه کن، تا می‌توانی») این جلسه با `git fetch`/`git log origin/main` شروع شد که هماهنگ بود (هیچ نشست موازی دیگری در آن لحظه در جریان نبود). تمرکز روی «مدیریت چرخه‌ی عمر» یک مسیر یادگیری بود — چیزی که تا این‌جا نبود: یک مسیر یا برای همیشه فعال می‌ماند یا کاربر آن را حذف می‌کرد (بدون آرشیو/تاریخچه).

**تغییرات (`app.jsx`، فقط `SubsectionCard`/`LearningHub`):**
1. **توقف موقت (Pause):** فیلد `paused`/`pausedSince` به مدل زیرمسیر اضافه شد. `isTrackDueOn` حالا هنگام `paused`/`archived` همیشه `false` برمی‌گرداند و `computeTrackBalance` هم در همین حالت `null` می‌دهد — یعنی حین توقف، نه بدهی جمع می‌شود و نه مسیر در فرم ثبت سریعِ «گزارش روزانه» ظاهر می‌شود.
   - **نکته‌ی ظریف که عمداً رعایت شد:** چون `computeTrackBalance` بدهی را هر بار از `createdDate` تا امروز از نو محاسبه می‌کند (نه این‌که یک عدد ذخیره‌شده را جمع بزند)، اگر موقع ازسرگیری صرفاً `paused` به `false` برمی‌گشت، کل بازه‌ی توقف هم به‌اشتباه «باید انجام می‌شد» حساب می‌شد (یک بدهیِ ناگهانی و گمراه‌کننده بعد از ازسرگیری). برای رفع این مشکل، هنگام ازسرگیری `createdDate` به‌اندازه‌ی تعداد روزهای توقف جلو کشیده می‌شود (دو تابع کمکی تازه: `addDaysToKey`، `daysBetweenKeys`). این منطق را جدا از باندل، با یک اسکریپت Node مستقل و ۱۳ تست (شامل یک سناریوی کامل ۵ روز توقف) قبل از commit تأیید کردم.
2. **آرشیو (Archive):** فیلد `archived`. مسیرهای آرشیوشده به‌طور پیش‌فرض از لیست `LearningHub` پنهانند؛ یک دکمه‌ی «نمایش آرشیوشده‌ها (N)» آن‌ها را برمی‌گرداند. آرشیو کردن هر توقف موقت فعال را هم لغو می‌کند (یک مسیر هم‌زمان نمی‌تواند paused و archived باشد).
3. **یادداشت آزاد (`notes`):** یک `textarea` اختیاری در فرم ویرایش هر زیرمسیر — برای نکاتی مثل منبع یا روش تمرین که در ساختار فعلی (واحد/سهمیه/بازه) جایی نداشتند.
4. **کپی/کلون:** دکمه‌ی «کپی مسیر» یک زیرمسیر تازه با همان واحد/سهمیه/بازه/تکرار می‌سازد (پیشرفت صفر، تسک پیوندی تازه) — برای مسیرهای مشابه (مثلاً همان قرائت با ترتیب متفاوت) بدون تایپ دوباره‌ی تنظیمات.
5. **مرتب‌سازی:** یک ردیف چیپ («پیش‌فرض» / «فوریت» / «الفبا») بالای لیست زیرمسیرها؛ حالت «فوریت» بر اساس `computeTrackBalance(...).balance` نزولی مرتب می‌کند (عقب‌افتاده‌ترین‌ها اول).
6. `DailyReportView`: فیلتر ردیف‌های فرم ثبت سریع حالا `sec.paused`/`sec.archived` را هم رد می‌کند (قبلاً فقط `quotaPerPeriod` چک می‌شد).
7. آیکون تازه‌ی `pause` (دو خط عمودی، SVG ساده) به `ICON_PATHS` اضافه شد؛ برای «ازسرگیری» از آیکون موجود `play` و برای «آرشیو» از آیکون موجود `folder` استفاده شد تا جدول آیکون‌ها بی‌جهت بزرگ نشود.
8. `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، `npm run build`، `node --check main.js`، بررسی نبود کاراکتر خراب انکودینگ، و اسکریپت تستِ مستقلِ بالا (۱۳/۱۳ پاس) — همگی تأیید شدند.

**⚠️ برخورد با نشست موازی هنگام push:** قبل از این‌که این کار push شود، سه کامیت تازه از یک نشست دیگر (جلسه‌های ۲۲–۲۴، بازطراحی مودال‌ها) روی `origin/main` ظاهر شده بود. با `git fetch` تشخیص داده شد («diverged: ۱ کامیت من، ۳ کامیت remote»). چون فایل‌های تغییریافته در دو طرف تقریباً مجزا بودند (من فقط `SubsectionCard`/`LearningHub`/توابع کمکی تاریخ را دست زدم؛ نشست دیگر `ModalShell` و مودال‌های افزودن را)، `git rebase origin/main` بدون تعارض در `src/app.jsx` انجام شد — فقط `main.js` (فایل باندل‌شده) تعارض داشت که با `npm run build` دوباره از سورسِ merge‌شده ساخته و جایگزین شد. بعد از rebase، همه‌ی بررسی‌های بالا (build/tsc/syntax/encoding/تست مستقل) دوباره تکرار شد تا مطمئن شوم چیزی در ادغام خراب نشده، سپس push انجام شد.

**⚠️ محدودیت شناخته‌شده:** تست بصری واقعی هنوز انجام نشده. مدل فعلی «آرشیو» ساده است — اگر کاربر یک مسیر را از حالت آرشیو خارج کند، `createdDate` تغییر نمی‌کند (برخلاف ازسرگیریِ توقف موقت)، یعنی ممکن است بدهیِ کل بازه‌ی آرشیو یک‌جا ظاهر شود؛ چون آرشیو برای «بازنشستگیِ دائمی» طراحی شده نه یک وقفه‌ی کوتاه، این محدودیت عمداً پذیرفته شد و مستند می‌شود.

**نتیجه:** کار ۵ یک لایه‌ی «مدیریت» تازه گرفت که تا امروز نبود؛ ادامه‌ی درخواست کاربر (streak، نمودار فعالیت، جشن تکمیل هدف، خروجی گزارش یک موضوع) در جلسه‌ی ۲۶ (همین نشست، بعد از این push) پیگیری شد.

---

### جلسه‌ی ۲۶ (۶ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶) — ادامه‌ی مستقیم جلسه‌ی ۲۵: streak، نمودار فعالیت، جشن تکمیل هدف، خروجی گزارش موضوع

همان نشست/دستور جلسه‌ی ۲۵ («تا می‌توانی قابلیت اضافه کن») مستقیماً ادامه پیدا کرد؛ چهار قابلیت مجزا، هرکدام با commit + build/tsc/encoding + push جداگانه (طبق قرارداد «بعد از هر مرحله‌ی معنادار پوش کن»)، بدون نیاز دوباره به `git fetch` چون remote بین این چهار کامیت توسط نشست دیگری تغییر نکرد.

**۱. Streak (رشته‌ی روزهای متوالی) — `computeTrackStreak(subsection, topic, task)`:**
از امروز به‌عقب می‌شمارد؛ روزهای غیرموظف (due=false) را رد می‌کند و روی اولین روزِ **گذشته**‌ی موظف-ولی-ثبت‌نشده متوقف می‌شود. **نکته‌ی مهم UX که عمداً لحاظ شد:** اگر *امروز* موظف است ولی هنوز ثبت نشده (چون روز هنوز تمام نشده)، رشته را قطع نمی‌کند — فقط شمارش را از دیروز شروع می‌کند؛ در غیر این صورت کاربری که هر روز عصر ثبت می‌کند، از صبح تا عصر رشته‌اش صفر می‌دید. این منطق قبل از اتصال به کد اصلی با یک اسکریپت Node مستقل و ۴ سناریو (رشته‌ی کامل، امروزِ ناتمام، شکاف واقعی در گذشته، تکرار هفتگی) تأیید شد. نمایش: یک نشان کوچک شعله‌ای رنگ نارنجی کنار عنوان هر زیرمسیر در `SubsectionCard` (فقط وقتی streak > 0 و مسیر paused/archived نباشد).

**۲. نمودار حرارتی فعالیت (`TrackHeatmap`)، شبیه GitHub:**
یک SVG با ۱۲ ستون (هفته) × ۷ ردیف (روز) از `progressLog`، رنگ هر خانه بر اساس نسبت مقدار ثبت‌شده‌ی آن روز به سهمیه (یا صرفاً حضور/عدم‌حضور اگر سهمیه‌ای تعریف نشده)؛ هر خانه یک `<title>` SVG بومی برای تولتیپ تاریخ/مقدار دارد. پشت یک لینکِ تاشوی کوچک در `SubsectionCard` مخفی است (پیش‌فرض بسته) تا کارت شلوغ نشود. **ستون‌ها همیشه چپ=قدیم/راست=جدید هستند حتی در رابط RTL** — طبق قرارداد جهانیِ خواندن این نوع نمودار، نه جهت متن فارسی؛ این تصمیم عمداً مستند می‌شود چون برخلاف بقیه‌ی رابط (RTL) است.

**۳. جشن تکمیل هدف + «افزایش هدف»:**
یک باگ/محدودیت واقعی در همین بررسی پیدا شد: `addTaskProgress` پیشرفت را در `progressTarget` سقف می‌زند و `ProgressiveTaskBar` با رسیدن به ۱۰۰٪ کل فرم ثبت را مخفی می‌کند — یعنی هر مسیر یادگیریِ **مکرر و سهمیه‌ای** (مثل «هرروز یک صفحه مرور» با یک هدف عددی صرفاً نمادین) با اولین رسیدن به هدف برای همیشه قفل می‌شد و دیگر امکان ثبت نداشت. راه‌حل: به‌جای فرم مخفی‌شده، یک بنر تبریک با دکمه‌ی «افزودن/افزایش هدف» نشان داده می‌شود که `progressTarget` را (۳۰ روز سهمیه یا دوبرابرِ هدف فعلی اگر سهمیه‌ای نیست) بالا می‌برد و `status` را به `todo` برمی‌گرداند. **عمداً محدود به ماژول یادگیری شد** (`SubsectionCard`/`LearningHub`)، نه خودِ `ProgressiveTaskBar`/`TaskRow` مشترک — تا رفتار تسک‌های روند‌دارِ غیریادگیری دست‌نخورده بماند.

**۴. خروجی Markdown یک موضوع یادگیری (`buildLearningTopicMarkdownReport`):**
برای هر زیرمسیر: وضعیت (فعال/متوقف/آرشیوشده)، سهمیه/بازه، پیشرفت و درصد، streak فعلی، یادداشت، و کل تاریخچه‌ی `progressLog` (به‌ترتیب تاریخ). از همان پل عمومی `plugin.exportDailyReport(filename, content)` استفاده شد (که با وجود نامش، از قبل عمومی/مستقل از «روزانه» بود). **یک ریسک واقعی در همین جلسه اصلاح شد:** عنوان موضوع (متن آزاد کاربر) مستقیماً در نام فایل استفاده می‌شد؛ چون `exportDailyReport` در `main.ts` هیچ پاکسازی‌ای روی filename انجام نمی‌دهد، کاراکترهایی مثل `/` می‌توانستند مسیر فایل را بشکنند یا زیرپوشه‌ی ناخواسته بسازند — قبل از استفاده، عنوان با یک `replace` کاراکترهای `\/:*?"<>|` را با `-` جایگزین می‌کند.

**بررسی‌های تکراری در طول هر ۴ گام:** `npm run build`، `node --check main.js`، `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، بررسی نبود کاراکتر خراب انکودینگ، و — چون نشست ۲۲ کشف کرده بود که `styles.css` استاتیک/از‌پیش‌کامپایل‌شده است — یک بررسی صریح کلاس‌به‌کلاس با `grep` روی `styles.css` برای هر کلاس Tailwind تازه‌ای که استفاده شد. با همین بررسی، `inline-flex` و `italic` (که برای نشان streak و نمایش یادداشت استفاده شده بودند) کامپایل‌شده نبودند و به `style` inline (`display:"inline-flex"`, `fontStyle:"italic"`) تغییر داده شدند تا بی‌صدا بی‌اثر نمانند.

**⚠️ محدودیت‌های شناخته‌شده:**
- تست بصری واقعی هنوز در یک ولت Obsidian واقعی انجام نشده (این محیط Obsidian ندارد).
- افزایش خودکار هدف (مقدار ثابت «۳۰ روز سهمیه» یا «دوبرابر») یک پیش‌فرض ساده است، نه چیزی که از کاربر پرسیده شود؛ اگر عدد نامناسب به‌نظر برسد (مثلاً برای یک هدف خیلی بزرگ/کوچک)، باید در جلسه‌ی بعد قابل‌تنظیم یا حداقل قابل‌ویرایش‌دستی بعد از افزایش شود (فعلاً کاربر می‌تواند از طریق «ویرایش» زیرمسیر آن را دستی عوض کند، چون این عدد فقط `progressTarget` تسک را عوض می‌کند، نه چیز پنهانی).
- نمودار حرارتی هیچ‌جا لینک/تعامل کلیکی ندارد (فقط نمایشی)؛ افزودن امکان ثبت پیشرفت مستقیم از یک خانه‌ی نمودار می‌تواند قدم بعدی باشد.

**نتیجه:** هر ۵ آیتم درخواست‌شده‌ی کاربر برای «بهبود بخش یادگیری» (مدیریت مسیر، streak، نمودار فعالیت، جشن هدف، خروجی گزارش) در جلسه‌ی ۲۵ و ۲۶ تکمیل و push شد؛ در همین مسیر دو باگ/ریسک واقعی (نه فقط ظاهری) هم کشف و رفع شد: قفل‌شدن دائمی مسیرهای سهمیه‌ای بعد از رسیدن به هدف، و امکان شکستن مسیر فایل خروجی با عنوان آزاد کاربر.

---

### جلسه‌ی ۲۷ (۶ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶) — نمای هفتگیِ ساعتی در تقویم

طبق دستور کاربر («ریپو را بررسی کن... ولی کار قبلی‌ات را ادامه بده. زحمات قبلی را دور نریز»): ابتدا `git fetch origin` چک شد — remote از ۸ کامیت (جلسات ۲۲ تا ۲۶، بازطراحی مودال‌ها + مدیریت مسیر یادگیری + streak/heatmap/هدف/خروجی) جلوتر بود. چون این نشست هیچ تغییر کامیت‌نشده‌ای نداشت، با `git merge --ff-only origin/main` (نه `reset --hard`) هماهنگ شد — fast-forward خالص، **هیچ کاری از هیچ نشستی دور ریخته نشد**. کل `PROGRESS.md` (۷۴۲ خط) از اول تا آخر خوانده شد.

**چرا این آیتم:** طبق دستور صریح کاربر مبنی بر ادامه‌ی «کار قبلیِ خودم» (نه شروع یک مسیر تصادفی تازه)، و چون مسیر این نشست در جلسات ۲۰/۲۱ روی کار ۳ (بازطراحی تقویم) متمرکز بود، از دو قلم باقیمانده‌ی کار ۳ («تفکیک نوع آیتم» در برابر «نمای هفتگیِ ساعتی»)، دومی انتخاب شد چون یک افزودنیِ کاملاً مجزا و کم‌ریسک بود (کامپوننت تازه، بدون دست‌زدن به مدل داده‌ی تسک)؛ اولی («تفکیک نوع آیتم») یک تغییر مدل داده در سرتاسر فایل می‌طلبد که ریسک بیشتری دارد و برای یک نشست دیگر می‌ماند.

**⚠️ نکته‌ی فنی حیاتیِ به‌ارث‌رسیده از جلسه‌ی ۲۲ (رعایت شد):** چون `styles.css` یک فایل Tailwind از‌پیش‌کامپایل‌شده‌ی *استاتیک* است (نه بیلد زنده)، **هر کلاس تازه باید قبل از استفاده در برابر `styles.css` واقعی چک شود**، وگرنه بی‌صدا هیچ اثری نخواهد داشت. یک اسکریپت پایتون مستقل نوشته شد که تمام کلاس‌های استفاده‌شده در کد تازه را استخراج و یکی‌یکی در برابر `styles.css` صحت‌سنجی می‌کند. **یک یافته‌ی جالب از همین بررسی:** کلاس `p-0.5` که در سوییچر ویوی تقویم *از قبل* (کد قدیمی، دست‌نخورده از جلسات پیش از این نشست) استفاده می‌شد، اصلاً کامپایل نشده بود (فقط `py-0.5`/`gap-0.5` کامپایل شده‌اند، نه `p-0.5` خودش) — یعنی آن ظرفِ سوییچر ویو همیشه بدون padding داخلی رندر می‌شده (یک نقص ظاهریِ کوچکِ از‌قبل‌موجود، نه چیزی که این نشست ایجاد کرده باشد). چون این الگو را عیناً برای سوییچ تازه‌ی «خلاصه/ساعتی» کپی کرده بودم، هر دو مورد (قدیمی و تازه) با یک `sed` به `p-1` (که واقعاً کامپایل شده) تغییر کردند — یک تصحیح کوچکِ جانبی، مستند شده تا در جلسه‌ی بعد دوباره کشف نشود.

**تغییرات (`app.jsx`؛ هیچ فایل دیگری لمس نشد):**
1. **`WeekHourlyView` (کامپوننت تازه):** یک جدول ساعتی افقی با ۷ ستون روز (کنار هم، با اسکرول افقی روی صفحه‌های باریک) × ردیف‌های ۳۰دقیقه‌ای (همان بازه‌ی `CAL_HOURS` که `DayPlannerView` هم استفاده می‌کند)، با یک ستون برچسبِ ساعت در راست. برای هر روز، تسک‌های زمان‌بندی‌شده (`isTaskDueOn` + `tsk.time`) به‌شکل بلوک‌های کوچک رنگی (رنگ ربع ماتریس) در جای درست رندر می‌شوند؛ کلیک روی یک بلوک ویرایش می‌کند (`onEdit`)، کلیک روی یک ردیف خالی تسک تازه با همان زمان می‌سازد (`onCreateAt` — دقیقاً همان مکانیزم جلسه‌ی ۲۱، نه چیز جدید).
2. **⚠️ تصمیم عمدی محدودسازی دامنه:** این نسخه‌ی اول **فقط نمایشی/کلیکی است — درگ‌ودراپ یا تغییرِاندازه ندارد** (برخلاف `DayPlannerView`). دلیل: پیاده‌سازی درگ‌ودراپِ *بین‌روزی* (یک تسک را از روز سه‌شنبه به پنج‌شنبه بکشی) پیچیدگی و ریسک محسوسی بیشتر از نسخه‌ی تک‌روزه دارد (باید محور افقیِ روز هم از مختصات پیکسلی استخراج شود، نه فقط محور عمودیِ زمان) — طبق همان قاعده‌ی «کوچک‌ترین قدم مفید» که در جلسات ۸/۲۱/۲۵ هم استفاده شد، این به یک جلسه‌ی جدا موکول شد (در چک‌لیست بالای کار ۳ ثبت شد).
3. **`CalendarViews`:** به‌جای اضافه‌کردن نمای هفتگیِ ساعتی به‌عنوان یک گزینه‌ی کاملاً جدا در سوییچر بالای صفحه (که با ۵ گزینه‌ی فعلی شلوغ می‌شد)، یک state محلی تازه‌ی `weekSubView` ("cards" | "hourly", پیش‌فرض "cards") اضافه شد؛ وقتی `view === "week"` است، یک سوییچ کوچکِ «خلاصه/ساعتی» بالای محتوای هفته نشان داده می‌شود که بین `WeekView` (نمای مینی‌کارت قبلی، دست‌نخورده) و `WeekHourlyView` (تازه) جابه‌جا می‌کند. این یعنی رفتار پیش‌فرض هیچ‌کس عوض نشد (کسی که هیچ‌وقت این سوییچ را نمی‌زند، دقیقاً همان تجربه‌ی قبلی را می‌بیند).
4. `npm run build`، `node --check main.js`، `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، بررسی نبود کاراکتر خراب انکودینگ، و بررسیِ سیستماتیکِ کلاس‌به‌کلاس در برابر `styles.css` — همگی تأیید شدند.

**⚠️ محدودیت‌های شناخته‌شده:**
- بدون درگ‌ودراپ/تغییرِاندازه (فقط کلیک) — طبق تصمیم بالا.
- روی صفحه‌های خیلی باریک (موبایل) این نما نیازمند اسکرول افقی است (`overflow-x-auto` با `min-width` ثابت ۶۲۰px) — قابل استفاده ولی نه ایده‌آل؛ اگر کاربر عمدتاً موبایل استفاده می‌کند شاید نمای «خلاصه» (پیش‌فرض) یا «روز» مناسب‌تر بماند.
- تست بصری واقعی هنوز در یک Obsidian واقعی انجام نشده.

**نتیجه:** «نمای هفتگیِ ساعتی» از قلم‌های باقیمانده‌ی کار ۳ بسته شد. از قلم‌های اصلیِ این کار، فقط «تفکیک نوع آیتم» و دو مورد کم‌اهمیت‌تر (نمای ۳روزه، بازآرایی سبد تسک‌های زمان‌بندی‌نشده) باقی مانده‌اند.

---

## ۴. لاگ لِین‌ها (از سیستم لِین‌بندیِ بخش ۰ به بعد — هر لِین فقط زیربخش خودش را می‌نویسد)

### ۴.۱ لِین تسک‌ها (`tasks` + `AddTaskModal`)

⚠️ **هشدار شاخه (طبق بخش ۰.۳):** از این لحظه تا اطلاع ثانوی روی شاخه‌ی `refactor/modal-native-rebuild` کار می‌کنم، نه مستقیم روی `main`. علت: بعد از سه دور رفت‌وبرگشت با اسکرین‌شات کاربر روی همین مودال، جلسه‌ی ۳ (پایین‌تر) کشف کرد که کل زیرساخت CSS کامپایل‌شده‌ی Tailwind فقط زیر کلاس `.lifeflow-plugin-host` کار می‌کند (نه `.lifeflow-app-root` که قبلاً فقط همان به `contentEl` اضافه شده بود) — یعنی فیکسِ جلسه‌ی ۳ ناقص بود و باید دوباره و با احتیاط بیشتر امتحان شود. چون `ModalShell` یک کامپوننت مشترک بین همه‌ی لِین‌هاست و ممکن است این‌بار چند دور تست/اصلاح لازم شود، ترجیح دادم این رفت‌وبرگشت‌ها روی یک شاخه‌ی جدا بمانند تا `main` بین این‌بین به‌خاطر یک مودالِ نیمه‌کاره خراب نشود. لِین‌های دیگر می‌توانند طبق روال عادی روی `main` ادامه دهند. قبل از merge نهایی، دوباره `git fetch`/rebase می‌زنم.

**جلسه‌ی ۱ (۶ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶) — دو باگ واقعی از روی اسکرین‌شات کاربر در `AddTaskModal`**

کاربر یک اسکرین‌شات واقعی از پنجره‌ی «تسک جدید» (رندرشده در Obsidian واقعی) فرستاد و پرسید آیا هنوز به‌هم‌ریخته است. بررسی دقیق تصویر (نه فقط کد) دو مشکل واقعی و قابل‌اثبات نشان داد که در جلسات قبلی (بازطراحی مودال‌ها، جلسه‌های ۲۲/۲۳ در لاگ قدیمی) از قلم افتاده بودند:

1. **متن تکراری در چیپ نوع تسک:** برچسب گزینه‌ی «تسک روند‌دار» به‌صورت `t("progress_task", "fa") + " (روند‌دار)"` ساخته می‌شد، ولی خودِ `t("progress_task", "fa")` از قبل رشته‌ی «تسک روند‌دار» را برمی‌گرداند — نتیجه: متن نمایش‌داده‌شده روی چیپ لفظاً «تسک روند‌دار (روند‌دار)» بود (تکرار). پسوند اضافه حذف شد.
2. **رنگِ فعالِ چیپ‌ها عملاً بی‌اثر بود:** کامپوننت مشترک `Chip` برای چیپ فعال با یک `color` مشخص (اولویت، ربع آیزنهاور، مدت‌زمان) فقط `borderColor`/`color` را به‌صورت inline ست می‌کرد. ولی کلاس CSS مشترک `.lf-chip-active` (در `styles.css`) دقیقاً همین دو ویژگی را با `!important` ست کرده — و `!important` در یک stylesheet همیشه روی inline style برنده می‌شود مگر خودِ inline هم `!important` داشته باشد. نتیجه: `color` prop عملاً هیچ‌وقت روی صفحه دیده نمی‌شد؛ در اسکرین‌شات هم همین دیده می‌شد — چیپ فعالِ «متوسط» (اولویت) و چیپِ فعالِ ربع آیزنهاور خیلی کم از چیپ‌های غیرفعال قابل‌تشخیص بودند. راه‌حل: `Chip` حالا وقتی فعال و `color` دارد، `background: color` را هم inline ست می‌کند — چون قانون پس‌زمینه‌ی خودِ `.lf-chip-active` در styles.css اصلاً `!important` ندارد، این override واقعاً اثر می‌کند و چیپ یک پرشدگیِ رنگیِ واقعی می‌گیرد (حاشیه/متن همچنان از طریق کلاس مشترک با accent هماهنگ می‌ماند).

⚠️ **هماهنگی:** `Chip` طبق جدول بخش ۰.۲ یک کامپوننت **مشترک بین همه‌ی لِین‌ها**ست. این تغییر فقط یک مقدار inline اضافه می‌کند (بدون تغییر امضای prop یا رفتار برای چیپ‌های بدون `color`)، پس ریسک شکستن رفتار در بقیه‌ی لِین‌ها (که همه از همین `Chip` برای فیلترها/انتخاب‌گرها استفاده می‌کنند) پایین است، ولی هر لِینی که چیپِ رنگیِ خودش را در جای دیگری استفاده می‌کند خوب است این تغییر را در نظر بگیرد.

**فایل‌های لمس‌شده:** فقط `src/app.jsx` (کامپوننت مشترک `Chip` + یک برچسب داخل `AddTaskModal`) + `main.js` تولیدشده.

`npx tsc --noEmit`، `npm run build`، `node --check main.js`، و بررسی نبود کاراکتر خراب انکودینگ تأیید شدند.

⚠️ **محدودیت شناخته‌شده:** بخش «توضیح/عنوان» در بالای پنجره در اسکرین‌شات کمی نامرتب به‌نظر می‌رسید (جعبه‌ی توضیح کوچک‌تر بالای عنوان)، ولی بررسی کد نشان داد ترتیب DOM درست است (عنوان اول، توضیح دوم، هیچ float/absolute-positioning‌ای هم در کار نیست) — این احتمالاً یا یک توهم بصری از فشرده‌سازی تصویر است یا یک حالت رندرِ خیلی جزئی که بدون تست زنده در Obsidian واقعی نمی‌شود مطمئن شد؛ تغییری در ترتیب اعمال نشد تا رگرسیون بی‌مورد ایجاد نشود.

**جلسه‌ی ۲ (۶ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶) — ریشه‌ی اصلیِ «به‌هم‌ریختگی»: کل مودال با جهت LTR رندر می‌شد**

کاربر یک اسکرین‌شات دوم فرستاد (بعد از دیدن اثر مثبت رفع رنگ چیپ‌ها در جلسه‌ی قبل — رنگ‌ها حالا درست پر می‌شوند) و صریحاً گفت: «یکی از مشکلاتش چپ‌به‌راست است... دوم فاصله‌های بین فیلدها درهم‌ریخته... خودت بررسی کن». به‌جای حدس‌زدنِ تک‌تکِ فاصله‌ها، دنبال یک **علتِ ریشه‌ای واحد** گشتم که بتواند هم‌زمان توضیح‌دهنده‌ی چیدمانِ برعکسِ سربرگ (عنوان «تسک جدید» باید سمت راست باشد ولی چپ بود، دکمه‌ی X باید چپ باشد ولی راست بود)، هم ترتیبِ برعکسِ گرید ربع‌های آیزنهاور، و هم موقعیتِ اسکرول‌بار (سمت راست به‌جای چپ) باشد.

**کشف:** `ModalShell` (پوسته‌ی مشترکِ همه‌ی ۹ مودال اپ) از طریق `ReactDOM.createPortal(content, document.body)` رندر می‌شود — یعنی کل درخت DOM مودال مستقیماً به `<body>` اضافه می‌شود، **بیرون از ریشه‌ی اصلیِ اپ**. تنها جایی که در کل فایل خصیصه‌ی `dir` (بر اساس زبان انتخابی) ست می‌شود، ریشه‌ی همان اپ اصلی است (`dir: langDir` روی `lifeflow-app-root`) — که مودال‌ها چون بیرون از آن رندر می‌شوند، اصلاً آن را به ارث نمی‌برند! نتیجه: از همان اول (احتمالاً از وقتی مودال‌ها ساخته شدند)، **هر مودالِ این پلاگین با جهتِ پیش‌فرضِ مرورگر (LTR) چیده می‌شد**، نه بر اساس زبان انتخابیِ کاربر — چون `document.body` در خودِ Obsidian دلیلی ندارد RTL باشد فقط چون زبان این پلاگین فارسی است. همین یک نکته، سربرگ برعکس، گریدِ ربع‌ها، و سمتِ اسکرول‌بار را همزمان توضیح می‌دهد.

**راه‌حل:** داخل خودِ `ModalShell`، مستقل از ریشه‌ی اپ، زبانِ فعلی از طریق همان `loadSettings().language` (تابعی که از قبل در فایل بود) خوانده و جهتِ متناظرش از `LANGUAGES` محاسبه می‌شود؛ این `dir` روی خودِ عنصر بیرونیِ backdrop ست می‌شود. چون پرتال فقط **یک** نقطه در کل فایل است (`createPortal` فقط همین‌جا استفاده شده)، این یک تغییرِ کوچک و متمرکز، هر ۹ مودال را یک‌جا درست می‌کند — نیازی به پاس‌دادن prop تازه به تک‌تکِ مودال‌ها نبود.

**چرا این یک تغییر بزرگ ولی کم‌ریسک است:** بعد از این فیکس، تقریباً تمامِ چینش‌های flex/grid داخلِ مودال (که با کلاس‌های Tailwind معمولی مثل `flex`, `grid-cols-2`, `justify-between` نوشته شده‌اند، نه با کلاس‌های فیزیکیِ `mr-`/`ml-`/`text-left`) به‌طور خودکار (توسط خودِ مرورگر، بر اساس `dir`) درست می‌چینند — بدون این‌که لازم باشد تک‌تکِ آن‌ها دستی اصلاح شوند. قبل از commit، `ToggleSwitch` (که از `right:` فیزیکی برای موقعیتِ دکمه استفاده می‌کند) و ردیفِ چیپ‌های زیرتسک بررسی شدند و از قبل با فرضِ RTL نوشته شده بودند — پس با این فیکس هم‌خوان می‌مانند، نه متناقض.

**فایل‌های لمس‌شده:** فقط `src/app.jsx` (`ModalShell`، تنها مصرف‌کننده‌ی `createPortal` در کل فایل) + `main.js` تولیدشده.

`npx tsc --noEmit`، `npm run build`، `node --check main.js`، و بررسی نبود کاراکتر خراب انکودینگ تأیید شدند.

⚠️ **محدودیت شناخته‌شده:** تست بصری واقعی در Obsidian هنوز انجام نشده — این تحلیل کاملاً از روی بررسیِ کد (مسیر `createPortal` و نبودِ `dir` در آن) و تطبیق‌دادنِ دقیقِ آن با جزئیاتِ اسکرین‌شات (سربرگ، گرید، اسکرول‌بار) انجام شد، نه با رندر زنده. اگر بعد از این فیکس هنوز جای خاصی نامرتب بود (مثلاً ورودی سفارشیِ مدت‌زمان که در اسکرین‌شات کمی پهن به‌نظر می‌رسید)، به احتمال زیاد فقط عارضه‌ی جانبیِ همین باگ جهت بوده و با این فیکس رفع می‌شود؛ اگر نشد، در اسکرین‌شات بعدی مشخص می‌شود.

**جلسه‌ی ۳ (۶ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶) — بازسازی معماری مودال: از شبیه‌سازیِ دستی به `Modal` رسمیِ Obsidian**

فیکسِ جهت (جلسه‌ی ۲) مشکل چپ‌به‌راست بودن را حل کرد، ولی کاربر یک اسکرین‌شاتِ سوم فرستاد: حالا جهت درست بود (سربرگ/گرید/رنگ چیپ‌ها همه درست) ولی **خودِ پنجره پایین صفحه باز می‌شد، نه وسط** — و صریحاً خواست: «باید بیشتر شبیه پنجره‌ی ساخت تسک در پلاگین‌های رایج آبسیدین بشود... اگر نیاز است از اول بساز پنجره را».

**تصمیم:** به‌جای اضافه‌کردن یک لایه‌ی دیگر از CSS دستی برای *شبیه‌سازیِ* یک مودالِ وسط‌چین (که همچنان هیچ‌وقت دقیقاً مثل مودال‌های واقعیِ Obsidian رفتار/ظاهر نمی‌شد و برای هر تمِ کاربر باید جدا تست می‌شد)، تصمیم گرفته شد از خودِ کلاس رسمیِ `Modal` در Obsidian API استفاده شود — چیزی که هر پلاگین دیگری هم برایش استفاده می‌کند، پس همیشه دقیقاً با تمِ فعالِ کاربر (وسط‌چین، پس‌زمینه‌ی تیره‌شده، سایه، گردیِ گوشه‌ها، بستن با Escape/کلیک‌بیرون) هماهنگ می‌ماند، بدون این‌که ما مجبور باشیم هرکدام از این رفتارها را خودمان بازسازی کنیم.

**پیاده‌سازی (بدون دست‌زدن به تک‌تکِ ۹ مودال — فقط `ModalShell`، تنها مصرف‌کننده‌ی `createPortal` در کل فایل):**

1. **`main.ts`:** یک کلاسِ کوچکِ `LifeFlowReactModal extends Modal` اضافه شد — هیچ رندرِ Reactـی خودش انجام نمی‌دهد، فقط یک فیلد `onExternalClose` دارد که وقتی خودِ Obsidian مودال را می‌بندد (Escape، کلیک روی پس‌زمینه‌ی تیره) صدا زده می‌شود. متد تازه‌ی `LifeFlowPlugin.openReactModal(onExternalClose)` یک نمونه از این کلاس می‌سازد، بازش می‌کند (`contentEl` بلافاصله بعد از ساختنِ نمونه، حتی قبل از `open()`، آماده است — طبق constructor خودِ `Modal`)، و خودِ نمونه (با `contentEl`/`close()`/`setTitle()`) را برمی‌گرداند. از همان بریج سراسری `window.__lifeflowPlugin` در دسترس React قرار گرفت.
2. **`app.jsx` (`ModalShell`):** به‌جای `ReactDOM.createPortal(content, document.body)` با یک backdrop و «sheet» دستی، حالا در یک `useEffect` (فقط یک‌بار در mount) `plugin.openReactModal(...)` را صدا می‌زند، جهت (`dir`) را روی `contentEl` ست می‌کند، و همان محتوای React (فیلدها + فوتر) را با `ReactDOM.createPortal(body, modal.contentEl)` به داخلِ مودالِ واقعی پرتال می‌کند. عنوان (`title`) از طریق `modal.setTitle(title)` ست می‌شود — یعنی دیگر یک `<h3>`/دکمه‌ی X دستیِ خودمان لازم نیست؛ سربرگ و دکمه‌ی بستن حالا کاملاً خودِ Obsidian است. موقعِ unmount شدنِ کامپوننت (چون state والد `showAdd`/`showSettings`/... را `false` کرد)، خودمان `modal.close()` را صدا می‌زنیم (و قبلش `onExternalClose` را null می‌کنیم تا `onClose` دوباره صدا زده نشود)؛ اگر برعکس، خودِ کاربر با Escape/کلیک‌بیرون مودال را ببندد، `onExternalClose` صدا زده می‌شود که همان `onClose` propِ React است — یعنی state والد هم به‌روز می‌شود، دو طرف همیشه هماهنگ می‌مانند.
3. **یک باگِ رنگیِ نهفته که همین بازسازی برملا کرد:** تمام override های رنگیِ تم (`text-white`→`var(--text-normal)`, `text-slate-*`→`var(--text-muted)` و ...) در `styles.css` با سلکتور `.lifeflow-app-root [class*="..."]` نوشته شده بودند — یعنی فقط وقتی کار می‌کنند که المان داخلِ ریشه‌ی اصلیِ اپ باشد. چون پرتالِ مودال (چه نسخه‌ی قدیمیِ `document.body`، چه نسخه‌ی تازه‌ی `contentEl`) از اول بیرون از این ریشه بوده، این override ها هیچ‌وقت روی محتوای مودال اثر نمی‌کردند — متن‌ها با رنگِ خامِ کامپایل‌شده‌ی Tailwind (سفیدِ ثابت، خاکستریِ ثابت) نمایش داده می‌شدند، که فقط در تمِ تیره تصادفاً قابل‌قبول به‌نظر می‌رسید و در تمِ روشن می‌توانست کاملاً ناخوانا شود. راه‌حل: کلاسِ `lifeflow-app-root` (علاوه‌بر یک کلاسِ تازه‌ی `lifeflow-modal-content`) مستقیماً روی `contentEl` هم اضافه شد — یعنی همان‌ده‌ها قانونِ از‌قبل‌نوشته‌شده، بدون بازنویسیِ styles.css، حالا داخلِ مودال هم اعمال می‌شوند.
4. **پاک‌سازی:** کلاس‌های مرده‌ی `.lf-modal-backdrop`/`.lf-modal-sheet` (دیگر در JSX استفاده نمی‌شوند) از `styles.css` حذف و با یک قانونِ حداقلیِ `.lifeflow-modal-content{ padding: 4px 2px; }` جایگزین شدند.
5. **بررسیِ آگاهانه، بدون تغییر:** `GlobalSearchModal` از `ModalShell` استفاده نمی‌کند (پیاده‌سازیِ جدای خودش را دارد، به‌شکلِ یک overlay معمولی نه پرتال) — چون هنوز داخلِ `lifeflow-app-root` رندر می‌شود، از قبل رنگ/جهتش درست بوده و مشمولِ این باگ نبوده؛ عمداً دست‌نخورده ماند.
6. `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط — و این‌بار صریحاً وجودِ `Modal.setTitle`/`contentEl`/`titleEl`/`open`/`close` هم مستقیماً از `node_modules/obsidian/obsidian.d.ts` تأیید شد)، `npm run build`، `node --check main.js`، تعادلِ آکولادهای `styles.css`، و بررسی نبود کاراکتر خراب انکودینگ همگی تأیید شدند.

**فایل‌های لمس‌شده:** `src/main.ts` (کلاس/متد تازه)، `src/app.jsx` (فقط `ModalShell`)، `styles.css` (حذفِ دو قانونِ مرده + یک قانونِ تازه‌ی کوچک).

⚠️ **محدودیت شناخته‌شده:** تست بصری واقعی در یک Obsidian واقعی (با چند تمِ مختلف، هم روشن هم تاریک) هنوز انجام نشده — این تغییرِ معماری بر پایه‌ی مستنداتِ رسمیِ نوع‌های Obsidian API تأیید شد، نه رندرِ زنده. عرضِ پیش‌فرضِ مودال هم به‌طورِ کامل به تمِ فعالِ Obsidian واگذار شد (هیچ `maxWidth` سفارشی‌ای ست نشد) تا واقعاً «شبیهِ پلاگین‌های رایج» باشد؛ اگر در عمل خیلی باریک/پهن بود، باید در قدمِ بعدی با یک مقدارِ اختیاری تنظیم شود.

**جلسه‌ی ۴ (۶ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶) — سه علت ریشه‌ایِ واقعی، به‌جای بازنویسیِ کامل**

⚠️ این جلسه روی شاخه‌ی `refactor/modal-native-rebuild` انجام شد (طبق هشدار بالا). کاربر یک اسکرین‌شاتِ چهارم فرستاد — با عصبانیتِ صریح («هنوز به‌هم‌ریخته‌س!!!») — که نشان می‌داد بعد از فیکسِ جلسه‌ی ۳ (استفاده از `Modal` بومی)، مودال حالا **وسط‌چین** بود ولی حاشیه‌های بنفشِ درخشان دورِ تقریباً هر فیلدی (عنوان، توضیح، تگ، ورودیِ زیرتسک، ردیفِ یادآوری) دیده می‌شد و فاصله‌گذاری هنوز نامنظم بود؛ کاربر خواست کل پنجره «از اول» بازنویسی شود.

**به‌جای بازنویسیِ کامل، اول دنبال علتِ ریشه‌ای گشتم** — چون بازنویسیِ کامل منطقِ کارکردیِ جاافتاده (تکرار، زیرتسک، نوعِ پیشرفت و ...) را به‌خطر می‌انداخت بدون این‌که معلوم باشد مشکلِ واقعی چیست. سه علتِ مجزا و اثبات‌شده پیدا شد که هرسه با هم دقیقاً همین ظاهر را توضیح می‌دهند:

1. **کل زیرساختِ Tailwindِ کامپایل‌شده فقط زیر `.lifeflow-plugin-host` کار می‌کند، نه `.lifeflow-app-root`.** بررسیِ خودِ `styles.css` نشان داد صدها کلاسِ یوتیلیتیِ کامپایل‌شده (`flex`, `gap-*`, `rounded-*`, `px-*`/`py-*`, `text-[11px]` و ...) همگی با پیشوندِ `.lifeflow-plugin-host` نوشته شده‌اند (کلاسی که فقط روی کانتینرِ خودِ View اصلی هست، نه چیزی که در جلسه‌ی ۳ به `contentEl` اضافه شد). یعنی فیکسِ جلسه‌ی ۳ (اضافه‌کردنِ فقط `lifeflow-app-root`) رنگ‌ها را درست کرد ولی **تمامِ چینش/فاصله‌گذاری** را بی‌اثر گذاشت — دقیقاً همان «فاصله‌های درهم‌ریخته» که کاربر گفت. رفع شد با اضافه‌کردنِ `lifeflow-plugin-host` هم به `contentEl` (کنارِ `lifeflow-app-root`)، به‌علاوه‌ی یک قانونِ کوچکِ override در `styles.css` که سه ویژگیِ خودارجاعِ نامناسب برای یک مودال (`height:100%`, `overflow:auto`, `background` ثابت) را دوباره به مقادیرِ مناسب برمی‌گرداند (بدون این‌که به قوانینِ فرزندِ آن کلاس دست بزند).
2. **خیلی از اینپوت‌ها اصلاً `type="text"` نداشتند.** قانونِ محافظِ رنگِ ورودی‌ها در `styles.css` با سلکتورِ `input[type="text"]` نوشته شده — که فقط وقتی اثر می‌کند که خصیصه‌ی `type` **به‌صراحت** روی DOM باشد؛ اگر یک `<input>` هیچ `type`ای نداشته باشد (که در HTML یعنی متنی رفتار می‌کند، ولی برای این سلکتورِ CSS «نامرئی» است)، این قانون هیچ‌وقت اجرا نمی‌شود و ورودی به رنگِ پیش‌فرضِ تمِ فعالِ Obsidian (که در این مورد ظاهراً بنفش/صورتیِ درخشان بوده) سقوط می‌کند — دقیقاً همان حاشیه‌های بنفشِ نامرتبط‌به‌هم در اسکرین‌شات. یک بررسیِ سراسریِ کل فایل (نه فقط `AddTaskModal`) نشان داد **۸ اینپوتِ خام دیگر هم در بقیه‌ی اپ** (جست‌وجوی سراسری، فرم‌های یادگیری، عنوانِ سریعِ تسک، نامِ گروه، ویرایشِ عنوانِ لیست، آیتمِ جدیدِ لیست) همین مشکل را داشتند — یعنی این یک باگِ نهفته‌ی سراسری بود، نه فقط مالِ این مودال. همه (۸ مورد + کامپوننتِ مشترکِ `TextInput` که حالا خودش پیش‌فرض `type: "text"` می‌گذارد و همه‌ی مصرف‌کننده‌هایش را یک‌جا پوشش می‌دهد) رفع شدند.
3. یادآوریِ فیکسِ جلسه‌ی ۱ (رنگِ چیپِ فعال) و جلسه‌ی ۲ (جهتِ RTL) هنوز پابرجا و درست بودند — این‌ها بخشِ رنگِ درستِ چیپ‌ها و سربرگِ درستِ اسکرین‌شاتِ سوم را توضیح می‌دهند که خودِ کاربر هم تأیید کرده بود.

**چرا بازنویسیِ کامل انتخاب نشد:** با این سه علتِ مشخص و رفع‌شده، تقریباً تمامِ نشانه‌های گزارش‌شده (فاصله‌های نامنظم، حاشیه‌های رنگیِ عجیب، به‌هم‌ریختگیِ کلی) توضیح داده می‌شوند بدون نیاز به بازنویسیِ منطقِ کارکردی. یک بازنویسیِ کاملِ JSX ریسکِ ازدست‌رفتنِ رفتارهای ظریفِ جاافتاده (تکرار، زیرتسک، نوعِ پیشرفت) را داشت بدون تضمینِ حلِ مشکلِ واقعی، درحالی‌که این‌ها علت‌های واقعی و قابل‌اثباتِ خودِ کد بودند.

**فایل‌های لمس‌شده:** `src/app.jsx` (`ModalShell`، `TextInput`، ۸ اینپوتِ خامِ پراکنده)، `styles.css` (یک قانونِ override).

`npx tsc --noEmit`، `npm run build`، `node --check main.js`، تعادلِ آکولادهای `styles.css`، بررسیِ نبودِ کاراکترِ خرابِ انکودینگ، و یک اسکریپتِ پایتونِ کوچک برای تأیید صفر-اینپوتِ-بدون-type در کلِ فایل — همگی تأیید شدند.

⚠️ **محدودیتِ شناخته‌شده:** هنوز تستِ بصریِ زنده در Obsidian واقعی انجام نشده — این‌بار سه علتِ مجزا با شواهدِ مستقیم از خودِ `styles.css` پیدا و رفع شد، ولی تا اسکرین‌شاتِ بعدی قطعی نمی‌شود که همه‌چیز کاملاً درست شده.

**مرحله‌ی بعد:** طبق پروتکلِ بخشِ ۰.۳، قبل از merge به `main`، دوباره `git fetch`/rebase زده می‌شود.

**جلسه‌ی ۵ (۶ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶) — یک گپِ مشابهِ دیگر پیدا و کل الگو مقاوم‌تر شد**

بدون اسکرین‌شاتِ تازه، قبل از ادامه یک بازبینیِ فعالانه روی همان الگوی جلسه‌ی ۴ انجام شد: آیا نوعِ دیگری از `<input>` هم از فهرستِ مجازِ CSS جا مانده؟ بله — فیلدِ کلیدِ API (`type="password"`, در `SettingsModal`) اصلاً در فهرستِ `input[type="text"], input[type="number"], ...` نبود، پس دقیقاً همان مشکلِ جلسه‌ی ۴ را داشت (رنگِ پیش‌فرضِ عجیبِ تم، نه رنگِ محافظِ ما).

**به‌جای اضافه‌کردنِ `input[type="password"]` به فهرست (که فقط این یک مورد را رفع می‌کرد و باز هم برای هر نوعِ تازه‌ای که در آینده اضافه شود همین باگ تکرار می‌شد)،** کل الگو از «فهرستِ مجاز» به «استثنا» تغییر کرد: `input:not([type="checkbox"]):not([type="radio"]):not([type="range"]):not([type="color"]):not([type="file"]):not([type="submit"]):not([type="button"])` — یعنی از این به بعد هر `<input>` تازه‌ای (هر `type`ای، یا اصلاً بدون `type`) به‌طورِ پیش‌فرض استایلِ درست می‌گیرد، مگر آن‌که واقعاً به ظاهرِ بومیِ سیستم‌عامل نیاز داشته باشد (چک‌باکس، رادیو، رنج، رنگ، فایل، دکمه). این کلاسِ کاملِ باگ را برای همیشه می‌بندد، نه فقط این یک نمونه را.

`npm run build`، `node --check main.js`، و تعادلِ آکولادهای `styles.css` تأیید شدند. فایلِ لمس‌شده: فقط `styles.css`.

⚠️ **یادآوری وضعیت:** شاخه‌ی `refactor/modal-audit-pass2` (جلسه‌های ۴ تا ۸، شاملِ رفعِ ریشه‌ایِ مشکلِ چیدمان/رنگِ مودال + تأییدِ jsdom) کامل و تست‌شده push شده ولی **هنوز merge نشده** — منتظرِ اجازه‌ی صریحِ کاربر است.

⚠️ **هشدارِ شاخه‌ی تازه:** طبقِ صفِ کارِ همین لِین در بخشِ ۰.۲ (آیتمِ ۱)، از این لحظه روی شاخه‌ی `feature/task-item-type` کار می‌کنم — «تفکیکِ نوعِ آیتم» (تسک/رویداد/روتین/یادگیری). این یک ویژگیِ تازه است (نه ادامه‌ی همان کارِ مودال)، پس شاخه‌ی جداگانه گرفته شد تا با شاخه‌ی قبلی که هنوز منتظرِ merge است قاطی نشود. طبقِ دستورِ کاربر، قبل از merge هم این شاخه اجازه گرفته می‌شود.

### ۴.۲ لِین یادگیری (`learning`)

**جلسه‌ی ۲ (۷ شهریور ۱۴۰۵ / ۳۰ آگوست ۲۰۲۶) — ⚠️ هشدار شاخه:** طبق کار ۱ در صفِ همین لِین («تخصیص یک جلسه‌ی پومودورو به چند مسیر یادگیری هم‌زمان با سهم مشخص»)، از این لحظه تا اطلاع ثانوی روی شاخه‌ی `feature/pomodoro-multi-track-split` کار می‌کنم، نه مستقیم روی `main` — چون این کار احتمالاً `PomodoroTimerView` (قلمرو لِین ۴) را هم لمس می‌کند. طراحی: به‌جای تقسیمِ درصدیِ یک عددِ واحد بین مسیرها (که چون واحدهای مسیرهای مختلف می‌تواند متفاوت باشد—صفحه در برابر کلمه—از نظر ابعادی بی‌معنی است)، مدل انتخاب‌شده این است: چند **تسکِ روند‌دار** (نه لزوماً فقط مسیر یادگیری؛ همان مکانیزم `progressType === "progressive"` که مسیرهای یادگیری هم از آن استفاده می‌کنند) هم‌زمان انتخاب می‌شوند، هرکدام سهمِ زمانیِ خودش را می‌گیرد (فقط برای گزارش/نمایش، نه محاسبه)، و وقتی جلسه تمام شد، برای **هرکدام جدا** (با واحد خودش) پیشرفت پرسیده می‌شود — نه یک عدد واحد که بین‌شان تقسیم شود. این طراحی نیاز به prop تازه‌ای مثل `projects` در `PomodoroTimerView` ندارد (چون فقط از `tasks` موجود فیلتر می‌شود)، پس ردِ پای تغییر در آن فایل کوچک می‌ماند. لِین‌های دیگر طبق روال عادی روی `main` ادامه دهند؛ اگر لِین ۴ هم‌زمان دارد `PomodoroTimerView` را دست می‌زند، لطفاً همین‌جا پیام بگذارید. **طبق قانونِ تازه‌ی بخش ۰.۳: قبل از merge نهایی این شاخه، از کاربر اجازه گرفته می‌شود، نه فقط هماهنگی با لِین‌های دیگر.**

**⚠️ محدودیتِ شناخته‌شده:** جلساتِ چندتاییِ تقسیم‌شده `taskId: null` دارند، پس دو محاسبه‌ی موجودِ «دقیقه‌ی پومودورو به‌ازای هر تسک» (که از قبل `if (s.taskId)` چک می‌کردند) این جلسات را به هیچ تسکِ واحدی نسبت نمی‌دهند — رفتارِ درست، ولی یعنی آمارِ «چقدر زمان صرفِ هر تسک شد» فعلاً جلساتِ تقسیم‌شده را نمی‌بیند؛ اینجا رفع نشد.

**جلسه‌ی ۳ (۷ شهریور ۱۴۰۵ / ۳۱ آگوست ۲۰۲۶) — کار ۲ صف: تست/جلای ترکیب «روتین سطح موضوع» با «سهمیه‌ی سطح مسیر» (مستقیم روی `main`، بدون شاخه — یک رفع باگِ کوچک و خوش‌تعریف بود، نه رفکتورِ بزرگ):**

یک باگِ واقعی و قبلاً کشف‌نشده پیدا شد: `isTrackDueOn` مستقیم و زنده از `topic.recurrence` می‌خواند، پس تغییرِ روتینِ سطح‌موضوع (از طریق `LearningRoutineEditor`) بلافاصله روی محاسباتِ خودِ Learning Hub (سررسید/موجودی/streak) اثر می‌گذاشت — ولی `onChange` آن ادیتور فقط `updateTopic()` را صدا می‌زد، هرگز تسکِ لینک‌شده‌ی زیربخش‌هایی که از روتینِ موضوع پیروی می‌کنند (`recurrenceOverride === null`) را sync نمی‌کرد. نتیجه: اگر موضوعی از «هرروز» به «هفتگی، فقط دوشنبه‌ها» عوض می‌شد، Learning Hub درست می‌گفت «سه‌شنبه سررسید نیست»، ولی تب تسک‌ها/تقویم (که از `isTaskDueOn` روی همان تسکِ لینک‌شده استفاده می‌کند) همچنان هرروز نشانش می‌داد — دو نمای متفاوت از یک مسیر که با هم اختلاف داشتند، بدون هیچ خطا/کرشی، فقط ساکت و نامرئی.

باگ اول با یک اسکریپتِ Node بازتولید شد (تأیید اینکه واقعاً وجود دارد)، بعد رفع شد: تابعِ تازه‌ی `updateTopicRoutine` که `updateTopic` را wrap می‌کند و بعد `recurrence`/`recurrenceWeekdays` را برای تسکِ لینک‌شده‌ی هر زیربخشِ **بدون override** sync می‌کند — دقیقاً همان کاری که `updateSubsection` از قبل برای override سطح‌مسیر انجام می‌داد. عمداً فقط همین دو فیلد را دست می‌زند، نه `recurrenceDay`/`recurrenceMonth` (که طبق متنِ خودِ `LearningRoutineEditor` جزئیاتی مخصوصِ تب تسک‌هاست برای ماهانه/سالانه؛ دست‌زدن به آن‌ها اینجا یک رگرسیون برای کسی بود که از تب تسک‌ها روزِ خاصی تنظیم کرده). تست شد که مسیرهای دارایِ override کاملاً دست‌نخورده می‌مانند (استقلال حفظ شد) — ۶/۶ پاس.

**بررسیِ سایرِ نقاطِ تعامل (چیزی دیگر پیدا نشد، فقط تأیید شد که درست کار می‌کنند):** `duplicateSubsection` از قبل مقادیرِ واقعیِ تسکِ مبدأ را کپی می‌کند (نه از‌نو از روتینِ موضوع مشتق می‌کند)، پس کلونِ یک مسیرِ دارایِ override درست باقی می‌ماند؛ ادیتورِ cadence سطح‌مسیر در `SubsectionCard` از قبل تضمین می‌کند حداقل یک روزِ هفته انتخاب شده باشد قبلِ ذخیره (پیش‌فرض به روزِ جاری برمی‌گردد اگر چیزی انتخاب نشده)، پس حالتِ «آرایه‌ی خالیِ weekdays» که کامنتِ خودِ `isTrackDueOn` هشدار می‌دهد، تأیید شد از UI غیرِقابل‌دسترسی است (نه یک باگِ زنده).

`npm run build`/`node --check`/`npx tsc --noEmit` (همان ۳ خطای قدیمی)/انکودینگ/ممیزیِ کلاس‌های Tailwind — همه تأیید شدند.

**⚠️ محدودیتِ شناخته‌شده:** جلساتِ چندتاییِ تقسیم‌شده `taskId: null` دارند، پس دو محاسبه‌ی موجودِ «دقیقه‌ی پومودورو به‌ازای هر تسک» (که از قبل `if (s.taskId)` چک می‌کردند) این جلسات را به هیچ تسکِ واحدی نسبت نمی‌دهند — رفتارِ درست، ولی یعنی آمارِ «چقدر زمان صرفِ هر تسک شد» فعلاً جلساتِ تقسیم‌شده را نمی‌بیند؛ اینجا رفع نشد.

**جلسه‌ی ۱ (۶ شهریور ۱۴۰۵ / ۳۰ آگوست ۲۰۲۶) — پیش‌فرض=سهمیه، دکمه‌های +/-، اعداد فارسی، و هسته‌ی «بانک ذخیره»**

ادامه‌ی مستقیم کارِ همین نشست روی یادگیری (جلسه‌های ۲۵/۲۶ در لاگِ قدیمیِ بخش ۳، قبل از وجودِ سیستم لِین‌بندی). کاربر یک درخواستِ ۶ بخشیِ مفصل داد؛ چون بخشی از آن (تخصیصِ دستیِ ذخیره به روزهای دلخواه، دامنه‌ی دقیقِ «شخصی‌سازیِ UI») معماریِ داده‌ی تازه‌ای می‌طلبید، ابتدا برداشتِ خودم را نوشتم و سه سؤالِ مشخص پرسیدم (پاسخ هنوز از کاربر نرسیده — پایین را ببین)، ولی بخش‌های بدون‌ابهام را همان لحظه پیاده‌سازی کردم:

1. **پیش‌فرض ورودیِ ثبت = سهمیه‌ی تعریف‌شده:** قبلاً در `SubsectionCard` (که از `ProgressiveTaskBar` مشترک استفاده می‌کرد) این ورودی همیشه خالی بود. یک کامپوننتِ تازه‌ی مخصوصِ یادگیری، `LearningProgressEntry`، جایگزینِ آن شد که با `subsection.quotaPerPeriod` پیش‌پر می‌شود. **عمداً به‌جای ویرایشِ خودِ `ProgressiveTaskBar` مشترک** (که تسک‌های روند‌دارِ غیریادگیریِ لِین‌های دیگر هم از آن استفاده می‌کنند) — طبق قاعده‌ی احتیاط با کامپوننت‌های مشترک در بخش ۰.۲.
2. **دکمه‌های +/-:** در `LearningProgressEntry` و در `DailyReportTrackRow` (ردیفِ ثبتِ سریع در گزارش روزانه/برنامه‌ریزی) — هر دو.
3. **اعداد فارسی:** `toFa()`/`faDigitsToEn()`/`parseFaNumber()` تازه اضافه شدند (نزدیکِ بقیه‌ی توابعِ کمکیِ تاریخ). `toFa` از `Number.toLocaleString("fa-IR")` واقعیِ موتورِ جاوااسکریپت استفاده می‌کند (نه یک نقشه‌ی دستیِ رقم‌به‌رقم) که هم ارقام و هم جداکننده‌ی هزارگان (٬) را درست می‌دهد؛ `parseFaNumber` مسیرِ برعکس را برای ورودیِ کاربر انجام می‌دهد (چون `<input>`های خام هرچه کاربر تایپ کند را دست‌نخورده نگه می‌دارند، رقمِ فارسی/عربی را خودش عدد نمی‌کند). **محدوده‌ی عمدی: فقط داخلِ ماژولِ یادگیری** — بقیه‌ی اپ همچنان ارقامِ لاتین دارد، چون دقیقاً همین خواسته شده بود، نه یک تغییرِ سراسری. تستِ round-trip مستقل: ۱۰/۱۰ پاس.
4. **هسته‌ی «بانکِ ذخیره» (خواسته‌ی ششم و اصلی‌ترین بخشِ درخواست):** دو تابعِ تازه —
   - `computeTrackSurplusInfo(subsection, topic, task)`: مانده‌ی مثبتِ `computeTrackBalance` (یعنی «جلوتر بودن») را به چیزِ ملموس تبدیل می‌کند: چند روزِ *کاملِ* آینده از قبل پوشش داده شده و تا چه تاریخی.
   - `computeTodayStillOwed(subsection, topic, task)`: «امروز واقعاً چقدر باید انجام شود» بعد از کسرِ ذخیره‌ی *قبل از امروز* (تا خودِ امروز را در محاسبه دوبار حساب نکند).
   هر دو با یک تستِ مستقلِ Node با «امروزِ» ثابت‌شده (نه `new Date()` واقعی، تا از رگرسیونِ تصادفیِ ناشی از گذشتِ زمانِ واقعی مصون بماند) تأیید شدند — ۱۰/۱۰ پاس، شاملِ حالت‌های استثنا (مسیرِ متوقف/آرشیوشده، مسیرِ غیرموظف امروز، ذخیره‌ی جزئی).
5. **ادغام با «برنامه‌ریزی» (خواسته‌ی کاربر برای نمایش خودکار):** `DailyReportTrackRow` بازنویسی شد — پیش‌فرض/برچسبِ ورودی حالا `computeTodayStillOwed` است (نه سهمیه‌ی خام)، با سهمیه‌ی خام هم در زیرنویس برای مرجع. یک خطِ سبزِ «🎁 ذخیره داری» وقتی `surplus > 0` نمایش داده می‌شود.
6. **یک باگِ واقعی و قدیمی که در همین مسیر کشف شد:** طبق کشفِ جلسه‌ی ۲۲ (لاگِ قدیمی)، `styles.css` یک فایلِ Tailwind از‌پیش‌کامپایل‌شده‌ی استاتیک است. یک اسکریپتِ Python نوشتم که هر رشته‌ی `className` در کلِ `app.jsx` را استخراج و با `styles.css` مقایسه می‌کند (نه فقط حدسِ دستی). نتیجه: چند کلاس در کدی که داشتم می‌نوشتم/کپی می‌کردم اصلاً کامپایل نشده بودند و بی‌صدا هیچ اثری نداشتند — `bg-white/5`، `max-h-24`، `text-cyan-400`، `pt-1`، `bg-cyan-500/20`، `text-[12px]`، و مهم‌تر از همه سه‌تایی‌ِ گرادیانِ `bg-gradient-to-r`/`from-cyan-500`/`to-fuchsia-500` (یعنی نوارِ پیشرفتِ رنگی در `ProgressiveTaskBar`ی *مشترک* هم همیشه بی‌رنگ بوده، احتمالاً از همان اول). همه با معادلِ کامپایل‌شده یا `style` inline جایگزین شدند؛ فیکسِ `bg-cyan-500/20` روی دکمه‌ی ثبتِ `ProgressiveTaskBar` مشترک هم اعمال شد (یک خط، بدون تغییرِ رفتار — فقط رنگِ پس‌زمینه‌ای که تا الان اصلاً رندر نمی‌شد).

**فایل‌های لمس‌شده:** فقط `src/app.jsx` (توابعِ تازه + `SubsectionCard`/`LearningHub`/`DailyReportTrackRow`/یک خط در `ProgressiveTaskBar` مشترک) + `main.js` تولیدشده.

`npm run build`، `node --check main.js`، `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، بررسی نبود کاراکترِ خرابِ انکودینگ، و اسکریپتِ سیستماتیکِ کلاس‌های Tailwind — همه تأیید شدند.

⚠️ **برخورد با نشست‌های موازی:** در طولِ این جلسه، ریموت **دو بار** جلوتر رفت (یک‌بار سیستمِ لِین‌بندی معرفی شد، یک‌بار ۷ کامیتِ دیگر از لِین‌های ۱/۴ رسید). هر دو بار با `git fetch` + بررسیِ `--stat` تشخیص داده شد که تغییرات کاملاً در فایل‌ها/توابعِ دیگری بودند (نه `LearningHub`/`SubsectionCard`)، پس هر دو بار `git rebase origin/main` بدون تعارض در `src/app.jsx` انجام شد — فقط `main.js` (تولیدی) تعارض داشت که هر بار با `npm run build` دوباره از سورسِ merge‌شده ساخته شد.

**❓ سؤال‌های بازِ من از کاربر (هنوز پاسخ نگرفته — قبل از ادامه‌ی بقیه‌ی خواسته‌ی ۶ لازم است):**
1. **تخصیصِ دستیِ ذخیره:** آیا مدلِ خودکار/زنجیره‌ای (اولین روزهای آینده به‌ترتیب پوشش داده می‌شوند، همینی که الان پیاده شد) کافی است، یا واقعاً باید بتوان یک مقدارِ مشخص از ذخیره را برای یک تاریخِ دلخواه (نه لزوماً روزِ بعد) کنار گذاشت؟ دومی نیازمندِ یک ساختارِ دادهٔ تازه (دفترِ تخصیص) است که هنوز ساخته نشده.
2. **«خرج‌کردن در برنامه‌ریزی»:** آیا کاهشِ خودکارِ عددِ نمایش‌داده‌شده (همینی که در `DailyReportTrackRow` پیاده شد) منظور بود، یا یک عملِ تعاملیِ جداگانه (مثلاً دکمه‌ی صریحِ «خرج کن»)؟
3. **دامنه‌ی «شخصی‌سازیِ کلیِ UI»:** رنگِ هر موضوع؟ تراکم/چیدمان (که یک نسخه‌ی سراسری از قبل در تنظیمات هست)؟ چیزِ دیگر؟

**باقی‌مانده‌ی صفِ همین درخواست (هنوز شروع نشده):** سفارشی‌سازیِ نمای فعالیتِ ۱۲هفته‌ای (بازه‌ی قابل‌تغییر + نمای جایگزین مثل نمودارِ میله‌ای)، یک نمودار/تایم‌لاینِ اختصاصی برای «بررسیِ زمان و کار کردن»، و شخصی‌سازیِ UI (منتظرِ پاسخِ سؤالِ ۳).

⚠️ **محدودیتِ شناخته‌شده:** تستِ بصریِ واقعی هنوز در یک Obsidian واقعی انجام نشده — تمامِ تأییدها از طریقِ build/tsc/syntax و اسکریپت‌های تستِ مستقلِ Node (نه رندرِ زنده) بوده‌اند.

### ۴.۳ لِین برنامه‌ریزی و تقویم (`planning` + `calendar`)

⚠️ **هماهنگی:** این نشست از قبل (جلسات ۲۰/۲۱/۲۷ در لاگ قدیمیِ سراسری) روی همین قلمرو کار کرده؛ از این جلسه به بعد لاگش را اینجا (طبق سیستم تازه‌ی لِین‌بندی) می‌نویسد. هنگام sync با remote، کامیت «کار ۷ (اندروید)» (بخش ۴.۶ پایین) پیدا شد که همان تابع `DayPlannerView` را برای سازگاری لمسی بازنویسی کرده بود — طبق پروتکل ۰.۴، به‌جای force/overwrite با `git rebase origin/main` هماهنگ شد؛ `src/app.jsx` بدون تعارض auto-merge شد (نشانه‌ی خوب: تغییرات واقعاً در نقاط متفاوتِ همان تابع بودند)، فقط `PROGRESS.md`/`main.js` تعارض داشتند که دستی حل و از نو build شدند. جزئیات این هماهنگی در پایین «مرحله‌ی ۱» آمده.

طبق دستور صریح کاربر: «بخش برنامه‌ریزی را دقیقاً هرکاری که Google Calendar و TaskNotes و ... می‌کنند را به آن اضافه کن. این کار را در چند مرحله انجام بده و هر مرحله پوش کن. با دقت کار کن و دیباگ کن. به‌هم‌ریختگی‌های ظاهری را هم بررسی و رفع کن.»

⚠️ **صداقت درباره‌ی دامنه:** «دقیقاً هرکاری» این دو ابزار می‌کنند عملاً شامل ده‌ها قابلیت است (دعوت/همکاری چندنفره، همگام‌سازی زمانی جهانی، پارس زبان طبیعی برای تاریخ، اتصال به تقویم‌های خارجی و ...) که یا برای یک پلاگین تک‌کاربره‌ی Obsidian بی‌معنی‌اند یا خارج از دامنه‌ی این پروژه‌اند. این برنامه روی **قابلیت‌های بامعنا و پیاده‌سازی‌پذیر** این دو ابزار در بافت یک پلاگین Obsidian تمرکز می‌کند، در چند مرحله‌ی مستقل که هرکدام جدا build/test/push می‌شوند:

1. **خط «اکنون» (now-line)** در نمای روز و نمای هفتگیِ ساعتی — قابلیت شاخصِ Google Calendar.
2. **درگ‌ودراپ و تغییرِاندازه‌ی بین‌روزی** در نمای هفتگیِ ساعتی (تا الان فقط نمای روز این را داشت — محدودیت مستندشده‌ی جلسه‌ی ۲۷).
3. **نمای ۳روزه** (آیتم باز چک‌لیست کار ۳).
4. **نوار تسک‌های بدون‌زمان** در نمای هفتگی/۳روزه (الان فقط نمای روز سبد تسک‌های زمان‌بندی‌نشده دارد).
5. **نوار فیلتر/جست‌وجوی تسک‌ها** (سبک TaskNotes: فیلتر بر اساس ربع/برچسب/وضعیت + مرتب‌سازی) برای تب تسک‌ها.
6. **بازبینی ظاهری** کل بخش‌های تقویم/برنامه‌ریزی برای به‌هم‌ریختگی (چیدمان، فاصله‌گذاری، کلاس‌های Tailwind واقعاً کامپایل‌نشده — طبق درسِ جلسه‌ی ۲۲).

هر مرحله زیر مستقل commit/push می‌شود؛ اگر مرحله‌ای در وسط کار متوقف بماند، مراحل بعدی در نشست‌های آینده از همین‌جا قابل‌ادامه‌اند.

---

### مرحله‌ی ۱ — خط «اکنون» (now-line) در نمای روز و نمای هفتگیِ ساعتی

در هر دو `DayPlannerView` و `WeekHourlyView` یک state محلی `now` (به‌روزشونده هر ۶۰ ثانیه با `setInterval`) اضافه شد؛ یک خط باریک قرمز (`var(--color-red)`، یکی از متغیرهای پایه‌ی رنگ خودِ Obsidian، نه رنگ ثابت هاردکد) با یک نقطه‌ی کوچک در لبه‌ی راست، دقیقاً در ارتفاع زمان فعلی رسم می‌شود — فقط وقتی روز نمایش‌داده‌شده «امروز» باشد (`Jalali.isSameJalaliDay`) و زمان فعلی داخل بازه‌ی نمایش‌داده‌شده (۰۶:۰۰–۲۳:۳۰) باشد. در `WeekHourlyView`، این خط فقط داخل ستونِ روزِ «امروز» رسم می‌شود، نه در همه‌ی ۷ ستون.

**نکته‌ی فنی:** اولین تلاش برای افزودن این عنصر به انتهای عبارتِ تودرتوی `DayPlannerView` یک پرانتز اضافه داشت (`esbuild` بلافاصله خطای «Expected ";" but found ")"» داد) — دقیقاً همان نوع اشتباهی که در جلسه‌ی ۶ هم افتاده بود. این بار به‌جای حدس دوباره، با یک اسکریپت پایتون کوچک عمق‌شمارِ پرانتز کل تابع محاسبه شد (`-۱` یعنی یک پرانتزِ اضافه)، خطا دقیقاً پیدا و رفع شد، و بعد از رفع، همان اسکریپت دوباره اجرا شد تا عمق صفر (متوازن) را قبل از build مجدد تأیید کند. **درسِ تکمیلی برای جلسات بعد: برای هر ویرایشِ پرانتزی روی عبارت‌های تودرتوی موجود (نه نوشتن از صفر با متغیر میانی)، اول با اسکریپت عمق‌شمار پرانتز تابع را قبل و بعد از ویرایش مقایسه کن، به‌جای اینکه فقط چشمی بشماری.**

`npm run build`، `node --check main.js`، `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، بررسی نبود کاراکتر خراب انکودینگ — تأیید شدند.

---

### مرحله‌ی ۲ — جابه‌جایی/تغییرِاندازه با کشیدن در نمای هفتگیِ ساعتی (فقط داخل همان روز)

**تصمیم مهم قبل از کدنویسی:** طبق کشفِ جلسه‌ی ۲۱ (تسک‌ها هیچ فیلد «تاریخ» ندارند، فقط `time` + الگوی `recurrence`)، «کشیدن یک تسک از روز سه‌شنبه به پنج‌شنبه» معنای واحد و بی‌ابهامی ندارد: برای تسکِ `recurrence:"none"`/`"daily"` (که همین حالا هر روز نمایش داده می‌شود) جابه‌جاییِ ستون هیچ اثر داده‌ای معناداری ندارد، و برای تسکِ `recurrence:"weekly"` نیازمند یک قابلیتِ جداگانه‌ی «جابه‌جاییِ روزهای هفته» است. به‌جای حدس‌زدن یک رفتار برای این ابهام، دامنه‌ی این مرحله عمداً محدود شد به: **جابه‌جایی/تغییرِاندازه‌ی عمودی (فقط تغییر زمان/مدت)، محدود به همان ستونِ روز** — دقیقاً هم‌ارزِ قابلیت‌های موجودِ `DayPlannerView`، نه چیزی بیشتر. جابه‌جاییِ واقعی بین‌روزی برای یک نشست جداگانه (با تصمیم روشن درباره‌ی رفتار `weekly`) باقی ماند.

**پیاده‌سازی:** چون این جابه‌جایی فقط بر اساس **دلتای عمودیِ اشاره‌گر** (`ev.clientY - startY`) کار می‌کند، نه موقعیت مطلق، نیازی به هیچ `ref`/`getBoundingClientRect`ای نبود — همین به‌خودی‌خود تضمین می‌کند اثر کاملاً به همان ستون محدود بماند (چون محور افقی اصلاً خوانده نمی‌شود). `startWeekMove`/`startWeekResize` دقیقاً همان الگوی `startMove`/`startResize` جلسه‌ی ۲۸ (Task 7، سازگاری اندروید) را با Pointer Events تکرار می‌کنند — یعنی این قابلیتِ تازه از همان اول با لمس هم کار می‌کند، نه اینکه بعداً نیاز به یک پاس رفع‌باگِ لمسیِ دیگر داشته باشد. یک state تازه‌ی `livePreview` (هم‌نامِ همان الگو در `DayPlannerView`) در سطح `WeekHourlyView` اضافه شد. `onSchedule` یک prop تازه برای این کامپوننت شد (قبلاً نداشت)؛ چون `CalendarViews` از قبل `onSchedule` را از `LifeFlowApp` می‌گرفت (برای `DayPlannerView`)، فقط لازم بود همان مقدار به فراخوانیِ `WeekHourlyView` هم پاس داده شود — هیچ prop-threading تازه‌ای در سطوح بالاتر لازم نبود.

**⚠️ یافته‌ی جانبی (برای مرحله‌ی ۶ ثبت شد، نه الان رفع شد):** هنگام بررسیِ کلاس‌به‌کلاس (طبق درسِ جلسه‌ی ۲۲)، مشخص شد کلاس‌های `cursor-grab`, `active:cursor-grabbing`, `cursor-ns-resize`, `select-none` که هم در `DayPlannerView` (از قبل، جلسات پیشین) و هم در این کدِ تازه استفاده می‌شوند، **اصلاً در `styles.css` کامپایل نشده‌اند** — دقیقاً همان نوع نقصِ کوچکِ ازقبل‌موجود که کلاس `p-0.5` در جلسه‌ی ۲۷ نشان داد. چون این کلاس‌ها فقط شکل نشانگر ماوس روی هاور را کنترل می‌کنند (نه منطق واقعیِ درگ که کاملاً از طریق Pointer Events جاوااسکریپتی کار می‌کند، مستقل از CSS)، **هیچ اثر عملکردی ندارند** — فقط یک نقص بصریِ کوچک (نشانگر ماوس به‌جای «grab»/«resize» همان shape پیش‌فرض را نشان می‌دهد). چون جایگزینِ کامپایل‌شده‌ی آماده‌ای برای این کلاس‌های خاص وجود ندارد (Tailwind اسم دیگری برای «grab cursor» ندارد)، این مورد به‌جای رفعِ نصفه‌ونیمه، برای «مرحله‌ی ۶: بازبینی ظاهری» (که دقیقاً برای همین نوع یافته طراحی شده) ثبت شد.

`npm run build`، `node --check main.js`، `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، بررسی نبود کاراکتر خراب انکودینگ، و بررسیِ کلاس‌به‌کلاس در برابر `styles.css` — همگی تأیید شدند.

---

### مرحله‌ی ۳ — نمای «۳روزه»

`WeekHourlyView` (که در مرحله‌ی ۲ برای هر تعداد روز به‌طور کلی نوشته شده بود، هرچند فقط با ۷ روز صدا زده می‌شد) با یک prop تازه‌ی `dayCount` (پیش‌فرض ۷، سازگار با قبل) عمومی شد:
- وقتی `dayCount === 7` رفتار قبلی حفظ شد (شروع از اول هفته‌ی جلالی، شنبه).
- برای هر `dayCount` دیگر (اینجا ۳)، بازه از خودِ `cursor` شروع می‌شود، نه از اول هفته — یعنی «۳ روز آینده از روزِ انتخاب‌شده»، نه لزوماً هم‌راستا با مرزهای هفته (رفتار استانداردِ نمای چندروزه در تقویم‌های معروف).
- **یک باگِ نهفته هنگام تعمیم پیدا و رفع شد:** برچسبِ روزِ هفته قبلاً با `WEEKDAY_SHORT_ORDER[i]` محاسبه می‌شد — یعنی فرض می‌کرد `i`اُمین روزِ آرایه همیشه `i`اُمین روزِ هفته از شنبه است، که فقط وقتی بازه از شنبه شروع شود درست است. برای نمای ۳روزه (که از یک روزِ دلخواه شروع می‌شود) این فرض غلط از آب درمی‌آمد و برچسب‌های روزِ هفته اشتباه نمایش داده می‌شدند. با فرمول عمومیِ `WEEKDAY_SHORT_ORDER[(d.getDay() + 1) % 7]` (که روزِ واقعیِ هر تاریخ را مستقل از نقطه‌ی شروعِ بازه محاسبه می‌کند) جایگزین شد — درست برای هر دو حالت (۳روزه و هفتگی).
- عرضِ حداقلِ ظرفِ افقی‌اسکرول‌شونده از یک عدد ثابت (۶۲۰px، فقط برای ۷ ستون معنادار) به فرمول `40 + dayCount * 85` تغییر کرد تا با هر تعداد روز متناسب بماند.
- در `CalendarHeader`: گزینه‌ی «۳روزه» به سوییچرِ ویو اضافه شد (حالا ۶ دکمه)؛ یک شاخه‌ی عنوانِ تازه («از تاریخ X تا Y») اضافه شد؛ در `CalendarViews`، تابعِ `step` یک شاخه‌ی تازه گرفت (دکمه‌ی بعدی/قبلی در این ویو ۳ روز جلو/عقب می‌رود، نه ۱ یا ۷).
- درگ‌ودراپ/تغییرِاندازه‌ی مرحله‌ی ۲ و خط «اکنون»ِ مرحله‌ی ۱، چون هر دو از قبل عمومی/مستقل از تعداد روز نوشته شده بودند، بدون هیچ تغییری در نمای ۳روزه هم کار می‌کنند.

⚠️ **یافته‌ی جانبیِ ظاهری (برای مرحله‌ی ۶ ثبت شد):** سوییچرِ بالای تقویم حالا ۶ دکمه دارد (روز/۳روزه/هفته/ماه/سال/فهرست)؛ روی صفحه‌های خیلی باریک ممکن است فشرده/تنگ به‌نظر برسد (هرچند خودِ کارت والد `flex-wrap` دارد، سوییچرِ داخلی خودش `flex` بدون wrap است). چیزی نشکسته، فقط یک نکته‌ی بازبینیِ ظاهریِ احتمالی برای مرحله‌ی ۶.

`npm run build`، `node --check main.js`، `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، بررسی نبود کاراکتر خراب انکودینگ — تأیید شدند.

---

### مرحله‌ی ۴ — نمایش تسک‌های بدون‌زمان در نمای هفتگی/۳روزه

**باگ واقعیِ کشف‌شده (نه فقط کمبودِ قابلیت):** در `WeekHourlyView`، `dayTasks` همیشه با شرط `isTaskDueOn(tsk, d) && tsk.time` فیلتر می‌شد — یعنی تسک‌هایی که موعدشان امروز/آن‌روز بود ولی زمان مشخصی نداشتند، **کلاً از نمای هفتگی و ۳روزه ناپدید می‌شدند** (نه اینکه جای دیگری نشان داده شوند). این با نمای «روز» (`DayPlannerView`) که از قبل یک سبدِ کاملِ تسک‌های بدون‌زمان دارد ناهم‌خوان بود.

**رفع:** برای هر ستونِ روز، یک ردیفِ کوچک (۱۶px ارتفاع) بین سرتیتر تاریخ و جدول ساعتی اضافه شد که تسک‌های بدون‌زمانِ همان روز را به‌شکل نقطه‌های کوچکِ رنگیِ کلیک‌پذیر (رنگِ ربع ماتریس، با `title` برای نمایش عنوان روی هاور) نشان می‌دهد؛ کلیک هرکدام مستقیم ویرایش (`onEdit`) را باز می‌کند. اگر بیش از ۵ تسکِ بدون‌زمان باشد، فقط ۵ نقطه‌ی اول به‌علاوه‌ی یک برچسبِ «+N» نمایش داده می‌شود (به‌جای شکستنِ چیدمانِ ستونِ باریک با تعداد نامحدود نقطه).

**تصمیم عمدیِ محدودسازی دامنه:** این نسخه فقط *نمایشی/کلیکی* است، نه قابلِ‌درگ — بر خلافِ سبدِ تسک‌های بدون‌زمانِ `DayPlannerView` که می‌شود آن‌ها را روی جدول کشید. چون هر ستون در نمای هفتگی خیلی باریک است (کمتر از ۹۰px)، جا دادنِ یک ناحیه‌ی درگ‌پذیرِ کامل با متنِ خوانا در این عرض عملاً یا خیلی شلوغ می‌شود یا نیازمندِ یک طراحیِ جداگانه (شاید یک popover) است — به‌جای یک پیاده‌سازیِ نصفه‌ونیمه، این به یک نشستِ آینده موکول شد. تغییرِ ارتفاعِ headerَ (`headerHeight + untimedHeight`) روی `marginTop` ستونِ برچسب‌های ساعت هم اعمال شد تا همه‌چیز هم‌تراز بماند.

`npm run build`، `node --check main.js`، `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، بررسی نبود کاراکتر خراب انکودینگ، و بررسیِ کلاس‌به‌کلاس در برابر `styles.css` — همگی تأیید شدند.

---

### ۴.۴ لِین پومودورو / تناسب‌اندام / مطالعه (`pomodoro` + `fitness` + `study`)

#### جلسه‌ی ۱ (۶ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶) — نوار وضعیت پومودورو در Obsidian

اولین آیتم صف لِین ۴ برداشته شد: **نوار پیشرفت پومودورو در نوار وضعیت (status bar) Obsidian**، همان محدودیت مهمی که در جلسه‌ی ۸ (شماره‌گذاری سراسری قدیم) کشف و عمداً واگذار شده بود.

**چالش اصلی:** `running`/`secondsLeft`/`mode` تایمر کاملاً local state داخل `PomodoroTimerView` بودند؛ با خروج از تب پومودورو کامپوننت unmount و تایمر واقعاً متوقف می‌شد. یک status bar که فقط وقتی تب باز است کار کند، ارزش واقعی ندارد.

**راه‌حل (بدون رونویسیِ حلقه‌ی تیک تست‌شده‌ی موجود):** به‌جای بالابردن کامل state تایمر به `LifeFlowApp` (تغییر معماری بزرگ و پرخطر)، یک لایه‌ی داده‌ی کوچک و **مشتق‌پذیر بر اساس زمان سپری‌شده** اضافه شد:
- `pomodoro.activeTimer` (بخشی از همان `pomodoro` که از قبل به `LifeFlowApp` لیفت شده و در `lifeflow_data_v1` ذخیره می‌شود) یک عکس‌لحظه‌ای نگه می‌دارد: `{ mode, running, totalSeconds, secondsLeftAtAnchor, anchorAt }`. «الان چند ثانیه مانده» با فرمول ساده مشتق می‌شود: اگر `running` باشد، `secondsLeftAtAnchor - (اکنون - anchorAt)`؛ اگر نه، همان `secondsLeftAtAnchor` ثابت می‌ماند.
- `PomodoroTimerView` این عکس‌لحظه را فقط در **نقاط گذار** (شروع، مکث، ریست، رد کردن، تغییر حالت، پایان جلسه، mount، unmount) می‌نویسد — نه هر ثانیه — پس حلقه‌ی `setInterval` تست‌شده‌ی موجود برای شمارش معکوس دست‌نخورده ماند (صفر ریسک برای رفتار امتحان‌پس‌داده‌ی موجود مثل زنجیره‌ی خودکارشروعی/شمارش دور/صدای پایان).
- در شاخه‌ی «تسک نیمه‌کاره» (`interrupted`) داخل `finishSession`، متغیر کمکی `nextMode` قبلاً برای این حالت به‌روزرسانی نمی‌شد (بی‌ضرر چون هیچ‌جای دیگر استفاده نمی‌شد)؛ برای درستیِ عکس‌لحظه یک خط `nextMode = mode;` اضافه شد — تغییری خنثی روی رفتار قبلی، فقط برای درست‌بودنِ دادهٔ تازه.
- در `main.ts`، یک `addStatusBarItem()` با `registerInterval` هر ۱ ثانیه، مستقیماً از `getDataValue("lifeflow_data_v1")` می‌خواند (همان bridge سینک همیشگی، نه از طریق کامپوننت React) و متن را به‌صورت `🍅 24:59` / `☕ 04:12 (متوقف)` نمایش می‌دهد؛ کلیک روی آن `activateView()` را صدا می‌زند. اگر چیزی برای نمایش نباشد (نه در حال اجرا و در ابتدای بازه)، متن خالی می‌ماند.

**محدودیت شناخته‌شده و صادقانه مستندشده:** اگر تایمر درست وقتی تب پومودورو بسته است به صفر برسد، status bar درست «۰۰:۰۰» نشان می‌دهد ولی خودِ رخدادهای پایانِ جلسه (ثبت session، صدای پایان، رفتن به دور بعد، خودکارشروعیِ بعدی) که همه داخل حلقه‌ی `PomodoroTimerView` هستند، تا وقتی کاربر دوباره تب را باز نکند اجرا نمی‌شوند. رفعِ کاملِ این محدودیت نیازمند اجرای واقعیِ منطقِ پایانِ جلسه در سطح پلاگین (نه فقط نمایش) است — یک قدم بزرگ‌تر که عمداً برای جلسه‌ای دیگر (در صورت درخواست) گذاشته شد.

**تأیید:** فرمول مشتق‌سازی (running/paused/overdue-clamp/idle-at-full) با یک اسکریپت Node مستقل تست شد. `npm run build` + `node --check main.js` + `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط) + بررسی نبود کاراکتر خراب انکودینگ روی هر سه فایل (`app.jsx`, `main.ts`, `main.js` باندل‌شده) انجام شد؛ همچنین با `grep` روی `main.js` باندل‌شده تأیید شد `activeTimer`/`pushActiveTimer`/`pomodoroStatusBarEl`/`updatePomodoroStatusBar` درست کامپایل و minify شده‌اند.

#### جلسه‌ی ۲ (۶ شهریور ۱۴۰۵ / ۳۰ آگوست ۲۰۲۶) — رفعِ یک باگِ خودشکن در همان قابلیت (نه رونویسی، فقط رفع)

⚠️ طبق دستور صریح کاربر («ممکن است یک کامیت ناهماهنگ شده باشد... اول کامیت درست را بزن... تغییراتش را حذف نکن، تغییرات خودت را اضافه کن»)، این جلسه commit جلسه‌ی ۱ بالا را **خط‌به‌خط بازبینی کرد**، نه فقط `git log` را نگاه کرد.

**باگ پیدا شد:** در افکتِ mount/unmountِ `pushActiveTimer` (بخش «راه‌حل» بالا، خط سوم)، تابع cleanup به‌جای استفاده از وضعیت واقعیِ `running` در لحظه‌ی unmount، مقدار `false` را **هاردکد** کرده بود:
```js
useEffect(() => {
  pushActiveTimer(modeRef.current, false, secondsLeftRef.current);
  return () => {
    pushActiveTimer(modeRef.current, false, secondsLeftRef.current); // ⚠️ همیشه false
  };
}, []);
```
یعنی دقیقاً همان لحظه‌ای که کاربر یک پومودورویِ **در حال اجرا** را رها می‌کند و به تب دیگری می‌رود (یعنی همان سناریویی که کل این قابلیت برایش ساخته شده: «status bar باید هنگام دوربودن از تب هم کار کند»)، `activeTimer.running` در `data.json` روی `false` منجمد می‌شد. نتیجه: `updatePomodoroStatusBar` در `main.ts` دیگر از فرمول «سپری‌شده = اکنون − anchorAt» استفاده نمی‌کرد و همان عدد ثابتِ لحظه‌ی خروج از تب را تا ابد نشان می‌داد — یعنی **این قابلیت دقیقاً نقطه‌مقابل هدف خودش عمل می‌کرد**: نوار وضعیت درست همان زمانی که کاربر از تب خارج می‌شود (تنها زمانی که واقعاً به آن نیاز دارد) از کار می‌افتاد.

**تأیید با تست مستقل (نه فقط خواندن کد):** یک اسکریپت Node جدا نوشته شد که سناریو را دقیقاً شبیه‌سازی کرد (شروع تایمر → ۳ دقیقه بعد خروج از تب → ۵ دقیقه‌ی دیگر می‌گذرد → status bar چه عددی نشان می‌دهد). نتیجه: با کد قدیم، عدد نمایشی بعد از ۵ دقیقه‌ی اضافه هیچ تغییری نمی‌کرد (منجمد)؛ با رفع، دقیقاً ۵ دقیقه کم می‌شد (زنده).

**رفع (جراحیِ کمینه، نه بازنویسی):**
```js
const runningRef = useRef(running); // ref تازه
useEffect(() => {
  modeRef.current = mode;
  secondsLeftRef.current = secondsLeft;
  runningRef.current = running; // اضافه شد
}, [mode, secondsLeft, running]); // running به وابستگی‌ها اضافه شد
useEffect(() => {
  pushActiveTimer(modeRef.current, false, secondsLeftRef.current); // mount: false همچنان درست است (تایمر تازه‌مانت‌شده همیشه متوقف است)
  return () => {
    pushActiveTimer(modeRef.current, runningRef.current, secondsLeftRef.current); // unmount: حالا واقعی
  };
}, []);
```
فراخوانیِ mount عمداً دست‌نخورده ماند (چون یک کامپوننتِ تازه‌مانت‌شده همیشه با `running=false` شروع می‌شود — آنجا هاردکد `false` از اول درست بود، فقط cleanup باگ داشت).

**آنچه عمداً در این جلسه رفع نشد (مستند شده برای بعد، نه فراموش‌شده):** حتی با این رفع، وقتی کاربر به تب پومودورو برمی‌گردد، خودِ UI (نه status bar) از `useState` محلی با مقدار اولیه‌ی ثابت شروع می‌شود (`mode: "work"`, `secondsLeft: settings.work*60`, `running: false`) — یعنی تایمر واقعیِ در پس‌زمینه در UI «resume» نمی‌شود، فقط status bar تا لحظه‌ی remount درست می‌شمارد و بعد خودش هم به idle برمی‌گردد (چون افکتِ mount دوباره `false` را push می‌کند). این یک عدم‌هماهنگیِ کوچک‌ترِ باقیمانده است (نه یک تناقض/باگِ خودشکن مثل قبلی) — هیدریت‌کردنِ کاملِ state اولیه از روی `pomodoro.activeTimer` یک تغییر جداگانه و کمی بزرگ‌تر است که در صف این لِین برای یک جلسه‌ی دیگر یادداشت شد.

`npm run build` + `node --check main.js` + `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط) + بررسی نبود کاراکتر خراب انکودینگ — همگی بعد از رفع دوباره تأیید شدند.



### ۴.۵ لِین داشبورد / یادداشت‌ها / زیرساخت و تنظیمات (`dashboard` + `notes` + تنظیمات)

**جلسه (کاربر مستقیماً دستور داد ادامه بدهم، روی شاخه‌ی جدا):** آخرین آیتم باز کار ۲ («رنگ‌های دسته‌بندی») روی شاخه‌ی `feature/category-colors-settings` کامل شد و با `--no-ff` merge تمیز به `main` برگشت (بدون تعارض؛ `main` بین ایجاد شاخه و merge جلو نرفته بود). چهار رنگ ربع ماتریس آیزنهاور حالا از تب تنظیمات با color-picker بومی Obsidian قابل‌تغییرند، با دکمه‌ی «بازگشت به پیش‌فرض» کنار هرکدام. پیاده‌سازی: آرایه‌ی سراسری `QUADRANTS` (که ۱۴+ جای فایل مستقیم می‌خوانندش) به‌جای بازنویسی همه‌ی نقاط خواندنش، در خودِ `loadSettings()` با مقادیر تنظیمات mutate می‌شود — همان الگوی «یک نقطه‌ی مشترک» که برای لایه‌ی storage هم جواب داده بود. جزئیات کامل فنی + تست در تاریخچه‌ی کامیت (`Task 2 complete: configurable Eisenhower matrix (quadrant) colors`). فایل‌های لمس‌شده: `src/app.jsx`, `src/settings-tab.ts`, `main.js`. **کار ۲ (گسترش تنظیمات) با این جلسه به‌طور کامل تمام شد** — هیچ آیتم بازی در چک‌لیستش نمانده.

_هنوز جلسه‌ای دیگری در این ساختار ثبت نشده.__هنوز جلسه‌ای در این ساختار ثبت نشده._

### ۴.۶ ابتکار متقاطع: سازگاری کامل با اندروید (Cross-Lane، به دستور مستقیم کاربر — نه یکی از ۵ لِین جدول بخش ۰.۲)

**جلسه‌ی ۲۸ (۵ شهریور ۱۴۰۵ / ۲۹ آگوست ۲۰۲۶):**

کاربر مستقیماً دستور داد: «اولین کار تو این است که پلاگین را کاملاً با اندروید سازگار کنی». چون این یک نگرانی متقاطع است (تعامل‌های لمسی در همه‌ی تب‌ها، نه فقط یکی)، به‌جای این‌که در قلمروی یک لِین خاص جا بگیرد، به‌عنوان یک ابتکار جداگانه لاگ می‌شود. جزئیات کامل یافته‌ها و رفع‌ها در «کار ۷» (بخش ۲) نوشته شده تا همه‌ی لِین‌ها آن را ببینند؛ این‌جا فقط خلاصه‌ی روند کار و نکات هماهنگی می‌آید.

**روند کار:** `git fetch` (۱۵ کامیت جدید از لِین‌های دیگر پیدا شد، `git pull` شد و کل `PROGRESS.md` تازه از اول خوانده شد تا سیستم لِین‌بندی تازه فهمیده شود) → بررسی `manifest.json`/`package.json`/`src/main.ts` (زیرساخت از قبل موبایل-سازگار بود، بدون نیاز به تغییر) → `grep` سراسری روی کل `app.jsx` برای الگوهای شناخته‌شده‌ی ناسازگار با لمس (`onMouseDown`, `draggable`, `onDragStart/Over/Drop`, `onContextMenu`, `group-hover:opacity-100`, `hover:opacity-100`) → دو مورد واقعی پیدا شد (هردو در بخش «کار ۷» بالا با جزئیات کامل توضیح داده شدند) → رفع هر دو با Pointer Events / تغییر opacity پیش‌فرض → تست ریاضیات با اسکریپت Node مستقل (`minutesFromClientY` round-trip روی همه‌ی بازه‌های ۵دقیقه‌ای ۰۶:۰۰ تا ۲۳:۳۰، کلمپ‌شدن مرزها، آستانه‌ی تشخیص درگ-در-برابر-تپ) → `npx tsc --noEmit` / `npm run build` / `node --check main.js` / بررسی نبود کاراکتر خراب انکودینگ.

**⚠️ هماهنگی مهم با لِین ۳:** تابعِ اصلاح‌شده (`DayPlannerView`) طبق جدول بخش ۰.۲ قلمروی لِین ۳ (برنامه‌ریزی و تقویم) است. این جلسه چون به دستور مستقیم کاربر و با اولویت اول شروع شد، وارد این قلمرو شد. اگر لِین ۳ کامیتی هم‌زمان روی همین تابع داشته باشد، طبق پروتکل ۰.۴ باید با دقت merge/rebase شود (نه blind overwrite) — این نکته صراحتاً این‌جا و در بخش «کار ۷» ثبت شد تا لِین ۳ غافلگیر نشود.

**فایل‌های لمس‌شده:** فقط `src/app.jsx` (دو تابع/کامپوننت: `DayPlannerView` و لیست چک‌آیتم‌های «یادداشت‌ها») + `main.js` تولیدشده. هیچ فایل دیگری (`main.ts`, `settings-tab.ts`, `styles.css`) در این جلسه لمس نشد.

**ادامه‌ی این ابتکار:** چک‌لیست کامل باز/بسته در «کار ۷» (بخش ۲) نگه‌داری می‌شود، نه این‌جا — چون آن بخش برای همه‌ی لِین‌ها قابل‌مشاهده‌تر است و به‌روزرسانی می‌شود.

**جلسه‌ی بعدی (همان روز):** کاربر گفت «ادامه بده، کارت تداخل با دیگران نداشته باشد». `git fetch` نشان داد ۱۲ کامیت جدید از لِین‌های دیگر آمده (از‌جمله کار لِین ۳ روی `WeekHourlyView` و بازسازی معماری مودال توسط لِین ۱)؛ `git pull` شد و کل فایل به‌روز خوانده شد. برای پرهیز از تداخل، به‌جای دست‌زدن دوباره به کدِ یک لِین دیگر، این جلسه **فقط بازبینی/تأیید + مستندسازی** بود (بدون تغییر کد): تأیید شد کار لِین ۳ خودش درست و ایمن (Pointer Events کامل) پیاده شده، تأیید شد بازسازی مودال لِین ۱ ریگرسیونی برای موبایل ندارد، `grep` سراسری تکرار شد و چیز تازه‌ای پیدا نشد، و یک ممیزی فقط-خواندنیِ اندازه‌ی نقاط لمسی انجام و در «کار ۷» به‌صورت یک چک‌لیست اولویت‌بندی‌شده ثبت شد (بدون تغییر کد، چون آن المان‌ها در قلمروی چند لِین مختلف پخش‌اند). فقط `PROGRESS.md` در این جلسه تغییر کرد.

**جلسه‌ی سوم (همان روز):** کاربر دوباره «ادامه بده» گفت؛ `git fetch` چیز تازه‌ای نشان نداد. این‌بار، به‌جای فقط مستندسازی، بالاترین‌اولویتِ چک‌لیست «اندازه‌ی نقاط لمسی» (از جلسه‌ی قبل) واقعاً رفع شد: دکمه‌ی تیک‌زدن چک‌لیست «یادداشت‌ها» و دکمه‌ی مشابه در `GroupCard` (**قلمروی لِین ۳**، همان تابع‌خانواده‌ی `DayPlannerView`/`WeekHourlyView`، پس دوباره صریحاً این‌جا اعلام می‌شود). حین کار یک یافته‌ی فنی مهم کشف شد: `styles.css` هیچ مرحله‌ی build ندارد (باندل ثابت Tailwind است)، پس کلاس Tailwind تازه‌ای که قبلاً استفاده نشده (مثل `-m-1.5` که برای تکنیک اول در نظر گرفته شده بود) هیچ اثر واقعی نمی‌داد — به‌جایش یک کلاس CSS دستیِ کوچک (`.lifeflow-tap-pad`) مستقیم به `styles.css` اضافه شد. جزئیات کامل در چک‌لیست «کار ۷» بالا. فایل‌های لمس‌شده: `src/app.jsx` (دو نقطه)، `styles.css` (یک کلاس تازه)، `main.js`.

---

## ۵. سوالات باز برای کاربر (بدون پاسخ مانده از جلسات قبلی)

1. آیا نسخه‌ی سورس اصلی (قبل از باندل، با ساختار پوشه‌ای دلخواه) جایی موجود است؟ اگر بله بهتر است همان جایگزین بازسازی فعلی شود تا چیزی گم نشود.
2. برای «گزارش روزانه» در کار ۵: وقتی کاربر می‌گوید «معادل یک روز و نیم قرآن حفظ کردم»، آیا این مقدار باید به‌صورت خودکار بین مسیرهای مختلف (حفظ جدید/مرور دور/مرور نزدیک) تقسیم شود یا کاربر خودش برای هر مسیر جدا وارد می‌کند؟
3. آیا تقویم جلالی باید در همه‌جای برنامه‌ریزی (نمای ماهانه و غیره) پیش‌فرض باشد؟ (در `jalali.js` موجود است، پس زیرساختش هست)
4. برای درگ‌ودراپ در تایم‌لاین، آیا کتابخانه‌ی خاصی مدنظر است یا پیاده‌سازی دستی با React کافی است؟ (پیش‌فرض: پیاده‌سازی دستی، چون افزودن کتابخانه‌ی سنگین در پلاگین Obsidian معمول نیست)
5. درباره‌ی کار ۱: الان رنگ‌های دسته‌بندی/دادهٔ پلاگین (بنفش/صورتی/فیروزه‌ای در ماتریس آیزنهاور، نمودار روز، نشان‌ها) دست‌نخورده ماندند و فقط رنگ‌های «chrome» (نوار کناری، دکمه‌ها، حالت انتخاب‌شده) به رنگ accent خود Obsidian وصل شدند. آیا این ترکیب مطلوب است، یا ترجیح می‌دهید کل پلاگین کاملاً تک‌رنگ و منطبق بر تم Obsidian باشد (حتی رنگ‌های دسته‌بندی)؟
6. تست بصری واقعی این تغییرات هنوز در یک ولت Obsidian واقعی انجام نشده (این محیط Obsidian ندارد). لطفاً پلاگین را در Obsidian باز کنید و اگر جایی هنوز شبیه اپ جداگانه به نظر می‌رسد (رنگ/فونت/دکمه‌ای که جا مانده)، بگویید تا در جلسه‌ی بعد دقیق اصلاح شود.

### ۴.۷ ابتکار متقاطع: تقویم جلالی + ارقام فارسی در سراسر اپ (Cross-Lane، به دستور مستقیم کاربر — پاسخ به سؤال باز #۳ بالا)

⚠️ **هشدار هماهنگی (طبق پروتکل بخش ۰.۳):** این یک تغییرِ گسترده و بین‌لِینی است — احتمالاً `CalendarHeader`/`WeekView`/`WeekHourlyView`/`DayPlannerView` (قلمرو لِین ۳)، `AddVideoModal`/`AddPodcastModal`/`BackupModal` (قلمرو لِین ۵)، و `ProgressLogList` مشترک را لمس می‌کند. قبل از شروع، `git fetch` شد و هیچ لِینِ دیگری هم‌زمان دقیقاً همین فایل‌ها را دست نزده بود (بررسیِ `--stat` روی آخرین کامیت‌ها). اگر لِین دیگری هم‌زمان روی یکی از این کامپوننت‌ها کار می‌کند، لطفاً در ادامه‌ی همین بخش پیام بگذارید تا هماهنگ شویم؛ کار به‌صورت گام‌به‌گام (چند کامیتِ کوچک، نه یک رفکتورِ غول‌پیکر) و با `git fetch` مکرر بین هر گام پیش می‌رود، دقیقاً طبق پروتکلِ بخش ۰.۴.

**نتیجه (تکمیل‌شده، merge شد):** کار روی شاخه‌ی `refactor/jalali-persian-digits-sweep` انجام و در ۳ کامیت به‌ترتیب تکمیل شد، سپس طبق پروتکل — قبل از merge دوباره `git fetch` زده شد؛ `main` از زمان انشعاب شاخه اصلاً جلو نرفته بود (۰ کامیت جدید)، پس با `git merge --ff-only` بدون نیاز به rebase merge شد و push گردید. شاخه بعد از merge حذف شد (هم local هم remote).

**خلاصه‌ی فنی:**
1. **هسته‌ی `jalali.js`:** `formatJalali`/`formatJalaliNumericFa` حالا ارقام فارسی می‌دهند، با یک `toFaDigits` تازه (نگاشت مستقیم رقم‌به‌رقم، **نه** `Number(...).toLocaleString("fa-IR")`) — چون آن روش یک باگ واقعی داشت: برای سال (مثلاً ۱۴۰۵) جداکننده‌ی هزارگان غلط اضافه می‌کرد (`۱٬۴۰۵`) و صفر ابتدایی ماه/روزِ پد‌شده (`"06"`) را حذف می‌کرد. این باگ حین نوشتن تست کشف و همان‌جا رفع شد.
2. تمام نمایش‌های مستقیم عدد جلالی در تقویم اصلی (سربرگ ماه/سال `CalendarHeader`، شبکه‌ی `MonthView`، کارت‌های هفته/آجندا، برچسب «هر ماه، روز N» در `AddTaskModal`) با `Jalali.toFaDigits` پوشانده شدند.
3. **کامپوننت تازه‌ی قابل‌استفاده‌ی مجدد `JalaliDateTimePicker`:** یک تقویم پاپ‌آپ کامل (شبکه‌ی روزهای ماه با `Jalali.jalaliMonthGrid`، ناوبری ماه، ارقام فارسی، انتخاب ساعت/دقیقه اختیاری) — جایگزین هر دو ورودی بومی `<input type="datetime-local">` در `AddVideoModal`/`AddPodcastModal` که قبلاً همیشه گرگوری بودند (هیچ راهی برای فارسی‌کردن یک ورودی بومیِ HTML وجود ندارد). **فرمت ذخیره‌سازی کاملاً با ورودی بومیِ قبلی یکسان نگه داشته شد** (`"YYYY-MM-DDTHH:mm"`, ISO گرگوری) — فقط UI انتخاب عوض شد، نه چیزی که ذخیره می‌شود، پس نیازی به مهاجرت داده نبود.
4. فیلد «تاریخ هدف» در `LearningGoalEditor` (که قبلاً ۳ فیلد جدا برای سال/ماه/روز بود) هم به همین پیکر ارتقا یافت، با یک تبدیل‌گر کوچک بین `{targetJy,targetJm,targetJd}` (اسکیمای ذخیره‌شده‌ی قبلی، دست‌نخورده ماند) و رشته‌ی ISO که پیکر می‌خواهد.
5. یک `formatDateKeyFa(key)` تازه اضافه شد که یک کلید داخلیِ `"YYYY-MM-DD"` (که همه‌جا به‌عنوان کلید داده استفاده می‌شود — ورودی‌های `progressLog`، `journal`، `todayKey()` — و عمداً دست‌نخورده ماند، چون تغییرش فرمت ذخیره‌سازی را می‌شکست) را فقط برای *نمایش* به جلالی+فارسی تبدیل می‌کند. این روی `ProgressLogList` مشترک، `LearningProgressLogList`، `JournalCard`، و هر ۴ سازنده‌ی گزارش Markdown (روزانه/هفتگی/ماهانه/موضوع یادگیری — هم سربرگ تاریخ، هم ردیف‌های تاریخ‌دار) اعمال شد. **نام فایل‌های خروجی عمداً به‌همان فرمت ISO ماندند** (قابل‌مرتب‌سازی)، فقط محتوایی که واقعاً خوانده می‌شود عوض شد.
6. بررسی شد که `getPersianDateLabel`/`formatBackupDate` (که از قبل بودند) نیازی به تغییر نداشتند — با یک تست در Node تأیید شد که لوکیل خامِ `"fa-IR"` (بدون پسوند `-u-ca-persian`) در موتور جاوااسکریپت به‌طور پیش‌فرض هم تقویم جلالی و هم ارقام فارسی می‌دهد.
7. یک بررسیِ کاملِ فایل (grep برای هر جایگذاریِ خامِ کلید-تاریخ و هر `toLocaleDateString`/`toLocaleString` بدون `fa-IR`) چیز دیگری در محدوده پیدا نکرد.

**تست‌ها:** ۵ اسکریپت مستقلِ Node (هسته‌ی jalali.js، رفت‌وبرگشتِ ذخیره‌سازیِ پیکر، تبدیل‌گرِ ادیتور هدف، `formatDateKeyFa`، رفت‌وبرگشتِ ارقامِ فارسی) — مجموعاً ۳۴/۳۴ پاس. اسکریپتِ پایتونیِ ممیزیِ کلاس‌های Tailwind (از نشست‌های قبلی) روی کلِ فایل دوباره اجرا شد — هیچ کلاسِ شکسته‌ی تازه‌ای از این تغییرات نیامد؛ ۲۶ مورد باقی‌مانده همگی قبل از این نشست موجود بودند و در قلمروِ لِین‌های دیگرند (کِرسِرهای درگ‌ودراپ، گرید تقویم، گرادیان‌های داشبورد) — عمداً دست‌نخورده ماندند. `npm run build`، `node --check main.js`، `npx tsc --noEmit` (همان ۳ خطای قدیمی/بی‌ربط)، بررسیِ نبودِ کاراکترِ خرابِ انکودینگ — همه بعد از merge دوباره تأیید شدند.

**⚠️ محدودیت‌های شناخته‌شده:**
- تست بصریِ واقعی در یک Obsidian واقعی هنوز انجام نشده (این محیط Obsidian ندارد) — به‌خصوص چیدمانِ پاپ‌آپِ `JalaliDateTimePicker` (موقعیت‌دهیِ `position: absolute` نسبت به دکمه‌ی محرک) در فضاهای باریکِ نوار کناری باید با چشم دیده شود.
- ورودی‌های عددیِ ساعت/دقیقه در `JalaliDateTimePicker` و ورودیِ «روز از ماه» در تکرارِ ماهانه‌ی `AddTaskModal` عمداً همچنان با ارقامِ لاتین تایپ می‌شوند (فقط *نمایش* فارسی شد، نه تایپ‌کردن) — تصمیمِ آگاهانه برای کاهشِ ریسک در کدِ لِین‌های دیگر؛ اگر تایپِ ارقامِ فارسی هم در همه‌ی این ورودی‌ها لازم است، یک درخواستِ جداگانه بدهید.
- سازگاریِ رنگ‌های دسته‌بندی/شخصی‌سازیِ UI که در پیامِ قبلیِ کاربر هم خواسته شده بود، در این جلسه دنبال نشد (خارج از محدوده‌ی این ابتکارِ مشخص).

---

## ۶. مشخصات جامع کاربر — نقشه‌ی راه بزرگ (دریافت‌شده ۷ شهریور ۱۴۰۵)

> **این بخش عیناً همان چیزی است که کاربر فرستاده — چیزی حذف یا خلاصه نشده.** فقط بعد از هر عنوان یک برچسب `[لِین X]` (طبق تخصیص بخش ۰.۶) و یک چک‌باکس اضافه شده تا پیگیری وضعیت ممکن باشد. برای نقشه‌ی مدیریتی/وابستگی‌ها/شاخه‌ها به بخش ۰.۶ برگرد؛ این‌جا فقط خودِ محتوای هر بند است.

### ۶.۱ اولویت ۱ — موارد اصلی و قطعی موردنیاز

این بخش شامل قابلیت‌ها و تغییراتی است که مستقیماً از نیازهای تعریف‌شده برای پروژه استخراج شده‌اند. توضیحات باید حفظ شوند و فقط از نظر ساختار، ترتیب و وضوح بهتر شوند.

#### ۱. فارسی‌سازی کامل رابط کاربری `[لِین ۵]` ☐

وقتی زبان پلاگین روی فارسی قرار دارد، تمام رابط کاربری باید به‌صورت کامل فارسی شود.

هیچ واژه انگلیسی غیرضروری نباید در حالت فارسی باقی بماند، مگر اینکه معادل فارسی واقعاً نامناسب یا غیرکاربردی باشد. این موضوع شامل نام قابلیت‌ها، دکمه‌ها، تنظیمات، پیام‌ها، عنوان پنجره‌ها، وضعیت‌ها، اصطلاحات پومودورو، تسک، پشتیبان‌گیری و سایر بخش‌ها می‌شود.

همچنین برای اصطلاحات مهم باید معادل فارسی مناسب انتخاب شود و این معادل‌ها در صورت نیاز از بخش تنظیمات نیز قابل تغییر باشند.

**فارسی بودن تمام اعداد** — وقتی زبان فارسی فعال است، تمام اعداد در تمام قسمت‌های پلاگین باید فارسی باشند. این موضوع شامل تقویم، برنامه‌ریزی، ساعت‌ها، تاریخ‌ها، پومودورو، شمارنده‌ها، گزارش‌ها، درصدها، فیلدهای عددی، ورودی‌های کاربر، نوار وضعیت، داشبورد، پنجره‌ها و تمام بخش‌های دیگر است. این موضوع باید به‌صورت سراسری اجرا شود و نباید جایی در حالت فارسی عدد انگلیسی باقی بماند.

> **یادداشت هماهنگی (پیش از شروع لِین ۵):** بخش اعداد فارسی از ابتکار ۴.۷ (`refactor/jalali-persian-digits-sweep`، merge‌شده) عمدتاً پوشش داده شده — jalali.js، سربرگ/شبکه‌ی تقویم، برچسب‌های تکرار، پیکر تاریخ/زمان، لاگ‌های پیشرفت، و همه‌ی گزارش‌های Markdown. آن‌جلسه صراحتاً یادداشت کرد که ورودی‌های عددیِ خودِ ساعت/دقیقه/روزِ‌ماه هنوز با ارقام لاتین *تایپ* می‌شوند (فقط نمایش فارسی شد). لِین ۵ باید (الف) این باقیمانده را کامل کند اگر لازم است، (ب) قسمتِ «متن» بند ۱ (نه اعداد) را از صفر ممیزی کند — یعنی گشتن دنبال رشته‌های انگلیسیِ باقیمانده در UI (برچسب‌ها، دکمه‌ها، تولتیپ‌ها، پیام‌های خطا) وقتی زبان فارسی است.

#### ۲. بازطراحی و نظم کلی رابط کاربری `[لِین ۵]` ☐

کل UI پلاگین باید به‌صورت جامع بررسی شود. هدف فقط اصلاح چند مورد مشخص نیست؛ باید تمام بخش‌ها از نظر چیدمان، فاصله‌ها، اندازه عناصر، هم‌ترازی، خوانایی، اندازه متن، نحوه قرارگیری دکمه‌ها، هم‌پوشانی عناصر، واکنش به تغییر اندازه پنجره، نمایش در اندازه‌های مختلف، و یکدست بودن طراحی بررسی شوند.

اگر یک پنجره کوچک یا بزرگ شود، UI نباید به‌هم بریزد. همچنین کارت‌ها، تسک‌ها، نوشته‌ها، دکمه‌ها، پنل‌ها و سایر عناصر نباید در اندازه‌های مختلف صفحه از جای مناسب خود خارج شوند.

> **یادداشت هماهنگی:** این بند سراسری/میان‌لِینی است. لِین ۵ مسئولِ اصلیِ *شروع* آن است (چون زیرساخت/CSS مشترک قلمروِ اوست)، ولی وقتی به کامپوننت‌های اختصاصیِ لِین‌های دیگر می‌رسد، باید طبق پروتکلِ همیشگی در لاگ اعلام کند تا آن لِین خبردار شود. از همان روشِ ممیزیِ کلاس‌های Tailwindِ شکسته (اسکریپتِ Python روی شاخه‌ی `chore/tailwind-class-audit`) به‌عنوانِ نقطه‌ی شروعِ فنی استفاده کن — رفعِ کلاس‌های پایه‌ای شکسته (`p-2`, `opacity-60`, ...) مستقیماً روی چیدمان/فاصله‌گذاریِ سراسری اثر می‌گذارد.

#### ۳. رفع باگ‌های عمومی `[سراسری — همه‌ی لِین‌ها در قلمروِ خودشان]` ☐

علاوه بر باگ‌هایی که به‌صورت مشخص در این مشخصات آمده‌اند، کل پلاگین باید از نظر وجود باگ، رفتار نادرست و حالت‌های غیرعادی بررسی شود.

هدف این است که توسعه فقط به اضافه کردن قابلیت‌های جدید محدود نشود و مشکلات موجود و مشکلات ایجادشده در اثر تغییرات جدید نیز بررسی و رفع شوند.

> این بند به هیچ لِین خاصی محدود نیست — هر لِین در قلمروِ خودش (دقیقاً مثل جلسه‌ی ۱۸ که یک باگِ واقعی در منطقِ تکرارِ هفتگی پیدا/رفع کرد) باید هوشیارِ باگ باشد، نه فقط قابلیتِ تازه اضافه کند.

#### ۴. پنجره‌ها و تعامل با پنجره‌ها `[لِین ۵، با هماهنگیِ لِین‌های صاحبِ هر پنجره]` ☐

تمام پنجره‌های پلاگین باید یک طراحی مشترک، تمیز و یکدست داشته باشند. پنجره‌هایی مانند پنجره زمان، پنجره تسک، پنجره ویرایش، پنجره برنامه‌ریزی، پنجره یادگیری، پنجره‌های مربوط به پومودورو باید از نظر ظاهر و رفتار هماهنگ باشند.

**رفع باگ خروج از پنجره ⚠️ فوریت بالا** — اگر یک پنجره باز باشد و کاربر خارج آن کلیک کند یا آن را ببندد، نباید View اصلی پلاگین از کار بیفتد. در وضعیت فعلی، در برخی موارد خروج از پنجره بدون تکمیل عملیات باعث به‌هم ریختن یا از کار افتادن کل صفحه پلاگین می‌شود و حتی ممکن است کاربر مجبور شود تب پلاگین را ببندد و دوباره باز کند. این مشکل باید به‌طور کامل رفع شود.

> **یادداشت هماهنگی:** شاخه‌ی از‌قبل‌موجودِ `refactor/modal-audit-pass2` احتمالاً دقیقاً همین موضوع است — قبل از شروع، آن شاخه را چک کن (`git log`/`git show --stat`) و اگر مرتبط است ادامه‌اش بده، شاخه‌ی تازه نساز. یادگیری‌های جلسه‌ی ۲۲ («بازطراحی بصری همه‌ی پنجره‌های افزودن») و بازسازیِ معماریِ مودال روی `Modal` بومیِ Obsidian (ابتکار ۴.۶) را حتماً بخوان — این باگِ خروج ممکن است دقیقاً همان‌جا (چرخه‌ی زندگیِ `Modal` بومی، `onClose`/`unmount`) ریشه داشته باشد.

#### ۵. مدیریت و جایگذاری دکمه‌ها `[لِین ۵ + هر لِین در کامپوننت‌های خودش]` ☐

دکمه‌ها باید بر اساس اهمیت و کاربردشان در محل مناسب قرار بگیرند. دکمه‌های اصلی باید قابل دسترس باشند و دکمه‌هایی که کاربرد ثانویه دارند نباید فضای اصلی رابط را اشغال کنند.

برای قابلیت‌های زیاد و تنظیمات پیشرفته می‌توان از منوهایی مانند سه‌نقطه، پنل‌های جمع‌شونده و بخش‌های ثانویه استفاده کرد تا رابط اصلی شلوغ نشود.

> این با الگوی «گزینه‌های بیشتر» که در جلسه‌ی ۱۶ برای `AddTaskModal` ساخته شد (progressive disclosure) هم‌راستاست — همان الگو را می‌توان جاهای دیگر هم تکرار کرد.

#### ۶. شخصی‌سازی گسترده پلاگین `[اصل کلی — همه‌ی لِین‌ها، هماهنگ‌شده توسط لِین ۵]` ☐

یکی از اصول اصلی این پلاگین باید قابلیت شخصی‌سازی بسیار گسترده باشد. کاربر باید در بخش‌های مختلف بتواند: قابلیت‌ها را فعال کند، قابلیت‌ها را غیرفعال کند، موارد جدید اضافه کند، موارد غیرضروری را حذف کند، نحوه نمایش را تعیین کند، ترتیب و چیدمان را تغییر دهد، رنگ‌ها را تنظیم کند، رفتار برخی قابلیت‌ها را مشخص کند.

با وجود گستردگی تنظیمات، ظاهر اصلی پلاگین نباید پیچیده شود. تنظیمات پیشرفته می‌توانند داخل منوهای فرعی و بخش‌های جمع‌شونده قرار بگیرند.

> این یک *اصل طراحی* است، نه یک تسک مجزا — دقیقاً همان چیزی که تا الان هم در «کار ۲: گسترش تنظیمات» (فونت/تراکم/رنگ‌های دسته‌بندی، جلسات ۱۴ تا ۲۸) دنبال شده. هر لِین وقتی قابلیت تازه می‌سازد، باید از همان ابتدا فکرِ «این چطور خاموش/روشن/شخصی‌سازی می‌شود؟» را هم بکند.

#### ۷. داشبورد `[لِین ۵]` ☐

**شخصی‌سازی کامل داشبورد** — کاربر باید بتواند تعیین کند: چه بخش‌هایی روی داشبورد وجود داشته باشند، چه بخش‌هایی نمایش داده نشوند، ترتیب بخش‌ها چگونه باشد، چیدمان چگونه باشد، چه اطلاعاتی از هر بخش نمایش داده شود. همچنین داشبورد باید قابلیت‌های متنوع زیادی داشته باشد تا هر کاربر بتواند آن را متناسب با نیاز خود بسازد.

**میانبرهای دلخواه** — کاربر باید بتواند موارد دلخواه را روی داشبورد قرار دهد؛ مثلاً تسک مهم، فعالیت یادگیری، یک بخش خاص، یک هدف، یک مورد پرکاربرد. این موارد باید به‌صورت دستی انتخاب و به داشبورد اضافه شوند.

#### ۸. سیستم تسک `[لِین ۱]` ☐

**ساخت مدل‌های سفارشی تسک** — این یکی از قابلیت‌های بزرگ و مهم پروژه است. کاربر نباید فقط به مدل‌های ثابت مانند «تسک ساده» و «تسک رونددار» محدود باشد. باید بتواند نوع جدیدی از تسک برای خودش ایجاد کند و برای آن مشخص کند: چه ویژگی‌هایی دارد، چه داده‌هایی دارد، فوریت آن چگونه تعیین می‌شود، چه رفتاری دارد، در داشبورد چگونه نمایش داده می‌شود، در برنامه‌ریزی چگونه رفتار می‌کند، با یادگیری چگونه ارتباط دارد، با گزارش چگونه ارتباط دارد، و چه قابلیت‌های دیگری برای آن فعال باشند. هدف ایجاد یک سیستم انعطاف‌پذیر برای تعریف مدل‌های مختلف تسک است.

**آیکون تسک** — کاربر بتواند برای هر تسک، در صورت تمایل، آیکون مشخص کند. این آیکون بتواند در قسمت‌هایی مانند داشبورد، برنامه‌ریزی، فهرست، گزارش، پومودورو نیز نمایش داده شود.

> این بند دقیقاً همان چیزی است که در «کار ۳ (بخش ۲)» به‌عنوان «تفکیک نوع آیتم (تسک/رویداد/روتین/یادگیری)» از قبل به لِین ۱ تخصیص داده شده بود (فیلد `itemType`). این بند آن ایده را به یک سیستم کامل و باز (نه فقط ۴ نوعِ ثابت) گسترش می‌دهد.

#### ۹. برنامه‌ریزی و تقویم — یک سیستم یکپارچه `[لِین ۳]` ☐

تقویم و برنامه‌ریزی باید به یک سیستم واحد تبدیل شوند. نماهای تقویم مانند روز، هفته، ماه، سال، فهرست، اهداف در همان سیستم باقی بمانند، اما برنامه‌ریزی ساعتی روز نیز داخل همین سیستم قرار گیرد. کاربر نباید بین «تقویم» و «برنامه‌ریزی» احساس کند با دو قابلیت جداگانه کار می‌کند.

#### ۱۰. برنامه‌ریزی روزانه `[لِین ۳]` ☐

هر روز باید برنامه مستقل خودش را داشته باشد. اگر برنامه یک روز تغییر کند، نباید برنامه روزهای دیگر تغییر کند. کاربر باید بتواند یک روز مشخص را انتخاب کند، زمان‌های آن را ببیند و تسک‌ها و فعالیت‌ها را در زمان‌های موردنظر قرار دهد.

#### ۱۱. بخش‌های صبح، ظهر، عصر و شب `[لِین ۳]` ☐

بخش‌بندی روز به قسمت‌هایی مانند صبح، ظهر، عصر و شب باید کاملاً قابل تنظیم باشد. این قابلیت: به‌صورت پیش‌فرض غیرفعال باشد، قابل فعال و غیرفعال کردن باشد، قابلیت اضافه کردن بخش جدید داشته باشد، قابلیت حذف بخش داشته باشد، تعداد بخش‌ها قابل تغییر باشد، نام هر بخش قابل تغییر باشد. مثلاً کاربر بتواند «صبح» را به «سحرگاه» تغییر دهد. این بخش بیشتر یک روش اختیاری برای دسته‌بندی و سازماندهی روز است و نباید ساختار اجباری برنامه‌ریزی را تحمیل کند.

#### ۱۲. تجربه کاربری حرفه‌ای در برنامه‌ریزی `[لِین ۳]` ☐

سیستم برنامه‌ریزی باید از نظر رفتار تعاملی در سطح ابزارهای مطرح باشد. به‌خصوص: Drag & Drop بسیار روان، حرکت طبیعی تسک، تغییر مدت با کشیدن لبه، تغییر زمان با کشیدن خود تسک، واکنش مناسب موس، دستگیره تغییر اندازه، جلوگیری از کلیک ناخواسته پس از Drag/Resize، انیمیشن و بازخورد مناسب، جلوگیری از پرش یا جابه‌جایی غیرمنتظره باید به‌صورت دقیق بررسی و اصلاح شوند.

> **وضعیت فعلی:** بخش بزرگی از این بند از قبل انجام شده (لاگ ۴.۳، مراحل ۱ تا ۴) + رفعِ سازگاریِ لمسی/اندروید (بخش ۴.۶) که دقیقاً «واکنش مناسب موس»/«جلوگیری از کلیک ناخواسته پس از Drag» را هم برای موس هم برای لمس با Pointer Events حل کرد. شاخه‌ی از‌قبل‌موجودِ `fix/calendar-interaction-bugs` را چک کن — احتمالاً همین بند است.

#### ۱۳. فهرست تسک کنار برنامه‌ریزی `[لِین ۳]` ☐

در کنار برنامه‌ریزی یک بخش برای نمایش تسک‌ها وجود داشته باشد. کاربر بتواند تسک را از آن قسمت بردارد و با Drag & Drop وارد تقویم کند. نمایش این فهرست نیز قابل شخصی‌سازی باشد. برای مثال: نمایش نواری و فشرده، نمایش کارت، نمایش مستطیلی، نمایش فقط نام، نمایش نام و جزئیات.

> «سبدِ تسک‌های برنامه‌ریزی‌نشده» در `DayPlannerView` از قبل وجود دارد (و در جلسه‌ی ۲۸ به Pointer Events مجهز شد)؛ این بند می‌خواهد نمایشِ آن هم قابل‌شخصی‌سازی شود.

#### ۱۴. انتقال دوطرفه تسک `[لِین ۳]` ☐

انتقال تسک باید در هر دو جهت امکان‌پذیر باشد. از فهرست به برنامه‌ریزی: تسک با Drag & Drop وارد برنامه شود. از برنامه‌ریزی به فهرست: تسک با Drag & Drop دوباره به فهرست برگردد. همچنین رفتار تسک بعد از ورود به برنامه‌ریزی قابل تنظیم باشد: از فهرست حذف شود، یا باقی بماند ولی کم‌رنگ شود، در حالت کم‌رنگ همچنان قابل انتخاب و استفاده باشد.

#### ۱۵. رفع باگ تغییر اندازه تسک `[لِین ۳]` ☐

وقتی کاربر لبه تسک را می‌کشد و مدت آن را تغییر می‌دهد، رها کردن ماوس نباید به‌عنوان کلیک معمولی ثبت شود. این کار نباید باعث باز شدن پنجره ویرایش یا انجام عملیات ناخواسته دیگری شود.

> در `DayPlannerView` این با Pointer Events (جلسه‌ی ۲۸) و عدم فراخوانیِ `preventDefault` روی `pointerdown` (تا کلیکِ ساده هنوز کار کند) مدیریت شد؛ رفتار دقیق در `WeekHourlyView` (لِین ۳، لاگِ ۴.۳ مرحله‌ی ۲) را هم چک/هم‌راستا کن.

#### ۱۶. ایجاد تسک از فضای خالی `[لِین ۳]` ☐

کاربر بتواند در فضای خالی برنامه‌ریزی تسک ایجاد کند یا از تسک‌های موجود انتخاب کند. این رفتار باید قابل تنظیم باشد و کاربر بتواند مشخص کند تسک‌های موجود برای این کار نمایش داده شوند یا نه.

> پایه‌ی این (کلیک روی بازه‌ی خالی → باز شدنِ `AddTaskModal` با زمانِ پرشده) از جلسه‌ی ۲۱ وجود دارد؛ بخشِ باقیمانده «انتخاب از تسک‌های موجود» و قابل‌تنظیم‌بودنِ این رفتار است.

#### ۱۷. پنل کناری پویا `[لِین ۳]` ☐

یک پنل کناری در برنامه‌ریزی وجود داشته باشد که محتوای آن بر اساس مورد انتخاب‌شده تغییر کند. مثلاً وقتی کاربر یک تسک را انتخاب می‌کند: اطلاعات تسک دیده شود، قابلیت ویرایش آن در پنل قرار گیرد، اطلاعات مربوط به زمان آن دیده شود، اطلاعات مربوط به یادگیری یا زیربخش‌ها در صورت وجود دیده شود، عملیات مرتبط با آن قابل انجام باشد. باز بودن پنل قابل تنظیم باشد: همیشه باز، فقط هنگام انتخاب، کاملاً خاموش.

#### ۱۸. نماهای تقویم `[لِین ۳]` ☐

نمای روز، هفته، ماه و سال باید به‌صورت کامل بازطراحی و مرتب شوند. به‌خصوص نمای هفته، نمای هفته خلاصه، نمای ماه، نمای سال باید بررسی شوند. نماها باید از نظر دیداری تمیز، مرتب و قابل استفاده باشند.

#### ۱۹. نماهای کاملاً سفارشی `[لِین ۳]` ☐

کاربر باید بتواند View کاملاً سفارشی بسازد. بتواند مشخص کند: چند روز نمایش داده شوند، چه مواردی نمایش داده شوند، چه مواردی نمایش داده نشوند، چه نوع تسک‌هایی دیده شوند، چه مقدار اطلاعات نمایش داده شود، نحوه نمایش تسک‌ها چگونه باشد. همچنین بتواند چند View سفارشی داشته باشد و بین آن‌ها جابه‌جا شود. ساخت این نماها باید ساده‌تر از سیستم‌های پیچیده‌ای مثل TasksNotes باشد، ولی انعطاف بسیار زیادی داشته باشد.

> نمای «۳روزه» (لاگ ۴.۳، مرحله‌ی ۳؛ `dayCount` به‌صورت عمومی در `WeekHourlyView`) اثبات می‌کند زیرساخت برای «چند روز نمایش داده شوند» از قبل انعطاف‌پذیر است — این بند می‌خواهد آن انعطاف را به کاربر (نه فقط کد) بدهد.

#### ۲۰. نمایش فشرده و متنوع تسک‌ها `[لِین ۳]` ☐

تسک‌ها در نماهای مختلف بتوانند با اندازه و شکل‌های متفاوت نمایش داده شوند. مثلاً: کارت کامل، کارت فشرده، نوار کوچک، مستطیل گوشه‌گرد، عنصر بسیار کوچک. کاربر بتواند بسته به نیاز خودش میزان جزئیات را تعیین کند.

#### ۲۱. نمایش حرفه‌ای اطلاعات داخل تسک `[لِین ۳]` ☐

در تسک‌های موجود در برنامه‌ریزی، اطلاعات نباید گوشه‌گوشه یا به‌شکل نامرتب قرار بگیرند. اطلاعات باید متناسب با اندازه زمانی تسک تنظیم شوند. مثلاً: نام تسک واضح باشد، مدت زمان مشخص باشد، بازه زمانی «از ... تا ...» به‌شکل مرتب نمایش داده شود، اگر تسک فضای کافی دارد جزئیات بیشتری نمایش داده شود، اگر فضای کمی دارد نمایش اطلاعات به شکل فشرده‌تر انجام شود.

#### ۲۲. رنگ‌بندی گسترده `[لِین ۳، با هماهنگیِ لِین ۵ روی زیرساختِ تنظیمات]` ☐

رنگ‌بندی باید بسیار قابل تنظیم باشد. کاربر بتواند: رنگ تسک، رنگ نوع فعالیت، رنگ بخش یادگیری، رنگ زیرتسک، رنگ زیربخش، رنگ دسته‌ها را خودش مشخص کند. همچنین رنگ‌ها بتوانند در صورت انتخاب کاربر به‌صورت خودکار بر اساس نوع، دسته یا اطلاعات تسک تعیین شوند. زیربخش‌های یک تسک رونددار نیز بتوانند رنگ‌های متفاوت داشته باشند.

> **زیرساخت از قبل آماده:** رنگ‌های ۴گانه‌ی ماتریسِ آیزنهاور از جلسه‌ی «رنگ‌های دسته‌بندی» (شاخه‌ی `feature/category-colors-settings`، merge‌شده) از تنظیمات قابل‌تغییرند، با الگوی «یک نقطه‌ی مشترکِ mutate» که ۱۴+ نقطه‌ی خواندن را بدون تغییر پوشش می‌دهد. این بند می‌خواهد همان الگو را به رنگِ نوع فعالیت/بخش یادگیری/زیرتسک/زیربخش/دسته هم تعمیم دهد.

#### ۲۳. Zoom و مقیاس زمانی `[لِین ۳]` ☐

کاربر بتواند مقیاس زمانی برنامه‌ریزی را تغییر دهد. مثلاً: نمایش یک‌ساعته، نیم‌ساعته، مقیاس دقیق‌تر. همچنین Zoom کلی برنامه‌ریزی قابل تنظیم باشد تا کاربر بتواند با بزرگ‌نمایی، جزئیات زمانی بیشتری مشاهده کند.

#### ۲۴. تسک رونددار و برنامه‌ریزی جزئی `[لِین ۲ + ۴، مشترک]` ☐

وقتی یک تسک رونددار انتخاب می‌شود، کاربر باید بتواند تعیین کند در یک بازه مشخص چه مقدار از آن انجام خواهد شد. اگر تسک دارای زیربخش باشد، کاربر باید بتواند وارد زیربخش‌ها شود و دقیقاً مشخص کند روی کدام بخش کار خواهد کرد. مثلاً: حفظ قرآن → حفظ → مرور → تثبیت، و برای هر بخش بتوان مقدار موردنظر را تعیین کرد.

> این همان بندی است که در جدولِ قدیمیِ ۰.۲ به‌عنوان «تخصیصِ یک جلسه‌ی پومودورو به چند مسیرِ یادگیری» بین لِین ۲ و ۴ مشترک تعریف شده بود — سمتِ مدل‌داده/تقسیمِ پیشرفت با لِین ۲، سمتِ UIِ انتخابِ چندتایی در `PomodoroTimerView` با لِین ۴.

#### ۲۵. ساختار یادگیری چندلایه `[لِین ۲]` ☐

سیستم یادگیری باید اجازه دهد ساختار درختی با چندین سطح ایجاد شود. مثلاً: المپیاد → کار عملی → بخش عملی → زیربخش جزئی‌تر، یا: زبان → کتاب‌خوانی → کتاب مشخص → فصل مشخص → بخش مشخص. تعداد این لایه‌ها نباید به دو سطح محدود باشد.

#### ۲۶. همگام‌سازی یادگیری با فعالیت‌های مختلف `[لِین ۲]` ☐

فعالیت‌های مختلف باید بتوانند با سیستم یادگیری یکپارچه شوند. مثلاً: مطالعه، ورزش، زبان، مهارت، حفظ قرآن، فعالیت‌های جدیدی که کاربر تعریف می‌کند. نباید فقط چند دسته ثابت وجود داشته باشد. کاربر باید بتواند بخش‌های جدید ایجاد کند و آن‌ها را با سیستم یادگیری مرتبط کند.

#### ۲۷. همگام‌سازی با افزونه‌های دیگر `[لِین ۲ — فقط طراحی/یادداشت، نه اجرا]` ☐

در آینده باید امکان ارتباط و همگام‌سازی با افزونه‌های مختلف نیز وجود داشته باشد. مثلاً اگر افزونه‌ای برای زبان یا فعالیت دیگری وجود دارد، بتوان داده‌های مرتبط را با بخش یادگیری این پلاگین هماهنگ کرد. جزئیات دقیق این بخش بعداً مشخص می‌شود.

> طبق متن خودِ کاربر («جزئیات دقیق این بخش بعداً مشخص می‌شود»)، این بند فعلاً فقط نیاز به ثبت/طراحیِ اولیه دارد، نه پیاده‌سازیِ واقعی.

#### ۲۸. هدف‌گذاری برای زیربخش‌ها `[لِین ۲]` ☐

هر زیربخش یادگیری بتواند هدف مستقل خودش را داشته باشد. برای هر زیربخش بتوان: هدف تعریف کرد، مقدار مشخص کرد، نتیجه مورد انتظار مشخص کرد، پیشرفت را ثبت کرد، برنامه‌ریزی کرد.

#### ۲۹. جزئی‌سازی اختیاری فعالیت‌های یادگیری `[لِین ۲]` ☐

کاربر مجبور نباشد برای هر فعالیت جزئیات زیاد وارد کند. اما در صورت نیاز بتواند فعالیت را کاملاً دقیق تعریف کند. مثلاً: کتاب‌خوانی → هری پاتر ۲ → صفحات X تا Y، یا: حفظ قرآن → صفحه X تا Y، یا: مرور قرآن → صفحه A تا B.

#### ۳۰. نمایش متفاوت یادگیری‌ها در فهرست `[لِین ۲]` ☐

یادگیری‌ها نباید الزاماً مانند تسک‌های عادی نمایش داده شوند. اگر مثلاً یک هدف یادگیری برای ۲۰ روز تعریف شده است، نباید الزاماً یک آیتم یکسان در هر ۲۰ روز تکرار شود. باید روش‌های مناسب‌تری برای نمایش فعالیت‌های یادگیری طولانی‌مدت، چندروزه و هدف‌محور وجود داشته باشد.

#### ۳۱. گزارش‌گیری کامل `[لِین ۴]` ☐

در کنار برنامه‌ریزی یک بخش گزارش وجود داشته باشد. این بخش: قابل فعال/غیرفعال شدن، قابل باز/بسته شدن، قابل تنظیم، و قابل شخصی‌سازی باشد. در حالت باز، فضای برنامه‌ریزی و گزارش کنار یکدیگر قرار گیرند؛ برای مثال یک بخش برای برنامه‌ریزی و بخشی از صفحه برای گزارش.

> **زیرساخت از قبل:** «کار ۴: گزارش‌گیری پیشرفته» (جلسات ۱۰–۱۳) گزارش روزانه/هفتگی/ماهانه به Markdown + نمودارها را از قبل ساخته، داخلِ کدِ تبِ برنامه‌ریزی (`DailyReportView`). این بند می‌خواهد آن را به یک پنلِ کنارِ برنامه‌ریزی (نه یک زیرتبِ جدا) تبدیل کند — هماهنگی با لِین ۳ لازم است چون فایل قلمروِ آن‌هاست.

#### ۳۲. گزارش خودکار و دستی `[لِین ۴]` ☐

گزارش باید دو منبع داشته باشد. **گزارش خودکار:** اطلاعات فعالیت‌ها به‌صورت خودکار وارد شوند. **گزارش دستی:** کاربر بتواند خودش توضیح بنویسد؛ برای مثال چه مقدار کار انجام شده، چه زمانی کار شده، چه مقدار یک تسک پیش رفته، چه اتفاقی افتاده، چه توضیحی درباره عملکرد وجود دارد. گزارش‌ها باید ذخیره شوند تا تاریخچه قابل مشاهده باشد.

#### ۳۳. گزارش پیشرفت تسک رونددار `[لِین ۴]` ☐

وقتی کاربر یک تسک رونددار را جلو می‌برد، این پیشرفت باید ثبت شود. اگر مثلاً یک فعالیت ۶۰ درصد جلو رفته باشد، این اطلاعات باید قابل ثبت و نمایش باشد. در صورت مرتبط بودن، پیشرفت باید با بخش یادگیری نیز هماهنگ شود.

#### ۳۴. گزارش پومودورو `[لِین ۴]` ☐

گزارش پومودورو باید بخشی از سیستم گزارش کلی برنامه‌ریزی باشد. مثلاً ثبت شود: «در این بازه زمانی، این فعالیت برای این مدت انجام شد.» کاربر بتواند تعیین کند که پومودورو: به‌صورت خودکار در گزارش ثبت شود، یا ثبت خودکار غیرفعال باشد. این گزینه به‌صورت پیش‌فرض فعال باشد.

#### ۳۵. پومودورو به‌عنوان سیستم کامل مدیریت جلسه `[لِین ۴]` ☐

پومودورو نباید صرفاً یک تایمر باشد. باید بتواند شامل: زمان کار، استراحت، تسک، زیربخش، برنامه جلسه، گزارش، میزان تمرکز، کیفیت جلسه، تاریخچه، آمار باشد.

#### ۳۶. اجرای پومودورو در پس‌زمینه `[لِین ۴]` ☐

پومودورو باید حتی زمانی که View اصلی پلاگین بسته یا پنهان است، به‌صورت صحیح ادامه پیدا کند.

> **یادداشت مهم از خودِ پروژه (کار ۶، بخش ۲):** یک محدودیتِ معماریِ واقعی از جلسه‌ی ۸ ثبت شده — تایمرِ پومودورو (`running`/`secondsLeft`) کاملاً local state داخلِ `PomodoroTimerView` بود، یعنی با خروج از تبِ پومودورو کامپوننت unmount و تایمر واقعاً متوقف می‌شد (نه فقط مخفی). این *دقیقاً* همان چیزی است که این بند می‌خواهد رفع شود. طبق لاگِ ۴.۴ (جلسه‌ی ۱)، نوارِ وضعیتِ Obsidian با یک «عکسِ لحظه‌ایِ مشتق‌شده» (derived snapshot) ساخته شده که ممکن است این مشکل را تا حدی حل کرده باشد — قبل از شروع حتماً لاگِ کاملِ ۴.۴ را بخوان تا دوباره‌کاری نشود.

#### ۳۷. برنامه چندمرحله‌ای پومودورو `[لِین ۴]` ☐

کاربر بتواند قبل از شروع یک برنامه کامل از مراحل مختلف ایجاد کند. مثلاً: ۲۰ دقیقه کار → ۵ دقیقه استراحت → ۲۵ دقیقه کار → استراحت → مرحله بعد. هر مرحله قابل تنظیم باشد. همچنین بتوان برای هر مرحله تسک مشخص کرد. زمان کار، استراحت کوتاه، استراحت بلند و سایر مراحل قابل تغییر باشند.

#### ۳۸. ذخیره الگوی پومودورو `[لِین ۴]` ☐

برنامه‌های چندمرحله‌ای بتوانند ذخیره شوند و در آینده دوباره استفاده شوند.

#### ۳۹. اختصاص دقیق پومودورو به تسک و زیربخش `[لِین ۴]` ☐

وقتی کاربر پومودورو را به یک تسک اختصاص می‌دهد، ساختار کامل آن تسک در دسترس باشد. مثلاً: یادگیری → حفظ قرآن → زیربخش → زیربخش جزئی‌تر. کاربر بتواند دقیقاً مشخص کند که این جلسه مربوط به کدام قسمت است و چه مقدار از آن انجام خواهد شد.

#### ۴۰. ثبت نتیجه پومودورو `[لِین ۴]` ☐

بعد از پایان پومودورو، کاربر بتواند مشخص کند: چقدر کار انجام شد، چقدر پیشرفت حاصل شد، جلسه چگونه پیش رفت، کیفیت کار چگونه بود، میزان تمرکز چقدر بود.

#### ۴۱. ثبت تمرکز `[لِین ۴]` ☐

میزان تمرکز را بتوان هم به‌شکل عددی، و هم با گزینه‌هایی مانند عالی، خوب، متوسط و ضعیف ثبت کرد.

#### ۴۲. صدای پومودورو و آلارم `[لِین ۴]` ☐

سیستم صدای پومودورو باید کاملاً قابل تنظیم باشد. کاربر بتواند: صدای رویدادها را مشخص کند، شدت صدا را تنظیم کند، برای مراحل مختلف صداهای مختلف تعیین کند، فایل صوتی دلخواه اضافه کند، مدت پخش را تعیین کند، مشخص کند آلارم تا چه زمانی ادامه داشته باشد. در صورت نیاز آلارم بتواند تا زمان تأیید کاربر ادامه پیدا کند.

#### ۴۳. نوار زنده / نوار وضعیت `[لِین ۴]` ☐

نوار زنده پایین Obsidian باید بازطراحی شود. این نوار بتواند اطلاعاتی مانند: وضعیت پومودورو، زمان باقی‌مانده، فعالیت در حال اجرا، تسک فعلی، سایر اطلاعات قابل انتخاب را نمایش دهد. آیکون‌های آن نیز نباید ایموجی باشند و باید از آیکون‌های وکتوری مناسب استفاده شود.

> **از قبل شروع شده:** طبق لاگِ ۴.۴ (جلسه‌ی ۱)، «نوار پیشرفتِ پومودورو در نوار وضعیت» ساخته شده. این بند می‌خواهد آن را کامل‌تر کند (اطلاعاتِ بیشتر، آیکونِ وکتوری تضمین‌شده).

#### ۴۴. هماهنگی کامل نوار و پومودورو `[لِین ۴]` ☐

نوار زنده و پومودورو باید همیشه یک وضعیت و زمان یکسان نشان دهند. اگر تایمر تغییر کند، نوار نیز باید همان لحظه وضعیت صحیح را نشان دهد.

#### ۴۵. قرار گرفتن پومودورو در گزارش برنامه‌ریزی `[لِین ۴]` ☐

در قسمت گزارش برنامه‌ریزی یک گزینه/تیک وجود داشته باشد که تعیین کند جلسه‌های پومودورو وارد گزارش شوند یا خیر. این گزینه به‌صورت پیش‌فرض فعال باشد و کاربر بتواند آن را غیرفعال کند.

#### ۴۶. نام فارسی مناسب برای پومودورو `[لِین ۴]` ☐

نام «پومودورو» در حالت فارسی بهتر است با معادل فارسی مناسب جایگزین شود. همچنین در تنظیمات بتوان معادل اصطلاحات اصلی را مدیریت کرد.

> این با بند ۱ (فارسی‌سازی کامل + معادل‌های قابل‌تغییر از تنظیمات) هم‌پوشانی دارد؛ لِین ۴ و لِین ۵ باید هماهنگ کنند کدام مسئولِ افزودنِ خودِ فیلدِ «معادل‌های اصطلاحات» به تنظیمات می‌شود (پیشنهاد: لِین ۵ چون زیرساختِ تنظیمات قلمروِ اوست، ولی مقدارِ پیش‌فرضِ واژه‌ی «پومودورو» را لِین ۴ تعیین کند).

#### ۴۷. شخصی‌سازی روتین `[لِین ۵، فاز دوم]` ☐

کاربر بتواند روتین‌های مختلف ایجاد کند. روتین‌ها باید: قابل اضافه شدن، قابل حذف شدن، قابل تنظیم، دارای جزئیات مختلف، دارای مدل‌های مختلف باشند و بتوانند در سیستم برنامه‌ریزی استفاده شوند.

#### ۴۸. بخش مستقل قرآن `[لِین ۵، فاز دوم — فقط ثبت/طراحیِ اولیه]` ☐

یک بخش مستقل و مهم برای امور قرآن در نظر گرفته شود. این بخش فعلاً فقط به‌عنوان یک قابلیت اصلی ثبت می‌شود و جزئیات ساختاری آن در مرحله بعد تعیین خواهد شد.

#### ۴۹. همگام‌سازی کلی بین سیستم‌ها `[لِین ۵، فاز دوم — آخرین آیتم، وابسته به تثبیتِ بقیه]` ☐

اطلاعات میان تسک، یادگیری، برنامه‌ریزی، پومودورو، گزارش، اهداف، روتین باید تا حد ممکن با یکدیگر هماهنگ باشند. مثلاً فعالیتی که در یک بخش ثبت می‌شود، در بخش‌های مرتبط دوباره از ابتدا وارد نشود و بتواند اطلاعات مشترک داشته باشد.

#### ۵۰. قابلیت افزودن بدون شلوغ کردن UI `[اصل کلی — همه‌ی لِین‌ها]` ☐

اصل کلی سیستم این باشد که کاربر بتواند در بخش‌های منطقی: دسته جدید، نوع تسک جدید، زیربخش جدید، نمای جدید، روتین جدید، فعالیت جدید، قابلیت جدید ایجاد کند. اما این امکانات نباید ظاهر اصلی پلاگین را پیچیده کنند.

---

### ۶.۲ اولویت ۲ — قابلیت‌های پیشنهادی استخراج‌شده از ابزارهای مطرح

> **این‌ها جزء درخواست‌های قطعی اولیه نیستند** و بعد از تکمیل اولویت ۱ باید درباره‌شان تصمیم‌گیری شود (طبق خواسته‌ی کاربر، مرحله‌ی هشتمِ اجرا). فعلاً کارِ فعالِ هیچ لِینی نیستند. این قابلیت‌ها از بررسی الگوهای مورد استفاده در Google Calendar، Outlook Calendar، Todoist، ClickUp، Motion، Reclaim، Sunsama، Routine، Notion Calendar، Trello و Obsidian Tasks استخراج شده‌اند. برچسبِ `[لِینِ مقصدِ احتمالی]` جلوی هرکدام صرفاً یک پیشنهاد برای وقتی است که triage آغاز شود — تعهدی نیست.

**الف. قابلیت‌های تکمیلی تقویم** `[لِین ۳]`
- ۵۱. تسک بدون ساعت مشخص — تسک بتواند فقط متعلق به یک روز باشد و هنوز ساعت نداشته باشد. ☐
- ۵۲. تسک تمام‌روز — یک فعالیت بتواند بدون ساعت مشخص به‌عنوان فعالیت تمام‌روز نمایش داده شود. ☐
- ۵۳. چند زمان برای یک تسک — یک تسک واحد بتواند چند بازه زمانی داشته باشد؛ مثلاً صبح ۳۰ دقیقه و عصر ۳۰ دقیقه، بدون اینکه دو تسک مستقل ایجاد شود. ☐
- ۵۴. مشاهده تمام جلسات یک تسک — داخل جزئیات تسک بتوان تمام دفعاتی که روی آن زمان گذاشته شده را دید. ☐
- ۵۵. تاریخ شروع و موعد نهایی جدا — برای یک فعالیت بتوان زمان شروع مجاز و موعد نهایی را جداگانه تعیین کرد. ☐

**ب. قابلیت‌های نما و نمایش** `[لِین ۳]`
- ۵۶. نمایش یا مخفی کردن آخرهفته — کاربر تعیین کند روزهای استراحت نمایش داده شوند یا نه. ☐
- ۵۷. نمایش شماره هفته — در نماهای تقویم امکان نمایش شماره هفته وجود داشته باشد. ☐
- ۵۸. رفتن سریع به امروز — از هر تاریخی بتوان فوراً به امروز برگشت. ☐
- ۵۹. حفظ آخرین موقعیت نما — پس از بازگشت به پلاگین، آخرین تاریخ و نمای مشاهده‌شده حفظ شود. ☐
- ۶۰. تراکم قابل تنظیم — کاربر بتواند بین حالت فشرده و حالت دارای جزئیات بیشتر انتخاب کند. ☐

**ج. فیلتر و جست‌وجو** `[لِین ۱]`
- ۶۱. فیلتر بر اساس تاریخ ☐
- ۶۲. فیلتر بر اساس اولویت ☐
- ۶۳. فیلتر بر اساس نوع تسک ☐
- ۶۴. فیلتر بر اساس برچسب ☐
- ۶۵. فیلتر بر اساس زیربخش ☐
- ۶۶. فیلترهای ترکیبی — مثلاً «تسک‌های یادگیری + امروز + انجام‌نشده». ☐
- ۶۷. مرتب‌سازی و گروه‌بندی — بر اساس تاریخ، نوع، اولویت، پروژه، برچسب، زیربخش و موارد مشابه. این مجموعه الگو در ابزارهایی مانند Todoist، ClickUp و Obsidian Tasks بسیار پررنگ است. ☐

**د. تکرار و روتین** `[لِین ۱ + ۵]`
- ۶۸. تسک تکرارشونده — روزانه، هفتگی، ماهانه و الگوهای سفارشی. ☐ *(نسخه‌ی پایه از قبل وجود دارد — `AddTaskModal`)*
- ۶۹. انتخاب روزهای مشخص — مثلاً شنبه، دوشنبه و چهارشنبه. ☐ *(از قبل وجود دارد)*
- ۷۰. پایان تکرار — پس از تعداد مشخص یا تا تاریخ مشخص. ☐
- ۷۱. تغییر فقط یک مورد از یک سری تکرارشونده — مثلاً یک جلسه از یک سری تغییر کند ولی بقیه تغییر نکنند. این الگو در Google Calendar و ابزارهای مشابه وجود دارد. ☐

**ه. ظرفیت و زمان‌بندی هوشمند** `[لِین ۳]`
- ۷۲. زمان‌بندی خودکار — سیستم بتواند با توجه به مدت، موعد، اولویت و زمان آزاد، جای مناسب تسک را پیدا کند. ☐
- ۷۳. برنامه‌ریزی مجدد خودکار — در صورت ایجاد تغییر جدید، برنامه بتواند دوباره تنظیم شود. ☐
- ۷۴. تقسیم خودکار تسک بزرگ — یک کار طولانی به چند جلسه تقسیم شود. ☐
- ۷۵. جلوگیری از تقسیم یک تسک — بعضی تسک‌ها حتماً باید یک جلسه یکپارچه باشند. ☐
- ۷۶. هشدار کمبود ظرفیت — اگر برنامه غیرواقعی و بیش از ظرفیت باشد، هشدار داده شود. ☐
- ۷۷. رعایت ساعات کاری — سیستم فقط در ساعات کاری تعریف‌شده برنامه‌ریزی کند. ☐
- ۷۸. چند بازه کاری در یک روز — مثلاً صبح و عصر. ☐
- ۷۹. زمان شروع مجاز — مشخص شود یک فعالیت از چه تاریخی اجازه برنامه‌ریزی دارد. این دسته عمدتاً از الگوهای Motion و Reclaim می‌آید. ☐

**و. ظرفیت روز و هفته** `[لِین ۳ + ۴]`
- ۸۰. نمایش مجموع زمان برنامه‌ریزی‌شده — مثلاً امروز ۵ ساعت و ۴۰ دقیقه. ☐
- ۸۱. نمایش ظرفیت روز — مثلاً ظرفیت امروز ۷ ساعت است. ☐
- ۸۲. هشدار بیش‌برنامه‌ریزی — اگر زمان برنامه‌ریزی‌شده از ظرفیت بیشتر شد، هشدار داده شود. ☐
- ۸۳. توازن کار بین روزهای هفته — سیستم بتواند مقدار کار را بین روزها متعادل‌تر پخش کند. این مفهوم در ابزارهایی مانند Sunsama، Motion و Reclaim دیده می‌شود. ☐

**ز. تمرکز و زمان محافظت‌شده** `[لِین ۴]`
- ۸۴. Focus Time — بازه‌هایی از روز به‌عنوان زمان تمرکز مشخص شوند. ☐
- ۸۵. هدف روزانه تمرکز ☐
- ۸۶. هدف هفتگی تمرکز ☐
- ۸۷. روز بدون جلسه ☐
- ۸۸. زمان حائل — قبل و بعد از بعضی فعالیت‌ها زمان خالی در نظر گرفته شود. این الگوها در Reclaim و ابزارهای مشابه وجود دارند. ☐

**ح. یادآوری** `[لِین ۱]`
- ۸۹. یادآوری خودکار ☐ *(نسخه‌ی پایه از قبل وجود دارد — `AddTaskModal`)*
- ۹۰. چند یادآوری برای یک مورد ☐
- ۹۱. یادآوری مستقل از تکرار ☐
- ۹۲. تعویق یادآوری ☐
- ۹۳. صدای قابل تنظیم برای یادآوری ☐

**ط. چند تقویم** `[لِین ۳]`
- ۹۴. چند تقویم مستقل — مثلاً شخصی، مطالعه، ورزش، پروژه، قرآن. ☐
- ۹۵. نمایش چند تقویم کنار یکدیگر ☐
- ۹۶. Overlay — نمایش چند تقویم روی یک محور زمانی با رنگ‌های مختلف. ☐
- ۹۷. رنگ مستقل هر تقویم ☐
- ۹۸. نمایش/مخفی کردن هر تقویم — این الگو در Outlook و Notion Calendar به‌صورت مشخص وجود دارد. ☐

**ی. مدیریت حرفه‌ای تسک** `[لِین ۱]`
- ۹۹. اولویت‌های چندسطحی ☐ *(نسخه‌ی پایه از قبل وجود دارد)*
- ۱۰۰. برچسب‌ها ☐ *(نسخه‌ی پایه از قبل وجود دارد — فیلد `tag`)*
- ۱۰۱. وابستگی بین تسک‌ها — مثلاً انجام B وابسته به پایان A باشد. ☐
- ۱۰۲. وضعیت‌های سفارشی ☐
- ۱۰۳. زمان تخمینی ☐ *(نسخه‌ی پایه از قبل وجود دارد — فیلد `duration`)*
- ۱۰۴. زمان واقعی ☐
- ۱۰۵. مقایسه زمان تخمینی و واقعی ☐
- ۱۰۶. زیرتسک مستقل ☐ *(نسخه‌ی پایه از قبل وجود دارد — جلسه‌ی ۲۳، تگ‌اینپوت)*
- ۱۰۷. تاریخ مستقل برای زیرتسک ☐
- ۱۰۸. نمایش زیرتسک در تقویم — این الگوها در Todoist، ClickUp و Obsidian Tasks بسیار رایج‌اند. ☐

**ک. Inbox و پردازش سریع** `[لِین ۱]`
- ۱۰۹. Inbox — مکانی برای ثبت سریع مواردی که هنوز برنامه‌ریزی نشده‌اند. ☐
- ۱۱۰. انتقال سریع Inbox به تقویم ☐
- ۱۱۱. تعویق کار ☐
- ۱۱۲. فهرست «این هفته ولی بدون ساعت» — این الگو در Routine و ابزارهای مشابه دیده می‌شود. ☐

**ل. برنامه‌ریزی روزانه و هفتگی** `[لِین ۳]`
- ۱۱۳. برنامه‌ریزی هدایت‌شده روز — یک جریان مشخص برای برنامه‌ریزی روز. ☐
- ۱۱۴. مرور روز گذشته ☐
- ۱۱۵. تعیین ترتیب کارها ☐
- ۱۱۶. برنامه‌ریزی روزهای آینده ☐
- ۱۱۷. برنامه‌ریزی مجدد در وسط روز ☐
- ۱۱۸. مرور هفتگی — Sunsama و Routine نمونه‌های پررنگ این مدل هستند. ☐

**م. تحلیل و گزارش پیشرفته** `[لِین ۴]`
- ۱۱۹. گزارش زمان واقعی ☐
- ۱۲۰. گزارش بر اساس نوع فعالیت ☐
- ۱۲۱. گزارش بر اساس تسک ☐
- ۱۲۲. گزارش روزانه/هفتگی/ماهانه ☐ *(نسخه‌ی پایه از قبل وجود دارد — کار ۴)*
- ۱۲۳. مقایسه برنامه و واقعیت ☐
- ۱۲۴. روند پیشرفت در طول زمان ☐
- ۱۲۵. آمار تمرکز ☐

**ن. اتوماسیون** `[لِین ۵]`
- ۱۲۶. قوانین خودکار — مثلاً اگر یک تسک کامل شد، عمل مشخصی انجام شود. ☐
- ۱۲۷. دکمه‌های عملیاتی سفارشی ☐
- ۱۲۸. عملیات زمان‌محور ☐
- ۱۲۹. عملیات بر اساس نزدیک شدن موعد — Trello نمونه برجسته این مدل از اتوماسیون است. ☐

**س. کنترل تغییرات** `[لِین ۵]`
- ۱۳۰. Preview قبل از اعمال تغییرات گسترده ☐
- ۱۳۱. بازگرداندن تغییرات گروهی ☐
- ۱۳۲. تشخیص تعارض زمانی ☐
- ۱۳۳. پیشنهاد زمان جایگزین — این قابلیت‌ها بیشتر از سیستم‌های زمان‌بندی هوشمند مانند Reclaim الهام گرفته شده‌اند. ☐

**ع. سرعت استفاده** `[لِین ۱]`
- ۱۳۴. میانبرهای صفحه‌کلید ☐
- ۱۳۵. ایجاد سریع تسک ☐
- ۱۳۶. ورود طبیعی تاریخ و زمان — مثلاً وارد کردن عبارت ساده‌ای که شامل روز و ساعت است. ☐
- ۱۳۷. جست‌وجوی سراسری ☐ *(نسخه‌ی پایه از قبل وجود دارد — `GlobalSearchModal`)*

**ف. ویژگی‌های پیشرفته** `[لِین ۵]`
- ۱۳۸. لینک مستقیم به هر تسک یا رویداد ☐
- ۱۳۹. انتخاب چند مورد هم‌زمان ☐
- ۱۴۰. عملیات گروهی — مثلاً تغییر رنگ یا زمان چند تسک با یک عملیات. ☐
- ۱۴۱. حفظ ساختار والد و زیربخش در تمام نماها ☐
- ۱۴۲. View/Query بسیار پیشرفته ☐

---

### ۶.۳ ترتیب پیشنهادی اجرای پروژه (عیناً از کاربر)

**مرحله اول** — رفع مشکلات پایه: باگ‌ها، UI، پنجره‌ها، فارسی‌سازی، اعداد، چیدمان، دکمه‌ها. *(≈ بندهای ۱–۷، لِین ۵)*

**مرحله دوم** — بازسازی هسته برنامه‌ریزی و تقویم: ادغام تقویم و برنامه‌ریزی، برنامه مستقل روزها، Drag & Drop، Resize، فهرست تسک، پنل کناری، نماها، View سفارشی، رنگ‌بندی، Zoom. *(≈ بندهای ۹–۲۳، لِین ۳)*

**مرحله سوم** — سیستم تسک: مدل‌های سفارشی تسک، تسک رونددار، زیربخش‌ها، آیکون، اختصاص دقیق فعالیت. *(≈ بند ۸، لِین ۱)*

**مرحله چهارم** — سیستم یادگیری: ساختار چندلایه، هدف‌گذاری زیربخش‌ها، جزئی‌سازی، همگام‌سازی فعالیت‌ها، نمایش اختصاصی یادگیری. *(≈ بندهای ۲۴–۳۰، لِین ۲)*

**مرحله پنجم** — سیستم گزارش: گزارش برنامه‌ریزی، گزارش خودکار، گزارش دستی، گزارش پیشرفت، گزارش پومودورو. *(≈ بندهای ۳۱–۳۴، لِین ۴)*

**مرحله ششم** — پومودورو: رفع باگ، اجرای پس‌زمینه، برنامه چندمرحله‌ای، الگوها، آلارم، صدای قابل تنظیم، تمرکز، گزارش، نوار زنده، هماهنگی با تسک و یادگیری. *(≈ بندهای ۳۵–۴۶، لِین ۴)*

**مرحله هفتم** — روتین، قرآن و همگام‌سازی‌های گسترده. *(≈ بندهای ۴۷–۴۹، لِین ۵ فاز دوم)*

**مرحله هشتم** — بررسی قابلیت‌های «اولویت ۲» و انتخاب مواردی که واقعاً ارزش اضافه شدن دارند. *(≈ بخش ۶.۲، لِین ۵ triage)*

> **یادداشت مهم درباره‌ی این ترتیب در مدل ۵لِینیِ موازی:** این ترتیب برای یک توسعه‌دهنده‌ی *تکی و ترتیبی* نوشته شده. در مدلِ ۵ عاملِ *موازی* این پروژه، همه‌ی ۵ مرحله‌ی اول (تا مرحله‌ی ششم) **هم‌زمان** شروع می‌شوند (چون هر کدام قلمروِ فایلیِ متفاوتی دارند)، نه پشتِ‌سرِهم — دقیقاً طبق جدولِ بخشِ ۰.۶. آنچه از این ترتیب حفظ می‌شود، **وابستگی‌های منطقی بینِ مرحله‌ها**ست (مثلاً مرحله‌ی پنجم واقعاً به مدل‌داده‌ی مرحله‌ی سوم/چهارم وابسته است)، نه توالیِ زمانیِ اجرا. مرحله‌ی هفتم و هشتم صراحتاً «آخر» می‌مانند چون خودِ کاربر و هم منطقِ وابستگی این را ایجاب می‌کند.
